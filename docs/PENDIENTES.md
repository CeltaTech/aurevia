# PENDIENTES.md — Lista maestra, obligatoria (ver Regla 12.4 de `CLAUDE.md`)

> Se revisa completa, línea por línea, antes de que cualquier sesión declare una tarea
> cerrada. Ningún pendiente se menciona en `PROGRESS.md` sin existir también acá. Estado se
> actualiza en el momento en que cambia, no se deja para "la próxima vez que se acuerden".

| # | Pendiente | Creado | Responsable | Condición de cierre | Estado |
|---|---|---|---|---|---|
| 93 | **Corregido 2026-07-26 (encuadre, no contenido — ver Estado).** Cerrado el pendiente #79 (Fase 7, Panel de Admin_plataforma: estado de pago/uso/riesgo), la funcionalidad de planes/módulos quedó construida, verificada y desplegada pero operando sobre datos **ficticios**: `planes` tiene 3 filas placeholder (Básico USD 100, Intermedio USD 200, Avanzado USD 300), `catalogo_modulos` tiene sus 9 módulos con `precio_addon` parejo ficticio (USD 15 cada uno), y `plan_modulos` está vacía (0 filas, ninguna asignación módulo→plan cargada). **No es una decisión de negocio pendiente del Desarrollador dentro del esquema actual de Aurevia** (encuadre original de esta fila, incorrecto): `ARQUITECTURA_NIVELES.md:264-267` ya deja registrado que la definición de contenido de los planes queda **en pausa** porque, bajo la decisión de separación en 3 niveles (Xeitra/Aurevia/Prestadora, `docs/project_arquitectura_3_niveles...` en memoria, 2026-07-24), `planes`/`plan_modulos` (parte comercial)/`prestadora_planes`/`facturas_licencia` están marcadas para **migrar** a la futura base de datos propia de Xeitra (Nivel 1) — cargar valores reales ahora en las tablas de Aurevia sería trabajo hecho en el esquema equivocado, a descartar o re-migrar cuando exista el repo/DB separado de Xeitra | 2026-07-25 (creado) / 2026-07-26 (re-encuadrado) | Desarrollador (decidió la separación de niveles) / Claude Code (detectó el encuadre incorrecto sin haber cruzado `ARQUITECTURA_NIVELES.md` al crear esta fila, corregido a pedido del Desarrollador) | Existe el inventario completo + plan aprobado de la separación de 3 niveles (`CLAUDE.md` §11, ver "Próximo paso" de `ARQUITECTURA_NIVELES.md`) y, dentro de ese plan, se define dónde y cuándo cargar el catálogo real de planes — no antes | 🔴 En pausa — depende de la separación Xeitra/Aurevia (Nivel 1/Nivel 2), no de que el Desarrollador defina precios hoy dentro del esquema actual |
| 91 | **Riesgo de disponibilidad: ninguna ruta Express del backend tenía manejo de errores asíncronos ni handler global — resuelto 2026-07-25.** Causa raíz del crash de `QR_COBRO_SECRET` (pendiente #90). Inventario completo (`CLAUDE.md` §11): 129 handlers `async` en 22 archivos de rutas, Express 4 confirmado (no reenvía solo excepciones/rechazos de un handler async al middleware de error, a diferencia de Express 5 — migrar de versión mayor se descartó explícitamente por alcance), los 8 jobs de `setInterval` ya tenían su propio `.catch()` y no estaban en riesgo. Plan presentado con control de características (`CLAUDE.md` §12) y aprobado por el Desarrollador ("si"): mecanismo aditivo, cero cambios a los 129 handlers existentes (único punto de verdad, Regla 12 §7). Implementado en `backend/src/server.js`: `import 'express-async-errors'` (antes de registrar rutas), un único middleware de error al final (nunca expone stack ni detalle interno al cliente, `CLAUDE.md` §6, solo loguea server-side) y red de seguridad de último recurso (`process.on('unhandledRejection'/'uncaughtException')`, solo loguea, no oculta la causa raíz). Verificado con un servidor Express aislado y descartable (mismo patrón exacto, incluyendo una excepción del mismo tipo que causó el crash de #90: acceso a propiedad de `undefined`): la excepción no atrapada devolvió 500 limpio (`{"error":"Cannot read properties of undefined..."}`, sin stack) y el proceso siguió vivo y respondiendo en la siguiente petición — archivo de prueba borrado sin dejar residuo en el repo. **Hallazgo lateral, no accionado**: `npm install express-async-errors` (sin dependencias propias) expuso 2 vulnerabilidades preexistentes de `npm audit`, no introducidas por este cambio: `body-parser <1.20.6` (DoS, fix disponible vía `npm audit fix`) y `xlsx` (Prototype Pollution + ReDoS, severidad alta, sin fix disponible) — comunicado al Desarrollador, pendiente de decisión, no forma parte del alcance de este pendiente | 2026-07-25 | Claude Code (hallazgo, inventario, implementación) / Desarrollador (aprobó el plan) | — (cerrado, ver Estado) | 🟢 Resuelto 2026-07-25 — verificado funcionalmente que el proceso sobrevive a una excepción no capturada en un handler async. **Deploy a Railway encontró un segundo problema real, no relacionado con el código de este pendiente**: el workflow "Deploy backend a Railway" venía fallando también en el commit anterior (que no tocaba `backend/`) con "Deploy failed" sin detalle — diagnosticado con `railway logs --build --latest`: `rootDirectory="backend"` (fijado durante el pendiente #90 para que el botón manual del dashboard de Railway, que sube el repo completo, encontrara la carpeta correcta) quedó en conflicto con el pipeline real, que ya corre `railway up` desde dentro de `backend/` (tanto el CLI local como el workflow, `working-directory: backend`) — con ese setting, Railway buscaba una subcarpeta `backend/backend` inexistente dentro del snapshot ya escopeado. Corregido reseteando `rootDirectory` a `""` vía la misma mutación GraphQL `serviceInstanceUpdate` usada en #90 (confirmado con una consulta posterior que devolvió `rootDirectory: ""`). Deploy disparado manualmente tras la corrección: `Deploy complete`, confirmado con `curl` real contra `https://aurevia-backend-production-e7ee.up.railway.app/health` → 200. Efecto colateral aceptado, no verificado en esta sesión: el botón manual "Deploy" del dashboard de Railway (que sube el repo completo) puede volver a fallar por la ambigüedad de carpeta que motivó el fix de #90 — no es el mecanismo de deploy real de este proyecto (ver comentario en `.github/workflows/deploy-backend.yml`), así que no se lo priorizó, pero queda como riesgo latente si alguna vez se usa ese botón en vez del workflow |
| 92 | **2 vulnerabilidades preexistentes de `npm audit` en `backend/`, encontradas como efecto lateral de instalar `express-async-errors` para el pendiente #91 (no introducidas por ese paquete, que no tiene dependencias propias).** `body-parser <1.20.6`: denegación de servicio cuando un valor de límite inválido desactiva silenciosamente la verificación de tamaño — fix disponible vía `npm audit fix`. `xlsx` (severidad alta): Prototype Pollution + ReDoS — sin fix disponible del mantenedor a la fecha. Sin triage ni decisión todavía: falta confirmar si `npm audit fix` de `body-parser` es seguro de aplicar sin romper nada (depende de qué versión de Express la fija) y decidir qué hacer con `xlsx` (¿reemplazar la librería, acotar su uso, aceptar el riesgo documentado?) | 2026-07-25 | Claude Code (hallazgo, sin accionar) | Decidir y aplicar (o descartar explícitamente con motivo) una resolución para cada una de las 2 vulnerabilidades | 🟢 Resuelto 2026-07-25 — **`body-parser`**: `npm audit fix` (sin `--force`) bastó, Express 4.22.2 pasó a traer `body-parser@1.20.6` como dependencia transitiva, sin cambio de versión mayor ni de API. **`xlsx`**: confirmado que el registro de npm no publica parche (`npm view xlsx versions` corta en `0.18.5`) porque SheetJS, desde hace un tiempo, deja de publicar ahí y sube las versiones parchadas a su propio CDN — instalada `xlsx@0.20.3` directo desde `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` (`package.json` ahora fija esa URL en vez de un rango semver de npm). Verificado con `npm audit` → `found 0 vulnerabilities`. Prueba funcional real (no solo instalación): round-trip `XLSX.utils.json_to_sheet` → `XLSX.write` → `XLSX.read({codepage:65001})` → `XLSX.utils.sheet_to_json({defval:''})`, replicando exactamente la llamada real de `backend/src/utils/importacionIA.js:36-38`, primero con datos ASCII y después con acentos/ñ (`"Ñoño Peña"`, `"Córdoba"`) — ambos casos preservaron los datos correctamente. Advertencia cosmética de consola ("Codepage tables are not loaded") no afectó el resultado, solo es un aviso de la librería. Sin cambios de código en `panelImportacion.js`/`importacionIA.js` — misma API (`XLSX.read`, `XLSX.utils.sheet_to_json`) entre 0.18.5 y 0.20.3 |
| 90 | **`QR_COBRO_SECRET` — resuelto 2026-07-25.** Encontrado durante la verificación Playwright del pendiente #89: el backend crashea (excepción no capturada, `TypeError [ERR_INVALID_ARG_TYPE]` en `createHmac('sha256', SECRETO)`, `backend/src/utils/qrCobroEfectivo.js:13`) al primer intento de generar un QR de cobro en efectivo si esta variable no está definida. El Desarrollador cargó la variable en el dashboard de Railway (`aurevia-backend`, servicio `57113178-...`), confirmada correcta en el Editor Sin Procesar, pero el contenedor en ejecución seguía sin verla incluso después de varios reinicios. **Causa raíz real, no supuesta — diagnosticada por `railway status --json` y comparación de dos rutas de despliegue**: un reinicio automático por política `ON_FAILURE` (o un `redeploy` de CLI) reutiliza la foto de variables de entorno tomada en el momento en que ESE despliegue se creó — no vuelve a leer las variables actuales del servicio. Solo un despliegue genuinamente nuevo, construido desde el código fuente, toma la foto vigente al momento de crearse. El intento de generar ese despliegue nuevo desde el botón "Deploy" del dashboard falló por una causa aparte: Railpack ya no podía adivinar sola qué carpeta construir (el repo pasó a tener 4 `package.json` en la raíz — `backend`/`panel`/`pwa-asistentes`/`pwa-familias` — y antes bastaba con una sola carpeta candidata). Corregido fijando explícitamente `rootDirectory="backend"` en la configuración del servicio (mutación `serviceInstanceUpdate` de la API GraphQL de Railway, mismo efecto que el campo "Root Directory" de Settings del dashboard, confirmado con una consulta posterior de `serviceInstance` que devolvió `rootDirectory: "backend"`), y disparando un despliegue nuevo (`serviceInstanceDeployV2`, id `f5982cf9-...`) que sí completó `SUCCESS` con `railpackInfo.detectedProviders: ["node"]` | 2026-07-25 | Desarrollador (cargó el secret en Railway) / Claude Code (diagnóstico de la causa raíz, corrección de `rootDirectory` vía API y verificación final) | — (cerrado, ver Estado) | 🟢 Resuelto 2026-07-25 — confirmado con una generación real de QR contra producción (`POST /api/app-familias/qr-cobro` autenticado con JWT real de la cuenta de prueba Familia, sobre una fila temporal de `suscripciones_marketplace`): respuesta 200 con token válido, sin crash. Fila y QR de prueba borrados, confirmado 0 filas residuales por SQL directo. Deploy del backend a producción del pendiente #89 ya no está bloqueado |
| 89 | **Grupo 3 Marketplace (ítem 2 del pendiente #85) — construido y verificado 2026-07-25.** Arquitectura de 6 rieles de pasarela de pago (MercadoPago, Stripe, Modo, débito bancario/DEBIN, red de cobranza en efectivo, efectivo manual con QR opcional) para la suscripción de Familia en modalidad marketplace, según `docs/PRD_07_Modalidad_Marketplace.md` y `docs/PRD_08_Dashboard_Modalidades.md`. Schema: `prestadora_pasarela_pago` (credenciales cifradas vía Supabase Vault, RPCs `guardar_credencial_pasarela_pago`/`leer_credencial_pasarela_pago`), `suscripciones_marketplace`, `cobros_marketplace`, `qr_cobro_efectivo`, `calificaciones_asistente.descargo_asistente`, seed de `advertencias_legales` (5 `funcion_clave`) — todas con RLS scoped por `prestadora_id = current_tenant()` (o equivalente por familia/asistente), verificado por consulta directa a `pg_policies` contra Supabase real, ninguna política abierta. Backend: adaptador de pasarela (`backend/src/pasarelas/index.js`, interfaz común + registro de 6 proveedores), rutas de Panel (configuración de pasarela, suscripciones, calificaciones, auditoría legal) + webhooks + generación/canje de QR de cobro (`backend/src/utils/qrCobroEfectivo.js`, token HMAC-SHA256 de un solo uso y 10 minutos de vigencia, canje siempre server-side vía `POST /api/panel/marketplace/qr-cobro/canjear`, nunca un UPDATE directo del cliente). Panel: pestaña "Pasarela de pago" en `Configuracion.jsx` y 3 pantallas nuevas bajo el grupo de navegación "Marketplace" (gateado por `tieneModalidad('marketplace')`, confirmado oculto en el menú para Prestadora Demo que no tiene esa modalidad activa, aunque las rutas siguen alcanzables por URL directa con los datos igual acotados por RLS): Familias (suscripciones + cobros + escaneo de QR con `html5-qrcode`), Calificaciones y descargos, Auditoría legal. PWA Familias: pantalla `SuscripcionMarketplace.jsx` (estado de suscripción, generación de QR de cobro con `qrcode`, sondeo cada 4 segundos hasta detectar el cobro). i18n completo en los 3 locales (es-AR/en/pt-BR) desde el principio. **Bug real encontrado y corregido durante esta verificación**: `QR_COBRO_SECRET` nunca se había cargado en `backend/.env` — el backend entero crasheaba (excepción no capturada) al primer intento de generar un QR de cobro, porque nunca se había ejercitado el flujo completo end-to-end antes de esta sesión. Corregido localmente (secreto nuevo generado y cargado). Verificado en navegador real (Playwright) contra Sandbox/Prestadora Demo con datos ficticios: los 4 estados (loading/error/vacío/listo) de las 4 pantallas nuevas, flujo completo de cobro por QR de punta a punta en dos pestañas simultáneas (Familia genera → Panel canjea vía API directa, ya que Playwright headless no puede simular la cámara → pantalla de Familia confirma solo por el sondeo), enforcement de un solo uso del QR (segundo intento de canje devuelve 409 sin crashear), ausencia de valores crudos (nombres reales en vez de UUID, enums traducidos) en las 4 pantallas. Aislamiento cruzado entre Prestadoras verificado a nivel de política RLS contra Supabase real (no había una segunda Prestadora real disponible para probarlo por navegador sin crear una Prestadora descartable solo para el test). Datos de prueba (1 fila de `suscripciones_marketplace`, 2 de `qr_cobro_efectivo`, 1 de `cobros_marketplace`) borrados y verificados en 0 filas al cerrar la verificación. Commit + push completo (`ca08e69`, 36 archivos). **Deploy a producción completado 2026-07-25** tras resolver el pendiente #90 (secreto cargado en Railway + corrección de `rootDirectory`); confirmado con una generación real de QR contra producción, sin crash. **Panel desplegado explícitamente a Vercel** (`vercel --prod`, deploy `dpl_4zKTS1sWb5JR8VxznX5zWooJZEoh`, alias `https://aurevia-panel.vercel.app` confirmado con `curl` real → 200) | 2026-07-25 | Claude Code (implementación autónoma autorizada por el Desarrollador con "suma y arranca") | Ítem 3 del pendiente #85 (resumen por modalidad en Dashboard) sigue sin empezar, ver esa entrada | 🟢 Backend y Panel desplegados y verificados en producción 2026-07-25 — ítem 2 del pendiente #85 cerrado del todo |
| 87 | Encontrado durante la implementación de la Fase 1 (pendiente #86), y **corregido en su premisa** durante la verificación del pendiente #88 (2026-07-25): originalmente se registró como "el Panel usa `window.confirm()` nativo en varios puntos en vez del componente propio (`useConfirmarDestructivo`)", asumiendo que ese hook era un modal propio del sistema de diseño. Verificación real del código mostró que **`useConfirmarDestructivo` en sí mismo era un wrapper de `window.confirm()`** — no armaba ningún modal propio. Se presentó el inventario completo (24 sitios de llamada en el Panel, todos consumiendo el mismo hook, ninguno en las PWAs) y el plan al Desarrollador, quien aprobó (2026-07-25) con el pedido explícito de que la solución quedara disponible también para cualquier sitio futuro que necesite el mismo patrón. Implementado: `TenantSessionContext.jsx` ahora expone `pedirConfirmacion` (estado + Promise) y renderiza un modal propio (`panel-modal-fondo`/`panel-modal`, mismas clases ya usadas por otros modales del Panel) con botones `t.comun.cancelar`/`t.comun.confirmar`. `useConfirmarDestructivo` mantiene la misma firma pero ahora devuelve una `Promise<boolean>` en vez de bloquear; los 24 sitios de llamada se actualizaron a `await`. Verificado en navegador real (Playwright, Sandbox): crear y borrar una zona de cobertura de prueba muestra el modal propio (no el diálogo nativo), tanto confirmando como cancelando; sin datos de prueba residuales (zona borrada al final). `npm run build` del Panel sin errores | 2026-07-24 (hallazgo original), corregido 2026-07-25, cerrado 2026-07-25 | Claude Code | — (cerrado) | 🟢 Resuelto 2026-07-25 |
| 86 | **Fase 1 del plan de rediseño de frontend — Diccionario de traducción de valores y capa anti-crudo**, construida y verificada 2026-07-24 (ver `docs/PROGRESS.md`). Inventario real (agente Explore) reveló que 2 de los 3 huecos originalmente listados en el plan (`docs/INVESTIGACION_UX_REDISENO_FRONTEND.md`) ya estaban resueltos de una sesión anterior no documentada como tal (JSON crudo de `VinculoCeseTab.jsx` y `window.prompt()` de `InformesObraSocial.jsx` — cerrados junto con el pendiente #71), y encontró en cambio 6 sitios nuevos de valor de enum sin traducir en `panel/src` no cubiertos por el plan original. Se portó `panel/src/i18n/valores.js` (`traducirValor`, idéntico al ya existente en `pwa-asistentes`/`pwa-familias` — mismo punto único de verdad, `CLAUDE.md` §7 regla 12) y se aplicó en: `Prestadoras.jsx` (columna Estado), `AdminPlataforma.jsx` (badge de estado de Licenciataria), `UsuariosPanel.jsx` (estado de Prestadora en el selector del modal "Nuevo usuario"), `Configuracion.jsx` (categoría y estado de plantillas de WhatsApp, columna de tabla y selector del modal "Nueva plantilla"), `InformesObraSocial.jsx` (estado de Guardia en el detalle de informe, reutilizando las claves `t.guardias.estado_*` ya existentes). Se agregaron las claves de traducción faltantes (`estado_prospecto`/`en_certificacion`/`certificada`/`suspendida`/`dada_de_baja` en `prestadoras` y `adminPlataforma`; `whatsapp_plantillas_categoria_utility`/`marketing`/`authentication`) en los 3 locales (es-AR/en/pt-BR). Se sacó también el nombre de tabla interna `escalas_legales` del texto de explicación del Score de Riesgo, reemplazado por lenguaje natural. **Corrección de tono agregada en la misma sesión, señalada por el Desarrollador** (no autodetectada): el paso de onboarding "Elegí tu modalidad de trabajo"/"Activá..." usaba voseo informal inapropiado para una app dirigida a Prestadoras desconocidas — corregido a formal ("Seleccione la modalidad de trabajo"/"Active...") en es-AR, alineado en pt-BR; guardado como memoria persistente (`feedback_tono_formal_ui.md`) para no repetirlo en el resto de la superficie de texto, cuya auditoría retroactiva completa queda explícitamente fuera de esta sesión, sin confirmar todavía con el Desarrollador | 2026-07-24 | Desarrollador (delegó la elección de fase con "elige tu con que fase continuar", corrigió el tono) + Claude Code (inventario, implementación y verificación) | — (cerrado, ver Estado) | 🟢 Resuelto 2026-07-24 — verificado en navegador real (Playwright) contra Sandbox: `Prestadoras.jsx` y `AdminPlataforma.jsx` (con cuenta de prueba `admin_plataforma` temporal, borrada sin residuos al terminar) muestran "Prospecto" en vez del valor crudo `prospecto`; `Configuracion.jsx` (selector de categoría del modal "Nueva plantilla") muestra "Utilidad"/"Marketing"/"Autenticación" — no se pudo probar el guardado real de una plantilla por un error 500 preexistente y no relacionado del backend (`POST /whatsapp/plantillas`, "Failed to fetch", registrado como hallazgo separado más abajo); `InformesObraSocial.jsx` no se pudo verificar con datos reales porque los Pacientes de prueba disponibles pertenecen a "Prestadora Demo", fuera del alcance del Superadmin (correctamente restringido a Sandbox, `CLAUDE.md` §5) — verificado por revisión de código como reuso exacto de un patrón y de claves de traducción ya probados en otro lado. 0 errores de consola nuevos en ninguna pantalla (los únicos presentes son el 403 preexistente y esperado de `/api/panel/sesion-tenant` para `superadmin`, y 2 errores 500 preexistentes y no relacionados en la pestaña WhatsApp de Configuración por falta de fila de configuración en Sandbox — ver pendiente #88). Commiteado (`77129a7`), pusheado a `origin/main`, Panel desplegado explícitamente a Vercel (`dpl_2XkjtXrdDCnptGMRBd32Q95t8sw6`, alias `https://aurevia-panel.vercel.app`) y reverificado contra producción con el mismo resultado |
| 88 | **Diagnosticado y corregido 2026-07-25.** Encontrado durante la verificación de la Fase 1 (pendiente #86): la pestaña "WhatsApp + IA" de `Configuracion.jsx` devolvía 500 en `GET /api/panel/configuracion/whatsapp` y `GET /api/panel/configuracion/escalada-coordinador` para Sandbox. Causa raíz real (verificada contra Supabase real, no supuesta): **no es un problema específico de Sandbox** — `configuracion_whatsapp_prestadora` y `configuracion_escalada_coordinador` estaban vacías para las 2 Prestadoras que existen (Sandbox y Prestadora Demo, `select count(*)` = 0 en ambas tablas), porque la fila solo se crea recién al primer `PATCH` (`upsert`, patrón correcto) — cualquier Prestadora nueva que todavía no guardó esa configuración se encuentra con `.single()` explotando porque espera exactamente 1 fila y hay 0. Es un bug real de "no manejar el estado vacío" (regla de los 4 estados, `CLAUDE.md` §7.3), no una falta de datos semilla de Sandbox. Corrección: `panelConfiguracion.js` — ambos `GET` cambiados de `.single()` a `.maybeSingle()`, devolviendo un objeto con los valores por defecto reales de la tabla (`activo=false`/resto null para WhatsApp; `minutos_antes_backup=15`, `umbrales_premura` con los 3 tramos por defecto, `fase_automatica_activa=false`, `minutos_antes_fase_automatica=120` para escalada — los mismos `DEFAULT` de la columna en Postgres, confirmados por `information_schema.columns`) cuando no existe fila todavía, en vez de sembrar filas fijas para las Prestadoras actuales (que no escalaría a Prestadoras futuras). El otro hallazgo, `POST /whatsapp/plantillas` con "Failed to fetch", **no se reprodujo**: se creó una plantilla de prueba real contra Sandbox local (nombre/categoría/texto ficticios) y se guardó sin error, con la fila visible en la tabla mostrando "Utilidad"/"Borrador" ya traducidos (confirma también, de paso, la última pieza sin verificar con datos reales del pendiente #86) — se concluye que fue un problema transitorio de esa sesión (posible cold-start del backend), no reproducible; se borró la plantilla de prueba sin dejar residuo (confirmado `count=0` por SQL). **Hallazgo adicional durante esta misma verificación**: al borrar la plantilla de prueba apareció un diálogo nativo `window.confirm()` del navegador — resulta que `useConfirmarDestructivo` (que sí se usa en ese punto) es en sí mismo un wrapper de `window.confirm()`, no un modal propio; esto corrige la premisa del pendiente #87 (ver esa entrada, alcance real más grande de lo que se había registrado) | 2026-07-24 (hallazgo), 2026-07-25 (diagnóstico y fix) | Claude Code | — (cerrado, ver Estado) | 🟢 Resuelto 2026-07-25 — verificado en navegador real (Playwright) contra Sandbox, primero en local (pestaña "WhatsApp + IA" carga sin 500, formulario en blanco + "Escalada a Coordinador" con 15/120 min por defecto; plantilla de prueba creada y borrada sin error ni residuo) y después contra producción tras el deploy (mismo resultado, solo el 403 preexistente de `/sesion-tenant`, sin 500 nuevo). Commiteado (`781f3c8`), pusheado a `origin/main`, backend redesplegado automáticamente a Railway (workflow "Deploy backend a Railway", run exitoso) y confirmado con `curl` real que `/health` responde `{"status":"ok"}` |
| 85 | **Rediseño de las 3 modalidades de trabajo de la Prestadora (directa/marketplace/cooperativa) en el dashboard de Admin_prestadora** — confirmadas combinables entre sí (una Prestadora puede operar varias a la vez). Diseño de negocio completo de la modalidad marketplace ya cerrado con el Desarrollador el 2026-07-24, documentado en `docs/PRD_07_Modalidad_Marketplace.md`: modelo de suscripción de la Familia (trial 30 días sin acceso a contacto, activación inmediata si accede antes, renovación automática sin mínimo forzoso, aviso previo al cobro, baja en un clic, período de gracia ante fallo de pago), incentivos de retención (prioridad de reemplazo sin garantía, historial documental, vigilancia de vencimientos, alertas críticas, contenido educativo — mediación de conflictos descartada explícitamente por riesgo legal), principio de "quién ejerce el control" (Familia sobre su propio vínculo = bajo riesgo, plataforma agregando entre todas las Familias = riesgo, mismo patrón que Uber BV vs Aslam UK 2021), tabla de riesgo legal invertido agregada a `docs/legal/argentina.md` (sección "Modalidad marketplace"), consentimiento explícito/revocable del Asistente para geolocalización (Ley 25.326), y alcance de PWA Familia/Asistente en esta modalidad. Investigación de cuidarlos.com/Care.com que alimentó estas decisiones no guardada aparte — quedó volcada directamente en el PRD. **Actualización 2026-07-24**: rearme del esquema completado y **aprobado por el Desarrollador** en `docs/PRD_08_Dashboard_Modalidades.md` — 4 grupos de navegación (Panel de control / Plantel de Asistentes compartido / Prestación directa / Marketplace), Cooperativa fuera del menú hasta tener diseño completo. Decisiones cerradas: tabla propia `prestadora_modalidades` (no se reusa `catalogo_modulos`, son decisiones distintas — regla 12 `CLAUDE.md` §7); onboarding suma paso explícito de elegir modalidad, sin default; Facturación de marketplace en pantalla propia; Informes de obra social/Lista de precios confirmados exclusivos de prestación directa; Verificación de Guardias/Continuidad confirmadas como infraestructura compartida leyendo el código real (`Evv.jsx`/`Continuidad.jsx`, ambas operan sobre `guardias` sin distinguir canal). Glosario: "Familia" se mantiene sin excepción. **Actualización 2026-07-24 (implementación parcial, sin el control de características de `CLAUDE.md` §12 presentado antes de programar — corregido recién, ver más abajo)**: construida la base — tabla `prestadora_modalidades` (migración + backfill `directa=true` para Prestadoras existentes, aplicada contra Supabase real), `ModalidadesContext.jsx` como punto único de verdad consumido por el menú, tab "Modalidades" en `Configuracion.jsx` con toggle real (`GET`/`PATCH /api/panel/configuracion/modalidades`), y `Layout.jsx` reagrupado en Grupo 1 (Plantel de Asistentes: Postulaciones/Solicitudes/Asistentes/Documentación/Continuidad/Verificación de Guardias, visible si directa o marketplace activa) + Grupo 2 (Prestación directa: Familias/Guardias/Facturación/Lista de precios/Informes de obra social, visible solo si directa activa) + Comunicación transversal fuera de ambos grupos, verificado en navegador real contra Sandbox. **Corregido en esta misma sesión, tras detectarlo en una segunda verificación pedida explícitamente por el Desarrollador**: la primera versión había puesto Verificación de Guardias y Comunicación dentro del grupo "directa" en vez de Grupo 1/transversal como fija el PRD — ya corregido y reverificado. **Actualización 2026-07-24 (misma sesión, tercer tramo)**: presentado explícitamente el checklist ✅/⚠️/❌ de `CLAUDE.md` §12 para lo que quedaba pendiente, con "adelante" del Desarrollador. Ítem 1 (onboarding) construido: paso "Elegí tu modalidad de trabajo" agregado como primer ítem del checklist "Primeros pasos" en `Dashboard.jsx` (`OnboardingChecklist`), marcado completo vía `tieneModalidad('directa') || tieneModalidad('marketplace')`, con guarda de carga (`modalidadesCargadas` de `useModalidades()`) para no mostrar un falso "pendiente" mientras el fetch de modalidades todavía no resolvió; i18n en los 3 locales. Verificado en navegador real (Playwright) primero en local (Sandbox: paso mostrado "Completado", "3/4 completados", coherente con `directa=true` ya activa) y después contra producción tras el deploy, mismo resultado. Commiteado (`8de183b`), pusheado a `origin/main`, Panel desplegado explícitamente a Vercel (`dpl_75JJ2EmH5UHphXyi6BLCTmCFQ5Jj`, alias `https://aurevia-panel.vercel.app` confirmado). **Ítems 2 y 3 quedan explícitamente excluidos de esta sesión, no "olvidados"**: (2) Grupo 3 Marketplace (pantallas de suscripción de Familia, calificaciones/descargos, auditoría de advertencias legales) requiere su propio ciclo de inventario→plan→aprobación de `CLAUDE.md` §11 antes de tocar código — no hay todavía diseño de pasarela de pago, máquina de estados de suscripción ni panel de auditoría legal; (3) resumen por modalidad en "Alertas y continuidad" del Dashboard queda fuera porque la tabla `guardias` no tiene columna de canal/modalidad de la que derivar ese resumen sin inventar el dato — agregar esa columna es en sí un cambio de modelo de datos que no se decidió en esta sesión. **Ítem 3 construido y verificado 2026-07-25** (a pedido explícito del Desarrollador, "actua, vamos a resolver este punto", tras inventario `CLAUDE.md` §11 y decisión explícita del Desarrollador vía pregunta estructurada: "Agregar columna nueva (recomendado)"): columna `canal_modalidad TEXT NOT NULL DEFAULT 'directa' CHECK IN ('directa','marketplace','cooperativa')` agregada a `guardias` y `series_guardias` (`backend/src/db/schema_guardias_canal_modalidad.sql`, aplicada contra Supabase real vía `apply_migration`, `NOTIFY pgrst` incluido) — no hizo falta tocar `NuevaGuardiaModal.jsx` ni `generacionSeriesGuardia.js` porque el `DEFAULT 'directa'` ya cubre automáticamente toda guardia creada hoy (marketplace todavía no tiene flujo propio de generación de guardias, confirmado por grep de `panelMarketplace.js`). `Dashboard.jsx`: `cargarAlertas()` ahora trae `guardias!incidentes_relevo_entrante_tenant_fk(canal_modalidad)` embebido en la consulta a `incidentes_relevo` (mismo punto único de verdad para el total y el desglose, Regla 12) y desglosa "Ausentes sin relevo previo" por modalidad (`ausentesPorModalidad`), mostrado condicionalmente solo si la Prestadora tiene más de una modalidad activa (`modalidades.length > 1`) reutilizando las etiquetas ya traducidas de `Layout.jsx` (`t.nav.grupo_prestacion_directa`/`grupo_marketplace`) en vez de un namespace de i18n nuevo. Explícitamente excluido del alcance (ver control de características presentado y aprobado): desglose de "documentos por vencer" por modalidad (pertenece al Asistente, no a una guardia/canal) y cualquier widget nuevo tipo "guardias sin cubrir hoy" (no pedido, fuera de alcance). Verificado con datos ficticios reales en Sandbox (cadena completa `auth.users`→`usuarios`→`asistentes`→`pacientes`→`guardias`→`incidentes_relevo`, 2 guardias con `canal_modalidad` distinto, 2 incidentes sin relevo): join SQL equivalente al que arma PostgREST confirmado correcto (total=2, directa=1, marketplace=1), datos de prueba borrados y confirmados en 0 filas residuales por SQL directo. `npx vite build` del Panel verificado sin errores. Panel desplegado a Vercel (`vercel --prod`, alias `https://aurevia-panel.vercel.app` confirmado con `curl` real → 200). i18n agregado en los 3 locales (es-AR/en/pt-BR). | 2026-07-24 | Desarrollador (rediseño del dashboard, aprobado; checklist de items restantes, aprobado con "adelante"; ítem 3 con "actua, vamos a resolver este punto") + Claude Code (implementación) | — (cerrado, ver Estado) | 🟢 Resuelto del todo 2026-07-25 — base, onboarding (ítem 1), Grupo 3 Marketplace (ítem 2, ver pendiente #89) y resumen por modalidad en Dashboard (ítem 3) construidos, verificados y desplegados a producción |
| 84 | **Registrar consumo real de IA por Prestadora para poder facturarlo**, derivado del pendiente #65 (carga de `ANTHROPIC_API_KEY` en Railway). **Origen del pedido** (Desarrollador, 2026-07-24, al configurar la clave de Anthropic en Railway): Aurevia usa IA en varios módulos ya construidos (`alertasIA.js`, `reporteIA.js`, `importacionIA.js`, `iaWhatsapp.js`), pero el gasto de la cuenta de Anthropic es global — no había ningún registro de qué Prestadora generó cada llamada, cuántos tokens/costo insumió, ni en qué función; el panel de Xeitra/Admin_plataforma (nunca el de cada Prestadora, por aislamiento multi-tenant) tiene que dejar registrado ese uso para poder facturarlo. Migración `backend/src/db/schema_uso_ia_01.sql` aplicada contra Supabase real: `es_admin_plataforma()`, tablas `precios_ia_modelo` (precios oficiales por modelo con vigencia por fecha, `UNIQUE(proveedor, modelo, vigente_desde)`, sembrada con precios reales de Anthropic Sonnet 5: 2.00/10.00 USD por millón de tokens desde 2026-07-24, 3.00/15.00 desde 2026-09-01), `uso_ia` (registro por Prestadora/módulo/modelo con costo calculado) y `cambios_precio_ia_pendientes` (detección mensual de cambio de precio, nunca auto-aplicado — requiere confirmación explícita del Admin_plataforma vía `POST /cambios-precio-ia/:id/confirmar` con `useConfirmarDestructivo`, `CLAUDE.md` §6), con RLS y `NOTIFY pgrst, 'reload schema'`. Backend: `registrarUsoIA.js` (cálculo de costo a partir de `usage.input_tokens`/`output_tokens` de la respuesta de Anthropic), ya invocado desde los 4 puntos reales de llamada a la IA (`reporteIA.js`, `importacionIA.js`, `iaWhatsapp.js`, `alertasIA.js`) y `verificarPreciosIA.js` (job mensual, integrado en `server.js` con la misma cadencia diaria que `revisarVencimientos`). Panel: dos secciones nuevas en `AdminPlataforma.jsx` (`UsoIASeccion`, `CambiosPrecioIASeccion`) | 2026-07-24 | Claude Code | Confirmar en producción que las 4 llamadas reales a la IA generan filas correctas en `uso_ia` con costo distinto de cero (esta sesión solo lo verificó con una inserción manual de prueba, no con una llamada real end-to-end) | 🟢 Resuelto 2026-07-24 (avance autónomo nocturno, autorizado explícitamente por el Desarrollador — "fijate si algo de esto no depende de decisiones mías y resolvelo"). Schema aplicado y verificado en Supabase real (semilla de precios confirmada por SQL); `registrarUsoIA` verificado con una inserción real de prueba (1000/500 tokens → costo `0.007000` USD, coincide con el cálculo manual); 2 bugs encontrados y corregidos en verificación end-to-end con Playwright: (1) `AdminPlataforma.jsx` tenía las dos secciones nuevas como hijas de `<EstadoLista>` de una lista no relacionada (`facturas`), que descarta `children` cuando esa lista está vacía — se movieron como hermanas fuera de ese wrapper; (2) la ruta de confirmar cambio de precio usaba `.insert()` contra `precios_ia_modelo`, que choca con la `UNIQUE(proveedor, modelo, vigente_desde)` cuando ya existe una fila con la fecha de hoy (caso real: la fila semilla) — se cambió a `.upsert(..., {onConflict:'proveedor,modelo,vigente_desde'})`. Cuenta de prueba `admin_plataforma` y datos ficticios usados para la verificación, borrados sin residuos. **Commiteado (`2286d1c`), pusheado a `origin/main`, backend redesplegado automáticamente a Railway** (workflow "Deploy backend a Railway", run exitoso, confirmado con `curl` real: `https://aurevia-backend-production-e7ee.up.railway.app/health` → `{"status":"ok"}`) **y Panel desplegado explícitamente a Vercel** (`vercel --prod`, deploy `dpl_9JPq6KVGPDYJFmYUBRmVPFf5dDCx`, confirmado con `curl` real que `https://aurevia-panel.vercel.app` responde 200). **Verificación final end-to-end 2026-07-24**: disparada una llamada real a la IA contra producción (sin datos ficticios creados — `POST /api/panel/importacion/analizar` con un CSV mínimo de prueba, que solo propone un mapeo, no crea ningún Asistente/Familia) usando la cuenta de prueba `admin_prestadora` (`aurevia.test.acount@gmail.com`) autenticada directamente contra Supabase Auth; confirmado por SQL real contra `uso_ia` que se insertó una fila real con `modulo='importacion'`, `tokens_entrada=499`, `tokens_salida=182`, `costo_usd=0.002818` — coherente con el precio sembrado de Sonnet 5. Los otros 3 puntos de llamada (`reporteIA.js`/`alertasIA.js`/`iaWhatsapp.js`) comparten exactamente la misma función `registrarUsoIA` ya probada dos veces (inserción manual + esta llamada real) y no se dispararon con datos reales por requerir una Guardia activa o un mensaje de WhatsApp Business entrante, que hubiera significado crear datos operativos de prueba en producción — riesgo mayor al beneficio de esta verificación puntual. Token de sesión temporal usado solo en memoria/archivos borrados al terminar, sin quedar en ningún lado del repo |
| 83 | **Fase 12 del plan de rediseño de frontend — Maquetación general del Panel (Dashboard + Asistentes + Familias + Guardias)**, construida y verificada 2026-07-24 (ver `docs/PROGRESS.md`). Alcance acordado explícitamente con el Desarrollador (`AskUserQuestion`, sin adivinar) para el primer corte: pantallas Dashboard/Asistentes/Familias; formato elegido por el Desarrollador fue tarjeta por fila (no la opción de menor riesgo que se había recomendado, que era mantener tabla mejorando solo el contenedor). Cambios del primer corte: `Dashboard.jsx` agrupa las métricas sueltas en 2 secciones con encabezado título+subtítulo ("Actividad de esta semana", "Alertas y continuidad"); `Asistentes.jsx` y `Familias.jsx` pasan de `<table className="panel-tabla">` a una grilla de tarjetas (`lista-tarjetas`/`lista-tarjeta` nuevas en `index.css`) con nombre+especialidades (o localidad) como título/subtítulo, resto de columnas como meta y botón "Ver detalle"; buscador/filtros y alta manual sin cambios funcionales. **Vuelta de Guardias/`GuardiasGrid.jsx` (misma sesión, más tarde 2026-07-24)**: a diferencia de Asistentes/Familias, acá el Desarrollador delegó la decisión de detalle ("hace lo que te parezca bien y despues me fijo") en vez de aprobar un plan específico — se optó por no tocar la grilla en sí (no es una tabla sino una matriz con drag-and-drop y su alternativa por teclado ya verificada en la Fase 4, WCAG 2.5.7) y envolver solo el contexto alrededor: filtros agrupados en una tarjeta (`panel-filtros-tarjeta`, nueva en `index.css`), encabezado de sección título+subtítulo reutilizando el patrón `dashboard-seccion` ya existente ("Grilla de guardias" / texto que explica el drag-and-drop y la alternativa de abrir el detalle), y la grilla envuelta en una tarjeta (`panel-guardias-grid-tarjeta`). Cero cambios de lógica en `GuardiasGrid.jsx` ni `GuardiaAcciones.jsx`. Sin tocar paleta/tipografía de `DESIGN_SYSTEM.md` en ningún corte — es reorganización, no rediseño visual | 2026-07-24 | Claude Code | Primer corte (Dashboard+Asistentes+Familias): verificado en navegador real (Playwright) contra Prestadora Demo, primero en local (dev server: 38 Asistentes, 17 Familias) y después contra producción tras el deploy. Commiteado (`9dc47ae`), pusheado a `origin/main` y desplegado a Vercel (`https://aurevia-panel.vercel.app`, deploy `dpl_8MC4cTaT98ppT4Y5cYNxs4UL5C2v`). Vuelta de Guardias: `npm run build` y `npm run lint` (oxlint) del Panel limpios; verificado en navegador real (Playwright) contra Prestadora Demo — grilla con datos reales (rango 2026-07-13/2026-07-22, único con guardias cargadas) renderiza dentro de la tarjeta nueva con el encabezado de sección, y el click sobre una guardia sigue abriendo correctamente el modal "Detalle de guardia" (Fecha/Horario/Asistente/Paciente/Modalidad/Estado) sin errores de consola. Commiteado (`10987cc`), pusheado a `origin/main` y desplegado a Vercel (`https://aurevia-panel.vercel.app`, deploy `dpl_17kDXZBx4A8f1Z3rHWARH4hKietK`), confirmado con `curl` real que responde 200 | 🟢 Resuelto 2026-07-24 — primer corte (Dashboard+Asistentes+Familias) y vuelta de Guardias, ambos commiteados/pusheados/desplegados a producción |
| 82 | **Fase 11 del plan de rediseño de frontend — WhatsApp reforzado para alertas críticas**, construida y verificada 2026-07-23 (ver `docs/PROGRESS.md`). Migración `schema_whatsapp_familia_asistente_01.sql` agrega `notificar_familia` a `configuracion_notificaciones` y siembra filas de configuración (antes inexistentes para las prestadoras reales) para `alerta_temprana_guardia`, `incidente_relevo_sin_resolver` y el evento nuevo `aviso_rutina_asistente`. Dos patrones distintos según el plan: (a) avisos de rutina a la Asistente (guardia asignada, mensaje del coordinador, recordatorio de guardia próxima) siguen yendo solo por push, con WhatsApp de respaldo únicamente si el push falla (`revisarRecordatoriosPush.js`, requirió que `enviarPush()` devuelva `true`/`false`); (b) alerta crítica de "Ausente sin relevo previo" ahora manda push + WhatsApp a la vez a la Familia (`revisarNotificacionesCoordinador.js`), condicionado a un toggle `notificar_familia` por Prestadora, apagado por defecto — decisión explícita del Desarrollador de que la política de avisar o no a la Familia es de cada Prestadora, no del sistema. Nueva columna y checkbox condicional (solo visible en la fila de `incidente_relevo_sin_resolver`) en la pestaña Notificaciones de `Configuracion.jsx`, con i18n completo es-AR/en/pt-BR. **Recorte de alcance no re-confirmado explícitamente con el Desarrollador**: el plan de control de características presentado mencionaba también una columna `notificar_asistente`, descartada durante la implementación por no tener ninguna función concreta que la consumiera todavía (regla de "no construir para hipotéticos") — se avisa acá porque no se comunicó como mensaje aparte en su momento, solo quedó reflejado en el código/commit | 2026-07-23 | Claude Code | Verificado en navegador real (Playwright) contra Prestadora Demo en producción: columna y checkbox condicional confirmados en los 3 locales (es-AR/en/pt-BR); guardado real de `notificar_familia=true` contra la base en vivo, confirmado por API, revertido a `false` al terminar para no dejar un toggle real activado sobre datos de prueba. `npm run build` del Panel limpio (el lint real del proyecto es `oxlint`, no `eslint` — el paquete no tiene `eslint.config.js`) | 🟢 Resuelto 2026-07-23 — verificación end-to-end completa (migración aplicada en Supabase real, backend en Railway, Panel en Vercel, los 3 locales); pendiente decisión del Desarrollador sobre si agregar `notificar_asistente` más adelante |
| 81 | **Fase 9 del plan de rediseño de frontend — Offline-first en PWA Asistentes**, construida y verificada 2026-07-23 (ver `docs/PROGRESS.md`). Cola local en IndexedDB (`pwa-asistentes/src/lib/colaOffline.js`, sin librería nueva) con reintento automático al volver la señal (`online`/`visibilitychange`, sin Background Sync API por no estar soportada en Safari/iOS) vía `pwa-asistentes/src/lib/sincronizarCola.js`; identificador `clienteUuid` generado en el dispositivo para evitar duplicados al sincronizar, reutilizando el guard de idempotencia ya existente en el backend (`backend/src/routes/appAsistentes.js`, checkin/reporte con `checkin_at`/`checkout_at` ya seteados) al que se sumó el flag `yaRegistrado: true` para que el cliente distinga "ya sincronizado antes" de un error real; estado visual explícito (ícono ⏳ + texto, nunca solo color) en `GuardiaActiva.jsx`, `ReporteDiario.jsx` y `MisGuardias.jsx`. Agregado no pedido en el plan pero necesario para que el flujo tenga sentido offline: botón "Completar a mano" en Reporte Diario cuando no hay señal, ya que "Analizar con IA" requiere conexión y sin este atajo el Asistente quedaría sin forma de llegar a la pantalla de confirmación estando offline | 2026-07-23 | Claude Code | Verificado en navegador real (Playwright) contra Prestadora Demo simulando corte de señal a nivel de `fetch`: check-in offline se encoló, mostró el estado pendiente y sincronizó solo al simular reconexión, con `checkin_at` confirmado por SQL directo contra Supabase; Reporte Diario offline deshabilitó IA y foto con texto explicativo, permitió completar a mano, encoló la confirmación y mostró la insignia "pendiente de enviar" en `MisGuardias.jsx`, que desapareció sola al reconectar — confirmado por SQL que el reporte quedó insertado y la guardia pasó a `estado='completada'` con `checkout_at` seteado. 0 errores de consola nuevos (2 warnings preexistentes de React Router, no relacionados) | 🟢 Resuelto 2026-07-23 — verificación end-to-end completa (cliente + backend + base de datos real); pendiente commit/push/deploy explícito |
| 80 | **Fase 8 del plan de rediseño de frontend — Onboarding diferenciado por rol para Prestadora nueva**, construida y verificada 2026-07-22/23 (ver `docs/PROGRESS.md`). Checklist de 3 pasos en el Dashboard (Asistente → Familia → invitar equipo), derivado en vivo sin tabla nueva, con variante informativa sin CTA para Admin_plataforma en modo tenant y estados vacíos con CTA único en Asistentes/Familias. `npm run lint`/`npm run build` limpios | 2026-07-22 | Claude Code | Verificado en navegador real contra la Organización Sandbox realmente vacía, con la credencial `superadmin` reseteada vía Admin API de Supabase el 2026-07-23 (la anterior, registrada 2026-07-18, ya no funcionaba) y actualizada en `No hacer commit/claves y contraseñas.txt`: Dashboard muestra "2/3 completados" (Asistente pendiente con CTA "Cargar Asistente" hacia `/asistentes`, Familia y Equipo con badge "Completado" — coincide con los datos reales de Sandbox, que ya tenía Familias y un Coordinador cargados pero 0 Asistentes); `/asistentes` muestra el estado vacío nuevo ("Todavía no cargaste ningún Asistente...") con botón "Nuevo Asistente" en vez del texto genérico. 0 errores de consola nuevos (el único error presente, 403 en `/api/panel/sesion-tenant`, es preexistente y esperado para `superadmin` sin sesión de tenant activa, no relacionado con esta fase) | 🟢 Resuelto 2026-07-23 — verificación visual completa en Sandbox, checklist y CTA de estado vacío confirmados funcionando como se diseñó |
| 79 | **Actualización 2026-07-24**: el Desarrollador definió la estructura de 3 niveles a cargar cuando se definan valores/contenido — nombres provisorios "Básico" (Prestadora chica), "Intermedio" (más volumen, suma funciones sobre Básico) y "Avanzado" (Prestadora grande, todo lo anterior + funciones de mayor sofisticación), más el mecanismo de add-ons ya construido (cualquier función del catálogo, prendida suelta fuera de cualquier plan). **Actualización 2026-07-24 (más tarde)**: cargados precios FICTICIOS a pedido explícito del Desarrollador — `planes`: Básico USD 100, Intermedio USD 200, Avanzado USD 300 (verificado contra Supabase real). `catalogo_modulos` no tenía campo de precio para add-ons; se agregaron columnas `precio_addon`/`moneda_addon` (migración `schema_admin_plataforma_07_precios_placeholder.sql`) y se cargó USD 15 ficticio en los 9 módulos (verificado). Sigue sin definir qué módulo va en qué plan (`plan_modulos` vacío) — eso es el "contenido" que el Desarrollador dijo que se define más adelante. **Estos precios son placeholder, no de venta real** — no usar para facturar de verdad hasta reemplazarlos. **Fase 7 del plan de rediseño de frontend — Panel de Admin_plataforma: estado de pago, uso y riesgo por Prestadora**, construida y verificada 2026-07-22 (ver `docs/PROGRESS.md` de esta fecha). Alcance: `GET /tenants` extendido con `estadoPago` (`vigente`/`vencido`, derivado de `facturas_licencia` sin pagar y vencidas — sin "en reintento" porque no hay pasarela de pago real todavía), `riesgo` (mismo criterio que `estadoPago` por decisión explícita del Desarrollador) y `uso` (conteos reales de Asistentes/Pacientes/usuarios del Panel, sin tope de plan — decisión explícita del Desarrollador, "solo mostrar los números"); endpoints nuevos `GET /tenants/:id/plan-preview/:planId` (qué funciones se ganan/pierden) y `PATCH /tenants/:id/plan` (aplica el cambio, sincronizando `prestadora_modulos` — las funciones `origen='plan'` que ya no vienen incluidas se desactivan, las nuevas se activan, los `addon` no se tocan). Frontend: columna "Pago" en la tabla de Licenciatarias, y el modal de detalle (renombrado interno a `TenantDetalleModal`) ahora también muestra alerta de riesgo, sección "Uso real" y la sección "Cambiar de plan" (selector + "Ver cambios" + confirmación explícita vía `useConfirmarDestructivo` antes de aplicar). i18n completo en es-AR/en/pt-BR. **Hallazgo real durante la verificación, no bloqueante para esta fase pero sí para uso real**: las tablas `planes`/`plan_modulos`/`prestadora_planes`/`prestadora_modulos`/`facturas_licencia` (creadas 2026-07-18) están vacías en producción — no existe ningún plan real cargado todavía, así que hoy ningún Admin_plataforma real podría asignarle un plan a una Prestadora real. Verificado con datos de prueba temporales (2 planes ficticios, 1 factura vencida ficticia sobre Prestadora Demo) vía Playwright: columna "Pago" mostró "Vencido"/"Al día" correctamente, alerta de riesgo visible, "Uso real" con conteos reales (44 Asistentes/18 Pacientes/2 usuarios de Panel), preview de cambio de plan mostró correctamente "Funciones que se ganan: Verificación de Guardias, Facturación y pagos", diálogo de confirmación disparado y aceptado, cambio aplicado y verificado en la UI (plan actualizado, módulos sincronizados sin tocar el addon "Comunicación"). 0 errores de consola nuevos (los 3 errores de consola presentes son preexistentes, `404` en `/api/configuracion-publica`, no relacionados). Datos de prueba y cuenta `admin_plataforma` de prueba borrados sin residuos, verificado por conteo en 0 | 2026-07-22 | Claude Code (implementación y verificación), Desarrollador (decisión: cargar el catálogo real de planes de Xeitra cuando corresponda) | — (cerrado, ver Estado) | 🟢 Resuelto 2026-07-25 — Fase 7 construida, verificada y desplegada: commit `17da950` confirmado ancestro de `main` HEAD (`git merge-base --is-ancestor`) y sus 3 archivos (`panelAdminPlataforma.js`, `AdminPlataforma.jsx`, `translations.js`, `index.css`) ya en producción (Panel en Vercel, backend en Railway, ambos redesplegados en esta sesión). **Decisión explícita del Desarrollador (2026-07-25, `AskUserQuestion`)**: no cargar el catálogo real de planes ahora — se cierra #79 reconociendo la función como construida/verificada/desplegada con datos ficticios, y la carga del catálogo real de planes/módulos queda como pendiente nuevo y separado, ver #93 |
| 72 | El Desarrollador confirmó (2026-07-21), a partir de un hallazgo durante la verificación multi-idioma de la Fase 1, que el cálculo de cese (`panel/src/lib/calcularCese.js`) debe ser **customizable según la legislación local de la jurisdicción donde opera cada Prestadora licenciataria** — no es solo un problema de textos sin traducir. Relevamiento hecho: (1) `escalas_legales` es una tabla **global**, sin columna de país ni de `prestadora_id` — hoy todas las Prestadoras del mundo comparten los mismos valores numéricos; (2) `calcularCese.js` no son solo números hardcodeados, la fórmula entera está construida sobre la Ley de Contrato de Trabajo argentina (preaviso, indemnización por antigüedad con tope, integración de mes de despido, agravamiento por embarazo/matrimonio) — otro país no usa esta misma metodología, no alcanza con parametrizar valores; (3) ya existen documentos legales por país en `docs/legal/` (20 países) pero solo cubren advertencias de riesgo de subordinación (art. 23 LCT y equivalentes, alcance §3 de CLAUDE.md), ninguno describe todavía fórmulas de cálculo de indemnización; (4) la pantalla `VinculoCeseTab.jsx` es genérica, no debería necesitar cambios grandes si la lógica de cálculo se vuelve conectable por jurisdicción. Efecto colateral: el hallazgo original de traducción (etiquetas del desglose y mensajes de advertencia de `calcularCese.js` sin traducir a en/pt-BR) queda subsumido acá — no tiene sentido traducir mensajes que van a reescribirse al rediseñar el módulo. **Resuelto 2026-07-27**: el Desarrollador retomó la tarea con directiva explícita — "nada debe estar harcodeado... Tiene que haber una formula por cada pais donde se venda la licencia", acotado a "coloquemos en variables todos los valores, formula incluida, que indica la legislacion argentina, y cuando se venda en otro pais habra que investigar su legislacion" (parametrizar completamente Argentina ahora, sin construir de antemano fórmulas de países sin venta). Plan presentado y aprobado (`CLAUDE.md` §11) antes de tocar código, incluida una decisión explícita del Desarrollador vía `AskUserQuestion` sobre RLS de `escalas_legales` ("Sí, solo superadmin"). Implementado: `escalas_legales` ganó columna `jurisdiccion` (backfill `'AR'`, RLS endurecida a solo `es_superadmin()` para escritura); tabla nueva `formulas_cese` (jurisdicción, causal, `definicion` JSONB con vocabulario cerrado de pasos + combinar, vigencia por fecha, mismo patrón "sin fila = sin cálculo automático, nunca aproximar con la fórmula de otro país" que `advertencias_legales`, `CLAUDE.md` §3), sembrada con las 9 causales de Argentina (renuncia, mutuo_acuerdo, periodo_de_prueba, despido_sin_causa, despido_por_embarazo_o_matrimonio, despido_con_justa_causa, abandono_de_trabajo, muerte_del_trabajador, muerte_del_empleador) con exactamente los mismos valores que antes vivían en el `switch(causal)` de JS. `calcularCese.js` reescrito de switch a intérprete genérico de pasos (`panel/src/lib/calcularCese.js`), sin ninguna causal ni país hardcodeado en la lógica; `escalasLegales.js` extendido con `resolverFormulasVigentes`/`obtenerFormulaVigente`; hooks `useEscalasLegales`/`useFormulasCese` filtran por la jurisdicción real de la Prestadora activa (`prestadoras.pais`); `VinculoCeseTab.jsx`/`SimuladorVinculoTab.jsx` actualizados para resolver y pasar esa jurisdicción. Sección nueva "Fórmula de cálculo de cese" agregada a `docs/legal/argentina.md` documentando en lenguaje humano la estructura. Migración aplicada contra Supabase real (`apply_migration`), RLS verificada por consulta directa a `pg_policies` (coincide exactamente con lo diseñado) y `get_advisors` (sin hallazgos nuevos atribuibles a esta migración). Test suite (`calcularCese.test.js`, `FORMULAS_FIXTURE` nueva) verificado 1:1 con los mismos valores numéricos de antes del refactor, más un caso nuevo de jurisdicción sin fórmula (`BR`) confirmando fail-closed (`requiereRevisionAbogado: true`, sin monto) | 2026-07-21 (creado) / 2026-07-27 (resuelto) | Desarrollador (prioridad, decisión de arquitectura, RLS) + Claude Code (plan e implementación) | — (cerrado, ver Estado) | 🟢 Resuelto 2026-07-27 — diseño parametrizado por jurisdicción construido, migrado a Supabase real y verificado a nivel de test unitario. Commiteado (`fc2b3de`), pusheado a `origin/main`, Panel desplegado explícitamente a Vercel (`vercel --prod`, deploy `dpl_HbsMiujgbTvrp4JqCr2yiJcDVowB`, alias `https://aurevia-panel.vercel.app` confirmado con `curl` real → 200). Pendiente: verificación funcional manual en Sandbox de varias causales (incluyendo `compone_sobre_causal` para embarazo/matrimonio y `mitad_de_componente` para causales de muerte) |
| 70 | El Desarrollador definió, durante el relevamiento de UX de las 3 apps (2026-07-21), un requisito nuevo para la futura página pública de cada Prestadora: no puede ser una landing fija, porque cada Prestadora ofrece servicios distintos (y puede sumar otros con el tiempo). Se necesita un constructor de páginas tipo CMS con editor visual (mismo concepto que WordPress+Elementor, Wix, Squarespace o Webflow), como producto separado de Xeitra — **fuera de Aurevia**, no una funcionalidad más del Panel — al que Aurevia se conecta por API para traer los datos de cada Prestadora (servicios que ofrece, textos, imágenes) y armar su página pública. Confirmado explícitamente por el Desarrollador como trabajo "para después", no para esta etapa | 2026-07-21 | Desarrollador (alcance) + Claude Code (diseño e implementación cuando se priorice) | Existe un documento de diseño (inventario de qué debe poder configurar cada Prestadora + decisión de arquitectura: producto propio de Xeitra vs. adoptar una herramienta existente vía API/embeds) presentado y aprobado por el Desarrollador **antes** de escribir código — coherente con CLAUDE.md §11 (cambio de arquitectura grande: nuevo producto de Xeitra) | 🔴 Abierto — post-MVP, explícitamente diferido |
| 71 | Encontrado durante la verificación en navegador de la Fase 1 del rediseño de frontend (2026-07-21): la página "Plantel de Asistentes" del Panel (`/asistentes`) no carga ningún dato — Postgres devuelve `infinite recursion detected in policy for relation "asistentes"`. Causa raíz identificada por consulta directa a `pg_policies`: referencia circular entre políticas RLS — `asistentes.familia_ve_asistente_asignado` hace `JOIN` contra `guardias`, y `guardias.coordinador_gestiona_guardias_de_su_zona` hace `JOIN` contra `asistentes`; al evaluar una se dispara la otra indefinidamente. Bug preexistente, no introducido por la Fase 1 — bloqueaba además la verificación visual de `VinculoCeseTab.jsx` (pestaña de Cese dentro de la ficha de Asistente) | 2026-07-21 | Desarrollador (prioridad, dio luz verde con "continua como tu criterio te indique") + Claude Code (fix) | Se reescribe una de las dos políticas para romper el ciclo (ej. resolver la pertenencia a zona del Asistente con una función `SECURITY DEFINER` en vez de un `JOIN` directo contra la tabla con RLS), se aplica contra Supabase, y se confirma en navegador que `/asistentes` carga la lista y que `VinculoCeseTab.jsx` es alcanzable y funciona | 🟢 Resuelto 2026-07-21 — nueva función `zonas_de_asistente()` (`SECURITY DEFINER`, mismo patrón que `current_tenant()`/`es_superadmin()`) reemplaza el `JOIN` directo en la policy `coordinador_gestiona_guardias_de_su_zona` de `guardias`. SQL en `backend/src/db/schema_fix_recursion_rls_asistentes_guardias.sql`, aplicado contra Supabase real vía migración `fix_recursion_rls_asistentes_guardias` (con `NOTIFY pgrst, 'reload schema'`). Verificado en navegador: `/asistentes` carga ~40 asistentes DEMO sin error; `VinculoCeseTab.jsx` alcanzable y su "Ver desglose del cálculo" renderiza una lista legible en vez de JSON crudo, cerrando también la última verificación pendiente de la Fase 1. Revisado con `get_advisors` (seguridad): la función queda ejecutable por roles `anon`/`authenticated` vía RPC, mismo patrón ya aceptado en el proyecto para `current_tenant()`/`es_superadmin()`/`tiene_permiso()` — no introduce una categoría de riesgo nueva, solo devuelve el array de zonas (dato no sensible), nunca datos de Paciente/Familia. Sin commit/push todavía — pendiente de que el Desarrollador lo pida explícitamente (ver nota abajo) |
| 69 | La PWA de Familias (Etapa 4, `pwa-familias`) no tiene iconos PNG reales para "Agregar a pantalla de inicio" — hoy solo hereda el SVG favicon del scaffold de Vite, mismo hueco ya conocido y abierto en `pwa-asistentes` (pendiente #64) | 2026-07-21 | Claude Code / Desarrollador | Generar y agregar el set de iconos PNG (192x192, 512x512, maskable) con el branding de Aurevia para `pwa-familias`, referenciados desde `vite.config.js`/`manifest` — idealmente junto con el mismo trabajo pendiente #64 de Asistentes, para no repetir el proceso dos veces | 🟢 Resuelto 2026-07-24 — generado a partir del logo real de Aurevia (`favicon.svg`, el mismo que usa `panel`), rasterizado a `icon-192.png`/`icon-512.png` (logo centrado sobre fondo blanco) e `icon-maskable-192.png`/`icon-maskable-512.png` (logo más chico, dentro de la zona segura del 40% que exige la máscara de Android/iOS) con `sharp`, agregados a `pwa-familias/public/` y referenciados en el manifest de `vite.config.js`. `npm run build` sin errores, `dist/manifest.webmanifest` confirmado con los 4 íconos nuevos. Resuelto junto con #64 en la misma sesión |
| 68 | `pwa-asistentes` no tiene `vercel.json` con rewrite a `index.html` — la navegación directa a cualquier ruta que no sea `/` (ej. `https://pwa-asistentes.vercel.app/perfil`, o recargar la página estando en `/guardias`) devuelve 404 de Vercel en vez de servir la SPA. Detectado verificando de punta a punta el flujo de notificaciones push de esta sesión; la navegación interna (clicks en los links del nav) funciona bien, solo falla la navegación directa por URL o el refresh del navegador | 2026-07-20 | Claude Code / Desarrollador | Agregar `pwa-asistentes/vercel.json` con un rewrite catch-all a `/index.html` (mismo patrón que debería tener `panel/` si no lo tiene ya) y redeployar; verificar que refrescar la página en `/perfil` o `/guardias/:id` ya no da 404 | 🟢 Resuelto 2026-07-24 — re-detectado de forma independiente durante la verificación en producción del pendiente #75 (el link de activación por email caía en esta misma ruta directa), sin haber cruzado antes con esta fila ya abierta desde el 2026-07-20. Agregado `pwa-asistentes/vercel.json` con el mismo rewrite catch-all que ya tenía `pwa-familias` (commit `2d3418f`), redesplegado a Vercel, y confirmado con `curl` real contra producción que `/activar-cuenta` y `/login` devuelven 200 en vez de 404 |
| 67 | Notificaciones push a Asistentes (Web Push API + VAPID) verificadas de punta a punta contra producción real: suscripción persistida en `push_subscriptions` (RLS `asistente_id = auth.uid()`), cron `revisarRecordatoriosPush.js` toma un mensaje de prueba insertado en `mensajes_asistente` y lo marca `push_enviado_at` | 2026-07-20 | Claude Code | Confirmar que `push_enviado_at` se pobló tras la corrida del cron y, si es posible, que no quedó ningún error de `web-push` en los logs de Railway para esa suscripción | 🟢 Resuelto 2026-07-20 — Monitor confirmó `push_enviado_at=2026-07-21T00:43:29.524+00:00` sobre la fila de prueba `mensajes_asistente` (id `72c4acec-ee65-4d18-b1e5-5f200359946d`) tras la corrida real del cron en producción (Railway). `enviarPushAsistente()` (`backend/src/utils/push.js:44-49`) solo borra la suscripción si `webpush.sendNotification()` devuelve 404/410 — se confirmó por SQL directo que la suscripción de la cuenta de prueba ("DEMO — Claudia García", endpoint FCM real) sigue existiendo en `push_subscriptions` después de la corrida, es decir que el envío no falló con esos códigos. `npx railway logs` solo mostró el log de arranque del contenedor, sin línea de error de `web-push` para esta suscripción (el código solo hace `console.error` en caso de fallo, así que ausencia de esa línea es consistente con envío exitoso) |
| 66 | `docs/PROGRESS.md:396` (entrada histórica de una sesión anterior) menciona `https://aurevia-backend-production.up.railway.app` como URL vigente del backend — esa URL ya no existe (`404 Application not found`). La URL real de producción es `https://aurevia-backend-production-e7ee.up.railway.app` (confirmado con `npx railway status` y con el bundle ya desplegado del Panel). Esta confusión causó que el primer deploy de `pwa-asistentes` a Vercel apuntara a la URL vieja y fallara por CORS hasta corregirlo | 2026-07-20 | Claude Code | Ninguna corrección al registro histórico (se deja intacto por integridad de `PROGRESS.md`) — este pendiente sirve como advertencia para no repetir el error: cualquier sesión futura que necesite la URL del backend de Railway debe confirmarla con `npx railway status` o extrayéndola del bundle ya desplegado, nunca copiarla de una mención vieja en `PROGRESS.md` | 🟢 Resuelto para esta sesión (deploy corregido); queda como nota preventiva para el futuro |
| 65 | `backend/.env` local no tiene `ANTHROPIC_API_KEY` configurada — no se pudo verificar en esta sesión la calidad real del mapeo de IA de `reporteIA.js` (Etapa 3, PWA Asistentes), solo su camino de fallback seguro (vuelca el texto libre en `observaciones`). Mismo hueco ya conocido y documentado para `importacionIA.js`/`iaWhatsapp.js` — presumiblemente la key sí está configurada en el backend desplegado de producción, pero eso no se verificó desde esta sesión | 2026-07-20 | Claude Code / Desarrollador | Confirmar que `ANTHROPIC_API_KEY` está cargada en el entorno de producción (Railway) y, si se quiere verificar localmente, cargarla también en `backend/.env` local | 🟢 Resuelto 2026-07-24 — se confirmó que la key **no** estaba cargada en Railway (`railway variables | grep ANTHROPIC` sin resultados, contradiciendo la presunción anterior). El Desarrollador creó una cuenta y API Key nueva en console.anthropic.com y se cargó vía `railway variables --set ANTHROPIC_API_KEY=...`. Redeploy automático confirmado con `GET /health` real contra Railway (`{"status":"ok"}`) tras el cambio. Pendiente aparte derivado de esta gestión: registrar consumo de IA por Prestadora para poder facturarlo — ver pendiente #84 |
| 64 | La PWA de Asistentes (Etapa 3) no tiene iconos PNG reales para "Agregar a pantalla de inicio" — hoy solo hereda el SVG favicon del scaffold de Vite, lo que degrada la calidad del ícono al instalar la PWA en el celular | 2026-07-20 | Claude Code / Desarrollador | Generar y agregar el set de iconos PNG (192x192, 512x512, maskable) con el branding de Aurevia, referenciados desde `vite.config.js`/`manifest` | 🟢 Resuelto 2026-07-24 — mismo trabajo que #69, ver ese pendiente para el detalle completo (`pwa-asistentes` usa el mismo logo/proceso que `pwa-familias`, `npm run build` sin errores) |
| 63 | `metros_tolerancia_checkin` (tolerancia de distancia GPS para el check-in de Asistentes, Etapa 3) no tiene pantalla propia en el Panel — hoy solo se puede editar por SQL directo contra Supabase, lo que viola la regla de "Configuración sobre programación" (§2 de CLAUDE.md) para esta variable en particular | 2026-07-20 | Claude Code / Desarrollador | Agregar el campo a una pantalla de Configuración del Panel (`Configuracion.jsx`), editable por `admin_prestadora`, sin tocar SQL a mano | 🟢 Resuelto 2026-07-24 — nueva sección "Ausencia automática por falta de check-in GPS" en la pestaña Servicios de `Configuracion.jsx`, con los 3 campos de `configuracion_ausencia_automatica` (activo, minutos y metros de tolerancia), endpoints `GET/PATCH /api/panel/configuracion/ausencia-automatica` (mismo patrón de scoping por prestadora que el resto de `panelConfiguracion.js`). **Hallazgo de esta sesión**: la columna `metros_tolerancia_checkin` ya existía en Supabase real (usada en `appAsistentes.js` desde 2026-07-20) pero no estaba documentada en ningún archivo `.sql` del repo — se agregó `schema_modulo6_guardias_04_metros_tolerancia.sql` (`ADD COLUMN IF NOT EXISTS`, sin tocar el dato real ya cargado) para que el repo refleje el estado real. Textos nuevos en `es-AR`/`en`/`pt-BR`. `npm run build` del panel sin errores. **Verificado en navegador real** contra la Prestadora Demo (cuenta de prueba `admin_prestadora`, `aurevia.test.acount@gmail.com`): la sección carga los 3 valores reales guardados en Supabase (activo=true, 15 minutos, 150 metros), se cambió metros a 200, se guardó sin error de consola, se recargó la página completa (F5) y el valor 200 persistió — confirma que el guardado es real contra la base y no solo estado local del navegador. Se volvió a dejar en 150 (valor original) y se guardó de nuevo para no alterar los datos de la cuenta de prueba. Aún pendiente: commit + push + deploy a Vercel (no hecho todavía, requiere confirmación del Desarrollador) |
| 61 | **Resuelto 2026-07-25 con un rediseño en capas, más amplio que la decisión binaria original.** El Desarrollador, en vez de elegir entre "conexión externa" o "conforme con solo archivo", aprobó un diseño propio de varias capas ("esta cebolla tiene varias capas..."), con la premisa explícita de que nunca haya conexión directa a un sistema externo ("archivo exportado de donde sea, nunca conexión directa a cualquier otro sistema que no conocemos, evitemos los problemas"). **Capa 1 (formato conocido, sin IA)**: cualquier formato que `xlsx` ya sepa leer (Excel/CSV/ODS/XML SpreadsheetML/DIF/SYLK/DBF, autodetectado por firma de archivo, sin código nuevo) más un parser nuevo de dump SQL estándar (`INSERT INTO tabla (cols) VALUES (...);`, `backend/src/utils/importacionIA.js` función `intentarParsearSQL`). **Capa 2 (juicio de viabilidad de IA)**: para cualquier archivo que no calce con la Capa 1, una llamada a Claude (`evaluarViabilidadIA`, mismo archivo) decide si puede interpretarse como tabla con certeza razonable; si no, rechaza la importación con motivo explícito — nunca inventa una estructura de columnas. **Capa 3 (ya existente)**: paso de revisión de mapeo humano (`/analizar` → `/confirmar`) sin cambios de fondo. **Capa 4 (freno humano nuevo, corregido a pedido del Desarrollador tras mi primera propuesta — "que sentido tiene tener un filtro de conformidad si los datos pasan de largo antes de obtenerla?")**: las filas creadas por importación quedan **ocultas e inoperables** desde el instante de creación (`pendiente_conformidad=true`, oculto vía política RLS `RESTRICTIVE` `oculta_pendientes_de_conformidad` en `asistentes`/`familias`/`pacientes`, único punto de verdad sobre las ~5 políticas `PERMISSIVE` existentes de cada tabla, `CLAUDE.md` §7 regla 12) hasta que la Prestadora conforma explícitamente el resultado real ya creado (`GET /revision/:id` → `POST /conformar/:id`, endpoint nuevo en `backend/src/routes/panelImportacion.js`, pantalla "paso 4" nueva en `panel/src/pages/Importacion.jsx`) o lo rechaza (`POST /rechazar/:id`, revierte el lote entero reutilizando el patrón existente de rollback de `crearCuentaConPerfil`/`borrarCuenta`). **Capa 5 (trazabilidad interna)**: columna `importacion_id` en las 3 tablas, nunca mostrada a la Prestadora ("el usuario no necesita siquiera enterarse de que existe"), solo para poder revertir un lote completo. **Capa 6 (legal/T&C)**: explícitamente diferida — queda para cuando el Desarrollador aborde los Términos y Condiciones del SaaS con asesoría legal propia, no se redactó nada acá. Migración `importacion_conformidad_01` aplicada contra Supabase real (`NOTIFY pgrst` incluido), políticas confirmadas por `pg_policies` en vivo. Verificado con una prueba funcional real de punta a punta (creación de fila pendiente → oculta → conformada visible → simulación de rechazo → revertida sin residuo), script temporal borrado sin dejar rastro en el repo. `node --check` y `npx vite build` del Panel sin errores. i18n agregado en los 3 locales (es-AR/en/pt-BR) | 2026-07-19 (creado), 2026-07-25 (rediseño aprobado e implementado) | Claude Code (diseño de detalle, implementación, verificación) / Desarrollador (aprobó el rediseño en capas, corrigió el punto de la Capa 4) | — (cerrado, ver Estado) | 🟢 Resuelto 2026-07-25 — pendiente de commit/push y deploy explícito a Railway/Vercel en esta misma sesión, ver `docs/PROGRESS.md` |
| 62 | **La importación masiva de Familias no admite `medicacion_habitual` del Paciente — resuelto 2026-07-25 con un alcance más amplio que el original.** Al discutir el cierre, el Desarrollador definió que la medicación no debe cargarse por importación masiva en absoluto (está en juego la salud de un Paciente) y requiere resguardos reales: qué remedio, dosis, frecuencia, vía, si el Asistente asignado está habilitado para administrarla, y vigencia. Diseño aprobado (`ExitPlanMode`) e implementado en un solo corte, las 4 piezas juntas para que el freno legal esté activo desde el día uno: (1) **PWA Familias** — pantalla "Medicación" para solicitar indicaciones (`appFamiliasMedicacion.js`); (2) **Panel** — cola de revisión aceptar/rechazar (`Medicacion.jsx` + `panelMedicacion.js`), tab "Habilitaciones" en la ficha de Asistente (`HabilitacionesTab.jsx`) y tab "Habilitación de medicación" en Configuración (`TabHabilitacionMedicacion`) para mapear vía→habilitación requerida por Prestadora; (3) **PWA Asistentes** — sección de solo lectura "Órdenes de medicación" en la guardia activa, filtrada para nunca mostrar una vía que el Asistente no esté habilitado a administrar (`appAsistentesMedicacion.js`). 3 tablas nuevas (`indicaciones_medicacion`, `habilitaciones_asistente`, `configuracion_habilitacion_via_medicacion`) con RLS, bucket privado `prescripciones-medicacion`, y advertencia legal seedeada (`medicacion_via_sin_habilitacion`, ver `docs/legal/argentina.md`) que avisa sin bloquear si se acepta una indicación sin Asistente habilitado — principio "advertir, nunca bloquear" de `CLAUDE.md` §3. `pacientes.medicacion_habitual` queda deprecado (no se borra el histórico, ver `docs/DATA_MODEL.md`); toda lectura de "medicación habitual" en el resto de la UI pasa a derivarse de `indicaciones_medicacion` (estado=`aceptada`, vigente hoy) vía punto único de verdad `backend/src/utils/medicacionIndicaciones.js`. Glosario (`CLAUDE.md` §4) actualizado con "Indicación de medicación" y "Habilitación". i18n completo en los 3 locales (Panel y PWA Asistentes) | 2026-07-19 | Claude Code / Desarrollador (aprobó el plan vía ExitPlanMode) | — (cerrado, ver Estado) | 🟢 Resuelto 2026-07-25 — RLS confirmada en vivo: `pg_policies` muestra las 3 tablas nuevas con `admin_prestadora` `ALL`/`coordinador` `SELECT` scopeados por `prestadora_id = current_tenant()` (o vía join a `asistentes.prestadora_id` en `habilitaciones_asistente`), y `pg_class.relrowsecurity=true` en las 3. Verificación funcional end-to-end contra Sandbox con datos de prueba reales (auth.users + usuarios + familia + paciente + 2 Asistentes, uno con habilitación `enfermero_matriculado` vigente y otro sin ella, ambos con guardia futura asignada al mismo Paciente): se insertó una indicación `pendiente` (vía `inyectable`) replicando exactamente la query de `POST /api/app-familias/medicacion/:pacienteId`; se confirmó que aparece en la cola replicando la query de `GET /api/panel/medicacion/pendientes`; se confirmó que `hayAsistenteAsignadoHabilitado` da `true` (uno de los 2 asignados tiene la habilitación) por lo que `sinHabilitacion` sería `false` y el Panel no dispararía la advertencia en este caso — y se confirmó por separado que si solo el Asistente sin habilitación estuviera asignado, `sinHabilitacion` sería `true` (0 filas en `habilitaciones_asistente` para ese Asistente+tipo); se aceptó la indicación (`estado='aceptada'`); se replicó `GET /api/app-asistentes/medicacion/:pacienteId` para ambos Asistentes: el habilitado ve la orden, el no habilitado la tiene filtrada (0 órdenes) — exactamente el comportamiento diseñado de "nunca aparece ni de lectura para quien no puede administrarla". Limpieza completa: las 8 tablas tocadas (incluida `auth.users`) confirmadas en 0 filas residuales tras el borrado. Commit `e8b124b` (`feat: indicaciones de medicacion...`) pusheado a `origin/main`; Railway redeployó el backend automáticamente (`GET /health` real post-deploy → `{"status":"ok"}`); Panel, PWA Familias y PWA Asistentes desplegados explícitamente a Vercel producción (`npx vercel --prod --yes` en los 3, los 3 con `readyState: READY` y alias de producción actualizado) |
| 1 | `RAILWAY_TOKEN` — crear Project Token en el dashboard de Railway (proyecto `aurevia-backend` → Settings → Tokens, entorno `production`) y cargarlo como secret del repo (`gh secret set RAILWAY_TOKEN`) | 2026-07-09 | Desarrollador (generar el token) + Claude Code (cargarlo) | `gh secret list` muestra `RAILWAY_TOKEN` existente **y** un push de prueba a `main` dispara el workflow con resultado exitoso (no solo que exista el secret — que el deploy real funcione) | 🟢 Resuelto 2026-07-11 — el Desarrollador generó el Project Token en Railway (proyecto `aurevia-backend`, entorno `production`); cargado con `gh secret set RAILWAY_TOKEN` y confirmado con `gh secret list`. Verificado con push real a `main` (commit `f4cda73`): el workflow "Deploy backend a Railway" (run `29159249405`) corrió completo y terminó en éxito en 58s — a diferencia de los dos intentos previos (2026-07-09 y 2026-07-10) que habían fallado, presumiblemente por la falta de este token |
| 2 | Credencial de Supabase (connection string completa, con contraseña) expuesta por error en la salida de un comando de diagnóstico durante `schema_etapa2o.sql` | 2026-07-09 | Desarrollador | Rotada desde el dashboard de Supabase (Project Settings → Database → Reset password), y actualizada en todos los lugares que la usan — **o, alternativamente, decisión explícita y permanente del Desarrollador de no rotar, que cierra este pendiente sin rotación** | 🟢 Cerrado 2026-07-24 — decisión explícita y definitiva del Desarrollador: **no se va a rotar esta credencial, no es un "todavía no" con fecha futura ni condicionado a terminar el proyecto** (a diferencia de como había quedado registrado el 2026-07-11/13/24 más temprano, que sonaba a aplazamiento) — se descarta el pendiente en sí, no solo se pospone. No es una recomendación de seguridad que este documento pueda anular: la credencial expuesta el 2026-07-09 sigue siendo la misma que está en uso hoy, sin rotar, por decisión del Desarrollador |
| 3 | "Panel — tenant en inserts directos" (extender `AuthContext.jsx` + 5 componentes + `DROP DEFAULT` en 8 columnas restantes) | 2026-07-09 | Claude Code | Las 8 columnas sin `DEFAULT`, verificado con insert real que vuelve a fallar sin `prestadora_id` explícito | 🟢 Resuelto 2026-07-11 — `AuthContext.jsx` expone `prestadora_id`; los 6 componentes (`AusenciasCoberturaTab.jsx`, `VinculoCeseTab.jsx`, `ListaPrecioDetalle.jsx`, `PrestacionesPaciente.jsx`, `CertificadoTab.jsx` + `NuevaGuardiaModal.jsx`, agregado por hallazgo durante la investigación) lo setean explícito; `backend/src/db/schema_multitenant_03.sql` aplicado contra Supabase real vía MCP; insert real sin `prestadora_id` contra `certificados` falló con `ERROR 23502` como se esperaba. Ver `docs/DATA_MODEL.md` sección de tenant. |
| 4 | Backup propio en almacenamiento de objetos (bucket), independiente de la copia local en `backend/backups/`. Decisión de proveedor tomada 2026-07-11 tras comparar Cloudflare R2, Backblaze B2, AWS S3 y GCS (durabilidad, antecedentes de caída, y nivel gratuito): **Cloudflare R2 como bucket principal** (10 GB gratis permanente, egress cero, 1M ops Clase A + 10M Clase B/mes gratis) + **Backblaze B2 como espejo secundario** (10 GB gratis permanente, egress libre hasta 3x almacenamiento/mes) — el mismo backup sube a los dos buckets, para que una falla, cambio de política o borrado accidental en uno no deje el proyecto sin respaldo en la nube | Señalado en `SECURITY.md`, reforzado 2026-07-10, proveedores decididos 2026-07-11 | Desarrollador (crear las 2 cuentas/credenciales) + Claude Code (implementar el doble upload) | Backup automático corriendo contra los dos buckets reales (no solo local), verificado con una restauración de prueba desde cada uno | 🟢 Resuelto 2026-07-11 — workflow `.github/workflows/backup-diario.yml` corre `backend/scripts/backup_a_buckets.mjs` (cron diario 03:00 Arg + disparo manual). Dos problemas de credenciales aparecieron y se corrigieron en el camino: (1) `pg_dump` de Ubuntu (v16) no podía volcar el servidor Supabase (v17.6) — se agregó el repo oficial `apt.postgresql.org` e instaló `postgresql-client-17` (`backup-diario.yml:17-25`); (2) tanto el Access Key ID de R2 como la Application Key de B2 habían quedado transcriptos con un carácter de menos (31/32 y 30/31 respectivamente) en una sesión anterior — R2 y B2 no permiten volver a ver una clave ya creada, así que se generaron tokens nuevos (`prestadora-demo-backup-script-v2` en R2, `prestadora-demo-backups-mirror-v2` en B2, ambos acotados a su bucket específico) y se actualizaron `backend/.env`, los secrets de GitHub (`gh secret set`) y `No hacer commit/claves y contraseñas.txt`. Run exitoso verificado: `gh run view 29171951217` → `completed success`, log muestra las 4 etapas (dump, subida R2, subida B2, mensaje final) sin error. Verificado además en la realidad, no solo por el log del script: `ListObjectsV2Command` contra ambos buckets confirma el archivo `prestadora_demo_backup_2026-07-11T23-20-30-347Z.sql.gz` presente en los dos (43557 bytes); se descargó ese archivo de ambos buckets, se confirmó `gzip -t` válido en los dos, y que el contenido descomprimido es un dump real de Postgres (encabezado `-- PostgreSQL database dump`, 63 `CREATE TABLE`) e idéntico byte a byte entre R2 y B2 (`diff` sin salida). **Aclaración honesta (Regla 12.5): esto verifica integridad y restaurabilidad del contenido, no se hizo una restauración completa dentro de una base Postgres viva** — no había un Postgres de prueba disponible en esta sesión para ese paso adicional; si se quiere ese nivel de certeza, falta ese ensayo puntual |
| 5 | `checkout_bloqueado` — aplicar `schema_modulo6_guardias_02.sql` contra Supabase real y verificar | 2026-07-10 | Desarrollador (token) + Claude Code (aplicar) | Constraint aplicado + insert real que viola la excepción confirmado fallando | 🟢 Resuelto 2026-07-11 — constraint `guardias_checkout_bloqueado_requiere_excepcion` aplicado vía MCP (`{"success":true}`). Verificado con insert real dentro de una transacción explícita (creando un Asistente/Paciente de prueba con FK válidas, ya que las tablas estaban vacías): falló con `ERROR 23514` al violar el CHECK como se esperaba; la transacción abortó sin dejar filas residuales (confirmado: `asistentes`/`pacientes`/`guardias` en 0 filas post-verificación). Sigue pendiente el resto del criterio de cierre de la Parte 1 de Módulo 6 (pendiente #6: prueba en navegador) |
| 6 | Prueba en navegador del flujo completo de Guardias Parte 1 (MCP de Playwright) | 2026-07-10 | Claude Code | Flujo completo probado en navegador real, no solo build/lint en verde | 🟢 Resuelto 2026-07-11 — probado contra el Panel real desplegado en Vercel (`https://aurevia-panel.vercel.app`), no local. Se crearon 6 Asistentes reales (cuentas Auth genuinas vía `supabase.auth.admin.createUser`), 30 Pacientes y 38 guardias de prueba cubriendo los 10 escenarios de `backend/scripts/seed_test_guardias.js` (ya eliminado, ver abajo): completada, programada, activa, cancelada (origen familia/prestadora, alcance parcial/total), ausente con relevo (resuelto vía suplente), "Ausente sin relevo previo" (`guardia_saliente_id NULL`, caso de mayor riesgo del glosario), `checkout_bloqueado` sin excepción y con excepción completa. Verificado en el navegador real: (1) login y agenda agrupada por día renderizan correctamente con `rol:'admin_prestadora'`; (2) alta de guardia suelta vía `NuevaGuardiaModal`; (3) alta de guardia recurrente (serie) con checkboxes de días de la semana — generó correctamente las fechas Lunes/Jueves entre `vigente_desde` y `vigente_hasta`; (4) ciclo completo checkpoint de salida → check-in → check-out con cambios de estado correctos; (5) **`checkout_bloqueado=true` sin excepción bloquea el checkout en la UI** — no se renderiza botón de check-out, solo el mensaje de protocolo de continuidad (confirma en la UI lo que el pendiente #5 ya había confirmado a nivel de constraint SQL); (6) cancelación vía UI con diálogo de confirmación nativo (Regla 4) y persistencia correcta tras reload; (7) "Marcar ausente" vía UI con diálogo de confirmación que menciona explícitamente que la Parte 2 (protocolo de continuidad) no está construida. Los dos scripts (`seed_test_guardias.js`, `cleanup_test_guardias.js`) y todos los datos de prueba (38 guardias, 1 serie, 2 incidentes_relevo, 30 pacientes, 6 asistentes + usuarios + cuentas Auth) se borraron al terminar — verificado con consulta post-limpieza: 0 filas restantes en las tres tablas. Dos hallazgos menores, no bloqueantes, agregados como pendientes #11 y #12 abajo |
| 7 | Caso ambiguo de branding: término de negocio mixto con nombre de marca ("Certificado [nombre anterior de la prestadora original]") — ¿parametrizar o genericar? | `PLAN_MULTITENANT_XEITRA.md` sección 5.4bis | Desarrollador (decidir) | Decisión tomada y aplicada | 🟢 Resuelto 2026-07-13 — diseño de 4 capas resuelto 2026-07-12 (ver `docs/PLAN_MULTITENANT_XEITRA.md` sección 5.4bis): (0) interruptor por prestadora para activar/desactivar el sistema de certificación propio; (1) si lo activa, certifica ella misma o designa un tercero como ente calificador; (2) independiente de lo anterior, un Asistente puede traer certificados de otro origen (otra prestadora licenciataria del sistema, o una entidad externa sin verificación propia) que la prestadora actual decide reconocer o no; (3) visibilidad pública por certificado, decidida por la prestadora, en la futura ficha pública del Asistente (Etapa 6, no construida). El 2026-07-13 el Desarrollador confirmó además el nombre definitivo del certificado en sí: **"Certificado de Aptitud"** (reemplaza "Certificado [nombre anterior de la prestadora original]" en todo el proyecto — docs, glosario, i18n de las 3 locales del Panel), por certificar aptitud para una tarea determinada, de uso mayormente interno de la prestadora. Falta todavía: diseño concreto de columnas/tabla en `certificados` (campo de entidad emisora + visibilidad pública), a abordar junto con el Bloque 4 multi-tenant — no implementado, solo diseñado. **"Exclusividad de facturación a la prestadora original" se sacó de este pendiente — es un tema aparte, no de certificación — ver pendiente #19** |
| 8 | Pantalla del Panel para que cada prestadora configure su propia `configuracion_escalada_relevo` (niveles, tiempos de demora, orden de prioridad, mensajes) — **no** son valores que la prestadora original/el Desarrollador definan una sola vez: la tabla ya es por-`prestadora_id` (`schema_modulo6_guardias.sql:319`, policy `admin_gestiona_configuracion_escalada_relevo` acotada al propio `admin_prestadora`), porque cada licenciataria tiene su propia metodología de escalada. Resuelto 2026-07-11 — pestaña "Servicios" agregada al Módulo 8 (Configuración) con CRUD completo: backend en `backend/src/routes/panelConfiguracion.js:82-127` (rutas `GET/POST/PATCH/DELETE /escalada-relevo[/:id]`, mismo patrón de filtrado manual por `prestadora_id` que `/zonas`), UI en `panel/src/pages/Configuracion.jsx` (componentes `TabServicios`/`NuevoNivelEscalada`, tabla con las 4 prioridades como combobox de roles canónicos del glosario — Suplente/Franquero/Personal de emergencia/Familiar — unidas con "→"), i18n en las 3 locales de `panel/src/i18n/translations.js`. Probado en navegador real (Playwright, Panel local en `localhost:5173` logueado como `prestadora.demo@gmail.com`) contra el backend local en `localhost:4000`: estado vacío ("No hay niveles de escalada configurados todavía"), alta de un nivel de prueba (nivel 1, 15 min, Suplente→Franquero, mensaje de prueba) — listó correctamente en la tabla —, borrado con diálogo de confirmación nativo (Regla 4) aceptado, vuelta al estado vacío sin datos residuales. `npm run lint` y `npm run build` sin errores nuevos. Hallazgo no bloqueante durante la prueba: había un proceso `node` huérfano de una sesión anterior (PID 20616, iniciado 8/7) ocupando el puerto 4000 con código viejo sin las rutas nuevas — causaba `Unexpected token '<'` en la pestaña porque el fetch devolvía el 404 HTML de Express en vez de JSON; se mató el proceso y se confirmó que no tenía relación con este pendiente, solo un resto de una sesión anterior no cerrada. No se hizo commit/push de este cambio todavía — pendiente de que el Desarrollador lo pida explícitamente | Conversación de diseño de Guardias, corregido 2026-07-11; resuelto 2026-07-11 | Claude Code (construir la pantalla, parte de la Parte 2 de Módulo 6 — Continuidad de guardia) | Pantalla en el Panel donde un `admin_prestadora` da de alta/edita sus propios niveles de escalada, probada en navegador real | 🟢 Resuelto |
| 9 | Proveedor de WhatsApp Business API. Decisión de fondo ya tomada 2026-07-11: integración directa con Meta Cloud API (no un BSP intermediario), credenciales por prestadora (nunca compartidas, costo lo paga cada licenciataria), con un agente de IA asistiendo en redacción de plantillas, trámite de aprobación ante Meta, y participación en respuestas entrantes — siempre resguardando que la decisión final sea del licenciatario. **Los 5 puntos que quedaban abiertos (A-E) se cerraron el 2026-07-13**, ver detalle y decisiones completas en `docs/PRD_06_WhatsApp_IA.md`: **(A)** si un evento no tiene plantilla aprobada por Meta, se avisa por email y la responsabilidad de actuar pasa al Coordinador — el sistema/IA responden por garantizar que ese aviso le llegue. **(B)** ningún mensaje entrante queda sin respuesta; el Coordinador siempre ve lo que la IA contestó; para casos comunes (configurable por prestadora) la IA puede actuar sola; sin respuesta clara, avisa de inmediato al Coordinador. **(C)** credenciales de Meta por prestadora en Supabase Vault, nunca legibles en texto plano desde el Panel (ni por `superadmin`), acceso solo para el `admin_prestadora` de esa prestadora. **(D)** catálogo inicial arranca con 2 eventos: escalada de relevo y vencimientos de documentación. **(E)** el orden entre insistir al Coordinador 1, pasar al Coordinador 2, y entrar en fase automática depende de la premura de la situación, y ese criterio de premura también es configurable por prestadora | Conversación de diseño de Guardias; ampliado 2026-07-11 y 2026-07-13; puntos A-E cerrados 2026-07-13 | Desarrollador (dar el kickoff de implementación) + Claude Code (implementar y probar en navegador real recién después) | Kickoff de implementación dado por el Desarrollador, código construido y probado en navegador real | 🟢 Resuelto 2026-07-13 — kickoff dado ("dale, empeza a construirlo"), código de backend y Panel construido (schema `backend/src/db/schema_whatsapp_ia_01.sql`, `backend/src/utils/whatsapp.js`, `iaWhatsapp.js`, `revisarNotificacionesCoordinador.js`, rutas nuevas en `panelConfiguracion.js`, webhook `whatsappWebhook.js`, UI en `Configuracion.jsx` con i18n completo en las 3 locales, parity verificada en es-AR/en/pt-BR). Schema aplicado y verificado contra Supabase real (pendiente #22). `npm install` corrido (pendiente #23). Desplegado: `vercel --prod` en `panel/` (Regla 13.1) y `git push` a `main` — commit `034ab3d`, workflow "Deploy backend a Railway" corrió exitoso (`gh run watch 29270946691`, 47s) — antes de este push las 3 rutas nuevas devolvían 404 porque el código nunca se había subido a GitHub, causa raíz identificada y corregida con confirmación explícita del Desarrollador ("si") para el commit/push. Probado en navegador real contra `https://aurevia-panel.vercel.app`: (1) pestaña "WhatsApp + IA" renderiza las 3 secciones sin error de consola; (2) credenciales — guardado y persistencia tras reload confirmados con datos de prueba (`+5491100000000`/`waba-test-123`/`pnid-test-456`); (3) plantillas — alta de una plantilla de prueba (`test_confirmacion_guardia`, categoría utility, texto con variable `{{1}}`) confirmada en la tabla con estado "Borrador", borrado con diálogo de confirmación nativo (Regla 4) aceptado; (4) escalada a Coordinador — cambio de minutos (15→20) y activación de "fase automática" guardados y confirmados tras reload. Datos de prueba limpiados al terminar: plantilla borrada, credenciales vueltas a vacío, escalada reseteada a los valores originales (15/120, fase automática desactivada) — confirmado con reload final que no quedó ningún dato residual. **Explícitamente diferido, no bloqueante para este cierre** (según instrucción del Desarrollador): el envío automático de la fase automática a Asistentes reales y el endurecimiento del webhook para producción (verify token real, firma `X-Hub-Signature-256`, payload real de Meta) quedan para el test con una prestadora real con cuenta Meta activa |

| 11 | Panel — el login (`/login`) no redirige automáticamente al dashboard tras un submit exitoso; la autenticación y la carga de `usuarios` (rol/prestadora_id) funcionan bien (confirmado por red: ambas respuestas 200 con datos correctos), pero la navegación a `/guardias` (o donde corresponda) hay que hacerla manual. Detectado 2026-07-11 probando `https://aurevia-panel.vercel.app/login` con Playwright durante el pendiente #6 | 2026-07-11 | Claude Code (investigar y corregir) | El submit del login redirige solo al dashboard sin navegación manual, verificado en navegador real | 🟢 Resuelto 2026-07-11 — causa raíz: `Login.jsx` llamaba `navigate('/')` apenas `login()` resolvía, pero `AuthContext.jsx` actualiza `session`/`usuario` de forma asíncrona vía el listener `onAuthStateChange` (`AuthContext.jsx:39-46`) — en ese instante `ProtectedRoute.jsx:14` todavía veía el estado viejo y rebotaba a `/login`. Corregido en `panel/src/pages/Login.jsx:18-27`: se agregó un `useEffect` que redirige recién cuando `session` y `usuario` del contexto ya están resueltos, en vez de navegar inmediatamente tras `login()`. Verificado en el Panel real desplegado (`https://aurevia-panel.vercel.app`, deploy `dpl_Ea16LgTENL5A6Veeo3wZEJ7oU4mo`): login con `prestadora.demo@gmail.com` redirige solo a `/` (Dashboard) sin navegación manual |
| 12 | Panel — tras un check-out exitoso (PATCH 204 confirmado), ni el modal de detalle abierto ni la card del listado del día se actualizan visualmente a "Completada"; solo se ve el estado correcto tras recargar la página completa. El dato en la base es correcto (confirmado con reload), es solo un gap de refresco del estado local tras la mutación. Detectado 2026-07-11 durante el pendiente #6 | 2026-07-11 | Claude Code (investigar y corregir) | El detalle y la card reflejan "Completada" inmediatamente después del check-out, sin necesitar recargar | 🟢 Resuelto 2026-07-11 — causa raíz: `GuardiaAcciones.jsx:26-27` (versión original, commit `336d886`) llamaba `onActualizada()` (recarga del listado) sin esperarla y de inmediato `onClose()`, dejando una ventana donde el modal ya se había cerrado pero la recarga del listado todavía no había terminado — visible como estado viejo hasta que esa recarga en curso finalmente terminaba (o, si fallaba silenciosamente, hasta el próximo reload manual). Corregido en `panel/src/pages/guardias/GuardiaAcciones.jsx:17-30`: ahora se hace `await onActualizada()` antes de `onClose()`, garantizando que el listado ya tiene el dato fresco en el momento en que el modal desaparece. Verificado en el Panel real desplegado con una guardia de prueba creada ad-hoc (Asistente/Paciente/guardia vía service role, borrados al terminar — 0 filas restantes confirmado): tras "Registrar llegada" la card pasó a "Activa" sin reload, y tras "Registrar check-out" la card pasó a "Completada" sin reload |
| 13 | **La prestadora original opera directo y marketplace de forma permanente y simultánea — no es una fase que reemplaza a la otra, los dos quedan activos siempre**, porque hay clientes que prefieren cada modalidad. Reclutamiento (PRD_03, todo el Proceso de Incorporación de Asistentes) y control de personal (PRD_02B — vínculo, cese, score de riesgo, ausencias/cobertura) son **infraestructura común a los dos modelos**, no exclusiva del directo — un solo plantel, un solo proceso de verificación, una sola base de Asistentes certificados, sea cual sea el canal por el que después trabajen. Catálogo de funciones de referencia para el diseño del marketplace en sí: ver documento de research de competidores de marketplace (histórico, fuera del repo). **(a) Campo de canal(es) del Asistente — resuelto 2026-07-13**: columnas `canales TEXT[]` (default `['directo','marketplace']`) + `motivo_exclusion_directo`/`motivo_exclusion_marketplace` (NULL mientras el canal esté activo) agregadas a `asistentes` — ver `docs/DATA_MODEL.md` y `backend/src/db/schema_asistentes_canales.sql`. **Falta aplicar ese `.sql` contra Supabase real** (sin MCP conectado en esta sesión, mismo patrón que otros pendientes de esquema). **(b) Si `vínculo`/`cese` aplican igual a un Asistente 100% marketplace — resuelto 2026-07-13**: vínculo/cese queda sin cambios, universal para todos los Asistentes (es "sigue en el plantel certificado", independiente del canal); la "disponibilidad de marketplace" que se buscaba ya la cubre el mecanismo de (a) (sacar/agregar `'marketplace'` de `canales` con su motivo); se agregó además una tabla nueva `calificaciones_asistente` (estrellas de la Familia, puramente informativa, la prestadora solo decide `visible_publica`, nunca edita el contenido ni dispara acción automática sobre el Asistente) — ver `docs/DATA_MODEL.md` y `backend/src/db/schema_calificaciones_asistente.sql`. Ambos `.sql` aplicados y verificados contra Supabase real 2026-07-13 (sesión con MCP de Supabase conectado, instruida desde otra conversación) — no se dio por bueno el `success:true` de `apply_migration` sin más, mismo criterio que exigió cerrar el pendiente #22 (ahí un batch no abortó pese a un INSERT fallido en silencio). **`schema_asistentes_canales.sql`**: `SELECT column_name FROM information_schema.columns WHERE table_name='asistentes' AND column_name IN ('canales','motivo_exclusion_directo','motivo_exclusion_marketplace')` devolvió las 3 filas. La segunda verificación pedida (`SELECT id, canales FROM asistentes LIMIT 3`, para confirmar el `DEFAULT` en una fila real) **no se pudo hacer** — la tabla `asistentes` está vacía en producción (0 filas, mismo estado ya confirmado en el pendiente #24), así que no hay ninguna fila existente donde chequear el default; queda sin verificar hasta que exista al menos un Asistente real. **`schema_calificaciones_asistente.sql`**: tabla `calificaciones_asistente` confirmada en `information_schema.tables`; `SELECT relrowsecurity FROM pg_class WHERE relname='calificaciones_asistente'` → `true`; además se confirmaron las 3 policies exactas por nombre (`familia_crea_su_calificacion`, `prestadora_ve_calificaciones`, `admin_prestadora_actualiza_visibilidad`) vía `pg_policies`, verificación no pedida explícitamente pero agregada para no confiar solo en el comentario del archivo | 2026-07-11 | Desarrollador o sesión con MCP de Supabase conectado (aplicar los 2 `.sql`) | Las 2 tablas/columnas verificadas en Supabase real (no solo en el repo) | 🟢 Resuelto 2026-07-13 — con la salvedad de que la verificación del `DEFAULT` de `canales` contra una fila real sigue pendiente por falta de datos (tabla vacía), no por falla del SQL |
| 14 | Medir el costo real de infraestructura compartida (Supabase/Vercel/Railway/backup) por prestadora y reflejarlo en el precio de licencia, en vez de un fee parejo para todas. Propuesto por el Desarrollador el 2026-07-11 durante la conversación de elección de proveedor de backup. **Avance 2026-07-15:** diseño completo en `docs/PLAN_MULTITENANT_PLM.md` sección 3.5 — fee base + tier por # Asistentes activos (reusando `'fee_fijo'`/`'por_personal'` existentes), costos fijos prorrateados por esa métrica, costo de IA/WhatsApp medido directo (confirmado que `mensajes_whatsapp` ya tiene `prestadora_id` pero no tokens/costo real), piso de margen interno. **Las 4 decisiones ya resueltas por el Desarrollador**: (1) enfoque aprobado tal cual, (2) valores de referencia iniciales: los packs varían entre USD 27 y USD 137 + impuestos, nunca hardcodeados en código, ajuste manual (no fórmula automática) por dólar/inflación/salarios u otra variable — investigado contra comparables de mercado (EE.UU.: AlayaCare/WellSky/CareSmartz360/AxisCare, descartados por el Desarrollador por no representar el poder adquisitivo argentino; Argentina: Xubio/Colppy/Tango como referencia real de lo que paga una pyme local), (3) el consumo de IA se factura dentro del tier, no aparte, (4) moneda la elige la prestadora (moneda local del país o USD, a su criterio). **Resta solo un detalle no bloqueante**: la asignación exacta de tramos intermedios (mediana/grande) dentro del rango USD 27–137, a definir recién cuando se implemente el módulo de facturación en serio | 2026-07-11 | — (las 4 decisiones de fondo ya están tomadas; el detalle de tramos intermedios queda para el momento de implementación) | Implementar `planes_facturacion` con estos valores cuando se aborde el módulo de facturación (según 4.3 de `PLAN_MULTITENANT_PLM.md`) | 🟡 Definición cerrada, implementación pendiente |
| 15 | Analizar, para cada servicio externo donde hoy dependemos de un solo proveedor (Supabase como base de datos, Railway como hosting del backend, y cualquier otro que aparezca en el inventario), si existe un proveedor de respaldo/plan de contingencia que permita seguir operando ante una caída o problema grave del proveedor principal — no solo para el bucket de backup (pendiente #4, ya resuelto con R2+B2), sino para la infraestructura operativa en sí. Propuesto por el Desarrollador el 2026-07-11 al ver la redundancia de dos proveedores aplicada al backup y preguntarse si aplica igual a Supabase/Railway. **Inventario completado 2026-07-11 (falta la decisión del Desarrollador):** (1) **Supabase — riesgo medio, el único con lock-in técnico real.** 117 policies RLS (`grep -c "CREATE POLICY" backend/src/db/*.sql`) más funciones/triggers Postgres (`schema_etapa2d.sql`, `schema_etapa2j.sql`, `schema_multitenant_02.sql`) — pero son Postgres estándar (`current_tenant()`/`es_superadmin()` son funciones propias, no específicas de Supabase), así que el dump/restore de la base en sí migra a cualquier Postgres administrado (Neon, RDS, Render Postgres) sin reescribir políticas. Lo que sí ata a Supabase como servicio (no como Postgres) son dos APIs propias: Auth (`supabase.auth.admin.createUser`, usado en `backend/src/middleware/requiereRolPanel.js`, `backend/src/utils/cuentasPanel.js`, `panel/src/context/AuthContext.jsx` y otros) y Storage (`certificado_url`, `docs/DATA_MODEL.md:367`) — migrar de Supabase implica reconstruir autenticación (Auth0/Clerk/JWT propio) y mover Storage a un bucket S3-compatible (mismo SDK `@aws-sdk/client-s3` que ya usamos para R2/B2 serviría). El backup diario a R2+B2 (pendiente #4) ya cubre "se pierde el dato"; no cubre "Supabase como servicio cae o cambia de política" — para eso falta un plan de migración documentado, no solo el dato a salvo. (2) **Railway — riesgo bajo.** Sin `railway.json` en el repo (config vive en su dashboard + nixpacks); el backend es Node/Express estándar sin ningún SDK propio de Railway en el código — migra a Render/Fly.io/un VPS con solo recargar las variables de entorno a mano. Lo único atado a Railway es el mecanismo de deploy (`.github/workflows/*.yml` llama al CLI de Railway específicamente). (3) **Vercel — riesgo bajo para ambos.** Panel: build estático de Vite, migra a Netlify/Cloudflare Pages sin fricción. Sitio-web (Next.js): se revisó (`sitio-web/src/middleware.js`, `sitio-web/next.config.js`) — usa Next.js Middleware estándar (redirección de idioma) sin `next/image` ni Edge Functions propias de Vercel; ese middleware tiene adaptadores en Netlify y Cloudflare Pages también, no es Vercel-exclusivo. (4) **Gmail SMTP (Nodemailer)** — riesgo bajo, cambiar de proveedor SMTP es una variable de entorno; el límite real es de volumen de envío diario de una cuenta Gmail/Workspace, más un tema de crecimiento que de caída del servicio. (5) **GitHub** — riesgo bajo, alta disponibilidad histórica; no se armó espejo en otro proveedor (GitLab) por juicio de costo/beneficio dado el tamaño actual del equipo. **Recomendación de Claude Code:** el único punto que amerita un plan de migración documentado (no necesariamente un respaldo activo/en caliente) es Supabase Auth + Storage — el resto migra con esfuerzo bajo si hiciera falta y no justifica pre-inversión hoy. **Roadmap de 5 puntos acordado con el Desarrollador 2026-07-13** (de lo simple/prioritario a lo complejo/no prioritario — "avanza con el 1 al 4 y despues vemos el 5 en profundidad"): 1) mirror de almacenamiento de archivos, 2) prueba real de restauración de backup, 3) plan de migración de Auth documentado, 4) runbooks de proveedores de bajo riesgo, 5) mirror en caliente de Supabase Auth (no recomendado). **Puntos 1-4 construidos y verificados 2026-07-13** (instrucción explícita del Desarrollador: "construye todo lo que tengas que construir, de ser posible no dejes nada en el aire") — ver `docs/PLAN_CONTINUIDAD_PROVEEDORES.md` para el detalle completo de los puntos 2, 3 y 4. **Hallazgo que corrigió el alcance del punto 1 antes de construir (Regla 12.5/12.6):** se había asumido que "mirror de almacenamiento" se aplicaba a la subida de archivo del Certificado de Aptitud (tabla `certificados`) — verificado contra el código real (`grep` en `backend/` y `panel/src`) y **esa suposición era incorrecta dos veces**: (a) el Certificado de Aptitud (`CertificadoTab.jsx`) nunca tuvo ni necesita archivo, es solo un QR generado; (b) la columna `certificado_url` que sí existía sin implementar (`docs/DATA_MODEL.md` línea 389) pertenece a la tabla `ausencias` (certificado médico que respalda una licencia por enfermedad/accidente — dato de salud sensible, Regla 7 de `CLAUDE.md`), no a `certificados`. Corregido el rumbo antes de escribir código. **Punto 1 construido:** ruta backend `backend/src/routes/panelAusencias.js` (subida vía `multer`, valida PDF/JPG/PNG hasta 10 MB, sube a bucket privado de Supabase Storage `certificados-medicos` y espeja a Cloudflare R2 con el mismo patrón `@aws-sdk/client-s3` del backup; endpoint de URL firmada de 60s para visualizar sin exponer el bucket como público), UI en `panel/src/pages/asistentes/AusenciasCoberturaTab.jsx` (input de archivo + botón deshabilitado mientras sube — Regla 5 —, estados vacío/cargando/cargado, i18n completo en las 3 locales verificado con `grep -c`), SQL `backend/src/db/schema_certificados_medicos_storage.sql` (bucket privado + RLS habilitada en `storage.objects`, termina con `NOTIFY pgrst, 'reload schema'` — Regla 13.2). **`.sql` aplicado y verificado contra Supabase real — ver pendiente #24, resuelto 2026-07-13.** Punto 5 sigue sin decisión, a discutir aparte | 2026-07-11 | Claude Code (investigar, proponer opciones, construir puntos 1-4) + Desarrollador (decidir el punto 5) | Inventario de servicios con dependencia de proveedor único, con opción de respaldo evaluada para cada uno, presentado y decidido por el Desarrollador | 🟢 Puntos 1-4 resueltos 2026-07-13 (ver `docs/PLAN_CONTINUIDAD_PROVEEDORES.md` y pendiente #24 para el detalle de aplicación/prueba end-to-end del punto 1); punto 5 pendiente de discusión aparte, no recomendado |
| 17 | Módulo 6 Parte 2 (Continuidad de guardia) construida y probada en navegador real 2026-07-12 (ver `docs/PROGRESS.md`, fila 2026-07-12 de "Archivos creados/modificados por sesión"), pero **sin commitear ni desplegar a Vercel** — trabajo hecho bajo autorización de avance autónomo nocturno, sin que el Desarrollador haya revisado el resultado todavía | 2026-07-12 | Desarrollador (revisar y autorizar el commit/push/deploy) | Cambios revisados por el Desarrollador y commiteados/desplegados (o devueltos con correcciones) | 🟢 Resuelto 2026-07-13 — el código ya estaba commiteado desde antes de esta sesión (`4e94afa` "Modulo 6 Parte 2 (Continuidad de guardia)..." y `d61b7f2` "deteccion automatica de ausencia + alertas tempranas", ambos visibles en `git log`), lo que faltaba era el despliegue a Vercel: nunca se había corrido `vercel --prod` tras esos commits (ver hallazgo detallado en pendiente #21). Desplegado en esta sesión con autorización explícita del Desarrollador y verificado funcionando en `https://aurevia-panel.vercel.app` — ver pruebas de navegador en pendiente #20 |
| 18 | **Pendiente permanente, no se cierra nunca del todo** (aclarado explícitamente por el Desarrollador 2026-07-13: no es una tarea de una sola vez, es una vigilancia continua). Estar atento en toda sesión futura — no solo en una revisión puntual — a políticas y formas de trabajo que queden fijadas de una sola manera (pensadas para la prestadora original) cuando, en el modelo de negocio de Xeitra como licenciante, cada prestadora licenciataria debería poder parametrizarlas a su gusto según su propia metodología de trabajo. Cualquier hallazgo nuevo (de cualquier sesión, tocando cualquier módulo) se suma a la lista de abajo con su propia fecha, nunca se pierde en una conversación aislada. La pestaña "Servicios" del Módulo 8 (Configuración) es el lugar natural donde deberían vivir una vez que el Desarrollador decida parametrizar alguno (empieza con `configuracion_escalada_relevo`, ver pendiente #8) | 2026-07-11 (reencuadrado como permanente 2026-07-13) | Claude Code (revisar activamente en cada sesión y sumar hallazgos nuevos) + Desarrollador (decidir cuáles parametrizar, cuándo) | No tiene condición de cierre única — se mantiene abierto de forma indefinida como lista viva; cada hallazgo individual sí puede resolverse (parametrizado o descartado con motivo), pero el pendiente en sí sigue existiendo para lo próximo que aparezca | 🟡 Abierto de forma permanente — primera tanda de 8 candidatos completada 2026-07-13 (avance autónomo nocturno, autorizado explícitamente). **Actualización 2026-07-15: candidatos (1) y (2) ya resueltos e implementados en código+base+deploy** (ver detalle abajo); candidatos (3), (4), (5), (7) y (8) siguen **sin decisión del Desarrollador**, nada implementado todavía para esos 5; candidato (6) decidido explícitamente "no parametrizar": **(1)** ~~`backend/src/utils/vencimientos.js:5` — `DIAS_ANTICIPACION = 30` (ventana de aviso de vencimientos de Monotributo/ART/Seguro), igual para todas las prestadoras; además la línea 28 filtra `.eq('prestadora_id', PRESTADORA_DEMO_ID)`, o sea el cron hoy ni siquiera corre para otras prestadoras.~~ **Resuelto en código 2026-07-14 (pendiente de aplicar contra Supabase real — ver fila nueva más abajo):** catálogo de tipos de documento a trackear ahora es una tabla por prestadora sin límite de cantidad (`tipos_documento_asistente`/`documentos_asistente`, con "Certificado de Antecedentes Penales" agregado como tipo sugerido además de Monotributo/ART/Seguro) y el plazo de aviso pasó de constante fija a columna `prestadoras.dias_aviso_vencimiento_documentos` editable por cada `admin_prestadora`. `vencimientos.js` reescrito para recorrer todas las prestadoras (ya no filtra por un solo `prestadora_id` fijo) y leer el plazo de cada una. Código construido: `backend/src/db/schema_documentos_asistente.sql`, `backend/src/routes/panelConfiguracion.js` (rutas `/documentos-tipo*`), `panel/src/pages/Configuracion.jsx` (pestaña "Documentos"), `panel/src/pages/asistentes/PerfilTab.jsx` (componente `DocumentosVencimiento`). Ver `docs/DATA_MODEL.md` y `docs/PRD_02B_Gestion_Personal.md` (función 9) para el diseño completo. **Aplicado y verificado contra Supabase real 2026-07-15** (sesión con MCP de Supabase conectado — no había quedado registrado en esta fila cuando se aplicó): tablas `tipos_documento_asistente`/`documentos_asistente`, columna `prestadoras.dias_aviso_vencimiento_documentos` y columnas `vencimiento_monotributo/art/seguro` ya eliminadas de `asistentes`, todo confirmado en producción. **Hallazgo al verificar:** el seed del catálogo sugerido y la fila de `configuracion_notificaciones` para el evento `vencimiento_documento_asistente` habían quedado sin correr (tabla completamente vacía, 0 filas para cualquier evento) — corregido en esta misma sesión, ahora hay 4 tipos de documento sugeridos y 1 fila de notificación para la prestadora existente. Commit `aa944ac` (2026-07-14) ya está incluido en el deploy de `panel` a Vercel del 2026-07-15 19:50, así que la UI también está en producción. **(2)** ~~`panel/src/pages/guardias/NuevaGuardiaModal.jsx:10` — `DIAS_GENERACION_SIN_VIGENCIA_HASTA = 90` (cuántos días adelante se generan guardias de una serie sin `vigente_hasta` definido).~~ **Resuelto en código 2026-07-14 (pendiente de aplicar contra Supabase real — ver fila nueva más abajo):** el Desarrollador aclaró que el esquema abierto (sin fecha de cierre) es el que usan todas las prestadoras y no es negociable — los 90 días eran siempre un artefacto técnico de generación por lotes, nunca una regla de negocio, y su ausencia de renovación era un bug real (una serie sin `vigente_hasta` dejaba de producir guardias nuevas en silencio pasado el día 90). Se separó en dos mecanismos: (a) `prestadoras.dias_generacion_series_guardia` (columna nueva, default 90, editable por cada `admin_prestadora` en Configuración → pestaña Servicios) reemplaza la constante fija; (b) cron nuevo `backend/src/utils/generacionSeriesGuardia.js` (`extenderSeriesGuardiaAbiertas`, registrado en `backend/src/server.js`, corre una vez por día) mantiene siempre ese horizonte de guardias generadas por delante de "hoy" para toda serie `activa` sin `vigente_hasta` — proceso 100% interno, nunca visible en ninguna pantalla. Además, durante la misma conversación el Desarrollador señaló que "cancelar una guardia" y "cancelar un servicio" son conceptos distintos y pidió diseñar en la misma sesión el **Cierre de servicio** a nivel Paciente (da de baja todas las Prestaciones/Paquetes vigentes y cancela todas las guardias futuras no iniciadas de un Paciente, con motivo obligatorio — `fin_demanda`/`fallecimiento`/`otro` —, sin borrar ningún registro): tabla nueva `cierres_servicio_paciente` con RLS restringida explícitamente a `admin_prestadora`+`coordinador` (instrucción explícita del Desarrollador, sin bypass de `superadmin` a diferencia del resto de las policies del proyecto), UI en `panel/src/pages/familias/PrestacionesPaciente.jsx`, backend de configuración en `backend/src/routes/panelConfiguracion.js` (rutas `/guardias/horizonte-generacion`). Código construido: `backend/src/db/schema_cierre_servicio_paciente.sql`, `backend/src/utils/generacionSeriesGuardia.js`, `backend/src/server.js`, `backend/src/routes/panelConfiguracion.js`, `panel/src/pages/guardias/NuevaGuardiaModal.jsx`, `panel/src/pages/familias/PrestacionesPaciente.jsx`, `panel/src/pages/Configuracion.jsx`, i18n completo en las 3 locales de `panel/src/i18n/translations.js`. **Aplicado y verificado contra Supabase real 2026-07-15** (sesión con MCP de Supabase conectado — no había quedado registrado en esta fila cuando se aplicó): tabla `cierres_servicio_paciente`, columna `prestadoras.dias_generacion_series_guardia` y policy `coordinador_y_admin_gestionan_cierres_servicio_paciente` (sin `es_superadmin()`, como estaba diseñado) confirmadas en producción. Commit `a726ee6` (2026-07-14) ya está incluido en el deploy de `panel` a Vercel del 2026-07-15 19:50 y el backend en Railway (deploy automático confirmado exitoso). **Prueba manual en navegador real con datos de prueba todavía no hecha en esta sesión** — queda como verificación pendiente, no como aplicación de esquema. **(3)** `panel/src/pages/guardias/GuardiaAcciones.jsx:236-239` + réplicas en las 3 locales de `panel/src/i18n/translations.js` — catálogo fijo de motivos de aviso previo (`salud`/`transporte`/`familiar`/`otro`) como `<select>` cerrado, aunque `alertas_tempranas_guardia.motivo` ya es TEXT libre en la base (`schema_modulo6_guardias_03.sql:64-65`) precisamente para no cerrar esta lista. **(4)** `panel/src/i18n/translations.js` (~139-150, con réplicas en/pt-BR) — `especialidades_labels`/`zonas_labels` (`zona_norte`/`zona_oeste`/`zona_sur`/`la_plata`, geografía AMBA) hardcodeados en el Panel para `Postulaciones.jsx`, duplicando lo que `zonas_cobertura` (`schema_etapa2h.sql`) ya resolvió como configurable por prestadora para el sitio público — el Panel no tiene ese mismo mecanismo. **(5)** `backend/src/db/schema_etapa2h.sql:47` — `zonas_cobertura.categoria` sigue con `CHECK IN ('caba','gba','otras')`, cerrado a geografía de Buenos Aires pese a que la tabla ya es por-prestadora. **(6)** `backend/src/routes/postulacionAsistente.js:8` — `IDIOMAS_SOPORTADOS = ['es-AR','en','pt-BR']` fijo en código (coincide con el `DEFAULT` de `schema_etapa2o.sql:4`); una prestadora en otro país/idioma no podría sumar el suyo sin tocar código. **Decisión del Desarrollador 2026-07-14: no parametrizar — dejar los 3 idiomas actuales.** Queda cerrado como "decidido no parametrizar", no como pendiente abierto. **(7)** `panel/src/pages/asistentes/VerificacionTab.jsx:10` + `backend/src/routes/panelCuentas.js:85-91` (`ETAPAS_INCORPORACION`) — el Proceso de Incorporación de Asistentes de 5 etapas fijas está duplicado en frontend/backend y además cerrado por el ENUM Postgres `etapa_filtro` (`schema_etapa2b.sql:102-104`); una prestadora con un proceso de 3 o 7 pasos no podría adaptarlo sin una migración de esquema. **(8)** `backend/src/utils/email.js:4-13` — el transporte SMTP (`smtp.gmail.com`, `SMTP_USER`/`SMTP_PASSWORD`) es uno solo para todas las prestadoras; los destinatarios ya son configurables (`configuracion_notificaciones`), pero no el remitente/servidor de envío. Los 8 candidatos fueron verificados línea por línea contra el código real antes de listarlos acá (no solo el reporte de la revisión). **Versión sin lenguaje técnico agregada 2026-07-13 a pedido del Desarrollador: `docs/Analisis_Parametrizacion_Prestadoras.md`** — mismos 8 puntos explicados como situaciones de negocio, sin referencias a código/base de datos, más un análisis de en cuáles de los 8 hay una oportunidad real de uso de Inteligencia Artificial (solo 2 de los 8: sugerir motivo de ausencia a partir de texto libre, y traducción automática asistida para sumar un idioma nuevo) y en cuáles no (los otros 6 son simples decisiones de configuración). Falta: que el Desarrollador revise esta lista y decida, ítem por ítem, cuál parametrizar (y en qué mecanismo — probablemente Módulo 8) y cuál dejar como está. **Decisiones tomadas por el Desarrollador 2026-07-27 (revisión caso por caso), nada implementado todavía para ninguna de las 5:** **(3)** Parametrizar por Prestadora — catálogo editable de motivos de aviso previo de guardia, sembrado con Salud/Transporte/Familiar/Otro como default de cualquier Prestadora nueva, editable después desde Configuración → Servicios (mismo lugar que `configuracion_escalada_relevo`, #8). **(4)** Parametrizar — el Panel (Postulaciones) deja de usar las labels fijas de `translations.js` (`zona_norte`/`zona_oeste`/`zona_sur`/`la_plata`) y pasa a leer las zonas reales cargadas por cada Prestadora en `zonas_cobertura`, sin ninguna lista hardcodeada. **(5)** Parametrizar — se quita el `CHECK IN ('caba','gba','otras')` de `zonas_cobertura.categoria` (`schema_etapa2h.sql:47`); cada Prestadora define sus propias categorías de zona sin límite geográfico, incluso fuera de Argentina. **(7)** Parametrizar — "cada prestadora debe tener la libertad de implementar su propio plan de incorporación de asistentes" (cantidad y nombre de etapas configurable, no fijo en 5). Es un cambio de modelo de datos (reemplaza el ENUM `etapa_filtro` fijo) — requiere primero el inventario + plan de `CLAUDE.md` §11, todavía no hecho. **(8)** Parametrizar — cada Prestadora, al configurar si quiere o no enviar notificaciones y por qué medio (email, WhatsApp, etc.), debe poder suministrar los datos necesarios; para email, una dirección propia desde la cual enviar (exclusiva o no), en vez de depender de la única cuenta Gmail compartida (`notificaciones.aurevia@gmail.com`) hoy usada por todas. Implica guardar credenciales de envío por Prestadora (dato sensible, nunca en variables de entorno globales ni hardcodeado — `CLAUDE.md` §6/§7 Regla 1) — también requiere diseño antes de implementar, dado que toca cómo se almacenan credenciales multi-tenant. **(9) Hallazgo 2026-07-13, durante el arranque de implementación del pendiente #9 (WhatsApp+IA):** `configuracion_notificaciones` (`backend/src/db/schema_etapa2h.sql:79-84`) **no tiene `prestadora_id`** — es una fila global por evento, compartida por todas las prestadoras licenciatarias (confirmado explícitamente excluida de la migración multi-tenant en `backend/src/db/schema_multitenant_02.sql:353-359`, mismo motivo que `configuracion_empresa`). A diferencia de los 8 candidatos anteriores (que son ajustes de negocio), este es un caso más serio: hoy, si dos prestadoras usaran el sistema, compartirían la misma lista de emails y el mismo on/off de notificaciones por evento — no es una preferencia sin resolver, es una brecha de aislamiento real. Bloquea directamente la construcción de la config de WhatsApp del pendiente #9 (que necesita vivir en esta misma tabla/mecanismo, por evento y por prestadora) — se resuelve como parte de esa construcción, agregando `prestadora_id` y RLS acotada, en vez de tratarse aparte. **Resuelto 2026-07-13** — `configuracion_notificaciones` ya tiene `prestadora_id UUID NOT NULL` (`backend/src/db/schema_whatsapp_ia_01.sql:21-28`), la PK pasó a ser compuesta `(evento, prestadora_id)` (línea 30), y las policies quedaron acotadas por tenant (`admin_gestiona_configuracion_notificaciones`/`coordinador_lee_configuracion_notificaciones`, líneas 34-46) — construido como parte del pendiente #9 (WhatsApp+IA), aplicado y verificado contra Supabase real junto con el resto de ese schema. **Implementado en código+base 2026-07-27 (las 5 decisiones de la revisión caso por caso de más arriba, aplicado y verificado contra Supabase real con MCP conectado):** **(3)** `backend/src/db/schema_motivos_aviso_previo_01.sql` (tabla `motivos_aviso_previo_guardia`, RLS `admin_prestadora_gestiona_.../coordinador_lee_...`, seed Salud/Transporte/Familiar/Otro por cada Prestadora existente), rutas `panelConfiguracion.js` (`/motivos-aviso-previo*`), hook `panel/src/hooks/useMotivosAvisoPrevio.js`, UI en `Configuracion.jsx` (pestaña Servicios) y `GuardiaAcciones.jsx` ya no usa el `<select>` fijo. **(4)+(5)** `backend/src/db/schema_zonas_cobertura_categoria_libre_01.sql` quita el `CHECK IN ('caba','gba','otras')` de `zonas_cobertura.categoria` (confirmado sin ese constraint en Supabase real tras aplicar); hook `panel/src/hooks/useZonasCobertura.js` reemplaza las labels fijas de i18n en `Postulaciones.jsx`/`PostulacionDetalle.jsx`; UI de administración de zonas ya existía en `Configuracion.jsx` contra las rutas `/zonas` ya existentes, ahora con categoría de texto libre. **(7)** `backend/src/db/schema_etapas_incorporacion_01.sql` (tabla `etapas_incorporacion_asistente`, `UNIQUE(prestadora_id, orden)` DEFERRABLE + función `intercambiar_orden_etapas_incorporacion` para reordenar atómicamente, seed de las 5 etapas actuales por Prestadora, migración de `verificaciones_asistente.etapa` de ENUM `etapa_filtro` a TEXT preservando valores); `VerificacionTab.jsx` y `panelCuentas.js` ya no usan el array fijo `ETAPAS_INCORPORACION`; UI de administración (alta/reordenar/activar-desactivar) en `Configuracion.jsx`. **Bug de seguridad propio detectado y corregido en esta misma sesión antes de exponerlo:** `intercambiar_orden_etapas_incorporacion` había quedado invocable por `anon`/`authenticated` vía RPC (el mismo patrón de bug ya documentado en `schema_whatsapp_ia_01.sql:128-132`) — detectado con `mcp__supabase__get_advisors`, corregido con un `REVOKE EXECUTE ... FROM anon, authenticated` explícito, confirmado limpio en advisors después. **(8)** Se decidió una tabla dedicada `configuracion_email_prestadora` (no columnas sobre `configuracion_notificaciones`, que es por-evento y no por-Prestadora — desviación deliberada de la redacción literal del plan aprobado, por Regla 12 de punto único de verdad) en `backend/src/db/schema_email_remitente_prestadora_01.sql`, con credencial SMTP guardada vía Supabase Vault (`guardar_credencial_smtp_prestadora`/`leer_credencial_smtp_prestadora`, mismo patrón que el token de WhatsApp, `service_role` únicamente desde el inicio); rutas `GET/PATCH /email-remitente` en `panelConfiguracion.js` (el GET nunca devuelve la contraseña ni el `credencial_secret_id`, solo un booleano `credencial_cargada`); `backend/src/utils/email.js` usa el remitente propio de la Prestadora si está activo y configurado, si no cae al transporter compartido de Xeitra (sin romper nada para quien no lo configure); UI `TabEmailRemitente` en `Configuracion.jsx` (pestaña Notificaciones). i18n de las 4 (3/4/5/7/8 en realidad, 9 claves de (8) incluidas) completo en es-AR/en/pt-BR. **Todas las migraciones aplicadas contra Supabase real y verificadas con `get_advisors` (sin warnings de seguridad nuevos) y con SQL directo (seed correcto para "Prestadora Demo" y "Sandbox Superadmin").** **Deploy y verificación funcional en el Panel real 2026-07-27** (commit `3e3be25`, push a `main`, backend auto-desplegado en Railway y Panel desplegado explícitamente a Vercel con `vercel --prod`, alias `aurevia-panel.vercel.app` confirmado): con Playwright contra el sitio ya desplegado (no contra código local) — **(4)+(5)** alta real de una zona con categoría `"cordoba"` (fuera de caba/gba/otras) desde la pantalla "Zonas de cobertura", guardada sin error, con el mensaje "Defina la categoría según su propio criterio... No hay valores fijos por sistema" confirmando que el `<select>` fijo ya no existe. **(3)** pestaña Servicios muestra el catálogo real (Salud/Transporte/Familiar/Otro) editable, ya no el `<select>` cerrado de `GuardiaAcciones.jsx`. **(7)** pestaña Servicios muestra las 5 etapas reales de "Prestadora Demo" con reordenamiento probado en vivo (subir "Verificación de identidad" de la posición 2 a la 1 y revertido a su orden original después de confirmar que funcionaba) — la función `intercambiar_orden_etapas_incorporacion` corrigida quedó validada en producción. **(8)** pestaña Notificaciones muestra "Remitente de notificaciones por email" con los defaults esperados (`smtp.gmail.com`/465); se inspeccionó la respuesta real de `GET /api/panel/configuracion/email-remitente` y se confirmó que no incluye `credencial_secret_id` ni ningún campo de contraseña, solo `credencial_cargada:false`. **Sigue pendiente, no verificable sin intervención del Desarrollador:** el envío de un email real de prueba con una credencial SMTP propia (cuenta de prueba real, no la compartida) para confirmar que el email llega con el remitente configurado — requiere que el Desarrollador provea o autorice esa cuenta de prueba; no se puede simular sin credenciales reales |
| 19 | Posible modelo de facturación con intermediario (tipo cooperativa/agencia de personal) para Asistentes monotributistas: si la Asistente factura a un tercero designado en vez de a la prestadora directamente, la prestadora pagaría a ese tercero en bloque, que redistribuye a cada Asistente. Idea planteada por Claude Code durante la discusión de certificaciones (2026-07-12, ver `docs/PLAN_MULTITENANT_PLM.md` sección 5.4bis), validada por el Desarrollador como potencialmente provechosa ("no es ninguna tontería, se le podría encontrar un costado provechoso"), pero pertenece al módulo de facturación/pagos, todavía no discutido — **no resolver ahora**, retomar cuando se aborde ese módulo. Posible cruce con el riesgo legal art. 23 LCT / caso Cabify (`CLAUDE.md`) si un intermediario agrupa y paga Asistentes — evaluar en ese momento si afecta quién ejerce el control/subordinación | 2026-07-12 | Desarrollador (definir cuándo abordar el módulo de facturación/pagos) | Se retoma y resuelve cuando se diseñe el módulo de facturación/pagos | 🔴 Abierto (anotado para más adelante, no bloqueante) |
| 20 | **Rediseño de Módulo 6 (Continuidad de guardia) — detección automática de ausencia + sistema extensible de alertas tempranas.** Lo construido en Parte 1/2 (2026-07-10/12) trata "marcar ausente" como una acción manual de un Coordinador; el Desarrollador corrigió esto el 2026-07-12 con dos requerimientos de fondo: **(a)** la ausencia debe detectarse **sola**, sin intervención humana, cuando no se registra el check correspondiente (check-in) dentro de un margen de tiempo esperado después del inicio de la guardia — el botón manual queda como excepción/override, no como mecanismo principal. **(b)** debe existir una **cadena de alertas tempranas, previa a que la ausencia se concrete**, para gestionar un relevo con tiempo y minimizar el impacto en el Paciente. El Desarrollador dio un primer ejemplo concreto de fuente de alerta temprana: cada Asistente tiene un medio de transporte habitual (a pie, colectivo, auto, etc.) hacia cada Paciente; con eso se calcula un tiempo de viaje estimado (vía API de mapas) y una "hora de salida esperada" (hora de inicio de guardia menos tiempo de viaje); si pasado un margen configurable por prestadora el GPS del Asistente muestra que sigue en su domicilio, se dispara una alerta temprana. Aclaró explícitamente que **esta no es la única fuente de alerta temprana** — otro ejemplo es una llamada del Asistente avisando que no concurrirá (con un checklist de motivos, para estadísticas) — **y que el sistema debe diseñarse para incorporar fuentes de alerta nuevas en el futuro sin rediseñar el mecanismo cada vez** (arquitectura tipo "fuente de alerta" enchufable, no una lista cerrada de casos). Ante cualquier alerta temprana, se dispara una escalada automatizada configurable por prestadora (mensajes automáticos, llamada telefónica, etc. — coincide con el mecanismo de niveles de escalada ya existente, `configuracion_escalada_relevo`, pero debe poder arrancar *antes* del incidente de ausencia, no solo después). Buena oportunidad de uso de IA (anotado también en `docs/BACKLOG_OPORTUNIDADES.md` si aplica). Intervención humana a minimizar al máximo, no a eliminar: el Coordinador siempre recibe la notificación de la anomalía y puede intervenir en la resolución. Ver también `docs/PRD_04_05_App_Servicio.md` sección "Alertas tempranas de ausencia" y `docs/DATA_MODEL.md` sección "Módulo 6" (ampliación 2026-07-12). **Escalada automatizada (mensajes/llamadas) explícitamente NO incluida en esta implementación** — depende de `docs/PRD_06_WhatsApp_IA.md` (pendiente #9, "en discusión, no implementar todavía"); por ahora la alerta temprana solo se hace visible en el Panel para acción manual del Coordinador. Fuente GPS-de-salida-del-domicilio (parte (a) del ejemplo del Desarrollador) tampoco incluida todavía — depende de la PWA Asistentes (Etapa 3), que no existe en el repo, y `docs/BUILD_ORDER.md` prohíbe empezar una etapa de código sin que la anterior esté funcionando en producción | 2026-07-12 | Desarrollador (aprobó diseño 2026-07-12) + Claude Code (diseñar, implementar, aplicar schema, desplegar y probar) | Diseño de datos/arquitectura de fuentes de alerta enchufables presentado y aprobado, e implementación construida y probada en navegador real | 🟢 Resuelto 2026-07-13 — schema aplicado contra Supabase real (ver pendiente #21) y Panel redesplegado a Vercel (`vercel --prod`, con autorización explícita del Desarrollador; hallazgo nuevo: el Panel no tiene auto-deploy, cada cambio necesita este paso manual — ver nota en pendiente #21). Probado en navegador real contra `https://aurevia-panel.vercel.app` con datos de prueba a gran escala (30 Pacientes, 60 Asistentes, 33 guardias cubriendo 7 escenarios, por pedido explícito del Desarrollador de "simular todas las situaciones imaginables"): (1) detección automática de ausencia confirmada para "sin relevo previo" (5/5) y "con relevo" (5/5, con `guardia_saliente_id` correctamente vinculado); (2) guardias futuras, con check-in a tiempo, y canceladas correctamente **no** tocadas por el cron; (3) sección "Alertas tempranas" muestra las 4 alertas abiertas de aviso telefónico (una por motivo) y **no** muestra la que ya estaba resuelta de antemano; (4) botón "Marcar como atendida" probado — la alerta desaparece de la lista; (5) botón "Resolver incidente" probado con asignación de Asistente suplente — el incidente desaparece de la lista; (6) acción "Registrar aviso previo" probada desde `GuardiaAcciones.jsx` sobre una guardia programada real — generó la alerta visible en Continuidad con el motivo correcto, sin marcar la guardia ausente. Los 30 Pacientes/Familias, 60 Asistentes (con sus cuentas Auth reales) y 33 guardias de prueba se borraron al terminar, verificado con consulta post-limpieza: 0 filas residuales |
| 22 | Aplicar `backend/src/db/schema_whatsapp_ia_01.sql` contra Supabase real — agrega `prestadora_id`/`whatsapp_activo` a `configuracion_notificaciones` (cierra la brecha de aislamiento del pendiente #18 candidato 9) y crea las tablas nuevas de WhatsApp+IA (`configuracion_whatsapp_prestadora`, `plantillas_whatsapp`, `configuracion_escalada_coordinador`, `conversaciones_whatsapp`, `mensajes_whatsapp`) más las funciones de Vault (`guardar_token_whatsapp`/`leer_token_whatsapp`). No había MCP de Supabase disponible en la sesión donde se escribió (2026-07-13) | 2026-07-13 | Desarrollador (correr el SQL, ya termina con `NOTIFY pgrst, 'reload schema';` incluido) | Las tablas y funciones existen en Supabase, verificado con consulta real; Panel puede leer/escribir `configuracion_whatsapp_prestadora` sin 404 | 🟢 Resuelto 2026-07-13 — migración aplicada contra Supabase real vía MCP en una segunda sesión. **Bug real encontrado durante la aplicación** (no una falsa alarma): las secciones 0/final del archivo tenían los `INSERT` de seed de `configuracion_escalada_coordinador` y `configuracion_whatsapp_prestadora` *antes* de los `CREATE TABLE` correspondientes (secciones 4 y 1) — `apply_migration` no aborta el resto del batch si un statement individual falla, así que el error quedó silencioso hasta verificarlo con una consulta aparte. Corregido en `schema_whatsapp_ia_01.sql`: los 2 `INSERT` se movieron a una sección 7 nueva al final del archivo, después de que sus tablas ya existen (correcto para una futura re-ejecución en otro entorno). Contra la base ya migrada se corrieron esos 2 `INSERT` sueltos (idempotentes, `ON CONFLICT DO NOTHING`) y se verificó con consulta real: 1 fila para la Prestadora Demo en cada una de las dos tablas. Verificación completa final: 5 tablas nuevas presentes (`information_schema.tables`) ✅; `configuracion_notificaciones` con las 7 filas de la Prestadora Demo con `prestadora_id` seteado, incluidos los 2 eventos nuevos ✅; PK compuesta `(evento, prestadora_id)` confirmada con `pg_get_constraintdef` ✅; insert de prueba sin `prestadora_id` en `plantillas_whatsapp` falló con `ERROR 23502` como se esperaba ✅; seed de `configuracion_escalada_coordinador` y `configuracion_whatsapp_prestadora` para la Prestadora Demo, 1 fila cada una ✅ |
| 23 | `npm install` en `backend/` para instalar `@anthropic-ai/sdk` (agregado a `package.json` en esta sesión para `iaWhatsapp.js`, primera dependencia de IA real del backend) — no corrido todavía | 2026-07-13 | Claude Code o Desarrollador | `node_modules/@anthropic-ai` presente y `npm run` del backend arranca sin error de módulo faltante | 🟢 Resuelto 2026-07-13 — `npm install` corrido, 22 paquetes agregados, 0 vulnerabilidades |
| 21 | Aplicar `backend/src/db/schema_modulo6_guardias_03.sql` contra Supabase real (tablas `configuracion_ausencia_automatica` y `alertas_tempranas_guardia` del pendiente #20) — no había herramienta MCP de Supabase disponible en la sesión donde se escribió el código, así que requería acción manual | 2026-07-12 | Desarrollador (correr el SQL) | Las dos tablas existen en Supabase, verificado con una consulta real (no solo que el archivo `.sql` exista en el repo) | 🟢 Resuelto 2026-07-13 — aplicado por el Desarrollador en una sesión nueva de Claude Code con MCP de Supabase conectado (mismo patrón ya usado para pendientes anteriores). Verificado con `information_schema.tables` (ambas tablas presentes) y una consulta directa a `configuracion_ausencia_automatica` (1 fila, seed de la Prestadora Demo correcto). **Hallazgo adicional no anticipado:** tras aplicar el schema, la pantalla `/continuidad` del Panel se rompía (página completamente en blanco) — causa raíz identificada en dos capas: (a) el caché de esquema de PostgREST no se había refrescado tras crear las tablas por SQL directo (resuelto con `NOTIFY pgrst, 'reload schema';`, corrido por el Desarrollador en el SQL Editor de Supabase); (b) el Panel en Vercel **no tiene auto-deploy** — a diferencia del backend en Railway, cada cambio de código necesita un `vercel --prod` manual, y el commit `d61b7f2` con esta funcionalidad nunca se había desplegado. Ambos se resolvieron en esta sesión (recarga de schema + `vercel --prod` con autorización del Desarrollador) y quedaron verificados en el navegador real — ver detalle en pendiente #20 |
| 25 | **Aplicar `backend/src/db/schema_multitenant_04.sql` contra Supabase real** — crea `configuracion_prestadora` (reemplazo multi-tenant del singleton `configuracion_empresa`, con `prestadora_id` y RLS por `current_tenant()`/`es_superadmin()`), migra la fila existente, y termina con `NOTIFY pgrst, 'reload schema';` (Regla 13.2). Backend ya apunta a la tabla nueva (`panelConfiguracion.js`, `panelNotificaciones.js`, `configuracionPublica.js`) — hasta que se aplique el SQL, esas rutas devuelven error porque `configuracion_prestadora` todavía no existe en la base real. Parte del barrido "escindir el código de un licenciatario, del de otro y del común" pedido explícitamente por el Desarrollador (2026-07-13) | 2026-07-13 | Desarrollador (correr el SQL, ya incluye el `NOTIFY`) o una sesión con el MCP de Supabase conectado | La tabla existe en Supabase (verificado con consulta real), y `GET /api/panel/configuracion/empresa` devuelve 200 con los datos migrados, no 404/500 | 🟢 Resuelto 2026-07-13 — aplicado por el Desarrollador en una sesión con el MCP de Supabase conectado. Verificado con `SELECT * FROM configuracion_prestadora;` → 1 fila real: `prestadora_id=874f54d7-4383-4d54-8b9f-f51d02f0dd11`, `nombre=Prestadora Demo`, `telefono=+54 9 11 3787 4193`, `whatsapp_numero=+54 9 11 3787 4193`, `email=prestadora.demo@gmail.com`, `dominio=prestadorademo.com.ar`, `zona_cobertura_texto=AMBA`. El `INSERT ... ON CONFLICT` corrió sin el bug de orden visto en el pendiente #22 (acá `configuracion_empresa`, la tabla origen del `SELECT`, ya existía de antes — no había una sección posterior creándola). RLS habilitada y `NOTIFY pgrst, 'reload schema';` final confirmado corrido |
| 26 | **Panel — UI para que el `superadmin` elija `prestadora_id` al dar de alta el primer usuario (`admin_prestadora`) de una prestadora nueva.** El backend (`backend/src/routes/panelUsuarios.js`, POST `/`) ya acepta `prestadora_id` opcional en el body cuando quien pide es `superadmin` (un `admin_prestadora` no puede overridearlo, siempre usa su propio tenant) — pero `panel/src/pages/UsuariosPanel.jsx` no envía ese campo ni tiene selector. Decisión tomada en esta sesión (2026-07-13): no construir esta pantalla ahora — es una feature nueva (catálogo/selector de prestadoras), fuera del alcance de "eliminar hardcodes de la prestadora original y unificar terminología" que pidió el Desarrollador; se deja registrado acá en vez de expandir el alcance en silencio. **Alcance acotado 2026-07-13 por aclaración explícita del Desarrollador: "los clientes de una prestadora seran dados de alta por dicha prestadora"** — esto NO es un selector para dar de alta usuarios en general (Asistentes, Familias, Coordinadores de una prestadora ya existente los da de alta esa misma prestadora, dentro de su propio tenant, con el mecanismo ya construido). Es exclusivamente para el momento puntual de onboarding de una prestadora licenciataria nueva: alguien (el `superadmin`, del lado de Xeitra) tiene que crear la fila en `prestadoras` y la primera cuenta `admin_prestadora` de esa prestadora — recién a partir de ahí esa prestadora se maneja sola y da de alta a sus propios clientes/usuarios sin intervención de `superadmin`. Acota el pendiente a una pantalla de uso raro (una vez por prestadora nueva), no a un mecanismo de uso diario | 2026-07-13 | Desarrollador (decidir cuándo priorizar esta pantalla, probablemente junto con el Bloque de onboarding de licenciatarias de `docs/PLAN_MULTITENANT_XEITRA.md`) | Selector de prestadora visible en el Panel para `superadmin` al crear la primera cuenta `admin_prestadora` de una prestadora nueva, probado en navegador real | 🟢 Resuelto 2026-07-14 — construido como parte del ítem I del pendiente #30 (ver esa fila para el detalle completo). Selector agregado en `panel/src/pages/UsuariosPanel.jsx:137-146,210-217,223` (poblado desde `GET /api/panel/prestadoras`, obligatorio cuando `superadmin` elige rol `admin_prestadora`). Probado en navegador real: cuenta `admin_prestadora` creada con `prestadora_id` verificado contra la base coincidiendo con lo elegido en el selector, luego borrada |
| 27 | **Barrido terminológico del nombre de la prestadora original en `docs/*.md` (18 archivos)** — pedido explícito del Desarrollador (2026-07-13) de que no se mezclara ese nombre ni en el código ni en la documentación. Se hizo el barrido en código (schemas SQL, comentarios de backend/panel/sitio-web, i18n) en esta sesión — genericando menciones que asumían a la prestadora original como único tenant ("Asistente de la prestadora original" → "Asistente de la prestadora", etc.) y preservando menciones genuinamente históricas/factuales (seeds reales con el UUID fijo de la primera prestadora, fechas de migración). **No se aplicó el mismo criterio todavía a `docs/*.md`** (`Analisis_Parametrizacion_Prestadoras.md`, `BUILD_ORDER.md`, `CONTEXT.md`, `DATA_MODEL.md`, `DESIGN_SYSTEM.md`, `PLAN_MULTITENANT_XEITRA.md`, los PRD, `PROGRESS.md`, `Prompt_Claude_Code_Kickoff_Implementacion.md`, `Prompt_Claude_Code_Xeitra_Multitenant.md`, `SECURITY.md`, manual de identidad de marca de la prestadora original) porque hay tensión real con `CLAUDE.md` mismo, en su versión de esa fecha, que indicaba explícitamente conservar la marca de la prestadora original como correcta y como primera prestadora/licenciataria real — no todo uso de ese nombre en esos documentos era un supuesto de arquitectura a corregir, algunos eran hechos de negocio genuinos. Necesita una revisión puntual, documento por documento, para separar lo histórico/correcto de lo que sí asume incorrectamente "una sola organización" — no un reemplazo mecánico de la palabra | 2026-07-13 | Desarrollador (confirmar el criterio) + Claude Code (aplicar documento por documento) | Cada uno de los 18 archivos revisado y, donde corresponda, corregido — con el mismo criterio histórico/factual vs. arquitectura ya aplicado al código | 🟢 Resuelto 2026-07-13 (nota: criterio superado 2026-07-16, ver más abajo — el Desarrollador pidió purgar el nombre de la prestadora original de toda la documentación salvo la carpeta restringida) — los 18 archivos leídos completos (`PLAN_MULTITENANT_XEITRA.md`, 1117 líneas, en 3 tandas secuenciales) y corregidos con el criterio del Desarrollador de esa fecha ("los documentos que tengan que ver con la prestadora original y sus particularidades es una cosa, los que tengan que ver con el codigo es otra"). **11 de 18 sin cambios en esa sesión** — ya describían correctamente "prestadora" en lo técnico y el nombre de la prestadora original solo en hechos reales/particularidades de negocio (`Analisis_Parametrizacion_Prestadoras.md`, `BUILD_ORDER.md`, `DESIGN_SYSTEM.md`, `PLAN_MULTITENANT_XEITRA.md`, `PRD_01_Sitio_Web.md`, `PRD_04_05_App_Servicio.md`, `PRD_06_WhatsApp_IA.md`, `Prompt_Claude_Code_Kickoff_Implementacion.md`, `Prompt_Claude_Code_Xeitra_Multitenant.md`). **7 con cambios genéricos aplicados** (arquitectura/código que asumía a la prestadora original como único tenant): `CONTEXT.md` (2 — "la prestadora original asigna un Asistente" → "la prestadora asigna"), `DATA_MODEL.md` (2 — "etapas oficiales de la prestadora original"/"diseño oficial de la prestadora original" → "del sistema"), `PRD_02B_Gestion_Personal.md` (4 — glosario "Vínculo", 2 causales de cese, indicadores de score de riesgo), `SECURITY.md` (1 — "la prestadora original usa Supabase Auth" → "la plataforma usa Supabase Auth", conforme a la regla ya vigente de no atribuirle a la prestadora original la infraestructura compartida). **3 hallazgos tangenciales, fuera del criterio de esta tarea pero corregidos por ser violaciones vigentes del glosario** (términos ya retirados que habían quedado sin actualizar): `CONTEXT.md` ("UI del nombre anterior, ya retirado, del proceso de verificación" → "UI del Proceso de Incorporación de Asistentes"), `PRD_03_Reclutamiento.md` (nota que decía que el nombre anterior seguía reservado para uso interno, contradecía el retiro total del término), manual de identidad de marca de la prestadora original ("Certificado" con el nombre anterior, listado como nombre correcto → "Certificado de Aptitud"). El manual de identidad de marca de la prestadora original mantuvo en esa fecha sus otras 59 menciones al nombre de la prestadora original sin tocar — era el manual de identidad de marca de esa prestadora, correcto que la nombrara |
| 30 | **Rediseño de los roles `superadmin`/`admin_plataforma` en el modelo multi-tenant.** Hasta 2026-07-13, `superadmin` estaba diseñado como "el rol de Xeitra, cross-tenant total, ve todas las prestadoras" (`docs/PLAN_MULTITENANT_XEITRA.md` sección 3.4, `docs/SECURITY.md`) — conflacionaba en un solo rol el acceso técnico (código/infra/base de datos) con el acceso administrativo de negocio (comercial, todas las prestadoras), sin ningún límite de sesión para el segundo. Acordado con el Desarrollador el 2026-07-13 (tras 4 rondas de refinamiento): se separan en dos roles. **`superadmin`** pasa a ser puramente técnico — acceso de Panel únicamente a una prestadora de prueba fija (sandbox), vedado el acceso a cualquier prestadora real por cualquier vía del Panel. **`admin_plataforma`** (rol nuevo) es quien tiene el alcance administrativo de negocio de toda la plataforma — entra a una prestadora real una a la vez, bajo un "modo dentro de una prestadora" (banner notorio, advertencia sumada a las confirmaciones de acciones destructivas de la Regla 4, log de auditoría de todo login/acción sensible, timeout de 5 min de inactividad + tope absoluto de 60 min con aviso a los 50). Ambos roles con cuentas nominales y MFA obligatorio. Diseño completo en `docs/PLAN_MULTITENANT_XEITRA.md` 3.4/3.4.1, reflejado también en el glosario de `CLAUDE.md` y en `docs/SECURITY.md` | 2026-07-13 | Desarrollador (diseño ya aprobado) + Claude Code (implementar en código) | El diseño está cerrado (este pendiente cubre esa parte); la implementación en código (mecanismo de `current_tenant()` dinámico por sesión, `ROLES_PANEL`, `requiereRolPanel.js`, `rolesGestionables()`, reescritura de las policies RLS que hoy usan `es_superadmin()` como bypass total) queda como trabajo aparte, todavía no empezado | 🟢 Resuelto 2026-07-15 — ítems A-I completos y probados (ver detalle completo abajo). Kickoff de implementación dado por el Desarrollador 2026-07-14 ("vamos con el pendiente 30"), alcance de esta sesión acordado como Fase 1 (ítems A + C del desglose: rol `admin_plataforma` + sesión de tenant dinámica), con instrucción explícita de que el pendiente sigue abierto hasta cubrir A-I. **Fase 1 completa y probada 2026-07-14**: `backend/src/db/schema_admin_plataforma_01.sql` aplicado contra Supabase real (rol `admin_plataforma` agregado al CHECK de `usuarios.rol`, `usuarios.prestadora_id` admite NULL solo para ese rol, tabla `sesiones_tenant_admin_plataforma` con índice único parcial de sesión vigente por admin, `current_tenant()` reescrita en `schema_admin_plataforma_01.sql:81-91` con `COALESCE` — sesión activa primero, fallback a `usuarios.prestadora_id` si no hay, 100% retrocompatible para los 5 roles existentes). `panel/src/lib/roles.js:9-13` y `backend/src/middleware/requiereRolPanel.js:22-38` reconocen el rol nuevo; este último resuelve `prestadoraId` desde la sesión activa. Rutas nuevas `backend/src/routes/panelSesionTenant.js` (GET/POST entrar/POST salir, registradas en `backend/src/server.js:9,31`) y `backend/src/routes/panelUsuarios.js:11-20` (scoping reutilizado sin ramas nuevas por rol, con corte explícito si no hay sesión activa — sin ese corte, `.eq('prestadora_id', null)` rompía contra Postgres, bug encontrado y corregido en esta misma prueba). **Probado de punta a punta con requests HTTP reales** (cuenta `admin_plataforma` de prueba, borrada al terminar): GET sin sesión → `sesion: null`; POST entrar → 200 con datos de la prestadora; POST entrar de nuevo sin salir → 409 (una sesión vigente por vez, según el índice único); GET `/api/panel/usuarios` con sesión activa → lista scoped correctamente a esa prestadora; POST salir + GET `/api/panel/usuarios` sin sesión → 400 explícito, no crash ni fuga de datos. **Ítem B completo y probado 2026-07-14** (kickoff explícito del Desarrollador el mismo día, con dos rondas de aclaración: la sandbox queda permanente para siempre, solo se borran los datos de prueba tras verificar; `escalas_legales` confirmado global a propósito, no se toca). `backend/src/db/schema_admin_plataforma_02_acotar_superadmin.sql` aplicado contra Supabase real vía MCP: (1) prestadora sandbox permanente creada (`id=5d727437-a5ff-432f-b9f6-10015e61ffef`, `estado='prospecto'`, uso técnico interno, nunca una prestadora real); (2) la única cuenta `superadmin` real reasignada a esa sandbox (`usuarios.prestadora_id`); (3) barrido mecánico vía `pg_policies`+`DO $$` que reescribió 37 policies, reemplazando `es_superadmin()` por `(es_superadmin() AND (prestadora_id = current_tenant()))` — verificado con consulta directa a `pg_policies` post-migración, las 37 muestran el texto nuevo; (4) 2 casos especiales tratados aparte: `prestadoras` (compara `id`, no `prestadora_id` — la tabla ES el tenant) y `verificaciones_asistente` (JOIN a `asistentes.prestadora_id`, no tiene columna propia); (5) `configuracion_empresa` y `escalas_legales` verificados sin tocar (confirmado con la misma consulta a `pg_policies`, siguen con el bypass total anterior — `configuracion_empresa` es tabla legacy sin queries reales, candidata a `DROP TABLE` en sesión aparte; `escalas_legales` es global a propósito, decisión del Desarrollador). Cerrado también un hueco relacionado en `backend/src/routes/panelUsuarios.js:70-77`: `superadmin` ya no puede overridear `prestadora_id` al crear otra cuenta `superadmin` (antes podía asignarle una prestadora real a un `superadmin` nuevo, evadiendo el acotamiento — ahora una cuenta `superadmin` nueva siempre nace en la sandbox del creador). **Probado de punta a punta con RLS real** (no Service Role Key): script `backend/scripts/test_item_b_scope_superadmin.js` crea una cuenta `superadmin` de prueba + un Asistente en la sandbox + un Asistente en la prestadora real original, inicia sesión como ese `superadmin` con su propio JWT, y confirma que ve el Asistente de la sandbox pero **no** el de la prestadora real — verificado, ambos resultados correctos. Datos de prueba borrados al terminar (verificado con consulta post-limpieza: 0 filas residuales), sandbox permanente intacta. **Ítems D (enforcement de los timeouts 5min/60min), E (banner "modo dentro de una prestadora"), F (advertencia en confirmaciones destructivas), G (log de auditoría), H (MFA obligatorio) e I (selector de prestadora en el Panel, también cierra el pendiente #26) siguen sin empezar** el reordenamiento: se detectó que D y E no eran probables en la práctica sin que I existiera primero (no había ningún punto de entrada del Panel para que `admin_plataforma` entrara a una prestadora), así que con confirmación explícita del Desarrollador ("si") se pasó a **I → E → D → F → G → H**. **Ítem I completo y probado 2026-07-14**: backend `GET /api/panel/prestadoras` (`backend/src/routes/panelPrestadoras.js`, registrado en `backend/src/server.js:10,33`) — lista `id, nombre_fantasia, estado`, acotado a `admin_plataforma`/`superadmin`. Frontend: página nueva `panel/src/pages/Prestadoras.jsx` (lista + botón "Entrar" por fila, banner de sesión activa con hora de vencimiento + botón "Salir de la prestadora"), ruta `/prestadoras` en `panel/src/App.jsx` con gate nuevo `roles={['admin_plataforma','superadmin']}` agregado a `ProtectedRoute.jsx`, nav link en `panel/src/components/layout/Layout.jsx` visible solo para esos 2 roles. i18n agregado en las 3 locales (`nav.prestadoras`, sección `prestadoras.*`, `usuarios_panel.campo_prestadora`/`prestadora_placeholder`) en `panel/src/i18n/translations.js`. **Hallazgo no previsto en el checklist original, corregido en el mismo movimiento**: `panel/src/components/layout/ProtectedRoute.jsx:14` no incluía `admin_plataforma` en la lista de roles válidos para iniciar sesión en el Panel — pese a que `roles.js`/`requiereRolPanel.js` ya lo trataban como rol válido desde Fase 1, cualquier cuenta `admin_plataforma` real habría sido rebotada a `/login` al intentar entrar. Corregido agregándolo a esa lista. `UsuariosPanel.jsx` (cierra el pendiente #26 en el mismo movimiento): selector `prestadora_id` nuevo, visible para `superadmin` cuando elige rol `admin_prestadora` al crear una cuenta nueva — obligatorio (botón Guardar deshabilitado sin elegir), poblado desde el mismo endpoint `GET /api/panel/prestadoras`. **Probado de punta a punta**: (1) `backend/scripts/test_item_i_prestadoras.js` — listar/entrar/bloquear-doble-entrada-con-409/salir, con cuenta `admin_plataforma` real y JWT propio (no Service Role Key), contra una prestadora dummy creada y borrada en el mismo script; (2) superadmin también puede listar prestadoras (verificado con una cuenta de prueba aparte); (3) navegador real (Playwright, Panel local + backend local): login con cuenta `admin_plataforma` de prueba (confirma que el hallazgo de `ProtectedRoute.jsx` de arriba efectivamente se arregló, antes hubiera fallado acá), página Prestadoras lista las 2 prestadoras reales, "Entrar" muestra el banner con hora de vencimiento y deshabilita ambos botones "Entrar", "Salir de la prestadora" limpia el banner y reactiva los botones; (4) login con cuenta `superadmin` de prueba, `UsuariosPanel.jsx` → Nuevo usuario → rol Admin → aparece el selector de Prestadora poblado con las 2 reales → cuenta creada con `prestadora_id` verificado por consulta directa a la base que coincide exactamente con lo elegido en el selector. Las 4 cuentas de prueba (`admin_plataforma`×2, `superadmin`×1, `admin_prestadora` nuevo×1) y la prestadora dummy se borraron al terminar. `npm run build` del panel sin errores. Desplegado con `vercel --prod` (Regla 13.1) tras el commit/push. **Hallazgo no bloqueante, no corregido en esta sesión** (fuera del alcance de I, agregado como pendiente #34): la columna "Rol" de la tabla de `UsuariosPanel.jsx` queda en blanco para cuentas `rol='admin_prestadora'` — el código busca la clave i18n `rol_${u.rol}` (`rol_admin_prestadora`), pero la clave real en `translations.js` es `rol_admin` (mismatch de nombre, no de dato). **Ítem E completo y probado 2026-07-14**: banner permanente y visualmente distinto ("modo dentro de una prestadora"), visible en **cualquier ruta** del Panel mientras `admin_plataforma`/`superadmin` tenga una sesión de tenant activa — no solo en `/prestadoras`. Implementado con un contexto nuevo compartido `panel/src/context/TenantSessionContext.jsx` (provee `sesion`/`recargar`/`salir`, consulta `GET /api/panel/sesion-tenant` solo para esos 2 roles, montado en `panel/src/App.jsx` envolviendo el `BrowserRouter`), consumido tanto por el banner nuevo (`BannerSesionTenant` dentro de `panel/src/components/layout/Layout.jsx`, sticky arriba del header, fondo `--naranja-alerta` para distinguirlo del resto del Panel, con botón "Salir de la prestadora" ahí mismo) como por `panel/src/pages/Prestadoras.jsx` (refactorizada para usar el mismo contexto en vez de duplicar su propio fetch de sesión — entrar/salir desde cualquiera de los dos lugares mantiene ambos sincronizados sin recargar la página). Reutiliza las claves i18n ya existentes de `prestadoras.*` (`sesion_activa_titulo`, `sesion_activa_expira`, `salir`, `saliendo`) — no hizo falta agregar texto nuevo. CSS nuevo en `panel/src/index.css` (`.banner-sesion-tenant`, `.banner-sesion-tenant-salir`). **Probado de punta a punta en navegador real** (Playwright, Panel local + backend local, cuenta `admin_plataforma` de prueba creada y borrada al terminar): login correcto (confirma otra vez el fix de `ProtectedRoute.jsx` del ítem I), entrar a la prestadora sandbox desde `/prestadoras` → banner aparece con el nombre correcto y hora de vencimiento; navegación a `/` (Dashboard, ruta sin relación con Prestadoras) → el banner sigue visible, confirmando que es global y no solo de la pantalla de Prestadoras; clic en "Salir de la prestadora" desde el banner (no desde la página Prestadoras) → banner desaparece correctamente. `npm run build` del panel sin errores. **Ítem D completo y probado 2026-07-14/15**: timeout doble — 5 min de inactividad (corte silencioso) + tope absoluto de 60 min con aviso a los 10 min restantes (≈50 min de sesión) y reconfirmación vía `POST /api/panel/sesion-tenant/renovar`. Como buena parte del Panel consulta Supabase directo desde el frontend con RLS (no solo vía backend Express), el corte tuvo que vivir en dos capas: (1) `current_tenant()` en `backend/src/db/schema_admin_plataforma_02_timeout.sql` (aplicado contra Supabase real vía MCP) ahora exige también `ultima_actividad_at > NOW() - INTERVAL '5 minutes'`, no solo `expira_at > NOW()` — cubre cualquier query RLS directa; (2) `backend/src/middleware/requiereRolPanel.js` replica el mismo chequeo para resolver `prestadoraId` en rutas Express, y bumpea `ultima_actividad_at` en cada request real (excluyendo las rutas propias de `/api/panel/sesion-tenant`, para que el propio polling de estado no cuente como actividad y anule el timeout). La "actividad" real la marca el frontend: `panel/src/context/TenantSessionContext.jsx` escucha `click`/`keydown` (throttled a 1/min) y llama a un endpoint nuevo `POST /sesion-tenant/actividad` — así el timeout depende de interacción real del usuario, no de cuántos requests dispara cada pantalla. El mismo contexto hace polling de `GET /sesion-tenant` cada 30s para que el banner refleje el corte automático sin recargar. `backend/src/routes/panelSesionTenant.js` — nueva función compartida `buscarSesionVigenteYCerrarSiVencio` (cierra `salida_at` si venció por tope o inactividad, la usan `GET /`, `POST /renovar` y `POST /actividad`); ruta nueva `POST /renovar` extiende `expira_at` otros 60 min y bumpea actividad, solo si la sesión sigue vigente. Frontend: `BannerSesionTenant` (`panel/src/components/layout/Layout.jsx`) cambia a una variante de advertencia (fondo `--rojo-peligro`, clave i18n `sesion_advertencia`) cuando faltan ≤10 min para `expira_at`, con botón nuevo "Seguir trabajando" (`seguir_trabajando`/`renovando`) que llama a `/renovar`. Claves i18n nuevas agregadas en las 3 locales. **Probado de punta a punta en navegador real** (Playwright, Panel local + backend local, cuenta `admin_plataforma` de prueba creada y borrada al terminar, prestadora sandbox): entrar a la prestadora → banner normal; `expira_at` adelantado a 8 min vía Supabase directo + recarga → banner de advertencia con "Seguir trabajando" aparece correctamente; clic en "Seguir trabajando" → banner vuelve a estado normal con `expira_at` extendido (confirmado con la hora mostrada); `ultima_actividad_at` atrasado 6 min vía Supabase directo + recarga → sesión desaparece del banner y ambos botones "Entrar" se reactivan — confirmado también contra la base que quedó con `salida_at` seteado (no es solo un efecto visual del cliente). `npm run build` del panel sin errores. Desplegado con `vercel --prod` (Regla 13.1) tras el commit/push. **Ítem F completo y probado 2026-07-15**: los 17 `window.confirm(...)` de Regla 4 (`Configuracion.jsx`×4, `Continuidad.jsx`×2, `ListaPrecioDetalle.jsx`, `guardias/GuardiaAcciones.jsx`×3, `PostulacionDetalle.jsx`×2, `asistentes/VinculoCeseTab.jsx`, `familias/PrestacionesPaciente.jsx`, `SolicitudDetalle.jsx`×2, `UsuariosPanel.jsx`) reemplazados por una función centralizada nueva, `useConfirmarDestructivo()` (`panel/src/context/TenantSessionContext.jsx`), que antepone una línea de advertencia con el nombre de la prestadora cuando hay una sesión de tenant activa, y se comporta como `window.confirm` normal (sin agregar nada) cuando no la hay — reutiliza el mismo `TenantSessionContext` del banner persistente del ítem E, sin duplicar el fetch de sesión. Clave i18n nueva `prestadoras.confirmar_advertencia_tenant` agregada en las 3 locales. **Probado en navegador real** (Playwright, cuenta `admin_plataforma` de prueba, prestadora sandbox): con sesión de tenant activa, clic en "Dar de baja" de un Coordinador de prueba (creado ad-hoc en `UsuariosPanel.jsx`) mostró el diálogo nativo con el texto "Estás dentro de Sandbox Superadmin, no en tu propia prestadora." antepuesto al mensaje original — confirmado con `browser_handle_dialog` capturando el texto exacto del diálogo, no solo inferido del código; diálogo cancelado sin ejecutar la baja. **Hallazgo no bloqueante para este ítem, registrado aparte como pendiente #36**: la sección de Configuración tiene su propio gate de rol en el backend (`panelConfiguracion.js`) que no incluye a `admin_plataforma`, así que no pudo usarse para esta prueba puntual — se probó con `UsuariosPanel.jsx` en su lugar, que sí lo incluye. `npm run build` del panel sin errores. Datos de prueba (cuenta `admin_plataforma` + Coordinador de prueba) borrados al terminar, verificado con consulta. Desplegado con `vercel --prod` (Regla 13.1) tras el commit/push. **Ítem G completo y probado 2026-07-15**: cobertura completa acordada con el Desarrollador vía pregunta explícita ("Cobertura completa con triggers de base de datos") — implementada como diseño combinado de dos capas, no solo triggers, tras detectar que el backend usa la Service Role Key (`backend/src/db/connection.js`) y por lo tanto `auth.uid()` da NULL dentro de cualquier trigger disparado por una escritura ruteada por Express. Tabla nueva `auditoria_admin_plataforma` (`backend/src/db/schema_admin_plataforma_03_auditoria.sql`, aplicada contra Supabase real vía MCP): `admin_id`, `prestadora_id`, `tipo_evento` (`login`/`logout`/`renovacion`/`mutacion`), `tabla_afectada`, `operacion`, `registro_id`, `detalle` JSONB; RLS de lectura: `superadmin` ve todo (`es_superadmin()`), `admin_prestadora` ve solo lo de su propia prestadora, sin policy de escritura para nadie (solo las funciones `SECURITY DEFINER` insertan). Capa 1 (sesión, vía Express, siempre con JWT real del admin): `backend/src/routes/panelSesionTenant.js` — `registrarAuditoria()` nuevo, wireado en `buscarSesionVigenteYCerrarSiVencio` (logout automático por tope 60min o inactividad 5min), POST `/` (login), POST `/renovar` (renovación), POST `/salir` (logout manual). Capa 2 (mutaciones vía Express): `backend/src/middleware/requiereRolPanel.js` — `registrarAuditoriaMutacionExpress()`, dispara solo para `admin_plataforma` con sesión de tenant activa, en métodos POST/PUT/PATCH/DELETE (excluyendo las rutas propias de sesión-tenant), solo si la respuesta terminó en 2xx. Capa 3 (trigger de base, para cualquier escritura que sí corra con JWT real, no con Service Role Key): función `es_sesion_tenant_admin_plataforma_activa()` (distinta de `current_tenant()` — esta chequea específicamente si hay una fila viva en `sesiones_tenant_admin_plataforma`, para no auditar de forma espuria la actividad normal de `admin_prestadora`/`coordinador`) + `fn_auditoria_admin_plataforma_mutacion()` aplicada como trigger genérico a las 35 tablas con `prestadora_id`/`current_tenant()`. Lectura: ruta nueva `backend/src/routes/panelAuditoria.js` (`GET /`, gateada a `superadmin`/`admin_prestadora`, 403 al resto, filtra por `prestadora_id` cuando quien pregunta es `admin_prestadora`), registrada en `backend/src/server.js:10,35`. Frontend: página nueva `panel/src/pages/Auditoria.jsx` (4 estados, tabla fecha/admin/prestadora/evento con `descripcionEvento()` traduciendo motivo de logout/mutación), ruta `/auditoria` en `panel/src/App.jsx:22,54` gateada a `['admin_prestadora','superadmin']`, nav link en `panel/src/components/layout/Layout.jsx:92` con el mismo gate, i18n completo (`nav.auditoria` + sección `auditoria.*`) en las 3 locales de `panel/src/i18n/translations.js`. **Probado de punta a punta** con 3 scripts descartables (borrados al terminar): login/mutación/renovación/logout capturados correctamente por las capas 1 y 2 con JWT real de una cuenta `admin_plataforma` de prueba sobre la prestadora sandbox; `npm run build` del panel sin errores. **Hallazgo crítico detectado y verificado, no corregido en esta sesión** (pertenece al ítem B de este mismo pendiente #30, ya cerrado con alcance explícitamente acotado — ver arriba, `schema_admin_plataforma_02_acotar_superadmin.sql`): ninguna de las 37 policies RLS reescritas en el ítem B, ni ninguna otra policy de las 35 tablas auditadas, le da a `admin_plataforma` acceso directo de lectura/escritura — confirmado con grep dirigido sobre todos los `.sql` de `backend/src/db/` y con una prueba real de RLS (JWT de `admin_plataforma`, no Service Role Key) que devolvió `[]` al leer `solicitudes` pese a tener una sesión de tenant activa. Consecuencia práctica: la Capa 3 (trigger de base) está correctamente construida pero hoy no se dispara nunca en el uso real del Panel, porque toda mutación de `admin_plataforma` pasa por Express con Service Role Key (capas 1 y 2, sí 100% verificadas). No es un bug de esta sesión — es la misma limitación de alcance que el ítem B ya documentó como pendiente aparte; no se le asigna número nuevo, queda registrada acá como precisión de por qué la Capa 3 hoy es una red de respaldo dormida y no la vía principal de cobertura. **Revisado y cerrado 2026-07-18, decisión explícita del Desarrollador:** se mantiene así a propósito — no se le da a `admin_plataforma` acceso RLS directo solo para activar la Capa 3, porque eso agrandaría su superficie de acceso real (hoy limitada a las Capas 1+2 vía Express, que sí cubren el 100% del uso real del Panel) a cambio de activar una capa que sería redundante. Si en el futuro aparece un camino real de escritura fuera de Express (una integración directa a Supabase, por ejemplo), ahí sí revisar si la Capa 3 pasa a ser necesaria. **Ítem H completo y probado 2026-07-15 — decisión revisada respecto del diseño original**: el diseño previo de este mismo pendiente (fila de arriba, `docs/PLAN_MULTITENANT_XEITRA.md` 3.4/3.4.1) decía "MFA obligatorio sin excepción" para `superadmin`/`admin_plataforma`. El Desarrollador lo revisó explícitamente el 2026-07-15 ("Hasta tanto el sistema esté pulido y el usuario acostumbrado no queremos complejizarlo más allá de lo necesario. que sea configurable on off por el superadministrador del sistema, es decir por mí") — pasa a ser **configurable on/off por el propio superadmin**, arrancando apagado por defecto. Documentado también en `CLAUDE.md` (nota junto al glosario de Superadmin/Admin_plataforma) y en `docs/PLAN_MULTITENANT_XEITRA.md:416` (nota "Revisado 2026-07-15"), para que no se reintroduzca "obligatorio sin excepción" sin leer el porqué. TOTP vía Supabase Auth nativo (`supabase.auth.mfa.*`), sin librería ni almacenamiento propio. Tabla nueva `configuracion_plataforma` (`backend/src/db/schema_admin_plataforma_04_mfa.sql`, aplicada contra Supabase real vía MCP, migración `item_h_mfa_toggle_configurable`) — singleton (`id BOOLEAN PRIMARY KEY DEFAULT TRUE`), columna `mfa_admin_obligatorio BOOLEAN DEFAULT FALSE`, RLS de lectura abierta a cualquier autenticado (necesario para que el propio login pueda chequear el toggle antes de lograr aal2, evita un deadlock circular) y escritura acotada a `es_superadmin()`. La función `es_superadmin()` (la misma que ya gatea las ~37 policies del ítem B) pasó de `LANGUAGE sql` a `LANGUAGE plpgsql`: ahora, solo si el toggle está en `TRUE`, exige además `(auth.jwt() ->> 'aal') = 'aal2'` — con el toggle en `FALSE` (default), se comporta exactamente igual que antes de este ítem. Capa Express: `backend/src/middleware/requiereRolPanel.js` — `leerAalDelToken()` nuevo (decodifica el claim `aal` del JWT ya validado, Supabase no lo expone en el objeto `user`), y para `superadmin`/`admin_plataforma` consulta el mismo toggle y devuelve `403 {codigo:'mfa_requerido'}` si está en `TRUE` sin `aal2` — `admin_plataforma` queda cubierto acá porque (ítem G, hallazgo ya documentado arriba) no tiene RLS directa de todos modos. Ruta nueva `backend/src/routes/panelConfiguracionPlataforma.js` (`GET`/`PATCH /mfa`, registrada en `backend/src/server.js`), acotada a `superadmin` únicamente — ni `admin_plataforma` puede tocar su propio requisito de MFA. Frontend: `panel/src/context/AuthContext.jsx` agrega `mfaEstado`/`evaluarMfa()`/`refrescarMfa()` (chequea rol → toggle → `getAuthenticatorAssuranceLevel()` → `listFactors()`); página nueva `panel/src/pages/Mfa.jsx` (enrolamiento con QR vía `mfa.enroll` la primera vez, challenge+verify las siguientes), ruta `/mfa` en `panel/src/App.jsx`; `panel/src/components/layout/ProtectedRoute.jsx` redirige a `/mfa` cuando corresponde; pestaña nueva "Seguridad" en `panel/src/pages/Configuracion.jsx`, visible solo para `superadmin`, con el checkbox on/off. i18n completo (9 claves bajo `auth`, 5 bajo `configuracion`) en las 3 locales. **Probado de punta a punta**: (1) script descartable `backend/scripts/test_item_h_mfa.mjs` (implementa TOTP RFC 6238 a mano con el módulo `crypto` nativo, sin sumar dependencia nueva solo para un script de un uso) — toggle apagado por defecto, ruta Express y RLS directa (`prestadoras`, único caso donde `es_superadmin()` es la única policy) ambas pasan sin aal2; toggle prendido → ambas capas bloquean (403 `mfa_requerido` en Express, 0 filas en RLS) sin aal2; enrolar + verificar con un código TOTP calculado del secreto real → aal2, ambas capas vuelven a pasar; toggle apagado de nuevo → requisito desaparece. (2) navegador real (Playwright, Panel + backend locales, cuenta `superadmin` de prueba en la sandbox): activar el toggle desde la pestaña Seguridad → siguiente carga redirige a `/mfa` con pantalla de enrolamiento (QR real, código verificado con el mismo cálculo TOTP) → vuelve al Dashboard; logout/login de nuevo → esta vez pantalla de verificación (no de enrolamiento, factor ya verificado) → código correcto → entra; toggle apagado de nuevo desde la misma pestaña. `npm run build` del panel sin errores. Cuenta de prueba y configuración vueltos a su estado original en ambas pruebas, verificado con consulta directa (0 filas residuales). **Decisión explícita del Desarrollador, no construida esta sesión**: clave alfanumérica + biometría opcional (WebAuthn) para el login de Familia — sin superficie de login todavía (PWA Familias es Etapa 4, `docs/BUILD_ORDER.md:14`, ninguna de las dos etapas empezada), documentado únicamente en `docs/PRD_04_05_App_Servicio.md` (secciones de login de PWA Asistente y PWA Familia) para construir cuando corresponda. **Sin mecanismo de recuperación por pérdida de dispositivo** (decisión explícita del Desarrollador, "no esta sesión") — ver pendiente #37. **Con el cierre de este ítem, el pendiente #30 completo (A-I) queda cerrado.** |
| 24 | Aplicar `backend/src/db/schema_certificados_medicos_storage.sql` contra Supabase real — crea el bucket privado `certificados-medicos` en Supabase Storage y habilita RLS en `storage.objects` (deny-by-default; el acceso real pasa por el backend con Service Role Key, ver `backend/src/routes/panelAusencias.js`). Parte del punto 1 del roadmap de continuidad de proveedores (pendiente #15) — feature de subida de certificado médico para `ausencias.certificado_url`, construida completa en código (backend + Panel + i18n) pero sin poder aplicarse contra Supabase real porque esta sesión (2026-07-13) no tenía el MCP de Supabase conectado (mismo patrón que pendientes #21/#22) | 2026-07-13 | Desarrollador (correr el SQL, ya termina con `NOTIFY pgrst, 'reload schema';`) o una sesión con el MCP de Supabase conectado | El bucket `certificados-medicos` existe en Supabase Storage (verificado con una consulta real a `storage.buckets`, no solo que el archivo `.sql` exista en el repo) y una subida de prueba desde el Panel funciona sin error 404/400 | 🟢 Resuelto 2026-07-13 — bucket creado con `INSERT INTO storage.buckets` vía MCP, verificado con `SELECT id, name, public FROM storage.buckets WHERE id='certificados-medicos'` → 1 fila, `public=false`. **Detalle a anotar:** el `ALTER TABLE storage.objects ENABLE ROW LEVEL SECURITY;` del archivo falló (`ERROR 42501: must be owner of table objects` — el rol `postgres` del MCP no es dueño de esa tabla en un proyecto Supabase gestionado); no bloqueó nada porque Supabase ya trae RLS habilitada en `storage.objects` por default, confirmado igual con `SELECT relrowsecurity FROM pg_class ...` → `true`. **Al momento de probar en el navegador se encontró que el código de este pendiente (`panelAusencias.js`, `AusenciasCoberturaTab.jsx`, este mismo `.sql`) todavía no estaba commiteado ni desplegado** (`git status` mostraba los 3 archivos como modificados/`??`) — se commiteó y desplegó (commit `bf2a745`, backend a Railway, Panel a Vercel) antes de continuar, verificado de forma independiente (`git log` mostrando el commit + `GET /api/panel/ausencias/.../certificado-url` pasando de 404 a 401 tras el deploy). Con el deploy confirmado, prueba end-to-end completa en `https://aurevia-panel.vercel.app` con datos de prueba creados ad-hoc (1 usuario Auth + Asistente + Ausencia, nombre "PRUEBA temporal — pendiente #24"): subida de `certificado_prueba.pdf` → apareció "Certificado cargado"; "Ver certificado" abrió `storage/v1/object/sign/certificados-medicos/...` en pestaña nueva con **200 OK** (confirmado con `browser_network_requests`, no solo visualmente). Limpieza al terminar, verificada con consultas: objeto borrado del bucket (`DELETE /storage/v1/object/...` → 200), mirror en R2 borrado (`DeleteObjectCommand` → 204), fila de `ausencias`/`asistentes`/`usuarios` borradas y cuenta de Auth de prueba eliminada (`DELETE .../auth/v1/admin/users/...` → 200) — conteo final `asistentes=0, ausencias=0, archivos_bucket=0` |

| 32 | Aplicar `backend/src/db/schema_cierre_servicio_paciente.sql` contra Supabase real — agrega `prestadoras.dias_generacion_series_guardia` (default 90) y crea `cierres_servicio_paciente` (RLS restringida a `admin_prestadora`+`coordinador`, sin bypass de `superadmin` por instrucción explícita del Desarrollador). Resuelve el candidato (2) del pendiente #18 | 2026-07-14 | Claude Code | La columna `prestadoras.dias_generacion_series_guardia` y la tabla `cierres_servicio_paciente` existen en Supabase (verificado con consulta real); probado en navegador real que Coordinador puede cerrar un servicio y Superadmin no | 🟢 **Cerrado 2026-07-14.** SQL aplicado y verificado vía MCP de Supabase: `information_schema.columns` confirma `dias_generacion_series_guardia smallint DEFAULT 90`; `pg_class.relrowsecurity=true`; `pg_policies` confirma la policy `coordinador_y_admin_gestionan_cierres_servicio_paciente` sin `es_superadmin()`; `pg_constraint` confirma las 6 constraints incluido el FK compuesto de tenant `(paciente_id, prestadora_id)`.

| 33 | `configuracion_empresa` (tabla legacy, reemplazada por `configuracion_prestadora` el 2026-07-13, pendiente #25) sigue existiendo en Supabase con su policy RLS `panel_edita_configuracion_empresa`/`panel_lee_configuracion_empresa` con bypass total de `superadmin` sin acotar — deliberadamente excluida del barrido del pendiente #30 ítem B (2026-07-14) por ser código muerto verificado (`grep` confirmó que ningún archivo de backend hace una query real contra ella, solo queda un comentario obsoleto en `backend/src/routes/panelConfiguracion.js:19`), pero la tabla y su policy sin acotar siguen existiendo en la base real — brecha latente, no explotable hoy porque ninguna ruta de la app la consulta, pero queda ahí sin limpiar | 2026-07-14 | Desarrollador (decidir si se hace `DROP TABLE configuracion_empresa` o se deja) | La tabla se elimina de Supabase (`DROP TABLE`), o el Desarrollador decide explícitamente mantenerla y este pendiente se cierra igual con esa decisión registrada | 🟢 Resuelto 2026-07-15 — decisión explícita del Desarrollador ("borrala"): sin FKs que la referenciaran (confirmado con `pg_constraint` antes de borrar), `DROP TABLE configuracion_empresa;` + `NOTIFY pgrst, 'reload schema';` ejecutados contra Supabase real, verificado con `information_schema.tables` que ya no existe. El comentario de `panelConfiguracion.js:19` se revisó y se dejó sin tocar — es una nota histórica correcta (explica por qué la ruta se llama `/empresa` aunque usa `configuracion_prestadora`), no una referencia viva rota. Las menciones restantes quedan en archivos `.sql` de migraciones ya aplicadas (registro histórico, no se reescriben) |
| 34 | En `panel/src/pages/UsuariosPanel.jsx:92`, la columna "Rol" de la tabla de usuarios queda en blanco para cuentas `rol='admin_prestadora'` — el código busca la clave i18n `t.usuarios_panel[\`rol_${u.rol}\`]`, que para ese valor sería `rol_admin_prestadora`, pero la clave real en `panel/src/i18n/translations.js` es `rol_admin` (mismatch de nombre entre el valor de dato `admin_prestadora` y la clave i18n `rol_admin`, no falta ningún dato). Encontrado incidentalmente el 2026-07-14 durante la prueba de navegador del ítem I del pendiente #30 (fila del selector "Admin"/"Admin de la prestadora original" con la celda de Rol vacía); deliberadamente no corregido en esa sesión para no expandir el alcance en silencio (Regla 12.1) | 2026-07-14 | Claude Code (fix chico de 1 línea, cualquier sesión futura) | La columna "Rol" muestra un texto no vacío para una cuenta `admin_prestadora` real en el Panel, probado en navegador | 🟢 Resuelto 2026-07-15 — renombrada la clave i18n `rol_admin` a `rol_admin_prestadora` en las 3 locales de `panel/src/i18n/translations.js` (mismo texto "Admin", solo el nombre de la clave, para que coincida con el valor real del dato `admin_prestadora`) y actualizada la única referencia directa en `UsuariosPanel.jsx:207` (el `<option>` del selector de rol al crear un usuario). Sin más referencias a `rol_admin` en `panel/src` (confirmado con grep). Probado en navegador real (Playwright): cuenta `admin_prestadora` de prueba creada ad-hoc en la Sandbox, vista desde una cuenta `superadmin` de prueba — la fila mostró "Admin" en la columna Rol en vez de blanco. Ambas cuentas de prueba borradas al terminar, verificado con consulta (`usuarios` vuelto a 1 sola fila real) |
| 35 | Barrido completo de menciones a la prestadora original en todo el proyecto (código backend/panel/sitio-web, `docs/`, tablas/datos de Supabase) — listar cada lugar donde aparece, para que el Desarrollador decida caso por caso cuáles son legítimas (marca real del negocio de cuidado domiciliario que se está construyendo, permitida por `CLAUDE.md` en nav/login/footer/PDFs de RRHH; o su fila de dato real en `prestadoras`) y cuáles son fugas de hardcoding o lenguaje que le da un estatus especial que no le corresponde (no es cliente, no tiene contrato de software firmado — ver `docs/PENDIENTES.md`/memoria sobre su estatus de cliente potencial). Pedido explícito del Desarrollador (2026-07-14) tras una tercera corrección repetida de la IA nombrando a la prestadora original como si ya conviviera con otras prestadoras reales en el sistema — el Desarrollador pidió el barrido para "sacarla del mapa" en la medida de lo posible. **A empezar apenas cierre el pendiente #30 y sus derivados (D, E, F, G, H)**, no antes | 2026-07-14 | Claude Code (barrido y lista), Desarrollador (decidir qué se corrige y qué se mantiene) | Lista completa entregada al Desarrollador con archivo:línea (o tabla:fila) de cada mención de la prestadora original encontrada, clasificada como legítima o a revisar | 🟢 Resuelto 2026-07-15 — barrido de 44 archivos / 411 ocurrencias entregado. Mayoría legítima (marca de negocio, scripts de test, docs). Hallazgo real del barrido: en la primera entrega, la propia IA describió comentarios SQL históricos como referidos a la prestadora original como "la única prestadora real a esa fecha" — el Desarrollador corrigió esto de forma explícita (esa prestadora nunca fue ni es una prestadora real: no nació como cliente, no tiene contrato firmado). Esa corrección llevó a encontrar un bug de dato real: `prestadoras.estado='certificada'` en su fila (seedeado así en `backend/src/db/schema_multitenant_01.sql:58` durante la migración mono-tenant→multi-tenant), que afirmaba una certificación/contrato que nunca ocurrió. Corregido a `estado='prospecto'` (mismo valor que usa la fila Sandbox Superadmin) vía UPDATE directo en Supabase, con aprobación explícita del Desarrollador tras disclosure del efecto colateral (ver pendiente #38). Dos hallazgos menores quedaron marcados como candidatos de inventario para la futura migración multi-tenant (`docs/Prompt_Claude_Code_Xeitra_Multitenant.md`), no corregidos ahora por no ser parte de una migración aprobada en curso: `panel/index.html:9` (título del Panel hardcodeado) y `.github/workflows/deploy-backend.yml:29` (nombre de servicio Railway `aurevia-backend`) |

**Bug de cascada encontrado en la prueba de navegador real (mismo día) y ya resuelto — dos capas:**

1. **Causa raíz real del bug de cascada** (las dos correcciones anteriores en `schema_cierre_servicio_zona_fix.sql`, agregar `WITH CHECK`, no arreglaban nada — diagnóstico equivocado). Es una regla de RLS de Postgres, no un bug de Supabase: una policy declarada solo `FOR UPDATE` no alcanza para que un `UPDATE ... WHERE <condición>` encuentre la fila, porque evaluar ese `WHERE` exige visibilidad de `SELECT` sobre la fila — visibilidad que dan las policies de `SELECT`/`ALL`, no las de `UPDATE`. El Coordinador de prueba no tenía ninguna policy de `SELECT`/`ALL` que lo habilitara sobre `series_guardias`/`guardias` de un Asistente fuera de su zona, así que el `UPDATE` no encontraba la fila — sin ningún error visible (Supabase no distingue "0 filas por RLS" de "0 filas porque no había ninguna que actualizar"). Confirmado de forma aislada con una policy `FOR UPDATE USING (true) WITH CHECK (true)`: seguía fallando; pasó a funcionar en cuanto se declaró `FOR ALL` en lugar de `FOR UPDATE`. **Fix real, tercera vuelta**: `backend/src/db/schema_cierre_servicio_zona_fix.sql` reescrito para declarar `coordinador_cierra_servicio_series_guardias`/`coordinador_cierra_servicio_guardias` como `FOR ALL` (no `FOR UPDATE`) — aplicado contra Supabase real (migración `cierre_servicio_zona_fix_v3_for_all_and_cleanup`) y verificado con requests HTTP reales autenticados como el Coordinador de prueba (JWT real vía `/auth/v1/token`, no simulación SQL de `mcp__supabase__execute_sql`, que había resultado poco confiable en la sesión anterior).
2. **Segundo bug, independiente**: en `panel/src/pages/familias/PrestacionesPaciente.jsx`, el pre-fetch de Asistentes/zonas involucrados (para decidir a quién notificar cruzadamente) corría *antes* del `INSERT` en `cierres_servicio_paciente` — como la policy del punto 1 depende de que ese registro ya exista, el Coordinador fuera de zona no veía ninguna fila por RLS en ese pre-fetch y la notificación cruzada nunca se disparaba. **Fix**: reordenado para que el `INSERT` en `cierres_servicio_paciente` corra primero, y el pre-fetch (y la cascada de `UPDATE`) después.

Ambos fixes verificados con una prueba end-to-end completa en navegador real (login como Coordinador de prueba, clic real en "Cerrar servicio", confirmación del diálogo): la cascada completa (`prestaciones`→`de_baja`, `paquetes_prestaciones`→`de_baja`, `series_guardias`→`cancelada`, `guardias`→`cancelada`) funciona, y `notificaciones_cierre_servicio` se inserta con todos los campos esperados.

**Verificación de que Superadmin no puede ejecutar el cierre (pedida explícitamente por el Desarrollador, confirmada 2026-07-14):** el botón/sección de "Cierre de servicio" no se renderiza para Superadmin en el frontend (`panel/src/pages/familias/PrestacionesPaciente.jsx:511`, gate `['admin_prestadora','coordinador'].includes(usuario.rol)`), y la policy RLS de `cierres_servicio_paciente` no tiene bypass de `es_superadmin()` — Superadmin no puede insertar el registro de cierre ni por API directa. Nota aparte, no un gap de este pendiente: las policies preexistentes `panel_gestiona_prestaciones`/`panel_gestiona_paquetes`/`panel_gestiona_guardias`/`panel_gestiona_series_guardias` sí tienen bypass de `es_superadmin()` (mismo patrón que el resto del Panel para el rol técnico) — Superadmin conserva acceso técnico genérico a esas 4 tablas por fuera del flujo de Cierre de servicio, consistente con su rol de infra documentado en `CLAUDE.md`.

Datos de prueba (paciente/familia/asistente/coordinador de prueba y todas sus filas asociadas) borrados de Supabase el 2026-07-14 tras la verificación — no quedan en la base. |
| 36 | En `backend/src/routes/panelConfiguracion.js:9-14`, `requiereAdminOSuperior()` solo aceptaba `['admin_prestadora', 'superadmin']` — no incluía `admin_plataforma`, a diferencia del mismo chequeo en `panelUsuarios.js:11-20` que sí lo incluye (con el corte explícito de "sin sesión de tenant activa" que hace falta para que `.eq('prestadora_id', null)` no rompa). Encontrado incidentalmente el 2026-07-15 probando el ítem F del pendiente #30 (`confirmarDestructivo` en Configuración) — con sesión de tenant activa, `admin_plataforma` recibía 403 "Solo Admin o Superadmin puede editar la configuración" en toda la sección de Configuración (zonas, servicios, personal de emergencia, documentos, notificaciones, WhatsApp+IA), pese a que `panel/src/lib/roles.js:14-16` (`esAdminOSuperior`) sí lo trata como equivalente y el nav le muestra el link. No es el mismo pendiente que el #33 (esa fila es sobre la tabla legacy `configuracion_empresa`, no sobre este gate de rutas) | 2026-07-15 | Claude Code (fix de 1 línea + el mismo corte de "sin sesión de tenant" que ya tiene `panelUsuarios.js`) | `admin_plataforma` con sesión de tenant activa puede editar Configuración de esa prestadora sin 403, probado en navegador real; sin sesión de tenant, recibe 400 explícito (no 403 ni crash) | 🟢 Resuelto 2026-07-15 — agregado `admin_plataforma` a `requiereAdminOSuperior()` en `panelConfiguracion.js:9-16`, replicando el mismo patrón de corte ("sin sesión de tenant activa" → 400 explícito, no `.eq('prestadora_id', null)` roto) que ya existía en `panelUsuarios.js`. **Efecto colateral encontrado y corregido en la verificación**: `GET /configuracion/empresa` con sesión de tenant activa en Sandbox daba `500 Cannot coerce the result to a single JSON object` — no era un bug del fix, sino que la fila de `configuracion_prestadora` de Sandbox Superadmin había sido borrada como parte del borrado total del pendiente #38 (esa tabla estaba incluida en la lista de tablas de la ronda "borrar todo salvo `usuarios`" para Sandbox). Resuelto insertando una fila mínima nueva (`prestadora_id` + `nombre='Sandbox Superadmin'`, únicas columnas `NOT NULL` de la tabla) vía SQL directo contra Supabase. Verificado de punta a punta dos veces: (1) script disponible `backend/scripts/test_pendiente_36.mjs` con JWT real (login real, `POST /sesion-tenant`, luego `GET /configuracion/empresa` y `/configuracion/zonas`) — 400 sin sesión de tenant, 200 en ambas rutas con sesión activa; (2) navegador real (Playwright): cuenta `admin_plataforma` de prueba, login, mensaje `400` explícito antes de entrar a una prestadora, clic en "Entrar" desde Prestadoras, pestaña "Datos de la empresa" de Configuración carga sin error. Cuenta de prueba y su sesión de tenant/auditoría borradas al terminar, verificado sin filas residuales |
| 37 | Sin mecanismo de recuperación de MFA por pérdida de dispositivo (celular con la app de autenticación) para `superadmin`/`admin_plataforma`. Decisión explícita del Desarrollador el 2026-07-15, dentro del mismo hilo de aprobación del ítem H del pendiente #30: no construir nada esta sesión — si pasa, se resuelve manualmente por SQL (`DELETE FROM auth.mfa_factors WHERE user_id = ...` o desactivar el toggle `configuracion_plataforma.mfa_admin_obligatorio`). Como el toggle es configurable y arranca apagado, el caso extremo real (cuenta bloqueada sin poder ni siquiera apagar el toggle) requiere acceso directo a Supabase, no al Panel | 2026-07-15 | Desarrollador (decidir cómo seguir con el hallazgo de bloqueo de puerto SMTP antes de poder cerrar) | Email de recuperación llega realmente a la bandeja del usuario en una prueba de punta a punta contra Railway real — no alcanza con que el código esté armado, tiene que llegar el mail | 🔴 Abierto — diseño y código construidos (recuperación por email de un solo uso, sin nada nuevo que anotar de antemano — el Desarrollador rechazó explícitamente una primera propuesta de códigos pre-generados por esa razón), pero la verificación de punta a punta contra Railway real **encontró un problema de infraestructura más grande que el propio pendiente**: `backend/src/utils/email.js` nunca logra conectar a `smtp.gmail.com` desde el backend desplegado. Dos causas distintas encontradas y solo una corregida: **(a) corregida** — nodemailer 9.0.3 resuelve A y AAAA del host y elige una IP al azar (`family` del transporter no tiene ningún efecto real, confirmado leyendo su código fuente), y la salida IPv6 de Railway da `ENETUNREACH`; se resolvió forzando IPv4 real vía `dns.resolve4` + `servername` explícito (commit `28346cf`), verificado también localmente (mismo síntoma reproducido en esta máquina). **(b) sin corregir, más grave** — con la IPv4 ya forzada, la conexión sigue sin establecerse: un endpoint de diagnóstico temporal (agregado y ya retirado, commits `b2e9b1a`→`7ba84bd`) probó los 3 puertos SMTP salientes (25/465/587) directo desde Railway y los 3 dieron `ETIMEDOUT` en ~260ms uniformes — no es un timeout real de conexión (tardaría segundos/minutos), es la firma de un bloqueo de red a nivel de infraestructura del proveedor: Railway no deja salir tráfico por esos puertos. Si esto es así, **ningún email del sistema se está entregando hoy desde el backend en producción** (ausencias, vencimientos, recuperación de MFA, todo lo que pasa por `email.js`) — no es un problema exclusivo de este pendiente, reabre la premisa del pendiente #44 (que asumía que el techo de volumen de Gmail era la única preocupación futura; si el puerto está bloqueado, el volumen es irrelevante, hoy es 0% de entrega). **Pendiente de decisión del Desarrollador:** (1) confirmar con soporte de Railway si el bloqueo se puede levantar (algunos planes lo permiten a pedido), o (2) migrar el envío a un proveedor transaccional por API HTTPS (SendGrid/Postmark/Resend/SES — puerto 443, nunca bloqueado), aprovechando la investigación de precios ya hecha en `docs/DECISION_EMAIL_ESCALA.md` para el pendiente #44. Ninguna opción se implementó todavía — es un cambio de arquitectura de infraestructura compartida por toda Aurevia (`CLAUDE.md` §11), no se avanza sin esa decisión. Datos de prueba de esta verificación (cuenta `admin_plataforma` temporal + filas de `mfa_codigos_recuperacion`) limpiados sin residuo, confirmado por consulta directa. **Actualización 2026-07-26 — se probó y descartó Resend**: con aprobación del Desarrollador se migró `email.js` a Resend (API HTTPS, evita el bloqueo de puertos de Railway), se guardó la API key, se instaló el paquete y se probó un envío real — **rechazado con 403**: en modo "sandbox" (sin dominio propio verificado) Resend solo deja enviar a la casilla dueña de la cuenta (`notificaciones.aurevia@gmail.com`), no a cualquier destinatario, y Aurevia todavía no tiene dominio real (pendiente #40 ítem 9). Se evaluó también SendGrid y se descartó sin probarlo: su plan "gratis" es en realidad un trial de 60 días, no permanente (`docs/DECISION_EMAIL_ESCALA.md:57`), peor opción que seguir con Gmail. **Decisión tomada:** migrar a la API de Gmail (OAuth2, HTTPS, puerto 443) reutilizando la misma cuenta `notificaciones.aurevia@gmail.com` — no depende de verificar ningún dominio. Requiere pasos manuales del Desarrollador en Google Cloud Console (crear proyecto, habilitar Gmail API, configurar pantalla de consentimiento OAuth, generar credenciales, completar el flujo de autorización una vez para obtener un refresh token) antes de poder implementar el código — **estos pasos todavía no se hicieron**, quedaron interrumpidos para retomar después. Todo el código y las variables de Resend fueron revertidos/eliminados (`git checkout` de `email.js`/`package.json`, paquete `resend` desinstalado, `RESEND_API_KEY`/`RESEND_FROM` borradas de Railway y de `backend/.env`) para no dejar código muerto apuntando a un proveedor descartado — la API key de Resend queda solo registrada en `No hacer commit/claves y contraseñas.txt` como historial, sin usarse. El fix de IPv4/nodemailer (commit `28346cf`) sigue vigente en `email.js`, es inofensivo aunque insuficiente. Cierre de este pendiente sigue exigiendo la misma condición de siempre: el mail tiene que llegar de verdad en una prueba real, ahora vía Gmail API. **En pausa 2026-07-26** — decisión explícita del Desarrollador de posponer la implementación de la API de Gmail (OAuth2) hasta después del MVP; el caso extremo real (cuenta bloqueada) sigue resoluble a mano por SQL mientras tanto, mismo mecanismo ya documentado arriba. **Confirmado de nuevo por el Desarrollador 2026-07-27**: sigue en pausa hasta después del MVP, sin cambios de alcance ni de mecanismo |
| 38 | Borrado total ejecutado a pedido explícito del Desarrollador (2026-07-15, mismo hilo del cierre del pendiente #35): "elimina toda actividad registrada en la base de datos de todas las prestadoras que tengas. luego elimina todas las prestadoras (ninguna es real)". Antes de ejecutar se disclosureó dos veces vía pregunta explícita (Regla de acciones irreversibles): (1) confirmación de que era un borrado irreversible sin backup previo — el Desarrollador eligió "Sí, borrar todo ahora"; (2) al descubrir que solo existían 2 filas en `prestadoras` (Sandbox Superadmin y Prestadora Demo) y que la única cuenta `superadmin` del sistema vivía en la fila Sandbox, se re-confirmó — el Desarrollador eligió conservar la fila Sandbox Superadmin y su usuario superadmin, para no perder el acceso técnico al Panel. Ejecutado en un solo bloque transaccional PL/pgSQL (todo o nada) contra las 39 tablas con columna `prestadora_id` propia + `verificaciones_asistente` (solo tiene `asistente_id`, resuelta aparte antes de borrar `asistentes`): para Prestadora Demo se borró actividad completa + sus 4 cuentas `usuarios`/`auth.users` (2 `admin_prestadora`, 1 `asistente`, 1 `familia`) + la fila `prestadoras` en sí; para Sandbox Superadmin se borró toda actividad salvo la tabla `usuarios` (se conservó su única fila, rol `superadmin`). Verificado post-borrado: `prestadoras` tiene 1 sola fila (Sandbox); `usuarios` tiene 3 filas (1 `superadmin` de Sandbox + 2 `admin_plataforma` con `prestadora_id=NULL`, correctamente no tocadas por no estar scopeadas a ninguna prestadora); `asistentes`/`familias`/`pacientes`/`guardias` en 0 filas | 2026-07-15 | Desarrollador (pidió el borrado) + Claude Code (ejecutó tras doble confirmación explícita) | Ya cerrado — el borrado se ejecutó y se verificó con consultas reales post-borrado (no solo el resultado sin error del bloque SQL) | 🟢 Resuelto 2026-07-15 — consecuencia directa y aceptada: el sistema queda sin ninguna prestadora "real" con datos de actividad; cualquier prueba futura de flujos que dependan de Asistentes/Pacientes/Familias/Guardias reales necesita datos de prueba nuevos, creados y borrados ad-hoc como ya es el patrón de las sesiones de testing en navegador (ver pendientes #6, #31). Los dos jobs diarios gateados en `estado='certificada'` (`revisarVencimientos()`, `extenderSeriesGuardiaAbiertas()`, ver pendiente #35) ahora corren sobre 0 prestadoras hasta que exista una prestadora real certificada |
| 31 | Aplicar `backend/src/db/schema_documentos_asistente.sql` contra Supabase real — crea `tipos_documento_asistente`/`documentos_asistente`, agrega `prestadoras.dias_aviso_vencimiento_documentos` (default 30), migra los valores de las 3 columnas viejas de `asistentes` (`vencimiento_monotributo`/`art`/`seguro`) a filas de `documentos_asistente`, siembra 4 tipos sugeridos por prestadora (Monotributo/ART/Seguro/Certificado de Antecedentes Penales), borra las 3 columnas viejas y reescribe el trigger `bloquear_edicion_laboral_coordinador()` sin ellas. Resuelve el candidato (1) del pendiente #18. No había MCP de Supabase conectado en la sesión donde se escribió el código (2026-07-14), mismo patrón que los pendientes #21/#22/#24 | 2026-07-14 | Desarrollador (correr el SQL, ya termina con `NOTIFY pgrst, 'reload schema';`) o una sesión con el MCP de Supabase conectado | Las 2 tablas nuevas y la columna `prestadoras.dias_aviso_vencimiento_documentos` existen en Supabase (verificado con consulta real, no solo que el archivo `.sql` exista en el repo); las 3 columnas viejas de `asistentes` ya no están; el Panel puede leer/escribir la pestaña "Documentos" de Configuración y el bloque de vencimientos del perfil de Asistente sin 404 | 🟢 Resuelto 2026-07-14 — SQL aplicado y verificado contra Supabase real (4 tipos por prestadora, 0 filas de `documentos_asistente`/`asistentes` sin pérdida de datos, columnas viejas ausentes, evento de notificación nuevo presente, `dias_aviso_vencimiento_documentos=30`). Código commiteado (`aa944ac`) y desplegado (Railway + `vercel --prod`). Probado en el Panel real (`https://aurevia-panel.vercel.app`): pestaña "Documentos de Asistentes" de Configuración — catálogo de 4 tipos visible, alta de un tipo de prueba confirmada y borrada; bloque "Documentos con vencimiento" del perfil de un Asistente de prueba (creado y borrado ad-hoc, Auth + `usuarios` + `asistentes`, 0 filas residuales al terminar). **Bug real encontrado y corregido en el camino:** el primer intento de guardar una fecha de vencimiento en un Asistente sin fila previa devolvió `403` — el `upsert` de `PerfilTab.jsx` no enviaba `prestadora_id`, violando el `NOT NULL`/RLS de `documentos_asistente`. Corregido agregando `usuario.prestadora_id` al payload (commit `e9e357d`), redesplegado, y reverificado con éxito (fila real con `prestadora_id` correcto confirmada por consulta) |

| 39 | Dos cuentas `usuarios`/`auth.users` de prueba huérfanas quedaron en la base real, ajenas a esta sesión: `admin_plataforma` con `nombre='PRUEBA temporal — browser test'` (`id=231f767e-a579-41c8-a2e8-b3486e78d974`) y `admin_plataforma` con `nombre='PRUEBA temporal — Item F'` (`id=40e7cc85-0671-417b-ab57-5819ce6a3a79`) — encontradas incidentalmente el 2026-07-15 al verificar que la limpieza de la propia cuenta de prueba de este pendiente (#36) hubiera dejado la tabla `usuarios` en 3 filas; en cambio tiene 3 filas pero dos de ellas son estas cuentas ajenas al pendiente #36 y una tercera es el `superadmin` de Sandbox. No se borraron en esta sesión por estar fuera del alcance de la tarea en curso (Regla 12.1, no expandir silenciosamente) | 2026-07-15 | Claude Code (borrado, mismo patrón de limpieza que el resto de los scripts de prueba de esta sesión — `usuarios` + `auth.users`, revisando antes FKs en `sesiones_tenant_admin_plataforma`/`auditoria_admin_plataforma`) | Las dos cuentas y sus filas asociadas (sesiones/auditoría si las tienen) ya no existen, verificado por consulta real; `usuarios` vuelve a tener solo cuentas reales (el `superadmin` de Sandbox + las que el Desarrollador cree a propósito) | 🟢 Resuelto 2026-07-15 — borradas a pedido explícito del Desarrollador ("borralas"). Sin filas en `sesiones_tenant_admin_plataforma`/`auditoria_admin_plataforma` para ninguna de las dos (no llegaron a generar actividad), borradas directamente de `usuarios` y `auth.users`. Verificado con consulta real: `usuarios` quedó con 1 sola fila, el `superadmin` real de Sandbox |

| 40 | Rebautizo de infraestructura real del nombre anterior de la prestadora original/"PLM" a la identidad vigente (Xeitra/Aurevia), con el criterio corregido de que artefactos de plataforma (compartidos por todas las Prestadoras) se nombran `aurevia-*`, y artefactos específicos de la Prestadora de prueba se nombran `prestadora-demo-*` — nunca al revés. Inventario de 11 ítems reales presentado al Desarrollador 2026-07-16 | 2026-07-16 | Claude Code (ejecutar los ítems sin riesgo/con impacto verificado), Desarrollador (ítem 11, rename manual de la carpeta raíz local) | Los 11 ítems del inventario resueltos o explícitamente diferidos con motivo | 🟡 En curso — resuelto hasta ahora: (1) repo GitHub `BetoSP/[nombre anterior]`→`BetoSP/aurevia` (`gh repo rename`); (2) proyecto Vercel del Panel `[nombre-anterior]-panel`→`aurevia-panel` (`vercel project rename`, `projectId` sin cambios, citas de URL en `docs/PROGRESS.md`/`docs/PENDIENTES.md` actualizadas a `https://aurevia-panel.vercel.app`); (3) backups locales `backend/backups/[nombre-anterior]_pre_modulo6_*`→`aurevia_pre_modulo6_*`; (4) proyecto + servicio Railway `[nombre-anterior]-backend`→`aurevia-backend` (sin soporte de rename en Railway CLI — ejecutado vía mutaciones GraphQL `projectUpdate`/`serviceUpdate` de la API pública de Railway con el token de la sesión CLI local, verificado con `railway status`; dominio público `[nombre-anterior]-backend-production.up.railway.app` **sin tocar a propósito**, porque Railway no lo renombra junto con el servicio y cambiarlo requiere resincronizar `VITE_API_URL` en Vercel sin causar downtime — decisión pendiente aparte); `.github/workflows/deploy-backend.yml:29` actualizado en el mismo paso (`--service aurevia-backend`) para no romper el próximo deploy automático; citas de `docs/PROGRESS.md` (líneas 415/589/605/620/873/878) y `docs/PENDIENTES.md:9` corregidas — esto también cierra el hallazgo que había quedado abierto en el pendiente #35 sobre `deploy-backend.yml:29`. **Verificado 2026-07-16 contra la infraestructura real (no contra esta misma fila, que había quedado desactualizada tras un corte de proceso anterior):** (5) bucket R2 `[nombre-anterior]-backups`→`aurevia-backups` **YA ESTABA RESUELTO** — confirmado listando objetos reales del bucket vía API S3 con las credenciales de `backend/.env`, hay backups con ese nombre desde 2026-07-11; esta fila lo tenía mal registrado como pendiente. (6) Ídem bucket B2 `[nombre-anterior]-backups-mirror`→`aurevia-backups-mirror`, **YA ESTABA RESUELTO**, mismo método de verificación, backups desde 2026-07-11. (10) `PANEL_TEST_EMAIL` — **RESUELTO 2026-07-16**: no existía ninguna cuenta `admin_prestadora` de prueba (la Prestadora Demo original fue borrada por completo el 2026-07-15, pendiente #38); recreada exclusivamente para testing con el email provisto por el Desarrollador `aurevia.test.acount@gmail.com` (prestadora nueva `estado='prospecto'`, `prestadora_id=4e84b0e7-1729-4d07-9e70-56fdbd54cd89`; credenciales en `No hacer commit/claves y contraseñas.txt`; `.claude/settings.local.json` actualizado). **Verificado/resuelto además 2026-07-16:** (7) nombre visible del proyecto Supabase (nombre anterior de la prestadora original + "Salud")→"Aurevia" — **RESUELTO**: la CLI local seguía logueada en una cuenta ajena sin acceso, pero el MCP de `supabase` tampoco expone rename; se encontró que el `SUPABASE_ACCESS_TOKEN` ya guardado en la sesión sí es válido contra la **Management API** de Supabase (`api.supabase.com/v1/projects`, distinta del MCP y de la CLI) — confirmado primero con `GET` (nombre anterior), y tras confirmación explícita del Desarrollador, ejecutado `PATCH /v1/projects/abcpmzfnnhpuiupmrsdi` con `{"name":"Aurevia"}`; verificado con un segundo `GET` que devuelve `"name":"Aurevia"`. Sin impacto en `ref`, connection strings ni claves — solo el nombre visible del dashboard. **Resuelto además 2026-07-16:** (9) dominio con el nombre anterior en `panel/.env VITE_SITE_URL` y `backend/src/db/schema_etapa2f.sql` — reemplazado por el placeholder `aurevia.app` (decisión explícita del Desarrollador: todavía no hay dominio real registrado), queda como placeholder hasta que se registre uno real. **Resuelto además 2026-07-18:** (11) carpeta raíz local — confirmado con `ls f:/proyectos/Xeitra/` que ya se llama `Aurevia` (renombrada por el Desarrollador). (8) `SMTP_USER` — el Desarrollador generó la contraseña de aplicación de `notificaciones.aurevia@gmail.com`; cargada en `backend/.env` (`SMTP_USER`/`SMTP_PASSWORD`) y como variables de Railway (`railway variables --set`, servicio `aurevia-backend`, verificado con `railway variables` mostrando ambos valores) y registrada en `No hacer commit/claves y contraseñas.txt`. Envío de emails reactivado. **Resuelto además 2026-07-18:** dominio de Railway — eliminado `sendler-backend-production.up.railway.app` (`railway domain delete`) y generado uno nuevo (`railway domain`); Railway no permitió el nombre limpio `aurevia-backend-production` (ya reservado/en uso), quedó `aurevia-backend-production-e7ee.up.railway.app` con sufijo aleatorio agregado por la plataforma. Downtime real: los pocos segundos entre el delete y el create (ambos comandos corridos en secuencia inmediata), sin ventana de espera adicional. `VITE_API_URL` de Vercel actualizado (`vercel env rm`/`vercel env add`) y Panel redeployado (`vercel --prod`); verificado con `curl` directo al nuevo dominio (200) y grepeando el bundle JS desplegado (`index-DMDGUe3u.js`) para confirmar que ya no queda ninguna referencia al dominio viejo. **Hallazgo nuevo en la verificación de este mismo paso:** el ítem (2) de esta misma fila (rename del proyecto Vercel del Panel) da por resuelta la URL `https://aurevia-panel.vercel.app`, pero esa URL devuelve 404 real — no existe. El alias que efectivamente sirve tráfico sigue siendo `https://sendler-panel.vercel.app` (200), más dos alias largos autogenerados (`aurevia-panel-betosps-projects.vercel.app`, `aurevia-panel-betosp-betosps-projects.vercel.app`). Es el mismo tipo de gap que tenía el dominio de Railway — resuelto aparte como pendiente #45 (`vercel domains add aurevia-panel.vercel.app` + eliminación del alias viejo `sendler-panel.vercel.app`). **Reaparición detectada y corregida 2026-07-24** (avance autónomo nocturno autorizado): el alias `sendler-panel.vercel.app` había vuelto a existir (`vercel alias ls` lo listaba de nuevo apuntando al último deployment, antigüedad "2d" — causa no determinada, probablemente Vercel lo recrea al reasignar el dominio "default" del proyecto en algún redeploy) y volvía a responder 200, contradiciendo el cierre de #45. Eliminado de nuevo con `vercel alias rm sendler-panel.vercel.app --yes`, verificado con `curl` que ahora da 404 y que `aurevia-panel.vercel.app` sigue sirviendo 200. Si vuelve a reaparecer en una sesión futura, revisar si el proyecto Vercel tiene algún dominio "raíz" configurado que lo esté regenerando automáticamente en cada deploy, en vez de asumir que alcanza con eliminarlo una vez más. **Re-verificado 2026-07-27 (revisión de prioridades de pendientes)**: se re-auditaron los 11 ítems contra la infraestructura real (no contra esta fila) — `curl` directo confirma `aurevia-panel.vercel.app` (200) y `aurevia-backend-production-e7ee.up.railway.app/health` (`{"status":"ok"}`), el bundle JS desplegado (`index-lNa4Umfd.js`) no tiene ninguna mención a "sendler" y sí trae horneado el dominio nuevo de Railway como `VITE_API_URL` — los ítems 1,3,4,5,6,7,8,9,10,11 siguen resueltos. **El alias `sendler-panel.vercel.app` había vuelto a reaparecer por 3ra vez** (`vercel alias ls` lo mostraba de nuevo, antigüedad "2d", sirviendo 200) — eliminado de nuevo (`vercel alias rm`), verificado 404. Se investigó la causa raíz esta vez: `panel/vercel.json` no define alias; `vercel domains ls` da 0 dominios registrados en la cuenta; no hay ninguna mención a "sendler" en `.github/workflows/`, `panel/package.json` ni `panel/.vercel/project.json` — no hay ninguna configuración en este repo que lo esté regenerando. Es, con alta probabilidad, un artefacto del lado de la plataforma Vercel (remanente del `vercel project rename` original que Vercel parece re-materializar en algunos redeploys de producción), no algo controlable desde el repo. **No se puede cerrar el ítem 2 como resuelto de forma permanente** — solo se puede seguir detectándolo y removiéndolo cada vez que se verifique el Panel en producción; se deja anotado para no volver a asumir "ya está" la próxima vez que se audite esta fila |

| 42 | Reescritura completa del historial de Git local con `git filter-repo` (2026-07-17) para purgar toda mención al nombre anterior de la prestadora original (mensajes de commit y contenido de archivos, vía regex case-insensitive que lo reemplazaba por "prestadora-original") y eliminar del historial 3 archivos obsoletos en rutas viejas pre-mudanza (con ese nombre anterior en el título del archivo), más — con permiso explícito puntual del Desarrollador — purgar del historial también el contenido completo de la carpeta restringida (`docs/Documentos Obsoletos/`) y agregarla a `.gitignore` (queda intacta en disco, solo sale del índice/historial de git). Backup previo del `.git` completo guardado como zip en `docs/Documentos Obsoletos/git_backup_pre_filter_repo_20260716_235629.zip` | 2026-07-17 | Claude Code (ejecutó la reescritura y el push), Desarrollador (autorizó el push) | El Desarrollador confirma explícitamente hacer (o no) `git push --force` de `main` a `origin` (`BetoSP/aurevia`) — acción irreversible para cualquier otro clon/fork existente | 🟢 Resuelto 2026-07-17 — con confirmación explícita del Desarrollador, se commiteó primero la limpieza de docs pendiente (commit `0b3ff05`) y se ejecutó `git push --force origin main` (`4153c82`→`0b3ff05`). GitHub (`BetoSP/aurevia`) ya refleja el historial reescrito |
| 41 | El sitio público (`sitio-web/`) hoy es un solo deploy multi-tenant que resuelve la Prestadora por dominio de origen (`backend/src/middleware/resolverPrestadoraPublica.js`) y completa nombre/teléfono/zonas de cobertura desde `configuracion_prestadora`, pero el **contenido real de cada landing no es homogéneo entre Prestadoras** — fotos distintas, catálogo de servicios distinto (una presta todos los servicios posibles, otra solo algunos), formato/estructura de página potencialmente distinta. El modelo actual de "una config con campos fijos" no alcanza para eso. El Desarrollador planteó que probablemente haga falta algún tipo de creador/editor de páginas (page builder) por Prestadora, no solo más campos de config. Explícitamente pospuesto — "no quiero complicarlo más ahora", a retomar recién cuando el resto de la implementación esté terminada, no antes | 2026-07-16 | Desarrollador (definir alcance del creador de páginas cuando llegue el momento) + Claude Code (diseñar e implementar) | Diseño de la solución (page builder o equivalente) presentado y aprobado, y funcionalidad construida — no antes de que el resto de la implementación esté cerrada | 🟢 **Absorbido por el pendiente #70 (2026-07-26).** Mismo tema (page builder/CMS para la landing pública de cada Prestadora) descrito dos veces en filas separadas; #70 (2026-07-21) tiene el encuadre más reciente y correcto — confirma explícitamente que debe ser un producto separado de Xeitra, fuera de Aurevia, conectado por API — dato que esta fila todavía no tenía al crearse. Se cierra esta fila para no duplicar el seguimiento; el trabajo real queda registrado y se retoma en #70 |
| 43 | Hallazgo detectado en esta sesión al explicarle al Desarrollador el pendiente #37 (MFA): el toggle `mfa_admin_obligatorio` (`panel/src/pages/Configuracion.jsx`, pestaña Seguridad) se activaba con un solo click del checkbox, sin ninguna confirmación previa — afecta de golpe a TODAS las cuentas `superadmin`/`admin_plataforma` a la vez, riesgo real de activación accidental, contra Regla 7.4 de `CLAUDE.md` (operación sensible sin confirmación explícita) | 2026-07-18 | Claude Code | Modal de confirmación agregado antes de activar (no antes de desactivar, que no tiene ese riesgo), con las 3 advertencias del caso, i18n completo en las 3 locales | 🟢 Resuelto 2026-07-18 — `TabSeguridad` en `panel/src/pages/Configuracion.jsx` ahora separa `handleToggle` (intercepta solo la activación) de `aplicarCambio` (el PATCH real); al activar se abre un modal (mismo patrón `panel-modal-fondo`/`panel-modal` que el resto del Panel) con 3 advertencias — alcance global del toggle, necesidad de preparar las apps de autenticación antes, y ausencia de recuperación automática si se pierde el celular — y un botón "Entiendo el riesgo, activar" que recién ahí dispara el PATCH; desactivar sigue sin modal por no tener ese riesgo. Claves nuevas `seguridad_mfa_confirmar_*` agregadas en `es-AR`/`en`/`pt-BR` en `panel/src/i18n/translations.js`. Botón deshabilitado mientras `guardando` está en curso (Regla 5). **No cierra el pendiente #37** (que sigue siendo sobre el mecanismo de recuperación en sí, todavía sin decisión del Desarrollador) — es un hueco distinto y más chico que se detectó y corrigió en el camino. **Desplegado a Vercel 2026-07-18** — commit `bd70ad1` pusheado a `origin/main` y `vercel --prod` corrido sobre `aurevia-panel`, deployment `dpl_6wdh4h85GdRTGW9CwMaZaTYppE3k` en producción |

| 44 | El envío de emails (`backend/src/utils/email.js`, `SMTP_USER`/`SMTP_PASSWORD` compartidos, detectados en el marco del pendiente #40 ítem 8) sale de **una sola cuenta Gmail compartida por todas las Prestadoras**. El aislamiento de destinatarios ya está resuelto (`configuracion_notificaciones` con `prestadora_id`), pero el tope de envío de Gmail (500/día en cuenta normal, 2000/día en Workspace) es **compartido entre todas las Prestadoras juntas, no por Prestadora** — con pocas decenas de Prestadoras activas mandando notificaciones normales ya se podría estar cerca de ese tope. Contradice la pregunta obligatoria de diseño del §2 de `CLAUDE.md` ("¿esto funciona con cientos de Prestadoras simultáneas?") — hoy no escalaría. Detectado 2026-07-18 a partir de una pregunta del Desarrollador sobre el volumen de emails, mientras se buscaba la contraseña de aplicación de `notificaciones.aurevia@gmail.com` para el pendiente #40 | 2026-07-18 | Desarrollador (decidir cuándo migrar) + Claude Code (implementar la migración cuando se decida) | Se decide y (si corresponde) se implementa una salida sin tope compartido — servicio transaccional dedicado (SendGrid/Postmark/SES/Resend) con cuota propia, o cuota/limitador por Prestadora sobre el mecanismo actual — antes de que el volumen real se acerque al tope de Gmail | 🟢 Resuelto 2026-07-26 — **decisión final del Desarrollador**, tras revisar opciones y costos (SES/Resend/Postmark/SendGrid, julio 2026) y confirmar que con 5-10 Prestadoras el volumen estimado (10-30 emails/día/Prestadora) sigue debajo del tope de Gmail: en vez de migrar de proveedor o limitar ahora mismo, se cierra con una **alarma configurable** que avisa al alcanzar el umbral de Prestadoras certificadas (default 5, antes de licenciar la 6ta) y apunta a `docs/DECISION_EMAIL_ESCALA.md` (resumen completo: inventario de disparadores de email, estimación de volumen, tabla de precios investigada, y qué decidir en ese momento). Implementación: columna `configuracion_plataforma.umbral_alerta_prestadoras` (singleton, default 5, `backend/src/db/schema_admin_plataforma_08_umbral_alerta_prestadoras.sql`, mismo patrón que `mfa_admin_obligatorio` — configurable, no hardcodeado, `CLAUDE.md` §7 Regla 1); `GET/PATCH /api/panel/configuracion-plataforma/umbral-prestadoras` (solo `superadmin`, `panelConfiguracionPlataforma.js`); `GET /api/panel/admin-plataforma/resumen` (`panelAdminPlataforma.js:41-81`) devuelve `alertaLimiteEmailCompartido`/`umbralAlertaPrestadoras`, calculado sobre `prestadoras.estado='certificada'` (excluye naturalmente Sandbox y Prestadora Demo, ambas en estado `prospecto`); banner de advertencia (`Alert variant="warning"`, `panel/src/pages/AdminPlataforma.jsx`, pestaña Resumen) visible para `admin_plataforma`, i18n completo en las 3 locales, variante CSS `.alert-warning` nueva en `panel/src/index.css`. **Probado de punta a punta en el Panel real** (Playwright, cuenta `admin_plataforma` temporal creada vía Admin API + 5 Prestadoras ficticias `estado='certificada'` insertadas vía script descartable, todo borrado al terminar sin residuos verificado por consulta directa): login como `admin_plataforma` → pestaña "Panel de Xeitra" → Resumen muestra "5" en Prestadoras activas y el banner de alerta con el texto y la referencia al documento. Desplegado (Railway + `vercel --prod`, Regla 13.1) tras el commit/push. **Hallazgo que reabre la premisa de este cierre (2026-07-26, durante la verificación de punta a punta del pendiente #37):** todo este análisis asumía que el techo de 500-2000 emails/día de Gmail era la única restricción futura a vigilar. La verificación real contra Railway encontró que el egress saliente a los puertos SMTP (25/465/587) parece estar bloqueado a nivel de infraestructura del proveedor (`ETIMEDOUT` uniforme e inmediato en los 3 puertos) — si es así, la entrega de email desde el backend desplegado está en 0% hoy, independientemente del volumen. Detalle completo, con qué se probó y qué decisión falta, en el pendiente #37 (no se duplica acá). **En pausa 2026-07-26** — decisión explícita del Desarrollador de posponer la migración de proveedor de email hasta después del MVP, mismo motivo que la pausa registrada en el pendiente #37 |
| 45 | El proyecto Vercel del Panel (`aurevia-panel`) no tenía ningún alias limpio `aurevia-panel.vercel.app` activo — ese dato figuraba como resuelto en el pendiente #40 ítem 2 pero era incorrecto: esa URL devolvía 404. El alias que realmente servía tráfico de producción seguía siendo el nombre anterior `sendler-panel.vercel.app` (200 OK), además de dos alias largos autogenerados por Vercel. Detectado 2026-07-18 al verificar el redeploy del Panel tras el rename del dominio de Railway (pendiente #40) | 2026-07-18 | Claude Code | Existe un alias `aurevia-panel.vercel.app` sirviendo 200 en producción, y `No hacer commit/claves y contraseñas.txt` refleja la URL real verificada | 🟢 Resuelto 2026-07-18 — `vercel alias set` solo no alcanzó (la URL quedaba detrás del SSO de Vercel, `vercel.com/sso-api`, porque no estaba registrada como dominio del proyecto); se resolvió con `vercel domains add aurevia-panel.vercel.app`, que sí la registra como dominio propio del proyecto y queda asignada automáticamente al último deployment de producción. Verificado con `curl -I` devolviendo 200 sin redirect. **Además, a pedido del Desarrollador, se eliminó el alias viejo** `sendler-panel.vercel.app` (`vercel alias rm`) — verificado con `curl` que ahora da 404 y que `aurevia-panel.vercel.app` sigue sirviendo 200. Los otros dos alias residuales del nombre viejo (`sendler-panel-betosp-betosps-projects.vercel.app`, `sendler-panel-betosps-projects.vercel.app`) también fueron eliminados a pedido del Desarrollador (`vercel alias rm`) — verificado con `curl` que ambos dan 404. No queda ningún alias con el nombre "sendler" apuntando al Panel |
| 46 | El deploy automático del backend en Railway está fallando en el commit `f74a423` (solo tocaba `docs/PROGRESS.md`, sin cambios de código) — dos intentos consecutivos (`6af2304b...` y, tras `railway redeploy --from-source`, `5b7aeaf6-478d-4c35-b641-64729fb6db57`) terminaron en `FAILED`, ambos con el mismo único log disponible (`"scheduling build on Metal builder..."`) y sin ningún log de build o deploy posterior pese a probar `--build`/`--deployment`/`--json`/distintos `--lines`. Como el commit no tocó código de backend, no parece un error de compilación sino un problema del lado de la infraestructura de build de Railway. **Sin impacto en producción hoy**: el tráfico real sigue sirviéndose desde el deployment anterior exitoso (`6475bf24-3d0d-495f-9dfa-093f837f2698`, 2026-07-16, estado `SUCCESS`, activo) — Railway no promueve un deploy fallido. Detectado 2026-07-18 investigando el punto 2 de una autorización del Desarrollador ("1 y 2") | 2026-07-18 | Claude Code (reintentar más adelante / escalar a soporte de Railway si persiste) | Un nuevo `railway redeploy --from-source` (o el próximo push real) termina en `SUCCESS`, o se identifica una causa concreta vía logs/soporte de Railway | 🟢 Resuelto 2026-07-24 — confirmado que fue un problema pasajero de la infraestructura de build de Railway, como se sospechaba: sin necesidad de ninguna acción manual, los pushes reales posteriores a este hallazgo (`gh run list --workflow="Deploy backend a Railway"`) muestran una racha larga de deploys en `SUCCESS` desde 2026-07-22 en adelante, incluido el redeploy de `2286d1c` (pendiente #84) el 2026-07-24; `railway status` confirma el servicio `aurevia-backend` `Online` en un deployment posterior (`9dc826c1-...`) al que había fallado. No hizo falta escalar a soporte de Railway |
| 47 | La config del servicio Railway `aurevia-backend` seguía con la conexión de GitHub apuntando a `BetoSP/Sendler` (nombre viejo del repo) — cosmético, sin impacto real porque GitHub redirige el nombre viejo, pero quedaba pendiente de limpieza (nota sin numerar dentro del pendiente #40) | 2026-07-18 | Claude Code | La config de Railway (`railway status --json`, campo `source.repo`) muestra `BetoSP/aurevia`, no `BetoSP/Sendler` | 🟢 Resuelto 2026-07-18 — corregido con la mutación GraphQL `serviceConnect` de la API pública de Railway (`backboard.railway.com/graphql/v2`, mismo método de autenticación con el `accessToken` de la sesión CLI local que se usó para el pendiente #40 ítem 4), pasando `input: {repo: "BetoSP/aurevia", branch: "main"}`. Verificado con `railway status --json` mostrando `"repo": "BetoSP/aurevia"` en ambos lugares donde aparecía antes `BetoSP/Sendler` |
| 48 | El sitio público viejo (proyecto Vercel `sitio-web`, alias `sendler-salud.vercel.app` y otros) mostraba branding y contenido de la empresa anterior ("Sendler Salud — Cuida tus afectos") tanto en el deployment de hace 9 días como en el de producción vigente — no era solo un tema de nombre de dominio, el contenido real de la página nunca se había actualizado a la identidad Xeitra/Aurevia. Detectado 2026-07-18 al investigar el pendiente #45 (aparece `sendler-salud.vercel.app` como alias de otro proyecto Vercel) | 2026-07-18 | Claude Code (ejecutó la baja con autorización explícita del Desarrollador) | El proyecto Vercel no existe más, la carpeta `sitio-web/` no existe ni en el árbol de archivos actual ni en el historial de git, y no queda ningún dominio del sitio viejo respondiendo | 🟢 Resuelto 2026-07-18 — decisión explícita del Desarrollador: no reemplazar el branding ahora (un sitio a medio armar sin el software listo detrás transmite peor imagen que no tener sitio), eliminar todo y crear el sitio real de Aurevia más adelante (ver pendiente #49). Ejecutado: (1) `vercel project rm sitio-web` — proyecto y todos sus deployments/alias eliminados, verificado con `curl` que `sendler-salud.vercel.app` y `sitio-web-drab.vercel.app` dan 404; (2) `git rm -r sitio-web` + commit; (3) backup completo de `.git` en `No hacer commit/git_backup_pre_filter_repo_sitio_web_20260718.zip`; (4) `git filter-repo --path sitio-web --invert-paths --force` para purgar la carpeta de **todo** el historial (no solo el árbol actual) — con confirmación explícita del Desarrollador antes del paso irreversible; (5) recreado el remote `origin` (filter-repo lo quita por diseño) y `git push --force origin main` (`bda0ef0`→`d4700a1`), confirmado por el Desarrollador. Restos no versionados (`sitio-web/node_modules`, `sitio-web/.vercel`) borrados físicamente del disco |
| 49 | Crear el sitio público real de Aurevia (mostrando el software de Xeitra/Aurevia, no el de la Prestadora original) — reemplaza al que se dio de baja en el pendiente #48. El Desarrollador decidió explícitamente esperar: no se construye hasta que el software esté lo bastante completo como para mostrarse correctamente | 2026-07-18 | Desarrollador (decidir el momento apropiado) + Claude Code (implementar cuando se autorice) | El Desarrollador da la orden explícita de empezar, y existe un sitio nuevo con identidad Aurevia real desplegado en producción | 🔴 Abierto — intencionalmente en espera, no bloqueante, sin fecha objetivo todavía |
| 50 | Auditoría de seguridad previa al arranque de "todo lo que falte de multitenant" (pedida por el Desarrollador 2026-07-18) encontró, de paso, una fuga cross-tenant real y explotable: `guardar_token_whatsapp`/`leer_token_whatsapp` (`backend/src/db/schema_whatsapp_ia_01.sql`) tenían EXECUTE otorgado a `anon`/`authenticated` además de `service_role` — Supabase asigna esos grants por defecto a funciones nuevas del schema `public` y el `REVOKE ALL FROM PUBLIC` original no los revocaba. Las funciones no validan `auth.uid()` ni tenant del caller: cualquiera podía leer/pisar el token de WhatsApp Business API de cualquier Prestadora vía RPC directo de PostgREST, sin pasar por RLS ni por el backend. De paso también se encontraron 2 funciones trigger (`marcar_prestaciones_a_revisar`, `bloquear_edicion_laboral_coordinador`) con `search_path` mutable (advisory WARN de Supabase, riesgo de search-path injection en `SECURITY DEFINER`), y que `docs/DATA_MODEL.md` no documentaba las tablas `sesiones_tenant_admin_plataforma`/`auditoria_admin_plataforma` del pendiente #30 ni el rol `admin_plataforma` en el `CHECK` de `usuarios`, con una nota además desactualizada ("`superadmin` ve todas las prestadoras") | 2026-07-18 | Claude Code (corregir) | Los 4 hallazgos corregidos y verificados contra Supabase real | 🟢 Resuelto 2026-07-18 — verificado contra Supabase real, no solo por el `success:true` de `apply_migration`: (1) `information_schema.routine_privileges` confirma que `anon`/`authenticated` ya no tienen EXECUTE en ninguna de las 2 funciones (solo `postgres`/`service_role`); fix reflejado también en el `.sql` fuente para no repetirse en otro ambiente. (2) `pg_proc.proconfig` confirma `search_path=public` fijo en ambas funciones trigger; corregido también en `schema_documentos_asistente.sql` (definición vigente) y `schema_etapa2d.sql` (definición original) — no en `schema_etapa2j.sql`, que quedó superseded por `schema_documentos_asistente.sql` y no es la definición que corre hoy. (3) `docs/DATA_MODEL.md` actualizado: `usuarios` ahora documenta `admin_plataforma` y `prestadora_id` nullable solo para ese rol (con el `CHECK` real, verificado con `pg_get_constraintdef`), nota vieja corregida, y sección nueva completa del mecanismo de `sesiones_tenant_admin_plataforma`/`auditoria_admin_plataforma`/`current_tenant()`. **Hallazgo NO corregido, reportado aparte**: la protección HaveIBeenPwned (`password_hibp_enabled`) sigue desactivada — intentado activar vía Management API, respondió `402 Payment Required` ("available on Pro Plans and up"), es una función exclusiva de planes Pro de Supabase, no resoluble con código; decisión de upgrade de plan queda para el Desarrollador |

| 51 | `docs/legal/argentina.md` creado 2026-07-18 (contenido mínimo: riesgo de indicio de subordinación bajo art. 23 LCT para las funciones de gestión de Asistentes listadas en `CLAUDE.md` §3), cumpliendo la condición de proceso antes de dar de alta una Prestadora real en Argentina. También se crearon, a pedido del Desarrollador, los placeholders de `docs/legal/` para el resto de Hispanoamérica (Bolivia, Chile, Colombia, Costa Rica, Cuba, Ecuador, El Salvador, Guatemala, Honduras, México, Nicaragua, Panamá, Paraguay, Perú, República Dominicana, Uruguay, Venezuela) más Brasil, España y Portugal — cada uno con nota explícita de que debe completarse con investigación real recién cuando se detecte la primera Prestadora licenciataria de esa jurisdicción. El mecanismo técnico que `CLAUDE.md` §3 describe para *usar* este contenido — tabla configurable jurisdicción→función→texto, advertencia mostrada al activar la función, auditoría de quién/cuándo/qué función/qué advertencia — tampoco existía en el código (sin tabla en Supabase, sin UI de advertencia, sin auditoría) | 2026-07-18 | Claude Code / Desarrollador (priorizar cuándo construir la integración con un toggle real) | Tabla configurable en Supabase con RLS por prestadora, advertencia mostrada en el Panel al activar cada función listada, y auditoría de activación — todo probado contra Supabase real | Infraestructura resuelta 2026-07-18, integración cerrada 2026-07-26 (ver detalle abajo). **Detalle**: se investigó el código antes de construir nada y se confirmó que ninguna de las funciones de gestión de Asistentes que dispararían esta advertencia (penalización de inasistencias, rankings, puntuaciones por aceptación de guardia, puntuaciones por calificación de Familia/Paciente, límite de oportunidades por rechazos, niveles/categorías, horarios fijos) existe todavía como toggle real en el producto — no había ningún consumidor real para engancharla ni para probarla de punta a punta. **Decisión explícita del Desarrollador**: construir de todos modos la infraestructura genérica ahora, dejando la integración con un toggle real para cuando ese toggle exista. Construido y verificado contra Supabase real: `backend/src/db/schema_advertencias_legales_01.sql` (aplicado vía MCP) — tabla `advertencias_legales` (jurisdicción = mismo código que `prestadoras.pais`, función_clave, texto; RLS de lectura para `admin_prestadora`/`admin_plataforma`/`superadmin`, escritura solo `superadmin`) y `auditoria_advertencias_legales` (snapshot del texto mostrado, no referencia viva; RLS de lectura acotada a la propia prestadora para `admin_prestadora` + todo para `superadmin`; inserción por RLS directa acotada a `current_tenant()` y al propio `usuario_id`, sin ruta Express dedicada por no haber consumidor real todavía). Verificado con consulta directa a `pg_policies` (4 policies con los nombres y `cmd` esperados) y sembrado el contenido real de Argentina (7 filas, una por función, migradas desde `docs/legal/argentina.md`) — el resto de jurisdicciones queda sin filas a propósito (sin fila = sin advertencia, comportamiento correcto según `CLAUDE.md` §3). Frontend: `panel/src/context/AdvertenciaLegalContext.jsx` (hook `useAdvertenciaLegal()` con `verificarAntesDeActivar(prestadoraId, funcionClave)`, modal reutilizando el patrón `panel-modal-fondo`/`panel-modal` ya existente), provider montado en `panel/src/App.jsx` dentro de `TenantSessionProvider`. i18n nuevo (`advertencias_legales.*`) en las 3 locales de `panel/src/i18n/translations.js`. `npm run build` del panel sin errores. **Actualización 2026-07-26 — integración cerrada con primer consumidor real y prueba de punta a punta.** El pendiente #62 (indicaciones de medicación) construyó el primer consumidor real: `panel/src/pages/Medicacion.jsx` envuelve "Aceptar" con `verificarAntesDeActivar(prestadoraId, 'medicacion_via_sin_habilitacion')`. Al probarlo en el Panel real (Prestadora Demo, cuenta `admin_prestadora`, Playwright contra `aurevia-panel.vercel.app`) se encontró un **bug real, no cosmético**: el modal de advertencia no aparecía — la indicación se aceptaba directo, con un 406 en consola (`GET .../prestadoras?select=pais&id=eq...`). Causa confirmada contra `pg_policies`: la tabla `prestadoras` no tenía ninguna policy de SELECT para `admin_prestadora` ni `coordinador` (solo para `admin_plataforma` y `superadmin` acotado a Sandbox) — la consulta de `verificarAntesDeActivar` fallaba y su propio manejo de error (`if (errorPrestadora || !prestadora) return true;`) **fallaba abierto**, saltando aviso y auditoría sin que nada lo mostrara en la UI. Corregido con `backend/src/db/schema_prestadoras_fix_rls_lectura_propia.sql` (aplicado vía MCP): 2 policies nuevas, `admin_prestadora_lee_su_prestadora` y `coordinador_lee_su_prestadora`, mismo patrón que `admin_prestadora_lee_sus_modulos` (`schema_admin_plataforma_06_planes_modulos.sql`) — scoped a `u.prestadora_id = prestadoras.id`. Re-probado de punta a punta tras el fix: indicación nueva de prueba (vía `inyectable`, sin Asistente habilitado asignado) → clic en "Aceptar" → modal "Riesgo legal a tener en cuenta" aparece con el texto correcto de `docs/legal/argentina.md` → confirmado → fila real verificada en `auditoria_advertencias_legales` (`funcion_clave='medicacion_via_sin_habilitacion'`, `usuario_id` de la cuenta de prueba, texto completo). Corregido también el comentario desactualizado en `AdvertenciaLegalContext.jsx` que decía "sin ningún consumidor real todavía". Datos de prueba (indicación, guardia, fila de auditoría) borrados sin residuos, confirmado con conteo en 0. Commit + push pendiente de este cierre. **Cerrado — infraestructura + integración + prueba de punta a punta, las 3 partes verificadas contra Supabase real y navegador real** | 🟢 Resuelto 2026-07-26

| 52 | Auditoría de consolidación pre-multitenant (pedida por el Desarrollador 2026-07-18, continuación de la que cerró el pendiente #50): `get_advisors` (tipo `security`) contra Supabase real muestra el mismo patrón de grants por defecto que el pendiente #50 ya corrigió para `guardar_token_whatsapp`/`leer_token_whatsapp`, pero en otras 6 funciones que esa corrección no alcanzó a cubrir porque el barrido de esa sesión fue acotado al schema de WhatsApp+IA, no a todo `public`: `current_tenant()`, `es_superadmin()`, `es_sesion_tenant_admin_plataforma_activa()`, `fn_auditoria_admin_plataforma_mutacion()`, `bloquear_edicion_laboral_coordinador()`, `marcar_prestaciones_a_revisar()` — todas con EXECUTE otorgado a `anon`/`authenticated` además de `service_role`/`postgres`. **Diferencia real con el caso ya corregido del #50, verificada antes de listar esto (no solo el advisory en bruto):** las 3 primeras solo devuelven `NULL`/`false` para un caller `anon` (dependen de `auth.uid()`, que es `NULL` sin sesión) y las 3 últimas son funciones `TRIGGER` que fallan si se invocan directo por RPC (sin `NEW`/`OLD` de fila) — ninguna filtra hoy datos de una Prestadora a otra ni a `anon` sin sesión, a diferencia del caso real y explotable del #50. Aun así, no hay ninguna razón legítima para que el frontend las llame directo vía RPC (están pensadas para uso interno en RLS/triggers), y dejarlas expuestas es la misma clase de descuido que ya causó una fuga real una vez — corresponde el mismo `REVOKE EXECUTE FROM anon, authenticated` por consistencia y para no depender de que ninguna de las 6 cambie de lógica interna en el futuro sin que alguien recuerde este matiz | 2026-07-18 | Claude Code | `information_schema.routine_privileges` confirma que `anon`/`authenticated` ya no tienen EXECUTE en ninguna de las 6 funciones (solo `postgres`/`service_role`), verificado contra Supabase real | 🟢 Resuelto 2026-07-18 — `REVOKE EXECUTE ... FROM anon, authenticated` aplicado primero contra Supabase real, pero la verificación posterior mostró que **ese primer intento quedó incompleto**: PostgreSQL otorga el EXECUTE también al pseudo-rol `PUBLIC`, del que `anon`/`authenticated` heredan automáticamente — revocarlo solo de esos dos roles nombrados no alcanza mientras `PUBLIC` lo siga teniendo (confirmado comparando contra `guardar_token_whatsapp`/`leer_token_whatsapp`, las funciones que el pendiente #50 ya había dejado bien: ahí no queda ningún grant a `PUBLIC`). Corregido con un segundo `REVOKE EXECUTE ... FROM PUBLIC` sobre las 6 funciones; verificado con `information_schema.routine_privileges`: solo quedan `postgres`/`service_role` en las 6. Mismo `REVOKE FROM PUBLIC` agregado también a los 4 archivos `.sql` fuente de la definición vigente de cada función (`schema_admin_plataforma_02_timeout.sql` para `current_tenant`, `schema_admin_plataforma_04_mfa.sql` para `es_superadmin`, `schema_admin_plataforma_03_auditoria.sql` para las otras 2 de esa sección, `schema_documentos_asistente.sql` para `bloquear_edicion_laboral_coordinador`, `schema_etapa2d.sql` para `marcar_prestaciones_a_revisar`) — mismo criterio que el pendiente #50, no repetir el gap en otro ambiente |
| 53 | Modalidad de vínculo "Cooperativa de trabajo" (asociado con liquidación cooperativa, no monotributo ni relación de dependencia) — gap detectado al comparar Aurevia contra un brief de investigación de mercado (segmento "Cooperativas de trabajo", ej. CUIDARnos). El motor `calcularCese` y el vínculo dual actual solo contemplan monotributo/dependencia. Explícitamente diferido a después del MVP por decisión del Desarrollador (2026-07-18) | 2026-07-18 | Desarrollador (priorizar) + Claude Code (implementar) | Existe una tercera modalidad de vínculo "cooperativa" soportada por el motor de liquidación/cese, parametrizada (no hardcodeada, ver CLAUDE.md §3), y validada contra al menos un caso de prueba en Sandbox | 🔴 Abierto — post-MVP |
| 54 | Documento legal de protección de datos personales (Ley 25.326, Argentina) — hoy `docs/legal/argentina.md` cubre solo riesgo laboral (indicio de subordinación, art. 23 LCT), no protección de datos personales/salud. Gap detectado al comparar Aurevia contra un brief de investigación de mercado. Explícitamente diferido a después del MVP por decisión del Desarrollador (2026-07-18) | 2026-07-18 | Desarrollador (contenido legal) + Claude Code (incorporar al documento) | `docs/legal/argentina.md` incluye una sección de protección de datos personales, coherente con el resto del documento (fuente de advertencias configurable, no hardcodeada) | 🔴 Abierto — post-MVP |
| 55 | Rol "Auditor/regulador" de solo lectura (para programas estatales/registros oficiales que requieren trazabilidad, ej. Cuidarlos San Juan) — no existe hoy en el sistema de roles (`CLAUDE.md §5`); relacionado con el rol de solo lectura para financiadores ya identificado como futuro en `docs/CONTEXT.md:64-66`. Gap detectado al comparar Aurevia contra un brief de investigación de mercado. Explícitamente diferido a después del MVP por decisión del Desarrollador (2026-07-18) | 2026-07-18 | Desarrollador (aprobar alcance y término de glosario) + Claude Code (implementar) | Término agregado y aprobado en el glosario (`CLAUDE.md §4`) antes de programar; rol implementado con el mismo principio de mínimo privilegio y aislamiento por Organización que los roles existentes | 🔴 Abierto — post-MVP |
| 56 | Decisión de modelo comercial de licenciamiento (planes + add-ons vs. otro esquema). Un brief de investigación de mercado (2026-07-18) propuso un esquema concreto de planes (tiers) + add-ons sueltos, con `tenant_features`/`tenant_addons` resueltos por unión. El Desarrollador no descartaba el esquema pero no lo había decidido explícitamente en esta sesión — pese a que, según `schema_admin_plataforma_06_planes_modulos.sql:1-2`, ya existía aplicado en Supabase desde el 2026-07-18 con nota "aprobado por el Desarrollador"; contradicción detectada 2026-07-24 entre ese archivo y el propio texto de este pendiente | 2026-07-18 | Desarrollador (decidir el esquema) | El Desarrollador aprueba explícitamente un esquema de licenciamiento (planes+add-ons u otro) | 🟢 Resuelto 2026-07-24 — el Desarrollador confirmó explícitamente en sesión que el esquema planes+add-ons (el mismo ya modelado en `prestadora_modulos.origen IN ('plan','addon')`) es el definitivo. El inventario de arquitectura que exigía `CLAUDE.md §11` ya existía de hecho: el motor completo (catálogo de módulos, planes versionados, asignación por Prestadora, preview de cambio de plan, aplicación sincronizando módulos, panel de Admin_plataforma) está construido y verificado — ver pendiente #79 (Fase 7), que sigue abierto solo por falta de catálogo real de planes cargado (decisión de negocio de precios/contenido de cada plan, no de arquitectura) |
| 57 | Módulo de gestión del ciclo de cobranza a obras sociales/prepagas (seguimiento de reclamos, rechazos y demoras de reintegro) — dolor real confirmado por investigación de mercado (2026-07-18): hay marco legal activo en Argentina de reclamos contra obras sociales/prepagas que demoran o rechazan cobertura de cuidador domiciliario (Superintendencia de Servicios de Salud, PMO como piso mínimo no techo), y afecta directo el flujo de caja de cualquier Prestadora que facture a obra social (perfil "Siempre"). Hoy el Módulo 8 (Precios/Prestaciones) solo resuelve precio/foto de precio por Prestación, no el estado de un reclamo de cobranza. Decisión del Desarrollador (2026-07-18): Aurevia debe incorporar una solución a esto en su propuesta, no dejarlo fuera del alcance | 2026-07-18 | Desarrollador (PRD/alcance) + Claude Code (implementar) | Existe un PRD o extensión del Módulo 8 con estados de reclamo por Prestación/Paciente facturado a obra social (enviado/en demora/rechazado/cobrado), alertas de demora configurables, y trazabilidad de cada reclamo — sin datos hardcodeados (CLAUDE.md §1) | 🔴 Abierto — post-MVP, alcance ya decidido a incorporar |
| 58 | Dashboard/KPI explícito de rotación y retención de Asistentes — dolor real confirmado por investigación de mercado (2026-07-18): la rotación de cuidadores es el problema #1 reportado por agencias del sector (hasta 65% anual en mercados maduros, costo de reemplazo US$2.600-5.000 por Asistente), y la flexibilidad de horario pasó a ser la principal palanca de retención. Aurevia hoy tiene Score de Riesgo de reclasificación (enfocado en riesgo legal de vínculo) y registro de Ausencias/Ceses, pero ningún KPI explícito de tasa de rotación/retención en el dashboard del Panel. Decisión del Desarrollador (2026-07-18): incorporar a la propuesta | 2026-07-18 | Desarrollador (definir métrica y umbral) + Claude Code (implementar) | Dashboard del Panel (Módulo 1) muestra tasa de rotación de Asistentes por período y por Prestadora, calculada desde `ceses`/`ausencias` ya existentes, sin tabla ni cálculo nuevo hardcodeado | 🔴 Abierto — post-MVP, alcance ya decidido a incorporar |
| 59 | Transparencia de facturación de cara a la Familia (detalle de cargos visible en el portal, no solo la reconciliación interna "a revisar" de precio de lista vs. Prestación) — dolor real confirmado por investigación de mercado (2026-07-18): "cobros indebidos/poco transparentes" es una queja recurrente de usuarios finales en el rubro (referencias España por escasez de reviews públicas de Argentina, mismo tipo de servicio). Hoy el Módulo 8 solo expone el precio final de la Prestación; no hay un desglose de cargos visible para la Familia en su propio portal. Decisión del Desarrollador (2026-07-18): incorporar a la propuesta | 2026-07-18 | Desarrollador (definir qué detalle se expone) + Claude Code (implementar) | La PWA de Familias (Etapa 4) muestra un desglose de cargos de su Prestación/Paquete, coherente con el precio final ya congelado por Módulo 8, sin exponer información interna de otra Familia (aislamiento ya vigente) | 🔴 Abierto — post-MVP, alcance ya decidido a incorporar |
| 60 | El Panel autenticado (sidebar y generación de documentos legales — `VinculoCeseTab.jsx`, `CertificadoTab.jsx`, `AusenciasCoberturaTab.jsx`) mostraba el nombre de Sandbox en vez del de la Prestadora del usuario logueado (reportado por el Desarrollador: "porque estoy en sanbox y no en prestadorta de prueba?"). Causa raíz: `EmpresaContext.jsx` usaba siempre el endpoint público `/api/configuracion-publica` (resolución por dominio, pensada para el sitio público sin sesión), que ante un dominio sin match cae a "si hay una sola fila en `configuracion_prestadora`, usar esa" — y esa única fila era la de Sandbox. De paso se encontró un hueco de seguridad real: la policy RLS `prestadora_lee_su_configuracion`/`prestadora_edita_su_configuracion` sobre `configuracion_prestadora` tenía `es_superadmin() OR (...)`, permitiendo a Superadmin leer/editar la configuración de **cualquier** Prestadora real, no solo Sandbox — contradice la restricción dura de `CLAUDE.md §5` | 2026-07-19 | Claude Code | Corregido y verificado contra Supabase real | 🟢 Resuelto 2026-07-19 — `EmpresaContext.jsx` ahora distingue sesión: sin sesión sigue usando el endpoint público (uso legítimo en `Login.jsx`); con sesión, consulta `configuracion_prestadora` directo con el cliente Supabase autenticado, ya con RLS scoping por `current_tenant()`. Policies corregidas moviendo `prestadora_id = current_tenant()` fuera del `OR` con `es_superadmin()` (aplicado contra Supabase real vía SQL y reflejado en `schema_multitenant_04.sql`), verificado con `pg_policy` que el `using_expr` quedó `(prestadora_id = current_tenant()) AND (es_superadmin() OR EXISTS(...))` en ambas policies — Superadmin ya solo puede leer/editar su propio tenant (Sandbox). Se creó además la fila faltante de `configuracion_prestadora` para Prestadora Demo (antes no existía ninguna, por eso mostraría vacío incluso con el fix). `npm run build` del panel sin errores. **Nuance detectada, no resuelta**: los documentos legales (telegrama de cese, liquidación final, certificados) siguen usando el campo genérico `nombre` de `configuracion_prestadora`, no la distinción `razon_social`/`nombre_fantasia` que sí existe en `prestadoras` — para un documento legal real, `razon_social` sería probablemente más correcto. No estaba dentro del alcance aprobado por el Desarrollador en esta sesión (solo aprobó "arreglar ambas cosas": el bug de origen de datos + la policy de Superadmin); queda para una decisión explícita aparte. Tampoco se probó en navegador real (sin credenciales de sesión disponibles en este entorno) — solo verificado por build + SQL directo contra Supabase |
| 75 | **No existe ningún flujo de "primera contraseña" para las cuentas que crea el Panel sin login propio del interesado** (Familia, Asistente, y también los miembros del Círculo de cuidado — Fase 5, `docs/PENDIENTES.md` #74). `crearCuentaConPerfil` (`backend/src/utils/cuentasPanel.js`) generaba un `passwordTemporal` aleatorio que ningún caller comunicaba por ningún canal. Encontrado durante la verificación de Fase 5 (`docs/PROGRESS.md` 2026-07-22) | 2026-07-22 | Claude Code (hallazgo), Desarrollador (decidió el mecanismo: token propio + email por SMTP existente, no `admin.inviteUserByEmail`) | Familia/Asistente/Círculo tienen un flujo real de primer acceso (invitación por email), documentado y probado en navegador | 🟢 Resuelto 2026-07-24 — construido: tabla `tokens_activacion_cuenta` (RLS estricta, solo accesible con service role), `backend/src/utils/activacionCuenta.js` (genera token de 7 días de validez + email multi-idioma vía `enviarEmail`), endpoint público `POST /api/activar-cuenta`, integrado en `crearCuentaConPerfil` para Familia/Asistente/Círculo (falla de envío de email no revierte la cuenta creada — queda recuperable con "Reenviar invitación"), botón "Reenviar invitación" en el Panel (`FamiliaDetalle.jsx` y `PerfilTab.jsx` de Asistentes, solo Admin), pantalla `ActivarCuenta.jsx` en `pwa-familias` y `pwa-asistentes` (ruta pública `/activar-cuenta`, i18n ×3, 4 estados). **Verificado con Playwright contra Prestadora Demo real** (cuenta de prueba creada y luego eliminada de Supabase): activación exitosa con contraseña nueva → pantalla de éxito con link a `/login`; mismo token reutilizado → mensaje de link inválido/vencido; token inexistente → mismo mensaje; sin token en la URL → mismo mensaje. 0 errores de consola nuevos en ningún paso. **Commiteado (`218f0d9`), pusheado a `origin/main`, variables `PWA_FAMILIAS_URL`/`PWA_ASISTENTES_URL` sumadas a Railway (backend de producción), y desplegado a Vercel** (`panel`, `pwa-familias`, `pwa-asistentes`, los 3 `readyState: READY`); `GET /health` y `POST /api/activar-cuenta` confirmados en vivo contra Railway. **Hallazgo real durante esta verificación en producción**: `pwa-asistentes` nunca tuvo `vercel.json` con el rewrite SPA que sí tenía `pwa-familias` — el link de activación por email (y cualquier ruta directa, ej. `/login`) devolvía 404 real en producción; corregido con `pwa-asistentes/vercel.json` nuevo (commit `2d3418f`), redesplegado y confirmado con `curl` real que `/activar-cuenta` y `/login` ya devuelven 200. El envío real de email por SMTP **no se pudo verificar de punta a punta en el entorno de desarrollo** (sandbox sin salida de red hacia Gmail SMTP, `ECONNREFUSED`) — el token se inserta en la base antes de ese fallo, que es lo que permitió probar la pantalla de activación en local; la entrega real del email en producción queda sin confirmar de punta a punta, pero el SMTP de producción (Railway) no tiene la misma restricción de red que este sandbox de desarrollo |
| 74 | **Fase 5 del plan de rediseño de frontend — Círculo de cuidado (PWA Familias)**, construida y aplicada contra Supabase real 2026-07-22 (ver `docs/PROGRESS.md` de esta fecha para el detalle completo). **Verificado de punta a punta con Playwright contra la Prestadora Demo real** (backend local + `panel` + `pwa-familias` corriendo en local): se encontraron y corrigieron 2 problemas reales durante la verificación (no hipotéticos) — (1) `GET /api/panel/cuentas/familia/:id/circulo` devolvía 500 real porque `miembros_familia` tiene dos FK hacia `usuarios` (`usuario_id` y `creado_por`) y el embed `usuarios(nombre)` quedaba ambiguo para PostgREST; corregido a `usuarios!miembros_familia_usuario_id_fkey(nombre)` en `backend/src/routes/panelCuentas.js:272`; (2) confirmado (no es bug, es un gap real preexistente que ya afectaba a Familia/Asistente antes de esta fase, ver comentario en `backend/src/utils/cuentasPanel.js:12-19`): `crearCuentaConPerfil` genera un `passwordTemporal` que hoy no se comunica a nadie por ningún canal — ni al invitado al círculo ni a una Familia nueva — así que en producción esa persona no podría loguearse nunca sin un flujo de invitación por email o "primera contraseña" que todavía no existe. Para la verificación se fijó una contraseña de prueba directamente vía Admin API con la service role key local (nunca expuesta en logs/URLs), no es una solución de producción. Con ese fix, verificado con 2 Familias reales de la Prestadora Demo: invitar desde el Panel (estado vacío→listo correcto, nombre/email/rol visibles), loguearse como el miembro invitado en la PWA (ve los mismos Pacientes, guardia en curso, Asistente asignado, reportes y alertas que el titular), bloqueo de calificación tanto en la UI (mensaje "solo lectura") como en el backend (403 real llamando directo al endpoint con el token de esa cuenta), aislamiento cruzado confirmado (404 real al pedir el Paciente de la otra Familia con ese mismo token), y revocación desde el Panel (vuelve a estado vacío, y el token de esa cuenta ya revocada devuelve 401 real porque `borrarCuenta` elimina el usuario de Supabase Auth). 0 errores de consola nuevos. Datos de prueba (2 miembros de círculo temporales) limpiados al cerrar la verificación. **Resuelto 2026-07-22**: commit `8913397` (`feat: círculo de cuidado en PWA Familias (Fase 5) con acceso de solo lectura`), push a `main` (dispara redeploy automático de Railway del backend con el fix del 500), y deploy explícito a Vercel de `panel` (`aurevia-panel-ntkzlqnf9-betosps-projects.vercel.app`, `readyState: READY`) y `pwa-familias` (`pwa-familias-hzjemo43c-betosps-projects.vercel.app`, `readyState: READY`) | 2026-07-22 | Claude Code | Commit + push, deploy a Vercel de `panel` y `pwa-familias`, y confirmar el redeploy de Railway con el fix del 500 | 🟢 Resuelto 2026-07-22 — el gap real de "primera contraseña" encontrado durante esta verificación queda registrado aparte como pendiente #75, no bloquea el cierre de esta fase |
| 78 | **Fase 6 del plan de rediseño de frontend — Estado de Hospitalización y causales de Cese de servicio**, construida 2026-07-22 (ver `docs/PROGRESS.md` de esta fecha para el detalle completo). Alcance: (1) entidad `servicios` estrictamente por-Familia (nunca cruza Familias, ver pendiente #77 para "Residencia") con `servicio_id` opcional en `prestaciones`/`guardias`/`facturas_familia_items` y trigger `validar_servicio_misma_familia` que rechaza asociar un Servicio de otra Familia; (2) entidad `hospitalizaciones_paciente` (institución/motivo/fecha inicio-fin) que pausa reversiblemente (nuevo estado `'pausada'`, distinto de `'cancelada'`) las guardias/series del Paciente internado, con cierre que las devuelve a su estado anterior; (3) `alertas_contingencia_hospitalizacion` — alerta informativa al Coordinador cuando otro Paciente de la misma Familia queda "conviviente" de un Paciente hospitalizado, sin tocar guardias de terceros automáticamente (mismo patrón que `alertas_tempranas_guardia`); (4) aviso de Cese de servicio a los Asistentes afectados en dos etapas: el Coordinador marca "avisé verbalmente" (`cierre_servicio_asistentes.avisado_verbalmente_at`), y si no lo hace dentro del plazo configurable por Prestadora (`configuracion_aviso_cese_asistente.horas_plazo_aviso_verbal`, UI nueva en Configuración → Notificaciones), un cron (`backend/src/utils/avisoAutomaticoCese.js`, corre cada 5 min junto al resto de los cron de minutos) dispara push + WhatsApp automático. UI completa en `PrestacionesPaciente.jsx` (alta/cierre de Hospitalización, lista de alertas de contingencia con "marcar resuelta", lista de Asistentes a avisar con su estado). i18n completo en es-AR/en/pt-BR, verificado por grep de paridad. Los 5 schemas SQL ya están aplicados contra Supabase real (confirmado con `list_tables`: `servicios`, `hospitalizaciones_paciente`, `alertas_contingencia_hospitalizacion`, `configuracion_aviso_cese_asistente` con 1 fila, `cierre_servicio_asistentes` existen y tienen RLS activo) | 2026-07-22 | Claude Code | Pendiente real restante: probar en navegador el disparo automático del aviso de Cese tras vencer el plazo (requiere Prestaciones/cierre de servicio reales con Asistentes asignados, no disponible en el dataset usado para esta verificación) | 🟢 Cerrado — construido, verificado en navegador contra Sandbox/Demo real (Familia Pirulito, 2 Pacientes: alta de Hospitalización sobre "Doña Pirulita" renderiza el estado y genera correctamente la alerta de contingencia sobre "Luis Alberto Pirulito" (mismo domicilio familiar); "Marcar como resuelta" la retira de la vista; "Cerrar hospitalización" (con diálogo de confirmación) revierte la UI a "Registrar hospitalización"; sin errores de consola en ningún paso; `TabAvisoCese` en Configuración → Notificaciones renderiza y guarda correctamente aun con la lista de notificaciones vacía), commiteado (`8b770ed`), pusheado a `origin/main` y desplegado a producción (`https://aurevia-panel.vercel.app`, deploy `dpl_6jDMkpbA94omRcJ63FK8eyrF5vxm`, 2026-07-22). Fila de prueba en `hospitalizaciones_paciente`/`alertas_contingencia_hospitalizacion` eliminada de Supabase real tras la verificación. Sin verificar en navegador: el disparo automático del aviso de Cese (cron ya construido y corriendo en backend, sin evidencia de ejecución real todavía) |
| 77 | **Residencia** — concepto explícitamente fuera de alcance de la Fase 6 (Hospitalización y Cese de servicio, `docs/PLAN` en `C:\Users\Usuario\.claude\plans\distributed-scribbling-wirth.md` sección "Fase 6"): un Servicio (`schema_servicios.sql`) pertenece siempre a una sola Familia por diseño explícito del Desarrollador ("No confundas Servicio con residencia, sería un error grosero. Servicio es la sumatoria de todas las prestaciones que se brindan al grupo familiar"). "Residencia" sería el concepto aparte que sí agrupa varios Pacientes de distintas Familias bajo un mismo establecimiento/domicilio compartido (ej. un geriátrico o residencia con varios Pacientes de Familias distintas) — no existe todavía ningún modelo de datos para esto | 2026-07-22 | Desarrollador (alcance, diferido explícitamente) | Existe un plan (cómo se modela una Residencia como entidad distinta de Servicio, cómo conviven Pacientes de Familias distintas bajo una misma Residencia sin romper el aislamiento de datos por Familia) presentado y aprobado por el Desarrollador antes de escribir código (CLAUDE.md §11, cambio de modelo de datos central) | 🔴 Abierto — diferido, no bloqueante |
| 76 | **Retención por jurisdicción** — concepto explícitamente fuera de alcance de la Fase 6: el archivado de `hospitalizaciones_paciente`/`cierres_servicio_paciente` hoy no borra nunca filas (coherente con CLAUDE.md §6), pero tampoco define todavía un plazo de retención configurable por jurisdicción — mismo tipo de brecha ya señalada para cálculos legales en el pendiente #72 (jurisdicción de cada Prestadora), pero para retención de datos en vez de cálculo de indemnización | 2026-07-22 | Desarrollador (alcance, diferido explícitamente) | Existe un documento por jurisdicción (`docs/legal/<país>.md`) que defina el plazo de retención exigido, y una decisión de cómo se aplica (soft-delete con fecha de purga, exportación antes de purgar, etc.) aprobada por el Desarrollador antes de escribir código | 🔴 Abierto — diferido, no bloqueante |
| 73 | **Etapa 6 — "Escanear Asistente" construida y verificada 2026-07-22 (ver `docs/claude_history.md` para la decisión de arquitectura).** Se descartó la página pública sin login (el sitio público por Prestadora que la iba a alojar ya no existe, dado de baja el 2026-07-18, pendiente #48). Diseño construido: dentro de la PWA de Familias, con sesión iniciada, la cámara lee el `qr_token` del Asistente (librería `html5-qrcode`, `pwa-familias/src/pages/EscanearAsistente.jsx`) y llama a un endpoint nuevo (`GET /api/app-familias/pacientes/:id/verificar-asistente/:qr_token`, autenticado, `backend/src/routes/appFamilias.js`) que confirma si ese Asistente es el asignado a la guardia de **hoy** de ese Paciente, no solo si está genéricamente certificado — nunca expone DNI/teléfono/email/dirección. El QR que genera el Panel (`panel/src/pages/asistentes/CertificadoTab.jsx`) se corrigió para codificar el `qr_token` crudo en vez de una URL a la página pública que ya no existe; se quitó la variable `VITE_SITE_URL`, ya sin uso. Cubre los 4 estados (pidiendo permiso de cámara / error de permiso o QR inválido / sin guardia hoy para comparar / resultado listo) y deshabilita la acción mientras está en curso. Textos nuevos en `es-AR`/`en`/`pt-BR` (`pwa-familias/src/i18n/translations.js`, sección `escaneo`). **Verificado end-to-end:** script de regresión `backend/scripts/test_pendiente_73_escanear_asistente.mjs` contra Supabase real (Sandbox) — los 4 casos posibles del endpoint (asignado, no asignado, sin guardia hoy, `qr_token` inexistente) dieron el resultado esperado, datos de prueba borrados sin residuos; navegador real (Playwright) confirmó el botón nuevo en `AsistenteAsignado.jsx`, la navegación a la pantalla nueva y el estado "Solicitando acceso a la cámara…" sin errores de consola. **No verificado en navegador real** (limitación del entorno headless, sin dispositivo de cámara): los estados de permiso denegado y de resultado ya escaneado — su lógica quedó cubierta indirectamente por la prueba del endpoint (mismo JSON que consume el componente) y por revisión de código, no por una corrida real de punta a punta con cámara. Detalle explícitamente fuera de este primer corte, no un pendiente nuevo: un tercero sin cuenta en el sistema (portero, otro familiar, el propio Paciente sin celular) no puede verificar sin login — si hace falta cubrir ese caso más adelante, ahí sí habría que retomar la pregunta de una página pública aparte | 2026-07-22 | Desarrollador (decisión de diseño) + Claude Code (implementación) | — | 🟢 Resuelto |

## Cómo se usa esta lista

- Cualquier pendiente nuevo que surja en cualquier sesión se agrega acá el mismo día, con los
  5 campos completos — no queda solo en `PROGRESS.md`.
- Antes de cerrar cualquier tarea, se recorre esta tabla completa y se reporta si algo
  cambió — aunque la respuesta sea "sigue exactamente igual".
- Cuando un ítem se cierra, no se borra la fila — se cambia el Estado a 🟢 Resuelto y se
  agrega la fecha, para que quede el historial de que existió y se resolvió.
