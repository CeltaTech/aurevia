# PENDIENTES.md — Lista maestra, obligatoria (ver Regla 12.4 de `CLAUDE.md`)

> Se revisa completa, línea por línea, antes de que cualquier sesión declare una tarea
> cerrada. Ningún pendiente se menciona en el diario de obra sin existir también acá.
> Estado se actualiza en el momento en que cambia, no se deja para "la próxima vez que se
> acuerden".
>
> **Acá está solo lo que sigue abierto, a medias o diferido.** Los pendientes ya cerrados
> se sacaron de esta lista el 2026-08-08 y quedaron guardados tal cual estaban.

| # | Pendiente | Creado | Responsable | Condición de cierre | Estado |
|---|---|---|---|---|---|
| 141 | **La pantalla de ingreso todavía muestra la marca del producto, no la de la Prestadora.** Encontrado el 2026-08-08 al terminar el `#95`. Adentro de la aplicación la marca ya es la correcta, pero la primera pantalla —la del email y la contraseña— dice "Careonys — Familias", porque nadie entró todavía y la aplicación no tiene forma de saber a qué Prestadora pertenece quien está mirando. **La salida conocida es la dirección web**: que cada Prestadora tenga la suya (`laprestadora.careonys.com`), así el nombre de la dirección le dice a la aplicación qué marca poner antes de que nadie escriba nada. Es lo que hacen los productos maduros del rubro. **No urge**: la pantalla se ve una vez y después la aplicación queda abierta. Y hay que mirarlo junto con el `#118`, que también toca cómo se presenta cada Prestadora. | 2026-08-08 | Claude Code (construirlo) + Desarrollador (decidir si se va a direcciones por Prestadora) | Alguien que abre `familias.careonys.com` sin haber entrado ve la marca de su Prestadora, no la del producto | 🔴 Abierto — no urgente, va junto con el #118 |
| 140 | **`configuracion_plataforma` sigue siendo una sola tabla y tenía que partirse en dos: es el último paso que le falta a la Etapa 2 de la separación CeltaTech/Careonys.** Encontrado el 2026-08-08 al ordenar la documentación: estaba anotado como "tarea 30" adentro de un plan de trabajo que se archivó, y en ningún otro lado. La tabla es de una sola fila por diseño (`backend/src/db/schema_admin_plataforma_04_mfa.sql:15-20`) y hoy mezcla dos cosas que ya no son de la misma empresa: si el segundo factor es obligatorio para los administradores —eso es de CeltaTech, que es quien maneja las cuentas técnicas— y el umbral de aviso sobre Prestadoras (`backend/src/db/schema_admin_plataforma_08_umbral_alerta_prestadoras.sql`), que es de la parte comercial y también quedó del lado de CeltaTech. La leen cuatro lugares: `backend/src/middleware/requiereRolPanel.js`, `backend/src/routes/panelConfiguracionPlataforma.js`, `panel/src/context/AuthContext.jsx` y `panel/src/pages/Mfa.jsx`. **Por qué conviene hacerlo ahora y no después:** hoy no hay ninguna Prestadora real usando el producto, así que partir la tabla no le cambia nada a nadie; el día que haya una, la misma partición pasa a ser una mudanza de datos vivos, con ventana de mantenimiento y plan de vuelta atrás. El plan que lo pedía es `celtatech/docs/PLAN_SEPARACION_CELTATECH.md`, Etapa 2. | 2026-08-08 | Claude Code | `configuracion_plataforma` queda partida según a quién pertenece cada valor, los cuatro lugares que la leen apuntan a donde corresponde, y la Etapa 2 del plan de separación queda cerrada en su propio documento | 🔴 Abierto |
| 139 | **Una Prestadora nueva no tiene ninguna primera experiencia: entra al Panel, se encuentra todo vacío y nadie le dice por dónde empezar.** Salió a la luz el 2026-08-08 al ordenar la documentación: estaba anotado en un estudio de experiencia de uso del 2026-07-21 que se archivó, y en ningún otro lado. **No es lo mismo que el pendiente #122**, que resuelve por debajo que las filas de configuración se creen solas; esto es lo de arriba, lo que la persona ve: una guía de primeros pasos que le diga qué cargar primero —sus datos, sus zonas, su lista de precios, sus tipos de Asistente, su gente— y le muestre cuánto le falta. Y no puede ser una sola guía para todos: no entra a lo mismo un Admin_prestadora que un Superadmin de CeltaTech abriendo una sesión de soporte técnico. **Por qué importa:** es literalmente la primera media hora de cada cliente nuevo con el producto, y hoy esa media hora es una pantalla en blanco. | 2026-08-08 | Claude Code (construye) + Desarrollador (decide qué se pide primero) | El Panel recibe a una Prestadora recién creada con una guía de primeros pasos propia del rol de quien entra, que muestra qué falta cargar y se apaga sola cuando ya está todo; comprobado dando de alta una Prestadora de prueba y recorriéndola de punta a punta | 🔴 Abierto |
| 138 | **Las tres aplicaciones nunca se revisaron contra una norma de accesibilidad, y la pregunta legal quedó sin contestar.** Rescatado el 2026-08-08 de un estudio de experiencia de uso del 2026-07-21 que se archivó. **Lo que ya está bien y no hay que rehacer** (queda escrito para que ninguna sesión futura lo repita): el contraste de los colores se resolvió con las variantes `-texto` de `variables.css`, hechas justamente para llegar al mínimo de 4,5 a 1 sobre fondo blanco, y la grilla de guardias ya tiene una alternativa por teclado además del arrastre con el mouse (`panel/src/pages/guardias/GuardiasGrid.jsx:99,106`). **Lo que falta:** nunca se hizo una pasada completa contra la norma WCAG 2.1 nivel AA, y se nota sobre todo en el texto alternativo para lectores de pantalla, que está casi ausente —solo 15 archivos de todo el frontend tienen alguno, entre 1 y 5 cada uno, contra 32 pantallas nada más en el Panel—, así que los botones que son solo un ícono no se anuncian y quien no ve la pantalla no sabe qué está apretando. **La parte legal, que es del Desarrollador:** en Argentina existe la Ley 26.653 de accesibilidad web con su decreto reglamentario, pero no está claro si alcanza a un software que se le vende a empresas, y no se encontró jurisprudencia ni interpretación oficial que lo confirme. Según `CLAUDE.md` §3 eso se contesta y se escribe en `docs/legal/argentina.md`, no se supone acá. **Dato de mercado que conviene tener a mano:** ningún competidor del rubro publica su declaración de accesibilidad, así que hacerlo sería una ventaja concreta frente a una Prestadora que está comparando plataformas. | 2026-08-08 | Claude Code (revisa y corrige las pantallas) + Desarrollador (consulta legal por país) | Hecha la pasada completa contra WCAG 2.1 nivel AA en las tres aplicaciones, con todos los controles que son solo ícono anunciados para lectores de pantalla, y escrita la respuesta legal en el documento legal del país que corresponda | 🔴 Abierto |
| 137 | **La Parte 3 del Módulo 6, "piezas de apoyo", figura como faltante desde hace un mes y nadie sabe qué es.** Encontrado el 2026-08-08 al ordenar la documentación. Vive en un solo lugar: `docs/BUILD_ORDER.md`, en la fila del Módulo 6, que dice que las Partes 1 y 2 están construidas y probadas y que falta la Parte 3, "piezas de apoyo", que no tiene documento propio. No hay ningún papel que diga qué entra ahí, ni fila en esta lista, así que hoy es un agujero con nombre y sin contenido: cualquier sesión futura lee "falta la Parte 3" y no tiene forma de saber si falta algo de verdad o si ya quedó absorbido por lo que se construyó después —el aviso de guardia sin cubrir, la escalada al Coordinador, el cierre de guardia con botón propio—. **Primer paso, y es barato:** buscar en el diario de obra y en el PRD del Panel qué se quiso decir con "piezas de apoyo", y después una de dos: o se escribe qué falta y se vuelve trabajo concreto, o se lo da por absorbido y se corrige esa fila para que deje de decir que falta algo. | 2026-08-08 | Claude Code | `docs/BUILD_ORDER.md` deja de decir "falta la Parte 3" sin explicar qué es: o queda escrito qué falta, con su propia fila en esta lista, o queda escrito que se absorbió y en qué se absorbió | 🔴 Abierto |
| 136 | **La Familia no puede ver su semana: la aplicación le muestra la guardia de ahora, o la que sigue, y nada más.** Rescatado el 2026-08-08 de la comparación de pantallas del 2026-08-02, que se archivó sin que esto quedara anotado en ningún lado (era su observación 8). Comprobado contra el código: `backend/src/routes/appFamilias.js` busca la guardia activa y, si no hay ninguna, la próxima programada, y ahí se termina — no hay agenda, ni semana, ni historial de guardias pasadas. **Por qué no es un adorno:** "¿quién viene el jueves?" es la pregunta número uno por la que una Familia levanta el teléfono y llama a la Prestadora, y cada una de esas llamadas es tiempo de la Coordinadora. El software maduro del rubro pone el calendario en el centro del portal de la familia, con el estado de cada visita: cumplida, cancelada, o el Asistente no se presentó. | 2026-08-08 | Claude Code | La aplicación de la Familia muestra la agenda de sus guardias —al menos la semana— con quién viene y cómo terminó cada una, y el Desarrollador lo prueba con un Paciente que tenga varias guardias cargadas | 🔴 Abierto |
| 135 | **Se construyó la mitad que ofrece una guardia y no la mitad que la recibe: el Asistente no puede ver ni aceptar desde su teléfono lo que se le ofrece.** Rescatado el 2026-08-08 de la comparación de pantallas del 2026-08-02, que se archivó sin que esto quedara anotado (era su observación 5). El Panel ya sabe ofrecerle una misma guardia a varios Asistentes con fecha límite, y la base guarda esos ofrecimientos. Del otro lado no hay nada, comprobado contra el código: la aplicación del Asistente tiene seis pantallas y ninguna es de ofertas (`pwa-asistentes/src/pages/`), y el backend de esa aplicación no tiene ningún camino para consultarlas ni para aceptarlas — las únicas direcciones que existen son perfil, guardias, dar el presente, reporte, cierre, ubicación, notificaciones y calificaciones (`backend/src/routes/appAsistentes.js`). **Consecuencia de hoy:** la oferta se le avisa por fuera del sistema, por teléfono o por mensaje, y lo que el Asistente contesta no queda registrado en ninguna parte. | 2026-08-08 | Claude Code | El Asistente ve en su aplicación las guardias que se le ofrecieron, con su fecha límite, y las acepta o las rechaza desde ahí; el Panel ve la respuesta en el momento y queda registrada | 🔴 Abierto |
| 134 | **`asistentes.especialidades` sigue vivo y en uso, aunque el glosario lo dio por retirado hace tiempo.** Encontrado el 2026-08-08 al ordenar la documentación: estaba anotado como "tarea 88" adentro de un plan de trabajo que se archivó y nunca llegó a esta lista. El glosario de `CLAUDE.md` §4 dice que "especialidad" no se usa más, y que lo que ocupa ese lugar es el Tipo de Asistente, que ya existe y funciona: la columna `asistentes.tipo_asistente_id` y las tablas `tipos_asistente` y `tareas_tipo_asistente` están construidas. Pero la columna vieja no se retiró y hoy conviven las dos: `backend/src/routes/appAsistentes.js:79`, `backend/src/routes/appFamilias.js:214` y `:256`, `backend/src/routes/panelCuentas.js:210,264,267`, `backend/src/routes/panelImportacion.js:107`, `backend/src/routes/postulacionAsistente.js`, y en pantalla `pwa-familias/src/pages/AsistenteAsignado.jsx:66`, que es justamente donde la Familia termina leyendo la palabra retirada. **Por qué importa más que un cambio de nombre:** mientras haya dos lugares que dicen qué sabe hacer un Asistente, los dos se van a desencontrar, y el que manda —el catálogo— es el que casi nadie carga. | 2026-08-08 | Claude Code | Ninguna ruta del backend ni pantalla usa `especialidades`: todo lee el Tipo de Asistente, y la columna queda marcada como retirada en la base sin borrar lo que ya está guardado (`CLAUDE.md` §7 regla 13) | 🔴 Abierto |
| 133 | **El catálogo de Tareas está construido, cargado, y no lo ve nadie: ni el Asistente que las tiene que hacer, ni la Familia que las tiene que esperar.** Rescatado el 2026-08-08 de dos documentos que se archivaron el mismo día: una tarea de trabajo que nunca llegó a esta lista y la observación 1 de la comparación de pantallas del 2026-08-02. El catálogo existe y anda: el Panel deja definir, por tipo de Asistente, las dos listas separadas —lo que le corresponde hacer y lo que no— (`panel/src/pages/configuracion/TiposAsistenteTab.jsx`, tabla `tareas_tipo_asistente`). **Del lado de la Familia está a mitad de camino, y esa mitad es barata de terminar:** el backend ya arma las dos listas y se las manda a la pantalla (`backend/src/routes/appFamilias.js`, la dirección del Asistente asignado), pero la pantalla las descarta — `pwa-familias/src/pages/AsistenteAsignado.jsx` toma de la respuesta solo el asistente, el certificado, las evaluaciones y la guardia, y en el lugar donde irían las tareas muestra la palabra retirada "especialidades" (ver #134). **Del lado del Asistente no hay nada:** la pantalla de la guardia muestra el nombre del Paciente, el domicilio, las patologías y una pila de botones, sin una sola tarea (`pwa-asistentes/src/pages/GuardiaActiva.jsx`), y el backend de esa aplicación ni siquiera consulta la tabla. **Por qué las dos listas pesan igual:** la segunda —lo que NO le corresponde— es la que evita la discusión en la puerta, y es el motivo por el que se construyó el catálogo. | 2026-08-08 | Claude Code | La Familia ve las dos listas de su Asistente en la pantalla del Asistente asignado, el Asistente ve las que le corresponden mientras dura su guardia, y las dos salen del mismo catálogo, sin copiarlo a ningún lado | 🟡 En curso — **la mitad de la Familia quedó hecha el mismo 2026-08-08** (tarea 89, commit `387910b`). La pantalla del Asistente asignado muestra ahora el tipo y las dos listas, y el nombre del tipo sale traducido si el tipo lo trae el producto o tal cual si lo escribió la Prestadora. Comprobado en el navegador contra la base local, con datos sembrados a propósito para los casos que arman la discusión en la puerta (limpiar la casa, cocinar para el resto de la familia, dar inyecciones). El párrafo de arriba describe cómo estaba antes de eso. **Falta la mitad del Asistente:** ni la pantalla de la guardia ni el backend de esa aplicación consultan todavía el catálogo |
| 132 | **Dos reglas de diseño que valen para siempre y no están escritas en ningún documento vigente.** Aparecieron el 2026-08-08 al ordenar la documentación: estaban anotadas de paso en un plan de trabajo y en ningún lado más, así que si nadie las escribe como regla se pierden. **(a) La moneda siempre se escribe.** Todo campo de precio, de factura o de monto tiene que decir en qué moneda está, sin dar por supuesto que es peso argentino, y cada comprobante tiene que guardar además el tipo de cambio de referencia que se usó. Importa por dos motivos: el producto se piensa para varios países desde el día uno, y un número guardado sin moneda, leído un año después, no se sabe cuánto vale ni se puede reconstruir. **(b) El financiador futuro tiene que estar contemplado en el diseño de roles, aunque no se construya todavía.** Es la Obra Social o la prepaga que paga el Servicio y que en algún momento va a pedir ver cómo se está cumpliendo. Lo que va a poder ver son métricas de cumplimiento agregadas —cuántas guardias se cubrieron, cuántas se cayeron, a tiempo o tarde— y nada del detalle clínico del Paciente. No se construye ahora: lo que se pide es que el esquema de roles y permisos deje el lugar hecho, para no tener que rehacer los permisos desde cero el día que se pida. | 2026-08-08 | Claude Code (redactar las dos reglas) + Desarrollador (aprobar el cambio) | Las dos quedan escritas como regla en `CLAUDE.md` §7 | 🔴 Abierto |
| 131 | **Se pidió que la base impida mezclar dos regímenes de trabajo en un mismo Asistente, y nunca se construyó ni quedó anotado.** Salió a la luz el 2026-08-08 al ordenar la documentación. El pedido original era concreto: que sea la base de datos misma —no la lógica del programa, que se puede saltear, tener un error o quedar sin aplicar en una ruta nueva— la que no deje que un mismo registro de Asistente tenga a la vez datos de personal contratado en forma directa y datos de personal de una empresa tercera licenciataria. **Por qué importa:** son dos regímenes distintos, con reglas, responsabilidades y forma de pago distintas, y un registro que tenga las dos cosas a la vez no representa a ninguna persona real. Es un dato roto que no molesta el día que se carga y aparece mucho después, cuando hay que liquidar, informar o responder por alguien. **Lo que hay que hacer:** decidir si se construye y, si va, escribir la restricción en la base y probar que efectivamente rechaza el caso mixto. | 2026-08-08 | Desarrollador (decide si se hace) + Claude Code (lo construye) | Decidido explícitamente: o la base rechaza el registro mixto y quedó probado con un intento real que falla, o quedó escrito por qué se descarta | 🔴 Abierto |
| 130 | **Las 15 filas de escalas legales que hay cargadas son valores de ejemplo, no valores reales.** Están en `backend/src/db/schema_etapa2b.sql:159-173` y ahí mismo figuran marcadas como provisorias, una por una: son los días de preaviso, el período de prueba, la indemnización por antigüedad, el tope indemnizatorio, el multiplicador agravado, los topes de licencia paga y los siete pesos del indicador de riesgo de dependencia. El sistema los usa para calcular, así que hoy hace bien la cuenta con números que no son los que corresponden. **Por qué es serio y por qué no es una tarea de programación:** el sistema ya está hecho como debe estar, tomando las escalas de una fuente configurable y a la fecha del hecho, así que no hay nada que arreglar en el código. Lo que falta es el contenido, y equivocarlo no da error: da un número creíble y mal. Si una Prestadora real liquida un cese con estos valores, liquida mal, y eso se paga en un juicio. **Lo que hace falta:** que un abogado laboralista revise las 15 filas y confirme o corrija cada valor con su fecha de vigencia, antes de que exista la primera Prestadora real. | 2026-08-08 | Desarrollador | Un abogado laboralista revisó las 15 filas y quedaron cargados los valores reales con su fecha de vigencia y su fuente, o quedó escrito cuáles siguen siendo provisorias y por qué | 🔴 Abierto |
| 129 | **El Certificado de Aptitud imprimible no tiene diseño definido en ningún lado.** Comprobado el 2026-08-08: hoy el sistema genera el código QR como imagen y ahí se termina. Cómo se ve el certificado cuando se imprime no está definido en ningún documento. **Lo que falta decidir:** el membrete —que va con la marca de la Prestadora y el producto en una línea al pie, como manda `CLAUDE.md` §7.1—, el tamaño de hoja, qué datos lleva (nombre del Asistente, tipo de Asistente, qué lo habilita, desde cuándo y hasta cuándo vale, quién lo emitió) y dónde va el código QR para que quien lo reciba pueda verificarlo. **Por qué importa:** es el papel que el Asistente muestra en la puerta de la casa de una Familia. Si sale incompleto o mal hecho, quien queda mal no es Careonys, es la Prestadora. | 2026-08-08 | Desarrollador (define qué datos lleva y cómo se ve) + Claude Code (lo construye) | El diseño del certificado impreso queda escrito en un documento vigente y el sistema lo genera así, probado imprimiendo uno de punta a punta | 🔴 Abierto |
| 128 | **Dos pantallas del Panel se especificaron y nunca se construyeron.** Encontradas el 2026-08-08 al ordenar la documentación. **(a) La "Vista mapa" de Postulaciones** figura como requisito en `docs/PRD_02_Panel_Admin.md:32` —muestra a los Asistentes del plantel activo agrupados por zona y, cuando llega una solicitud, resalta a los más cercanos— y no existe. **(b) "Asignar Asistente" desde una Solicitud** —pasar de la solicitud directamente a elegir quién la cubre, por zona, tipo de Asistente y disponibilidad, sin dar la vuelta por otra pantalla— tampoco se construyó, y encima no estaba anotado en ninguna parte hasta hoy. **Por qué importa:** un documento vigente que promete una pantalla que no existe es peor que no prometer nada, porque cualquiera que lo lea después va a dar por hecho que está y va a planificar encima de algo que no hay. **Lo que hay que hacer:** decidir una por una si se construyen o si se sacan del PRD. | 2026-08-08 | Desarrollador (decide si se construyen o se sacan) + Claude Code (ejecuta la decisión) | Las dos resueltas: construidas y probadas en el Panel, o quitadas de `docs/PRD_02_Panel_Admin.md` con el motivo escrito | 🔴 Abierto |
| 127 | **La lista de acciones que por defecto son solo de Administrador está escrita dos veces, y las dos no dicen lo mismo.** Apareció el 2026-08-06 al agregar el permiso `ver_pagos_asistente`. La misma decisión —"si la Prestadora no configuró nada, ¿el Coordinador puede o no?"— vive en dos lugares que no se hablan: en el programa, `backend/src/utils/permisos.js` (`ACCIONES_DEFAULT_SOLO_ADMIN`, hoy con cinco acciones), y en la base, la función SQL `tiene_permiso()` de la migración `supabase/migrations/20260728163000_base_existente.sql:445-477`, que tiene su propia lista con **dos**: `RETURN p_accion NOT IN ('alta_manual_asistente', 'alta_manual_familia');`. Es exactamente lo que prohíbe la regla 12 de `CLAUDE.md` §7 (punto único de verdad), y la propia regla ya dice cómo se arregla cuando una de las puntas es Postgres: una sola función SQL que consulten todas las políticas, y que el programa lea de ahí en vez de tener su copia. **Hoy no rompe nada** —ninguna política de RLS usa `ver_pagos_asistente`, así que la pantalla de pagos no depende de la lista de la base—, pero cualquier permiso nuevo que sí llegue a una política va a decidir distinto según se pregunte desde el programa o desde la base. **Por qué no se arregló en el momento:** hace falta una migración y tocar políticas que ya están en producción, y eso no entra de costado en una tarea de pantalla. | 2026-08-06 | Claude Code | Una sola lista de acciones por defecto, consultada por el programa y por las políticas de RLS, comprobada contra la base viva con las cinco acciones dando el mismo resultado por los dos caminos. | 🔴 Abierto |
| 126 | **No existe el registro de lo que se le pagó al Asistente: hay una cuenta, pero no hay recibo.** La pantalla de pagos (`panel/src/pages/PagosAsistentes.jsx`, hecha el 2026-08-06) calcula en el momento lo que le corresponde a cada Asistente en el mes, pero **no guarda nada**, porque en la base no hay dónde: entre las 91 tablas no existe ninguna de pagos ni de liquidaciones, y `guardias` no tiene ninguna columna de importe (comprobado el 2026-08-06). Consecuencias concretas: (a) **no se puede marcar un mes como pagado**, así que el sistema no sabe qué se pagó y qué no; (b) **el número cambia solo** — si mañana se corrige el valor hora de la ficha, el mes de marzo se recalcula con el valor de hoy, y lo que se pagó de verdad se pierde; (c) no hay comprobante, ni número, ni fecha de pago, ni forma de pago; (d) no hay cargas sociales, adicionales, antigüedad, presentismo ni descuentos, y esos no se pueden inventar en el código (regla 10 de `CLAUDE.md` §7): tienen que salir de una fuente configurable por jurisdicción y a la fecha del hecho, como manda `CLAUDE.md` §3. **Lo que hace falta, en orden:** una tabla de liquidación por Asistente y por período que **congele** el valor hora o el sueldo usado y las horas contadas (no que las vuelva a calcular), los conceptos de suma y resta como catálogo configurable por Prestadora y jurisdicción, y recién encima de eso el "marcar como pagado" y el comprobante. Es un módulo, no un botón. | 2026-08-06 | Claude Code | La liquidación guardada por Asistente y período, con los valores congelados a la fecha del hecho, los conceptos saliendo de configuración y no del código, y la pantalla de pagos leyendo de ahí en vez de calcular al vuelo. | 🔴 Abierto |
| 125 | **El Servicio está definido pero la base no lo soporta: hay que mudarlo del Paciente al arreglo.** El 2026-08-07 quedó definido qué es un Servicio (la definición vive en el glosario, `CLAUDE.md` §4) y al comprobarlo contra la base apareció que **el Servicio ya existe en el producto, repartido en cinco tablas y colgado del Paciente en vez del arreglo**: el precio en `prestaciones`, el calendario en `series_guardias`, el cierre en `cierres_servicio_paciente`, las Guardias en `guardias.paciente_id` y el paquete en `paquetes_prestaciones`. Y la tabla `servicios`, que debería ser el centro, **no la lee ni la escribe ninguna línea de código** (comprobado: cero resultados de `servicio_id` y de `from('servicios')` en `backend/src`, `panel/src`, `pwa-familias/src` y `pwa-asistentes/src`); las tres tablas que la referencian tienen su `servicio_id` opcional y siempre vacío. **Los cinco huecos concretos, todos verificados el 2026-08-07:** (a) `servicios.familia_id` es obligatorio, así que **una Obra Social no puede ser Cliente Contratante** — y la Obra Social ni siquiera existe como entidad, es un campo de texto en `pacientes.obra_social`. Decidido el 2026-08-07: se hace **el lugar** (tipo de contratante más identificador, en vez de `familia_id` obligatorio) porque es barato ahora y carísimo después, pero **la Obra Social como entidad de verdad queda para después del MVP**, junto con la Subcontratación (#123); (b) `prestaciones` no tiene `vigente_desde` ni `vigente_hasta`, solo `estado`, así que al dar de baja una prestación **se pierde cuándo dejó de estar vigente** y el mes se factura mal — `updated_at` no sirve porque lo pisa cualquier edición; (c) el precio y el calendario cuelgan del Paciente, de modo que el domicilio compartido no tiene dónde guardar el "menos del doble" (es la raíz del pendiente #94); (d) el cierre cuelga del Paciente y **una vez cerrado queda cerrado para siempre** — la ficha pregunta si existe *alguna* fila de cierre (`panel/src/pages/familias/PrestacionesPaciente.jsx:83`), así que quien suspende en marzo y vuelve en julio sigue figurando como cerrado, sin "reabrir"; (e) los motivos de cierre son motivos de Paciente (`fin_demanda`, `fallecimiento`, `otro`) y no entran "la Obra Social no renovó" ni "se pasó a otra Prestadora". **Tres reglas de diseño que hay que respetar cuando se construya** (estaban en un documento aparte que se borró el 2026-08-08, porque la definición de qué es un Servicio pasó al glosario y lo que faltaba construir se anotó acá): (1) **el horario es de cada prestación, no del Servicio** — el cuidado puede ser de lunes a viernes, la kinesiología martes y jueves y el traslado un día suelto, todo dentro del mismo Servicio, así que el calendario no se puede guardar una sola vez para todo el acuerdo; (2) **no toda prestación se convierte en Guardia** — el cuidado por horas sí y aparece en la grilla, pero un traslado o una limpieza se hacen y se cobran sin generar guardia, y la pantalla de Servicios tiene que mostrar las dos clases; (3) **un mismo Cliente puede tener varios Servicios abiertos a la vez** — si la madre y el padre están en arreglos separados son dos Servicios, y si están en el mismo arreglo es uno solo con dos Pacientes; lo elige quien carga, el producto no lo impone. **Por qué no se hace de costado:** toca facturación y generación de Guardias, que es lo que le cobra a la gente. Va con su propio inventario y plan aprobado, como pide `CLAUDE.md` §11. **Lo que sí se puede hacer antes:** la pantalla de Servicios de solo lectura, que muestra el Servicio como el centro del negocio con lo que hoy existe. | 2026-08-07 | Claude Code (el diseño ya está escrito; falta el inventario, el plan y la migración) | Los cinco huecos cerrados y verificados contra la base: el Cliente Contratante puede ser una Familia o un contratante de otro tipo; cada prestación tiene desde y hasta cuándo estuvo vigente; el precio y el calendario cuelgan del Servicio; el cierre cuelga del Servicio y admite reabrir el vínculo con ese Paciente en un Servicio nuevo; y los motivos de cierre cubren el fin del arreglo, no solo el fin del Paciente. (Las dos preguntas que estaban abiertas se contestaron el 2026-08-07: la palabra para quien contrata es **Cliente Contratante** —"Cliente" a secas en pantalla, `contratante` en el código—, y **la Obra Social entra a medias a propósito**: el lugar se hace ahora porque después sale carísimo, pero la Obra Social como entidad de verdad queda para después del MVP) | 🔴 Abierto — definición terminada el 2026-08-07, construcción sin empezar |
| 124 | **La caja fuerte de claves es un archivo de texto plano, desordenado, y nadie sabe qué hay adentro ni qué sigue sirviendo.** Hoy es `panel/no_commit/Claves y contraseñas.txt` (más `Backup-codes-cuentas.celtatech.txt` al lado). **Son dos problemas distintos:** (1) **desorden** — el 2026-08-07 se descubrió que la ficha de Supabase apunta a un proyecto que no es el de Careonys: los dos identificadores tienen 20 caracteres y no coincide ni uno, y la contraseña guardada no autentica contra la base que el producto usa de verdad (`abcpmzfnnhpuiupmrsdi`, confirmado leyendo a qué se conecta el panel publicado). Estuvo así meses sin que nadie lo notara, que es exactamente lo que pasa cuando la caja fuerte no tiene forma; (2) **dónde viven los valores** — texto plano en un disco, sin copia, sin registro de cuándo se cambió cada clave, y quien entre a esa máquina se lleva todo de una sola vez. **La forma madura, que es lo que hay que copiar: el repositorio guarda el mapa, no el tesoro.** Tres capas: **(a)** el valor vive donde se usa —secretos de GitHub, variables de Railway, Vault de Supabase, tokens de Cloudflare—, lugares que además no te dejan leerlo de vuelta; **(b)** la copia maestra de lo **irremplazable** vive en un gestor de contraseñas, no en un archivo; **(c)** en `docs/` queda un mapa con qué secreto existe, para qué sirve, dónde vive, quién lo usa y cuándo se cambió, **sin un solo valor adentro**. **El punto que ahorra la mitad del trabajo:** la mayoría de lo que hay en ese archivo **no hace falta guardarlo**. Una clave de API, una contraseña de base o una clave de servicio se regeneran en dos minutos; no son un tesoro sino una cerradura que se puede cambiar, y guardarlas solo agrega un lugar más del que robarlas. Lo único irremplazable es el **acceso a las cuentas**: correo, contraseña y códigos de recuperación de Supabase, GitHub, Cloudflare, Railway y el registrador de dominios. **Gestor propuesto: Bitwarden** — gratis para una persona sin límite de fichas, código abierto, ~4 dólares por persona por mes cuando entre alguien más, y con herramienta de línea de comandos, así que abriéndola una vez por sesión Claude Code puede seguir sacando las claves que necesite sin que el Desarrollador las tipee. **Se hace junto con el pendiente X-15 / la tarea de rotar las claves que quedaron escritas en conversaciones viejas** (las transcripciones `*.jsonl` quedan en el disco): la mudanza es el momento natural para cambiarlas. **Reparto:** el inventario, el mapa y el vaciado del archivo son de Claude Code; crear la cuenta del gestor y pegar ahí los accesos es del Desarrollador, porque crear cuentas y tipear contraseñas no lo hace Claude Code nunca | 2026-08-07 | Desarrollador (crear el gestor y cargar los accesos) + Claude Code (inventario, mapa y vaciado) | El mapa de secretos escrito en `docs/` sin un solo valor adentro; los accesos de cuenta cargados en el gestor; las claves regenerables rotadas y colocadas solo donde se usan; `Claves y contraseñas.txt` vaciado o reducido a un apuntador al gestor; y la ficha equivocada de Supabase corregida o borrada | 🔴 Abierto |
| 123 | **La modalidad Subcontratación existe como nombre pero no está construida: se difirió a post-MVP el 2026-08-07 (ver #115).** Hoy el valor `subcontratacion` está admitido en las tres restricciones de la base, pero el Panel no lo ofrece a propósito (`panel/src/pages/configuracion/Accesos.jsx:191`) porque no hay nada detrás. **Qué hay que resolver cuando entre en el plan, que son cuatro cimientos, no una pantalla:** (1) **el cliente puede no ser una Familia** — el Desarrollador la definió como *"servicio que tomaremos de un cliente (por ejemplo una obra social) y se lo derivemos para que lo cubran ellas"*, y hoy todo el modelo arranca en la Familia; (2) **la Guardia la cubre gente que no está en el plantel**, y `asistentes.canales` solo admite `directo` y `marketplace`; hay que decidir si esa gente se carga como Asistentes con un canal nuevo o si la unidad que se agenda deja de ser una persona y pasa a ser la empresa; (3) **el dinero pasa de dos libros a tres**: se le cobra al cliente y se le paga a la **Empresa subcontratada**, no al Asistente, así que la liquidación por Asistente no aplica; (4) **las dos decisiones del Desarrollador, diferidas junto con la modalidad y sin preguntar hasta entonces** — (a) **quién controla la matrícula y los documentos** de esa gente: ¿la Prestadora persona por persona, igual que con su propio plantel, o se le exige a la empresa subcontratada que responda ella y la Prestadora solo guarda el compromiso?; y (b) **quién marca el presente**: ¿la app del Asistente, con cada persona de la otra empresa dada de alta como usuario, o un parte que manda la empresa y alguien carga? La respuesta a (b) decide si el pase de guardia por QR (#113) aplica o no a esta modalidad. **Precondición para abrir esto:** que haya una Prestadora real que lo pida. Mientras no la haya, construirlo es rehacer media base para cero clientes | 2026-08-07 | Desarrollador (las dos decisiones) + Claude Code (el diseño) | La modalidad diseñada de punta a punta —cliente, quién cubre la guardia, los tres libros de dinero, matrícula y presente— con pantallas y menú propios, y las dos decisiones del Desarrollador respondidas y escritas acá | 🔵 Diferido a post-MVP |
| 122 | **Una Prestadora recién creada nace sin su configuración: hay que cargársela a mano.** Encontrado el 2026-08-07 mientras se construía el aviso de guardia sin cubrir (#106). Varias cosas del producto se apoyan en que la Prestadora tenga su fila de configuración: `configuracion_notificaciones` (a quién le llega cada aviso y por qué vía), `configuracion_aviso_cese_asistente` y ahora `configuracion_aviso_guardia_sin_cubrir`. **Esas filas hoy solo aparecen porque cada migración se las siembra a las Prestadoras que existían el día que se corrió la migración.** No hay nada en la base que las cree cuando nace una Prestadora nueva —ningún disparador sobre `prestadoras`— ni ninguna ruta del backend que las inserte al darla de alta. Consecuencia: la Prestadora número siguiente arranca sin ellas, y lo que no tiene fila de configuración simplemente no avisa; peor todavía, no falla ni se queja, así que nadie se entera hasta que se pierde una guardia. **Se agrava con cada aviso configurable nuevo que se agregue**, porque cada uno suma otra tabla que se olvida. La solución no es acordarse de sembrar: es que dar de alta una Prestadora deje su configuración completa por sí solo, con los valores de arranque de cada tabla, en un único lugar que no haya que actualizar tabla por tabla (`CLAUDE.md` §7.12) | 2026-08-07 | Claude Code | Crear una Prestadora nueva deja automáticamente todas sus filas de configuración con los valores de arranque, comprobado dando de alta una Prestadora de prueba y verificando que recibe los avisos sin que nadie le toque la base a mano | 🔴 Abierto |
| 119 | 🟢 **Cerrado** — ~~**Hay dos proyectos en Supabase —"aurevia" (pausado) y "CeltaTech's Project"— y no está confirmado cuál sobra.**~~ **CONTESTADO el mismo 2026-08-06: hacen falta los dos, ninguno sobra.** `abcpmzfnnhpuiupmrsdi` ("aurevia") es la base de **Careonys**, el producto — la usan las tres aplicaciones y el motor del sistema. `jpochqufisjjapoudjxj` ("CeltaTech's Project") es la base del **panel comercial de CeltaTech**, que es otro producto en otro repositorio (`panel/`) — comprobado el 2026-08-06: responde 401 (vivo), y su identificador aparece en `panel/supabase/.temp/project-ref`, `panel/README.md` y `panel/web/.env.nube.local`. Ya estaba escrito en `docs/CUENTAS.md:192` desde el 2026-07-28: *"Los dos proyectos de la empresa"*. **No se borra ninguno.** Lo que sí queda abierto es que el de Careonys está pausado — eso es #121. *Texto original de la duda, conservado:* El único proyecto que usa el sistema hoy es el de identificador `abcpmzfnnhpuiupmrsdi`: lo apuntan las tres aplicaciones y el motor del sistema (`VITE_SUPABASE_URL` y `SUPABASE_URL` de `pwa-asistentes/.env.production`, `panel/.env.production` y `backend/.env`, verificado el 2026-08-06). Ese identificador es el que el 2026-07-16 se rebautizó a "Careonys" desde el panel de Supabase (ver #40, ítem 7), así que **ninguno de los dos nombres que se ven hoy coincide con lo que quedó registrado** — puede ser que se lo haya vuelto a renombrar, o que se estén mirando dos cuentas distintas de Supabase (ya pasó antes: ver #16). **Lo que hay que mirar, y es un vistazo:** abrir cada uno de los dos proyectos y comparar su identificador con `abcpmzfnnhpuiupmrsdi`. El que coincide es el vivo y no se toca. El otro, si está pausado y vacío, se borra; si tiene datos, primero una copia y después se borra. **No se borra ninguno sin ese cotejo**, porque un proyecto de Supabase borrado no se recupera. | 2026-08-06 | Desarrollador (el cotejo y la decisión de borrar — es su cuenta y es irreversible) | Confirmado cuál de los dos es `abcpmzfnnhpuiupmrsdi`, y el otro borrado o dejado a propósito con el motivo escrito acá | 🔴 Abierto |
| 118 | **La Prestadora puede querer una marca distinta por cada modalidad de trabajo, sin que las Familias se den cuenta de que atrás es la misma empresa.** Planteado por el Desarrollador el 2026-08-06. Hoy el modelo de marca tiene dos escalones: CeltaTech es la empresa, Careonys es el producto que comercializa, y a la Familia y al Asistente les aparece la marca de **su** Prestadora, con el producto en una línea discreta al pie (`CLAUDE.md` §7.1 y `docs/MARCA.md` §0). Esto agrega un tercer escalón hacia abajo: la misma Prestadora, una marca por modalidad. **Por qué no es un capricho:** es una decisión de marketing legítima — la misma empresa puede atender por cooperativa/subcontratación y en relación directa con precios y públicos distintos, y no querer que un cliente compare los dos y saque conclusiones. **Lo que hay que resolver:** dónde vive el nombre de fantasía (hoy es uno solo, `prestadoras.nombre_fantasia`, así que probablemente pase a colgar de la modalidad); qué más viaja con la marca además del nombre (logo, colores, dirección de correo desde la que salen los avisos, dominio); y qué pasa cuando una Familia tiene dos Servicios de modalidades distintas con la misma Prestadora — porque ahí la misma persona ve dos marcas y el disfraz se cae solo. **Depende de #106**, que es donde se define qué es una modalidad; no se puede armar la marca por modalidad antes de que las modalidades existan. | 2026-08-06 | Claude Code (diseño y construcción) + Desarrollador (decidir hasta dónde llega la separación) | Una Prestadora puede configurar nombre y apariencia por modalidad, y las pantallas de Familia y Asistente muestran la que corresponde al Servicio que están mirando | 🔴 Abierto |
| 117 | **Una guardia que el Asistente se olvida de cerrar no se le puede facturar a la Obra Social, y hoy nadie del Panel se entera de que quedó abierta.** Efecto lateral del cambio del 2026-08-06 (pendiente #113): hasta ese día cerrar la guardia era un efecto secundario de mandar el reporte, así que era imposible que quedara sin cerrar; desde que cerrar es un botón aparte, el Asistente puede mandar el reporte y retirarse sin apretarlo. **Por qué es plata y no prolijidad:** el informe para la Obra Social cuenta únicamente las guardias en estado `completada` (`backend/src/routes/panelInformesObraSocial.js:80,83`), y una guardia sin cierre se queda en `activa` para siempre. Se prestó el servicio, quedó el reporte, y no se cobra. **Lo que ya se hizo para tapar el agujero, que alcanza para el Asistente y no para la Prestadora:** después de mandar el reporte la aplicación lo devuelve a la pantalla de la guardia, que es donde está el botón, y la lista de sus guardias le marca "Sin cerrar" las de días anteriores que siguen abiertas (`pwa-asistentes/src/pages/MisGuardias.jsx`). Pero el Coordinador no tiene ninguna pantalla que se lo muestre, y el Panel tampoco tiene forma de cerrarla por él. **Las dos decisiones que faltan:** si el sistema cierra solo una guardia vencida sin cierre (y con qué hora — la de fin programada, seguramente, porque inventar una hora de salida es falsear el registro que después se le muestra a la Obra Social), o si la deja abierta a propósito para que alguien la revise a mano. Va junto con el aviso de guardia sin cubrir (tarea 68). | 2026-08-06 | Claude Code (construye) + Desarrollador (decide si se cierra sola o se revisa a mano) | El Coordinador ve en el Panel las guardias que quedaron sin cerrar, puede cerrarlas él dejando registro de que las cerró el Panel y no el Asistente, y está escrito qué pasa con las que nadie cierra. | 🔴 Abierto |
| 113 | **El pase de guardia por QR se habló, se anotó como tarea #66 y nunca llegó a esta lista ni al código.** El 2026-08-03 el Desarrollador avisó que no lo veía en ninguna parte, y tenía razón. Lo que se habló en su momento: el Asistente da el "presente" **escaneando el código QR de la aplicación de la Familia, o el del Asistente al que releva**, y al terminar vuelve a escanear el de la Familia para cerrar. Es la firma de entrada y salida del turno, con un testigo del otro lado. **Qué hay hoy, que es otra cosa y conviene no confundir:** (1) el check-in del Asistente se hace apretando un botón en su propia aplicación y se comprueba contra el GPS, con una tolerancia en metros configurable (`metros_tolerancia_checkin`) — nadie del otro lado confirma nada; (2) el check-out **no tiene botón propio**: sucede solo, de costado, cuando el Asistente aprieta "Confirmar" al final del reporte diario (`backend/src/routes/appAsistentes.js:237-290`); (3) sí existe un QR, pero es para **verificar identidad**, no para el pase de guardia: la Familia escanea el código del Asistente para comprobar que la persona que golpeó la puerta es la que le mandaron (`backend/src/routes/appFamilias.js:236` y `pwa-familias/src/pages/EscanearAsistente.jsx`). **Por qué importa y no es un adorno:** el GPS dice dónde está el teléfono, no quién lo tiene ni si entró a la casa; el escaneo cruzado es lo que convierte el registro de horas en algo que la Prestadora puede mostrarle a una Obra Social sin que se lo discutan. Y el relevo entre dos Asistentes es el momento donde hoy no queda rastro de nada: uno se va y el otro llega, y el sistema no sabe si hubo hueco. **Lo que hay que definir antes de construir** (son decisiones del Desarrollador, no técnicas): qué pasa si la Familia no está o no tiene el teléfono a mano —porque el turno no se puede bloquear por eso—, si el escaneo reemplaza al GPS o se suman los dos, y si el relevo entre Asistentes cierra una guardia y abre la otra en un solo acto. Va junto con una observación ya registrada de la revisión de pantallas del 2026-08-02: el botón de cierre se llama "Confirmar" y no dice lo que hace. **El 2026-08-06 se construyó la mitad que no dependía de esas tres respuestas: cerrar la guardia dejó de ser un efecto secundario del reporte y pasó a ser un acto con botón propio** (`POST /guardias/:id/checkout` en `backend/src/routes/appAsistentes.js`, botón y confirmación en `pwa-asistentes/src/pages/GuardiaActiva.jsx`, con cola sin conexión igual que el check-in). En el mismo cambio se le dio pantalla al candado de continuidad de guardia (`guardias.checkout_bloqueado`), que existía en la base desde `schema_modulo6_guardias_02.sql` y ninguna aplicación miraba: el Asistente veía "ocurrió un error" sin saber que lo que pasaba era que no podía retirarse sin relevo. **Falta el QR propiamente dicho**, que sigue esperando las tres respuestas. | 2026-08-03 | Claude Code (construye) + Desarrollador (contesta las tres preguntas de arriba) | El Asistente abre y cierra su guardia escaneando el código de la Familia o del Asistente saliente, el sistema guarda quién escaneó a quién y a qué hora, el check-out deja de ser un efecto secundario del reporte, y el Desarrollador lo prueba de punta a punta en las dos aplicaciones. | 🟡 A medias — el cierre con botón propio y el candado de continuidad quedaron hechos el 2026-08-06; el escaneo cruzado espera las tres decisiones del Desarrollador |
| 112 | **Hay cosas de la apariencia de las pantallas que al Desarrollador no le gustan, y quedaron para el final a propósito.** El 2026-08-02 avisó que prefiere retocar el aspecto recién cuando la funcionalidad esté completa, para no rehacer lo mismo dos veces. Queda anotado para que no se pierda y para que ninguna sesión futura interprete el silencio como conformidad. **Dos advertencias que van con esto:** (1) lo que es puro aspecto —colores, tamaños, espacios, tipografía— sale barato al final porque todo pasa por el sistema de diseño (regla 6), así que la decisión es correcta; (2) lo que es **acomodo** —dónde vive un botón, en qué orden se piden los datos, si una pantalla se parte en dos— se encarece con cada pantalla nueva que se apoya encima, así que si alguna molestia es de ese tipo conviene decirla antes y no al final. Mientras tanto, todo lo que el Desarrollador vaya viendo y no le guste se agrega como sub-punto de este pendiente, con la pantalla y qué le molesta. **El 2026-08-02 se hizo la comparación contra el software maduro del rubro y salieron doce observaciones, cada una marcada como aspecto o acomodo, numeradas para que el Desarrollador conteste por número.** **Las seis de aspecto quedan escritas acá, porque hasta el 2026-08-08 vivían solamente en un documento que se archivó** (`docs/Documentos Obsoletos/REVISION_PANTALLAS.md`, que ya no manda): (3) "Mis guardias" es una lista corrida, sin cortar por día y sin destacar la que viene (`pwa-asistentes/src/pages/MisGuardias.jsx`); (4) el domicilio es texto suelto y no abre el mapa del teléfono, aunque las coordenadas del Paciente ya le llegan a la pantalla (`pwa-asistentes/src/pages/GuardiaActiva.jsx`, `backend/src/routes/appAsistentes.js`); (6) la medicación aparece recién cuando se aprieta un botón, en vez de estar a la vista mientras se cuida; (7) las dos aplicaciones de teléfono nunca pasaron por su etapa de diseño —los estilos siguen escritos a mano pantalla por pantalla, y encima las dos tienen el mismo `src/styles/variables.css` copiado palabra por palabra, o sea dos copias del mismo archivo en vez de una sola (`CLAUDE.md` §7 regla 12)—; (9) la aplicación de la Familia abre en la lista "Mis pacientes" y no en cómo está el Paciente hoy (`pwa-familias/src/App.jsx`); (10) la lista de reportes dice fecha y nombre y nada más, cuando el reporte ya tiene guardados el ánimo, los incidentes y los signos vitales (`pwa-familias/src/pages/Reportes.jsx`). **Las de acomodo de esa misma comparación no van acá**, porque no esperan al final: la 1 y la 5 son los pendientes #133 y #135, la 8 es el #136, la 2 ya está adentro del #113 y la 11 adentro del #59. | 2026-08-02 | Desarrollador (junta la lista) + Claude Code (la aplica) | Terminada la funcionalidad, se hace una pasada completa de apariencia sobre la lista que juntó el Desarrollador, pantalla por pantalla, y él la da por buena. | 🔴 Abierto |
| 110 | **Los errores se muestran en pantalla tal como los escupe el navegador, en inglés.** El 2026-08-02 el Desarrollador mandó tres capturas seguidas del Panel con franjas rojas que decían `Failed to fetch`, `Could not find the table 'public.configuracion_habilitacion_via_medicacion' in the schema cache` e `infinite recursion detected in policy for relation "asistentes"`. Las tres causas de fondo ya están arregladas, pero el problema de forma queda: hay **158 lugares en 52 archivos** que hacen `setError(err.message)` o `setErrorForm(err.message)` y vuelcan el mensaje técnico crudo directo a la pantalla — entre otros las cinco pantallas de `panel/src/pages/configuracion/` (~20 veces en total; hasta el 2026-08-02 estaban todas juntas en `panel/src/pages/Configuracion.jsx`, que ese día se partió en cinco), `Alertas.jsx:75`, `Auditoria.jsx:35`, `MatriculasTab.jsx:327,397`, `NuevoAsistenteModal.jsx:48`, `AusenciasCoberturaTab.jsx:110` y `pwa-asistentes/src/components/Matricula.jsx:88`. Eso rompe dos reglas de `CLAUDE.md` §7: la 1 (nada de texto visible escrito fuera de las traducciones) y la 2 (todo texto en los tres idiomas). Una Coordinadora no tiene por qué leer inglés ni saber qué es un *schema cache*. Lo que corresponde es un punto único de traducción (regla 12): una función que reciba el error, lo clasifique —sin red, no existe la tabla, sin permiso, sesión vencida, cualquier otro— y devuelva la clave de traducción que va en pantalla, dejando el texto técnico solo en la consola para quien programa. | 2026-08-02 | Claude Code | Existe una única función que traduce errores a texto de las traducciones, los 158 lugares la usan, y ninguna pantalla del Panel ni de las dos PWA muestra un mensaje en inglés ni un nombre de tabla. | 🔴 Abierto |
| 109 | **¿Un Paciente de nivel III exige además un tipo de Asistente determinado?** Hoy el nivel de complejidad del Paciente (`pacientes.nivel_complejidad`, valores I, II y III) y el tipo de Asistente son dos cosas sueltas: el sistema controla que el Asistente tenga su Matrícula vigente, pero no controla que *ese* tipo de Asistente sea el que ese Paciente necesita. Un Paciente de nivel III podría quedar asignado a alguien cuyo tipo no alcanza para su cuadro, y nadie se entera. La idea se detectó armando la lista de candidatos para cubrir una guardia y se dejó afuera a propósito: es una regla de negocio nueva, no un detalle de esa pantalla. Si la respuesta es que sí, es configuración por Prestadora (nivel de Paciente → tipos de Asistente admitidos), nunca una tabla escrita en el código (regla 1 de `CLAUDE.md` §7). | 2026-08-02 | Desarrollador (decisión), Claude Code (implementación) | Quedó escrito si el nivel del Paciente condiciona o no el tipo de Asistente que lo puede atender. Si condiciona, existe la configuración por Prestadora y el aviso en el momento de asignar. Si no condiciona, queda escrito el porqué para no volver a plantearlo. Aparte: hoy no está escrito en ningún lado qué significa cada nivel de complejidad — existe la columna y sus tres valores permitidos, nada más. Contestar esto exige definir primero eso. | 🔴 Abierto |
| 108 | **Después del lanzamiento: revisar si algún módulo de Careonys tiene que subir a la Plataforma CeltaTech.** El 2026-07-24 se decidió, con el Desarrollador, no sacar módulos comunes todavía: *"más adelante, no ahora, con una salvedad"* — se construye desacoplado, y se extrae recién cuando aparece el **segundo consumidor concreto**, no antes (`docs/ARQUITECTURA_NIVELES.md:226-243`, en la raíz de CeltaTech). El 2026-08-01, al revisar material de arquitectura, volvió a aparecer la idea contraria: preguntarse *antes* de construir cualquier componente si sirve para otro producto y, si sirve, ponerlo directamente en la Plataforma. Las dos posturas son razonables y se contradicen: la primera evita construir abstracciones para un usuario imaginario; la segunda evita rehacer trabajo. El Desarrollador decidió no resolverlo ahora sino dejarlo para **la primera revisión después del lanzamiento del producto**, cuando ya se sepa qué se usa de verdad. Candidatos a mirar ese día: identidad y sesiones, el sistema de diseño del Panel, el motor de avisos, el registro de actividades, la facturación. **Nada de esto se toca hasta esa fecha** — sigue vigente la regla del segundo consumidor. | 2026-08-01 | Desarrollador (decisión), Claude Code (inventario previo) | Se hizo la revisión después del lanzamiento y quedó escrito, módulo por módulo, cuál sube a la Plataforma CeltaTech y cuál se queda en Careonys — con el motivo de cada uno. Si la conclusión es "ninguno todavía", también cuenta como cerrado, siempre que quede escrito por qué. | 🔴 Abierto — congelado a propósito hasta después del lanzamiento, no bloqueante |
| 107 | **¿Qué significa "verificar" una matrícula: mirarla, o comprobarla contra el registro oficial?** El 2026-08-01 se decidió la regla de negocio: un tipo de Asistente puede requerir matrícula (médico, enfermero, kinesiólogo…), y si la requiere, sin matrícula cargada, vigente **y verificada** ese Asistente no puede atender a ningún Paciente ni se le puede derivar ninguno. Lo que queda sin responder es el alcance legal de esa verificación, y son dos cosas muy distintas en trabajo y en responsabilidad: **(a)** alcanza con que alguien de la Prestadora abra el archivo, lo mire y confirme que el número, el nombre y la fecha de vencimiento coinciden; o **(b)** hay que comprobar el número contra el registro público del colegio u organismo que la emitió, entrando al sitio de ese organismo, uno por uno, con la firma de quién lo comprobó y cuándo. La diferencia importa por dos motivos: la (b) es mucho más trabajo para la Prestadora y cambia según la provincia y la profesión, y —lo más serio— si la ley exige la (b) y el sistema solo ofrece la (a), la Prestadora podría creer que está cubierta cuando no lo está. **Cómo se construye mientras tanto:** se hace la versión estricta —la matrícula se carga, después alguien la verifica, recién ahí se desbloquea— porque aflojar después es cambiar una configuración en un solo lugar, mientras que endurecer después obligaría a revisar de nuevo todas las matrículas ya cargadas. Y se guarda **cómo** se verificó cada una (quién, cuándo y por qué medio), no un simple tilde de sí/no: si la respuesta legal llega cuando ya hay cientos de Asistentes cargados, con el método guardado se sabe exactamente cuáles hay que volver a mirar; con un tilde, no se sabe ninguna. Palabras del Desarrollador: *"Una vez que la misma haya sido verificada (al menos que legalmente no nos afecte) recién se aprueba y desbloquea. Tema legal de estudio para los detalles finales del proyecto"*. | 2026-08-01 | Desarrollador (consulta legal), Claude Code (deja el sistema preparado para las dos respuestas) | Hay una respuesta escrita, por país y —si corresponde— por profesión, sobre si alcanza con mirar la matrícula o hay que comprobarla contra el registro oficial. La respuesta se agrega al documento legal de esa jurisdicción (`CLAUDE.md` §3) y se ajusta, si hace falta, la configuración de verificación. | 🔴 Abierto — no frena la construcción, sí frena habilitar Careonys a una Prestadora real de ese país |
| 104 | **El único documento que describe la página pública del producto describe la página equivocada.** Salió a la luz el 2026-07-31, cuando el Desarrollador preguntó qué va en la raíz `careonys.com` y la respuesta obligó a leer `docs/PRD_01_Sitio_Web.md`. Ese documento define un sitio con dos públicos: *"familias que buscan el servicio + Asistentes que quieren trabajar"* (`PRD_01_Sitio_Web.md:8`), con páginas `/solicita-servicio` y `/trabaja-con-nosotros`, ocho servicios de cuidado con precios, y el título de portada *"Cuida tus afectos"* (`PRD_01_Sitio_Web.md:37`). **Eso es el sitio de una Prestadora, no el de Careonys.** `CLAUDE.md` §1 dice textualmente que Careonys *"no presta servicios de cuidado"*: es el software que las Prestadoras licencian. Una página en `careonys.com` que invite a una familia a pedir un cuidador estaría vendiendo algo que la empresa no vende, y encima le haría competencia a sus propios clientes. El documento no está mal escrito — está viejo: es de cuando el producto era el sitio de una sola empresa de cuidados, antes de la separación CeltaTech/Careonys. Sobrevivió al barrido de documentación del 2026-07-13 (fila #27 de esta misma lista) porque aquel barrido buscaba el nombre de la prestadora original, no supuestos de negocio. **Estado medido el 2026-07-31:** el sitio público no existe en ningún lado — no hay carpeta suya en el repositorio (`ls` de la raíz: `backend`, `panel`, `pwa-asistentes`, `pwa-familias`, `scripts`, `supabase`, `docs`), no hay proyecto suyo en Cloudflare Pages (solo los tres de las aplicaciones) y `careonys.com` pelado no tiene ningún registro de DNS. O sea que no hay nada publicado que corregir: es un documento a reescribir antes de construir, no un incendio. **Consecuencia inmediata, ya aplicada:** la raíz `careonys.com` se deja libre en vez de redirigirla al Panel; ver fila #99 **CONFIRMADO EL 2026-08-01:** no hacía falta preguntarlo, la respuesta ya estaba en este mismo párrafo — la página de Careonys le vende **software de gestión a empresas de cuidado**, no servicios de cuidado a familias. El documento se reescribe con ese encuadre y el Desarrollador lo revisa cuando esté escrito, no antes. | 2026-07-31 | Claude Code (reescribir el documento; el encuadre ya está decidido) | `PRD_01_Sitio_Web.md` reescrito para el producto real —una página que le vende software de gestión a empresas de cuidado, no servicios de cuidado a familias— o bien retirado de los documentos vigentes y reemplazado por uno nuevo. Y decidido si el sitio de las Prestadoras (el que sí le habla a las familias) es una función del producto, algo que cada Prestadora se hace por su cuenta, o nada | 🔴 Abierto — encuadre confirmado el 2026-08-01, falta reescribir. No bloquea nada: el sitio público todavía no existe |
| 102 | **Los documentos legales de los 21 países no dicen ni una palabra sobre la ubicación de las personas, y el sistema está diseñado para seguirlas.** Comprobado el 2026-07-30 con una búsqueda sobre los 21 archivos de `docs/legal/` (Argentina, Bolivia, Brasil, Chile, Colombia, Costa Rica, Cuba, Ecuador, El Salvador, España, Guatemala, Honduras, México, Nicaragua, Panamá, Paraguay, Perú, Portugal, República Dominicana, Uruguay, Venezuela): **cero apariciones** de "ubicación", "geolocalización", "GPS" o "Ley 25.326". Mientras tanto el esquema ya tiene diseñada la tabla `guardias_tracking_gps`, que guarda el trayecto continuo de una persona. **Esto no es una omisión menor: es probablemente el dato más sensible que va a manejar el producto.** El propio esquema lo vio venir y dejó el freno escrito (`schema_modulo6_guardias.sql:406-410`): la tabla puede existir, pero **la función que la escribe no se activa contra Asistentes reales hasta que haya una política de retención definida** — cuánto tiempo se guarda cada punto del recorrido y cuándo se borra. Ese freno sigue vigente y **no se levanta sin el documento legal**. **DECISIÓN DEL DESARROLLADOR (2026-07-30): tiene que existir un consentimiento expreso del Asistente para el seguimiento, tomado en el momento de su contratación.** Es correcto y es obligatorio: la Ley 25.326 exige consentimiento **libre, expreso e informado y por escrito** para tratar datos personales, y la ubicación de una persona lo es. Hoy **no existe nada de eso en el sistema**: comprobado el 2026-07-30, no hay una sola tabla ni columna de consentimiento en todo `backend/src/db/`. Así que esto no es solo una cláusula de contrato, es **una función a construir**, y tiene requisitos que conviene fijar ahora para no rehacerla: **(1) Consentimiento separado, no una cláusula escondida en el contrato general.** Un "acepto los términos" que mezcla veinte cosas no vale como consentimiento específico para ubicación; tiene que ser su propio acto, con su propia pantalla. **(2) Específico y detallado**: qué se registra (el trayecto hacia la guardia, no la vida entera), cuándo empieza y cuándo se corta (idealmente al llegar), quién lo ve (la Prestadora; la Familia solo la hora estimada), y cuánto tiempo se guarda — el dato de retención del que habla el freno del esquema. **(3) Con versión y constancia**: se guarda **qué texto exacto firmó y en qué fecha**, porque el texto va a cambiar y dentro de dos años hay que poder mostrar el que esa persona leyó, no el actual. Versionado, igual que la tabla configurable de advertencias de `CLAUDE.md` §3. **(4) Con un camino para retirarlo**, porque la ley lo exige. **(5) Retroactivo**: los Asistentes que ya estén incorporados cuando esto exista también tienen que pasar por la pantalla; no se los puede dar por consentidos. **(6) El texto lo redacta un abogado por jurisdicción, no Claude Code** — y es configurable por país, no escrito en el código. **PERO — y esto hay que decirlo antes de construir, no después:** tomar el consentimiento **en el momento de la contratación** es justamente el punto donde más se lo cuestiona, porque quien está por ser contratado no está en condiciones de decir que no. Un consentimiento que no se puede negar sin perder el trabajo **no es libre**, y si no es libre no es válido — con lo cual el papel firmado puede terminar dando **menos** protección de la que aparenta. **Esto no lo invalida como decisión: hay que tomarlo igual, porque sin consentimiento la situación es peor.** Lo que hay que hacer es no apoyarse **solo** en él: el respaldo fuerte es que el seguimiento tenga una finalidad concreta y proporcionada —saber si la guardia se va a cubrir, ver #101— y que esté limitado a eso. Se le pregunta al abogado, no se decide acá. Hay además una diferencia que el documento legal tiene que distinguir: **un Asistente de la modalidad marketplace no está siendo contratado como dependiente**, así que su consentimiento no arrastra este problema del mismo modo. Y de la misma familia es la otra pregunta abierta: **si a alguien se lo sanciona por retirar el permiso de ubicación, ¿ese permiso seguía siendo válido?** El consentimiento para datos personales tiene que poder retirarse libremente; si retirarlo trae castigo, deja de ser libre, y entonces lo que queda en falso es el consentimiento original — o sea toda la función, no solo la sanción. **Esto hay que consultarlo con un profesional, no resolverlo en una sesión de trabajo:** Claude Code lo señala como riesgo y no da la respuesta. Se cruza además con el riesgo del artículo 23 de la LCT que la regla `CLAUDE.md` §3 ya contempla, porque seguir a alguien mientras viaja, fuera del lugar de trabajo, es de los indicios más fuertes de relación de dependencia. **Regla del proyecto que lo vuelve bloqueante:** `CLAUDE.md` §3 exige que exista el documento legal de una jurisdicción **antes** de dar de alta la primera Prestadora real de ese país. Hoy los documentos existen pero no cubren esto, así que la función de seguimiento no está cubierta en ningún país. Ver pendiente #101, que es el diseño de la función | 2026-07-30 | Desarrollador (conseguir la respuesta profesional por jurisdicción y decidir la política de retención) / Claude Code (escribirla en los documentos legales y en la tabla de advertencias configurable) | Los documentos legales de al menos las jurisdicciones donde vaya a haber una Prestadora real tratan explícitamente: (a) qué se puede guardar del recorrido de un Asistente y por cuánto tiempo; (b) qué consentimiento hace falta y cómo se retira; (c) si retirar ese consentimiento puede tener consecuencias y cuáles; (d) el texto de advertencia que ve la Prestadora al activar la función, cargado en la tabla configurable de advertencias como manda `CLAUDE.md` §3; (e) el texto del consentimiento que firma el Asistente, distinto para dependiente y para marketplace. Definida la política de retención de `guardias_tracking_gps` para poder levantar el freno del esquema. Y construido el registro de consentimiento —pantalla propia dentro del Proceso de Incorporación de Asistentes, texto versionado y configurable por jurisdicción, constancia de qué versión firmó cada uno y cuándo, camino para retirarlo, y barrido retroactivo de los ya incorporados—, con la regla dura de que **sin consentimiento registrado el seguimiento no se enciende para esa persona** | 🟡 En curso — **el 2026-07-30 se construyó el registro de consentimiento completo (tarea #43), con textos de relleno**. Lo que existe hoy, comprobado contra la base local y no supuesto: la migración `20260730220000_consentimiento_seguimiento_asistente.sql` crea el catálogo `textos_consentimiento` (que escribe solo el Superadmin, o sea CeltaTech, y lee cualquier persona identificada) y el libro de constancias `consentimientos_asistente`, que guarda **la copia del texto que la persona leyó**, no un enlace al catálogo — así, si mañana el texto cambia, la constancia vieja sigue diciendo lo que decía. La tabla no acepta escrituras desde las aplicaciones **a propósito**: solo escribe el backend, porque es el único que puede garantizar que la copia guardada sea la del texto realmente mostrado. Una sola función, `consentimiento_seguimiento_vigente()`, decide si se puede registrar la ubicación de alguien (`CLAUDE.md` §7.12: un solo lugar decide, no la misma condición repetida). Devuelve verdadero **solo si** hay una decisión viva, es un sí, el texto que firmó sigue vigente y **no es un borrador**. **Ese último requisito es el freno que hace que esto no se pueda encender por accidente:** los seis textos sembrados (Argentina, dependiente y autónomo, en los tres idiomas) son de relleno y están marcados como borrador, así que la pantalla funciona entera pero el seguimiento no se enciende para nadie. **El interruptor real es cargar el texto del abogado con la marca de borrador apagada — o sea, la tarea #42 tiene que pasar antes.** Del lado de la aplicación: rutas `GET`/`POST`/`POST retirar` bajo `/api/app-asistentes/consentimientos`, la pantalla dentro de Mi Perfil (para que se pueda volver a mirar, no un trámite de una vez) y un aviso no bloqueante en Mis Guardias. **La pantalla no traba el uso de la aplicación y los dos botones pesan visualmente lo mismo**, por lo que dice esta misma ficha más arriba: un consentimiento que hay que dar para poder trabajar no es libre. El punto (5) de esta ficha, el barrido retroactivo, **quedó resuelto sin script**: como la decisión se evalúa en vivo, un Asistente ya incorporado simplemente no tiene constancia, y sin constancia no hay seguimiento y la pantalla le aparece. **Falta lo que no puede hacer una sesión de trabajo:** el texto del abogado por jurisdicción, la política de retención, y las respuestas a las dos preguntas abiertas (si el consentimiento tomado al contratar es libre, y si sancionar a quien lo retira lo invalida). Sigue bloqueando activar el seguimiento de trayecto con Asistentes reales en cualquier país |
| 101 | **Formato de las aplicaciones (web instalable o de tienda) y diseño del seguimiento del trayecto: conversación completa del 2026-07-30, escrita para no rehacerla.** Empezó con una pregunta del Desarrollador sobre las diferencias entre una aplicación web instalable (PWA) y una de tienda, y terminó definiendo cómo debería funcionar el aviso de demora. **(A) Aclaración de palabras:** una PWA **también se instala** —queda con su ícono, se abre a pantalla completa, anda sin internet—; la diferencia no es "instalable o no" sino **de dónde sale**: la PWA desde el navegador, la otra desde la tienda. **(B) Diferencias que importan de verdad.** En Android las notificaciones funcionan igual en las dos. **En iPhone una PWA solo recibe notificaciones si la persona la agregó a la pantalla de inicio**, y ese gesto está escondido en el menú de compartir: ese es el problema real de las PWA, y no es desconfianza sino desconocimiento. La PWA se actualiza al instante y bajo control propio; la de tienda pasa por revisión y depende de que el usuario actualice, y Apple puede rechazarla. **(C) ¿La gente desconfía de una PWA?** No: ni siquiera sabe que la categoría existe. El problema es que si no está en la tienda, no la encuentra. **Pero en Careonys eso casi no aplica**, y es una ventaja del modelo: nadie va a buscar el producto en la tienda, la Familia entra porque **su Prestadora le manda el enlace** y el Asistente porque la Prestadora le dice que entre — no hay descubrimiento, hay indicación, y la confianza viene de la agencia con la que ya firmaron, no de la tienda. Matiz honesto: una persona mayor suele estar más cómoda con "buscalo en la tienda" que con instalar desde el navegador. **(D) Qué hacen los maduros del rubro** (mismo criterio de contraste que pidió el Desarrollador para el pendiente #95): AlayaCare y WellSky publican **aplicaciones nativas en las tiendas**, con la marca del software. **(E) Cambiar de formato después es posible y barato, y esto es lo importante: lo hecho no se tira.** A Android se empaqueta la misma PWA y se publica, con un mecanismo que Google apoya oficialmente; a iPhone hay que envolverla con una herramienta, sigue siendo el mismo código adentro, pero Apple rechaza lo que sea "solo un sitio web empaquetado" y hay que darle algo propio del teléfono. **(F) REQUISITO DEL DESARROLLADOR (2026-07-30): cuando se pase a tienda, las aplicaciones web instaladas tienen que seguir funcionando y actualizándose — no se puede dejar sin cobertura a quien ya la esté usando.** Queda resuelto por el propio camino elegido: **la app de tienda no es otra aplicación, es la misma envuelta**, un solo código adentro, así que las dos se actualizan a la vez y no pueden quedar desfasadas. **(G) DECISIÓN DEL DESARROLLADOR (2026-07-30): el instructivo de instalación y uso se hará al final de todo, con videos tutoriales elaborados con IA.** **(H) CORRECCIÓN DE CLAUDE ESE MISMO DÍA.** Claude midió el código y dijo que la ubicación que usan las PWA es solo con la aplicación abierta (`pwa-asistentes/src/pages/GuardiaActiva.jsx:18` y `ReporteDiario.jsx:47`, los dos con `getCurrentPosition`, o sea una lectura puntual) y concluyó que una PWA alcanzaba. **Era cierto para el código de hoy pero no para lo planeado:** el Desarrollador recordó que al Asistente se le hace seguimiento **en el camino a tomar la guardia**. Eso es otra cosa: la persona viaja con el teléfono en el bolsillo y la pantalla apagada, y ahí **una PWA no funciona** —en iPhone directamente no existe la posibilidad, en Android tampoco de forma confiable—. **Conclusión: el seguimiento en camino, por sí solo, obliga a app de tienda para la aplicación de Asistentes.** Solo para esa: la de Familias no necesita nada de esto y puede quedarse como PWA para siempre. **(I) Qué se puede distinguir cuando se corta el registro.** Dato que cambia el análisis: **el GPS no necesita internet** —solo recibe, como una radio—, así que quedarse sin datos **no borra el trayecto**: se sigue anotando en el teléfono y se sube después. Del resto, el sistema operativo devuelve **mensajes distintos** para "el usuario denegó el permiso" y para "no pude ubicarte", así que **quitar el permiso o apagar el GPS se detecta con certeza total, con hora exacta, y no se puede disfrazar de túnel**. Cerrar la aplicación a la fuerza y apagar el teléfono se detectan, pero no dicen por qué: **eso lo resuelve guardar el nivel de batería junto a cada punto** —si el último registro fue con 4% se quedó sin batería, si fue con 78% lo apagó; no es prueba, es un indicio fuerte y no cuesta nada—. Lo que **no** se puede distinguir es alguien que deja el teléfono en la casa o lo mete en una mochila metálica: eso es idéntico a un túnel. **(J) Principio de diseño que salió de acá: que cada hueco tenga un motivo anotado, no que sea un hueco a secas**, y que **el sistema anote hechos, no veredictos** — "permiso de ubicación revocado a las 14:19, batería 78%", nunca "evadió el seguimiento". Lo primero es un dato comprobable; lo segundo es una acusación escrita por un programa que algún día va a leer un juez. La conclusión la saca el Coordinador, que es una persona y se hace cargo. **(K) LA VUELTA MÁS IMPORTANTE, planteada por el Desarrollador: registrar el acto deliberado de la Asistente, no solo detectar al que se escapa.** Hay que distinguir tres registros distintos: **el acto suyo** (apretó "salgo ahora"; avisó "voy demorado y por qué") que vale como **mérito**; **la cuenta que hace la máquina** (calcula que no llega y avisa sola) que es dato pero no mérito; y **la ausencia de los dos**, que también es un registro. Aunque la Familia se entere igual en los dos primeros casos, "avisó" y "el sistema se dio cuenta" **no son lo mismo** a la hora de evaluar a alguien. **Esto da vuelta el sentido del seguimiento: pasa de ser vigilancia sobre la Asistente a ser su coartada.** Si el colectivo se rompió y ella avisó, ese registro la protege de una sanción. No es un detalle simpático: es la diferencia entre que adopten la aplicación o no, porque nadie usa con ganas algo que lo vigila y todo el mundo usa algo que lo respalda cuando sale mal. Y baja el riesgo legal, porque **avisar es un acto voluntario, y un acto voluntario es autonomía, no subordinación**. **(L) Qué ve la Familia: "María viene en camino, llega alrededor de las 14:45" — NO el mapa con María moviéndose.** La Familia necesita saber si llega, no dónde está una trabajadora en este momento; mostrarle el punto en vivo es dar la ubicación en tiempo real de una persona a un tercero. **(M) "¿Y si se hace la boluda y anula la ubicación?"** Ese caso **es el que sí se detecta con certeza**, porque quitar el permiso deja un mensaje con nombre propio y hora. Lo que va a pasar es que dirá "no sabía, se desconfiguró solo". **Contra eso no se pelea probando la intención, se pelea sacándole la excusa:** la aplicación tiene que avisarle **en el momento** con un cartel imposible de no ver que el seguimiento está apagado, y **guardar que se lo mostró** ("aviso mostrado a las 14:19, no reactivado"). Con eso no se prueba que lo hizo aposta, se prueba que se le dijo y no hizo nada, y alcanza. **(N) Y la jugada que lo resuelve de raíz: si el proceso se para sobre EL ACTO y no sobre la posición, apagar el GPS no le sirve de nada** — lo que falta no es el punto en el mapa, falta el botón apretado, y de apretar un botón nadie se hace el desentendido. Se termina la discusión sobre interpretar huecos. **(O) OBJETIVO QUE MANDA, fijado por el Desarrollador (2026-07-30): "si hay mala intención de parte de la Asistente la Prestadora debe saberlo cuanto antes para tomar las medidas necesarias para cubrir esa guardia".** O sea que **el fin es cubrir la guardia, no armar un expediente**, y **la medida del sistema es cuántos minutos de anticipación da, no cuán sólida queda la prueba**: enterarse a las 14:19 con la guardia empezando a las 15:00 sirve; enterarse el martes siguiente con la prueba perfecta no sirve para nada. Todo lo demás se subordina a esto. **(P) Consecuencia que conviene mirar antes de comprometerse:** si el diseño se apoya en "salgo ahora" + "aviso si me demoro" + el cálculo automático de llegada, **puede que el rastreo continuo no haga falta**, o quede opcional para casos puntuales — y si no hace falta, se caen solos el freno de retención de datos y el indicio de subordinación (pendiente #102), y la aplicación de Asistentes podría volver a caber en una PWA. **No está decidido**: puede haber razones para querer el trayecto igual, y la decisión es del Desarrollador. **(Q) Lo que ya está construido y sirve de base:** `guardias.salida_checkin_at` / `salida_lat` / `salida_lng` / `medio_transporte` (`schema_modulo6_guardias.sql:102-106`, el "checkpoint de salida, previo al trayecto", descrito como infraestructura preventiva del protocolo de no-show); la tabla `guardias_tracking_gps` con su freno explícito; y `configuracion_escalada_relevo` con `minutos_demora` y `orden_prioridad` configurables por Prestadora (`schema_modulo6_guardias.sql:318-328`), todavía sin valores cargados a la espera de que el Desarrollador los defina. **CORRECCIÓN DEL ENCUADRE (2026-08-01):** quedaba anotado como pendiente del Desarrollador *"definir los minutos de demora y el orden de escalada"*, y eso estaba mal planteado. Cuántos minutos de atraso convierten una llegada tarde en un aviso **no es un número que se decida una vez para todo el producto**: es una regla de operación de cada Prestadora, y la regla 1 de `CLAUDE.md` §7 prohíbe justamente escribir ese tipo de reglas en el código. La tabla `configuracion_escalada_relevo` ya está hecha para eso —`minutos_demora` y `orden_prioridad` son por Prestadora—, así que lo que falta no es una decisión sino dos cosas construibles: **un valor de arranque razonable** para que el producto funcione desde el primer día, y **la pantalla en Configuración** donde cada Prestadora lo ajusta. **Y sobre el paso a tienda:** tampoco es una fecha a elegir de antemano, así que deja de estar anotado como pregunta. Es una condición: **se pasa a tienda cuando haya Prestadoras reales y aparezcan Asistentes con iPhone que no estén recibiendo los avisos.** Ese es el único problema concreto que resuelve publicar en la tienda; mientras no aparezca, la aplicación web instalable alcanza. Lo construido hasta ese día no se tira: se empaqueta el mismo código. | 2026-07-30 | Claude Code (implementación, con la demora y la escalada configurables por Prestadora). El paso a tienda ya no espera decisión de nadie: se dispara cuando se cumpla la condición escrita arriba | Definido y aprobado el diseño del aviso de demora centrado en el acto deliberado, con: botón de salida y de aviso de demora en la aplicación de Asistentes; cálculo automático de llegada estimada; aviso a la Familia con hora estimada y **sin** mapa en vivo; motivo del corte y nivel de batería guardados en cada interrupción del registro; cartel visible cuando el seguimiento está apagado, con constancia de que se mostró; y todos los textos redactados como hechos, no como veredictos. Y decidido, con fundamento escrito, si el rastreo continuo queda dentro o fuera. **Nada de esto se activa con Asistentes reales hasta cerrar el pendiente #102** | 🔴 Abierto — diseño conversado y escrito, implementación sin empezar. Bloqueado por #102 para la parte de rastreo continuo; la parte de actos deliberados no está bloqueada |
| 100 | **Los dominios comprados no llevan a ninguna parte, y el producto no tiene ni una casilla de correo propia.** Mapa real del 2026-07-29, medido y no supuesto. **Lo que hay en Cloudflare:** `celtatech.com` y `careonys.com`, los dos activos y en plan gratis. `celtatech.com` **ya tiene correo andando** por Enrutamiento de Correo de Cloudflare (comprobado: sus registros MX apuntan a `route1`, `route2` y `route3.mx.cloudflare.net`), y las casillas de la empresa se reenvían a un Gmail de CeltaTech. `careonys.com` **no tiene correo** y —esto vale para los dos— **no tiene ninguna página web**: hoy no responde nada, porque los despliegues siguen en las direcciones prestadas de Vercel (ver pendiente #99). **Lo que hay en NIC Argentina** (el registro oficial de los dominios `.ar`, comprobado con la pantalla del Desarrollador el 2026-07-29): `careonys.com.ar`, `celtatech.com.ar` y `celtatech.net.ar`, los tres **"Registrado"** pero los tres **"Delegado: NO"** — o sea comprados y pagos, pero sin decirle a nadie a qué servidores de nombres preguntar, así que no llevan a ningún lado. Alta 27 y 28 de julio de 2026, **vencimiento 27 y 28 de julio de 2027** (NIC.ar no renueva solo: si no se paga, se pierde). En esa misma cuenta hay otros tres dominios —`plmsystems.com.ar`, `dezamigrante.com.ar`, `galiciamigrante.com.ar`— que son **ajenos a este proyecto y no se tocan**. **Lo que NO existe** (comprobado el 2026-07-29 contra el registro mundial de `.net`, que respondió "no encontrado" en los dos casos): `careonys.net` y `celtatech.net` **no están registrados por nadie**, ni por el Desarrollador ni por un tercero. `careonys.net.ar` tampoco existe. Cualquier documento que los dé por comprados está equivocado. **Lo que hay que hacer, en este orden. (1) Los tres `.ar` no son sitios aparte, son atajos**: la idea del Desarrollador —que quien escriba cualquiera de ellos termine en el sitio bueno— es exactamente lo correcto y no implica mantener tres sitios; se resuelve con una **regla de redirección** en Cloudflare, que es un cartel de "mudamos a la vuelta". Destinos: `careonys.com.ar` → `careonys.com`; `celtatech.com.ar` y `celtatech.net.ar` → `celtatech.com`. Para que esa regla exista, cada dominio `.ar` tiene que estar **primero dado de alta en Cloudflare** (gratis), porque es Cloudflare quien va a contestar por él; recién entonces se lo delega en NIC.ar con el botón **DELEGAR**, cargando los dos servidores de nombres que Cloudflare indique para ese dominio (hoy la cuenta usa `anna.ns.cloudflare.com` y `otto.ns.cloudflare.com`, pero se confirma en pantalla, no se asume). **El paso de NIC.ar es del Desarrollador y no hay forma de evitarlo**: NIC.ar exige entrar con Clave Fiscal de AFIP y no ofrece ninguna interfaz para programas, así que la sesión de trabajo no puede hacerlo ni debe intentar entrar con credenciales suyas. La delegación tarda hasta unas horas en propagarse. **(2) El correo del producto: DECIDIDO EL 2026-07-30 QUE NO SE CONFIGURA NADA TODAVÍA.** Se estuvo a punto de armar un reenvío en `careonys.com` y el Desarrollador lo frenó con un argumento que es correcto y que conviene dejar escrito, porque es la regla que decide todos los casos que vengan: **hay dos mundos de correo y no se mezclan.** Uno es el **técnico o de adentro** — la casilla `cuentas.celtatech@gmail.com`, que es la identidad con la que están dadas de alta las cuentas de los servicios (Supabase, GitHub, Railway, Cloudflare). Esa casilla es la llave maestra de toda la infraestructura: ahí caen los correos de recuperación de contraseña de todo. El otro es el **público o de afuera** — `info@`, `ventas@`, atención al cliente. **Mandar lo público al buzón técnico es un error de seguridad, no una comodidad**: lo llena de ruido y convierte en blanco conocido la única casilla que nunca debería serlo. Por eso quedó **descartada** la idea de un "atrapar todo" (*catch-all*) apuntando ahí, que era lo que esta sesión había propuesto. Consecuencia práctica: **`careonys.com` se queda sin ninguna configuración de correo por ahora**, y el día que haya algo público que atender se define con una casilla propia, distinta de la técnica. **Lo único concreto que queda sin resolver mientras tanto**, y es chico: `identidadProducto.js` trae `emailSoporte: 'soporte@careonys.com'`, que **no aparece en ninguna pantalla ni texto de las tres aplicaciones** (comprobado) y tiene un solo consumidor real, `backend/src/utils/push.js`, donde se usa como dirección de contacto del servicio de notificaciones push — o sea, un aviso de máquina a operador, del mundo de adentro, no un canal de atención. Hoy esa dirección no existe, así que ese aviso se perdería. Es un riesgo bajo (ese servicio prácticamente nunca escribe) pero está mal encuadrado: usa un nombre que promete atención al cliente para algo que es infraestructura. **Al resolverlo, decidir si esa dirección técnica debe llevar el dominio del producto o el de la empresa** — por el criterio de arriba, parece que corresponde el de la empresa. **Dos límites a tener presentes cuando se retome:** el reenvío de Cloudflare **solo recibe, no envía**, así que *responder* desde una dirección del producto necesita otra cosa, deliberadamente postergada en `docs/DECISION_EMAIL_ESCALA.md`; y los correos que hoy manda el backend (activaciones, avisos) salen por el SMTP de variables de entorno y nada de esto los cambia. **Dato del estado actual, comprobado el 2026-07-30**: en Cloudflare, `careonys.com` tiene el enrutamiento **Desactivado**, 0 reglas y sin registros técnicos cargados; las direcciones de destino son de cuenta y se comparten entre dominios, y ahí `cuentas.celtatech@gmail.com` ya figura **Verificada**, mientras que `cuentas@celtatech.com` figura **Pendiente** — esta última conviene revisarla algún día, porque poner una dirección de `celtatech.com` como destino, cuando ese mismo dominio reenvía por Cloudflare, puede armar un circuito cerrado | 2026-07-29 | Desarrollador (el alta de los dominios en Cloudflare, los tres DELEGAR de NIC.ar —requieren Clave Fiscal de AFIP— y el clic de confirmación del reenvío) / Claude Code (las reglas de redirección y todo lo que se pueda hacer por comando, más la comprobación final) | Los tres dominios `.ar` delegados a Cloudflare y comprobado desde afuera que escribir cualquiera de ellos termina en el `.com` que corresponde; anotadas las fechas de vencimiento de julio de 2027 donde el Desarrollador las vea a tiempo; y resuelto el encuadre de `emailSoporte` — o bien apuntado a una dirección técnica de la empresa, o bien con una casilla propia del producto que **no** sea la técnica. La parte de correo público (`info@`, `ventas@`, atención) queda explícitamente fuera hasta que haya una Prestadora real | 🟡 En curso — **la parte del Desarrollador está hecha el 2026-07-29**: los tres dominios `.ar` dados de alta en Cloudflare (plan gratis) y los tres delegados en NIC.ar. Dato práctico que sirve para la próxima: **los tres usan el mismo par de servidores de nombres, `kenia.ns.cloudflare.com` y `rommy.ns.cloudflare.com`** — que **no** es el mismo par que usa `careonys.com` (ese usa `anna`/`otto`). Cloudflare asigna el par por dominio y hay que mirarlo en pantalla, no darlo por sabido: acá se dio por sabido una vez y estaba mal. Se cargaron sin IPv4 ni IPv6, que es lo correcto para servidores de nombres de otro dominio. **Falta solo que NIC.ar publique**, cosa que no es instantánea. Comprobado el 2026-07-29 preguntándole directo a los servidores del registro (`c.dns.ar`, `d.dns.ar`, `e.dns.ar`): los tres responden todavía "no existe". En Cloudflare los tres se ven con el cartel rojo "Servidores de nombres no válidos", que es **lo esperado** mientras el registro no haya publicado — se pone en verde solo, no hay que arreglar nada. Cuando eso pase, lo que queda son las reglas de redirección y el reenvío de correo, y ninguna de las dos cosas necesita al Desarrollador si existe un token de API de Cloudflare. Sigue dependiendo en parte del pendiente #99: mientras los `.com` no tengan sitio, la redirección de los `.ar` llega a una página vacía |
| 99 | **Los tres despliegues de Careonys viven en la cuenta personal de Vercel, no en una cuenta de la empresa.** Detectado por el Desarrollador el 2026-07-28, al ver que el comando de despliegue se identificaba como `betosp`. Es el mismo caso que ya se resolvió con GitHub (repositorios mudados a la cuenta `CeltaTech`) y con Supabase (base mudada a la organización "Celta Tech"), pero acá todavía no se hizo. Estado comprobado ese día con `vercel teams ls` y `vercel project ls`: **existe una sola cuenta**, "betosp's projects", en plan **Hobby**, con **11 proyectos**, de los cuales 3 son de Careonys (`aurevia-panel`, `pwa-familias`, `pwa-asistentes`) y los otros 8 son personales y de cursada (`galicia-migrante`, trabajos de Talento Tech, etc.). **No existe ningún equipo de CeltaTech.** Dos problemas distintos: (a) **de titularidad** — los despliegues del producto son de una persona, no de la empresa, mezclados con trabajos de la facultad; (b) **de condiciones de uso** — el plan Hobby de Vercel es para uso personal y no comercial, y trae un solo asiento, así que en cuanto haya una Prestadora real pagando, el plan deja de corresponder. **El costo es el freno:** mudar a un equipo de CeltaTech exige plan **Pro**, USD 20 por usuario por mes (consultado en `vercel.com/pricing` el 2026-07-28), que incluye USD 20 de crédito de uso. Es gasto del Desarrollador, igual que el pendiente #15. **Pero el costo no es un freno para empezar, y esto corrige la recomendación original de esta ficha** (lo planteó el Desarrollador el 2026-07-28, y tiene razón): se puede abrir **una segunda cuenta Hobby gratuita dedicada solo a Careonys**, separada de los 8 proyectos personales y de cursada, y **pasar esa misma cuenta de Hobby a Pro más adelante es un cambio de plan, no una segunda mudanza** — no hay que volver a mover proyectos, ni recargar variables de entorno, ni tocar dominios. O sea que la cuenta nueva no es trabajo duplicado sino el paso 1. Lo que **no** resuelve es el problema (b): el plan Hobby sigue siendo para uso personal y no comercial aunque esté a nombre de un mail de la empresa. Y hay una diferencia con lo que se hizo en GitHub y Supabase que conviene no confundir: allá se crearon **organizaciones** reales, que admiten varias personas gratis; **Vercel gratis no tiene organizaciones** — una cuenta Hobby es de una sola persona y no permite invitar a nadie, así que lo que se crea es una segunda cuenta personal con el mail de la empresa, no un equipo. Compartir esa contraseña no es una opción; para varias personas hay que pagar Pro igual. **Al crearla, entrar con "Continue with GitHub" usando la cuenta `CeltaTech`**, no con mail y contraseña: así no hay credencial nueva que guardar en la caja fuerte ni una segunda verificación en dos pasos que configurar (queda colgada de la de GitHub, pendiente `X-14`), y la cuenta queda atada desde el arranque a la organización dueña de los repositorios. **Confirmado el 2026-07-28 con `vercel project --help`: la línea de comandos no tiene comando para transferir un proyecto entre cuentas** — eso solo se hace desde la web de Vercel. Sí existen `vercel env pull` y `vercel env add`, así que el camino que sí se puede automatizar entero es recrear los 3 proyectos en la cuenta nueva y copiarles las variables por script. Ojo con un detalle: las direcciones gratis actuales (`aurevia-panel.vercel.app` y las dos PWA) quedan reservadas por los proyectos viejos, así que para reusar esos nombres habría que borrar los viejos primero — problema que desaparece solo si se hace junto con el cambio de dominio. **Conviene hacerlo junto con el cambio de nombre a `careonys.com`**, porque los tres proyectos se van a renombrar de todos modos y así no se toca dos veces. Nada está roto hoy: los tres despliegues funcionan y no hay riesgo de pérdida — Vercel permite transferir proyectos entre cuentas conservando el historial. **CAMBIO DE RUMBO 2026-07-29: no se crea ninguna cuenta en Vercel. Las tres aplicaciones se van a Cloudflare Pages y la cuenta personal de Vercel queda libre para uso particular del Desarrollador**, que es lo que él pidió expresamente. Lo disparó una pregunta suya que la ficha original no había contemplado: si CeltaTech va a tener más productos, ¿cuánto escala Vercel? Lo consultado ese día en la documentación de Vercel: **Pro cobra por asiento que despliega, no por proyecto** (USD 20/mes de tarifa de plataforma, 1 asiento incluido, USD 20 de crédito de uso, proyectos **ilimitados**), así que el techo por cantidad de productos no existe; pero **cada cuenta admite un solo equipo en el plan gratis**, o sea que "un espacio gratis por producto" obliga a **una cuenta de acceso distinta por producto**, con su mail y su contraseña, sin vista conjunta y sin poder invitar a nadie. Sumado a que el plan gratis sigue siendo explícitamente no comercial, la idea de la cuenta Hobby dedicada dejó de tener sentido. **Por qué Cloudflare Pages y no seguir en Vercel:** (a) lo que se despliega es **estático puro** — comprobado el 2026-07-29: 2,0 MB el Panel, 1,0 MB pwa-familias, 0,6 MB pwa-asistentes, **cero carpetas `api/`**, y el único `vercel.json` es una regla de reescritura de una línea para que el SPA no dé 404; el backend no está en Vercel (no tiene `.vercel/project.json`), está en Railway y ahí se queda, con la IA adentro; (b) **la cuenta de Cloudflare ya existe y ya tiene los dos dominios**, así que el camino Cloudflare **no le pide al Desarrollador crear ninguna cuenta**, mientras que el camino Vercel sí; (c) **USD 0** contra USD 20/mes, sin restricción de uso comercial; (d) el trabajo de armar la casa nueva (crear proyectos, copiar variables, enganchar dominios) hay que hacerlo igual, así que hacerlo una sola vez en Cloudflare evita repetirlo. Límites del plan gratis de Cloudflare Pages consultados el 2026-07-29: 100 proyectos por cuenta, 500 compilaciones por mes, 1 compilación a la vez, 20.000 archivos y 25 MiB por archivo, 100 dominios propios por proyecto — todo holgadísimo para estas tres aplicaciones. **Lo que hay que probar antes de mudar** (pendiente #37 de la lista de tareas): que el equivalente del `rewrites` de `vercel.json` funcione escrito como `_redirects` y que las dos PWA se sigan instalando bien (manifiesto y service worker). **Orden obligatorio al final, para no romper nada**: mudar → comprobar → repuntar `PWA_FAMILIAS_URL` y `PWA_ASISTENTES_URL` en Railway a las direcciones de `careonys.com` → comprobar que un correo de activación llega con el enlace nuevo → **recién entonces** borrar los proyectos de Vercel, porque al borrarlos se liberan `aurevia-panel.vercel.app`, `pwa-familias.vercel.app` y `pwa-asistentes.vercel.app`, que es a donde apuntan hoy los enlaces de esos correos. Verificado el 2026-07-29 que las tres direcciones responden 200 y ya sirven el nombre nuevo | 2026-07-28 (rumbo cambiado el 2026-07-29) | Claude Code (hace la mudanza entera) / Desarrollador (solo si hiciera falta un token de Cloudflare) | Las 3 aplicaciones sirviéndose desde Cloudflare Pages en la cuenta de Cloudflare de la empresa, con los dominios de `careonys.com` enganchados; `PWA_FAMILIAS_URL` y `PWA_ASISTENTES_URL` de Railway apuntando ahí y comprobado con un correo de activación real; los 3 proyectos borrados de la cuenta personal de Vercel y esa cuenta sin nada de la empresa; y comprobado en vivo que el Panel sigue llegando al backend | 🟡 En curso — **el 2026-07-30 las tres aplicaciones ya están arriba en Cloudflare Pages y funcionando.** El Desarrollador creó el token de API acotado (permisos: Cuenta→Cloudflare Pages→Editar y Zona→DNS→Editar, sin filtro de IP ni vencimiento), guardado en la caja fuerte del panel. Se subieron con `wrangler pages deploy`, sin intervención suya. Direcciones provisorias: `careonys-panel.pages.dev`, `careonys-familias.pages.dev`, `careonys-asistentes.pages.dev`. **Comprobado en vivo, no supuesto:** las tres devuelven 200 en la portada; una dirección inventada devuelve 200 con el `index.html` (o sea que el `_redirects` reemplaza bien al `rewrites` de `vercel.json` y no se rompe al recargar una pantalla interna); en pwa-familias el manifiesto y el `sw.js` se sirven con el tipo de contenido correcto, el código y los estilos cargan, se ve la pantalla de ingreso con la marca nueva y la consola del navegador no tira **ningún** error. **Dato encontrado de paso:** el "ID de cuenta" de Cloudflare anotado con las claves de R2 de Aurevia apunta a una cuenta que devuelve 403; el bueno es el que figura en las zonas (tarea #40). **Corrección del 2026-07-31: ese ID no estaba mal.** Hay **dos cuentas de Cloudflare distintas**, y cada una tiene su propio identificador: una guarda los respaldos de R2 y la otra los dominios y las tres aplicaciones. El 403 no era un ID equivocado sino un token acotado a una cuenta preguntando por la otra, que es exactamente lo que tiene que pasar. Comprobado: el respaldo diario a R2 corrió bien el 2026-07-30 usando ese mismo identificador (`gh run list --workflow backup-diario.yml`), y la dirección S3 anotada lo lleva adentro. Las dos cajas fuertes quedaron anotadas con la aclaración para que nadie vuelva a "arreglar" un número que está bien. **Falta:** enganchar los dominios de `careonys.com`, repuntar `PWA_FAMILIAS_URL` y `PWA_ASISTENTES_URL` en Railway, comprobar un correo de activación real y recién entonces borrar los proyectos de Vercel. **Nada de Vercel se tocó todavía: los tres despliegues viejos siguen en el aire.** <br><br>**2026-07-31 — las tres direcciones de `careonys.com` ya andan.** El Desarrollador eligió los nombres: `gestion.careonys.com` para el Panel, `familias.careonys.com` para la aplicación de la Familia y `asistentes.careonys.com` para la de la Asistente. Se engancharon con el token acotado, sin tocar la web de Cloudflare. **Detalle que conviene recordar:** enganchar el dominio en Pages **no** crea solo el registro de DNS — hubo que crear a mano los tres `CNAME` (apuntando cada uno a su `.pages.dev`, con el proxy de Cloudflare encendido); sin eso los dominios se quedan en "pendiente" para siempre. Tardaron unos 2 minutos en pasar a activo. **Comprobado desde afuera:** las tres devuelven 200 por HTTPS y cada una sirve la aplicación que le corresponde (títulos "Panel de Administración", "Careonys — Familias", "Careonys — Asistentes"). En Railway se repuntaron `PWA_FAMILIAS_URL` y `PWA_ASISTENTES_URL` a las direcciones nuevas y se reinició el backend; comprobado que volvió a responder (`/health` → 200). No hizo falta tocar nada de CORS: `server.js:46` usa `app.use(cors())`, o sea que acepta cualquier origen. **La raíz `careonys.com` se dejó libre a propósito** (no tiene ningún registro): ahí va la página pública del producto, no una redirección al Panel — ver la nota de abajo sobre `PRD_01_Sitio_Web.md`. **Lo único que falta para poder borrar Vercel:** comprobar un correo de activación real, que crea un usuario de verdad y manda un mail de verdad, así que lo dispara el Desarrollador, no Claude Code. |
| 97 | **Otras claves quedaron escritas en el registro de la sesión del 2026-07-28, esta vez a propósito.** Al ordenar el archivo de claves, para saber cuáles seguían sirviendo y cuáles estaban muertas hubo que **probarlas**, y probarlas significó pasarlas como texto dentro de los comandos. Quedaron así en el registro de la conversación: la clave de **Anthropic** (la que usa el módulo de WhatsApp), las de **Cloudflare R2** y **Backblaze B2** (los dos depósitos de respaldo), la **contraseña de la base del panel comercial**, y las claves **publicable** y **secreta** de Supabase de Careonys. Es exactamente lo mismo que pasó con el pendiente #96, con dos diferencias: acá fue deliberado y sabido, y el registro no salió de esta máquina. **Riesgo real, sin dramatizar:** la clave publicable no es un secreto (va en los tres frontends, es pública por diseño); las otras cinco sí lo son. La más delicada es la **secreta de Supabase**, porque se salta las reglas de aislamiento entre Prestadoras (RLS) y es la que usa el backend en Railway — cambiarla obliga a actualizarla en Railway o el backend deja de funcionar. **Alternativa a cambiarlas:** decidir que el registro de la sesión es material privado de la máquina del Desarrollador y no cambiar nada. Es una decisión suya, no técnica. **Lección de método, para no repetirlo:** para saber si una clave sirve alcanza con cargarla en una variable de entorno y que el comando la lea de ahí; nunca escribirla dentro del comando | 2026-07-28 | Desarrollador (decide si vale la pena cambiarlas) / sesión de trabajo (causó la exposición y haría el cambio) | Decidido explícitamente, clave por clave, si se cambia o se deja; y para cada una que se cambie: valor nuevo generado, actualizado en todos los lugares donde vive (Railway, secretos de GitHub, `backend/.env`, archivo de claves) y **comprobado conectándose**, más comprobado que el valor viejo ya no entra | 🔴 Abierto — a decidir por el Desarrollador |
| 93 | **Corregido 2026-07-26 (encuadre, no contenido — ver Estado).** Cerrado el pendiente #79 (Fase 7, Panel de Admin_plataforma: estado de pago/uso/riesgo), la funcionalidad de planes/módulos quedó construida, verificada y desplegada pero operando sobre datos **ficticios**: `planes` tiene 3 filas placeholder (Básico USD 100, Intermedio USD 200, Avanzado USD 300), `catalogo_modulos` tiene sus 9 módulos con `precio_addon` parejo ficticio (USD 15 cada uno), y `plan_modulos` está vacía (0 filas, ninguna asignación módulo→plan cargada). **No es una decisión de negocio pendiente del Desarrollador dentro del esquema actual de Careonys** (encuadre original de esta fila, incorrecto): `ARQUITECTURA_NIVELES.md:264-267` ya deja registrado que la definición de contenido de los planes queda **en pausa** porque, bajo la decisión de separación en 3 niveles (CeltaTech/Careonys/Prestadora, `docs/project_arquitectura_3_niveles...` en memoria, 2026-07-24), `planes`/`plan_modulos` (parte comercial)/`prestadora_planes`/`facturas_licencia` están marcadas para **migrar** a la futura base de datos propia de CeltaTech (Nivel 1) — cargar valores reales ahora en las tablas de Careonys sería trabajo hecho en el esquema equivocado, a descartar o re-migrar cuando exista el repo/DB separado de CeltaTech | 2026-07-25 (creado) / 2026-07-26 (re-encuadrado) | Desarrollador (decidió la separación de niveles) / Claude Code (detectó el encuadre incorrecto sin haber cruzado `ARQUITECTURA_NIVELES.md` al crear esta fila, corregido a pedido del Desarrollador) | Existe el inventario completo + plan aprobado de la separación de 3 niveles (`CLAUDE.md` §11, ver "Próximo paso" de `ARQUITECTURA_NIVELES.md`) y, dentro de ese plan, se define dónde y cuándo cargar el catálogo real de planes — no antes. **Primer corte de ese inventario ya existe** (2026-07-27): `F:\proyectos\CeltaTech\INVENTARIO_MIGRACION_NIVEL1.md` — no completo todavía (ver su sección 5, "qué falta"), no reemplaza el plan aprobado que sigue faltando | 🔴 En pausa — depende de la separación CeltaTech/Careonys (Nivel 1/Nivel 2), no de que el Desarrollador defina precios hoy dentro del esquema actual |
| 70 | El Desarrollador definió, durante el relevamiento de UX de las 3 apps (2026-07-21), un requisito nuevo para la futura página pública de cada Prestadora: no puede ser una landing fija, porque cada Prestadora ofrece servicios distintos (y puede sumar otros con el tiempo). Se necesita un constructor de páginas tipo CMS con editor visual (mismo concepto que WordPress+Elementor, Wix, Squarespace o Webflow), como producto separado de CeltaTech — **fuera de Careonys**, no una funcionalidad más del Panel — al que Careonys se conecta por API para traer los datos de cada Prestadora (servicios que ofrece, textos, imágenes) y armar su página pública. Confirmado explícitamente por el Desarrollador como trabajo "para después", no para esta etapa | 2026-07-21 | Desarrollador (alcance) + Claude Code (diseño e implementación cuando se priorice) | Existe un documento de diseño (inventario de qué debe poder configurar cada Prestadora + decisión de arquitectura: producto propio de CeltaTech vs. adoptar una herramienta existente vía API/embeds) presentado y aprobado por el Desarrollador **antes** de escribir código — coherente con CLAUDE.md §11 (cambio de arquitectura grande: nuevo producto de CeltaTech) | 🔴 Abierto — post-MVP, explícitamente diferido |
| 14 | Medir el costo real de infraestructura compartida (Supabase/Vercel/Railway/backup) por prestadora y reflejarlo en el precio de licencia, en vez de un fee parejo para todas. Propuesto por el Desarrollador el 2026-07-11 durante la conversación de elección de proveedor de backup. **Avance 2026-07-15:** diseño completo en `docs/PLAN_MULTITENANT_PLM.md` sección 3.5 — fee base + tier por # Asistentes activos (reusando `'fee_fijo'`/`'por_personal'` existentes), costos fijos prorrateados por esa métrica, costo de IA/WhatsApp medido directo (confirmado que `mensajes_whatsapp` ya tiene `prestadora_id` pero no tokens/costo real), piso de margen interno. **Las 4 decisiones ya resueltas por el Desarrollador**: (1) enfoque aprobado tal cual, (2) valores de referencia iniciales: los packs varían entre USD 27 y USD 137 + impuestos, nunca hardcodeados en código, ajuste manual (no fórmula automática) por dólar/inflación/salarios u otra variable — investigado contra comparables de mercado (EE.UU.: AlayaCare/WellSky/CareSmartz360/AxisCare, descartados por el Desarrollador por no representar el poder adquisitivo argentino; Argentina: Xubio/Colppy/Tango como referencia real de lo que paga una pyme local), (3) el consumo de IA se factura dentro del tier, no aparte, (4) moneda la elige la prestadora (moneda local del país o USD, a su criterio). **Resta solo un detalle no bloqueante**: la asignación exacta de tramos intermedios (mediana/grande) dentro del rango USD 27–137, a definir recién cuando se implemente el módulo de facturación en serio | 2026-07-11 | — (las 4 decisiones de fondo ya están tomadas; el detalle de tramos intermedios queda para el momento de implementación) | Implementar `planes_facturacion` con estos valores cuando se aborde el módulo de facturación (según 4.3 de `PLAN_MULTITENANT_PLM.md`) | 🟡 Definición cerrada, implementación pendiente |
| 18 | **Pendiente permanente, no se cierra nunca del todo** (aclarado explícitamente por el Desarrollador 2026-07-13: no es una tarea de una sola vez, es una vigilancia continua). Estar atento en toda sesión futura — no solo en una revisión puntual — a políticas y formas de trabajo que queden fijadas de una sola manera (pensadas para la prestadora original) cuando, en el modelo de negocio de CeltaTech como licenciante, cada prestadora licenciataria debería poder parametrizarlas a su gusto según su propia metodología de trabajo. Cualquier hallazgo nuevo (de cualquier sesión, tocando cualquier módulo) se suma a la lista de abajo con su propia fecha, nunca se pierde en una conversación aislada. La pestaña "Servicios" del Módulo 8 (Configuración) es el lugar natural donde deberían vivir una vez que el Desarrollador decida parametrizar alguno (empieza con `configuracion_escalada_relevo`, ver pendiente #8) | 2026-07-11 (reencuadrado como permanente 2026-07-13) | Claude Code (revisar activamente en cada sesión y sumar hallazgos nuevos) + Desarrollador (decidir cuáles parametrizar, cuándo) | No tiene condición de cierre única — se mantiene abierto de forma indefinida como lista viva; cada hallazgo individual sí puede resolverse (parametrizado o descartado con motivo), pero el pendiente en sí sigue existiendo para lo próximo que aparezca | 🟡 Abierto de forma permanente — primera tanda de 8 candidatos completada 2026-07-13 (avance autónomo nocturno, autorizado explícitamente). **Actualización 2026-07-15: candidatos (1) y (2) ya resueltos e implementados en código+base+deploy** (ver detalle abajo); candidatos (3), (4), (5), (7) y (8) siguen **sin decisión del Desarrollador**, nada implementado todavía para esos 5; candidato (6) decidido explícitamente "no parametrizar": **(1)** ~~`backend/src/utils/vencimientos.js:5` — `DIAS_ANTICIPACION = 30` (ventana de aviso de vencimientos de Monotributo/ART/Seguro), igual para todas las prestadoras; además la línea 28 filtra `.eq('prestadora_id', PRESTADORA_DEMO_ID)`, o sea el cron hoy ni siquiera corre para otras prestadoras.~~ **Resuelto en código 2026-07-14 (pendiente de aplicar contra Supabase real — ver fila nueva más abajo):** catálogo de tipos de documento a trackear ahora es una tabla por prestadora sin límite de cantidad (`tipos_documento_asistente`/`documentos_asistente`, con "Certificado de Antecedentes Penales" agregado como tipo sugerido además de Monotributo/ART/Seguro) y el plazo de aviso pasó de constante fija a columna `prestadoras.dias_aviso_vencimiento_documentos` editable por cada `admin_prestadora`. `vencimientos.js` reescrito para recorrer todas las prestadoras (ya no filtra por un solo `prestadora_id` fijo) y leer el plazo de cada una. Código construido: `backend/src/db/schema_documentos_asistente.sql`, `backend/src/routes/panelConfiguracion.js` (rutas `/documentos-tipo*`), `panel/src/pages/Configuracion.jsx` (pestaña "Documentos"), `panel/src/pages/asistentes/PerfilTab.jsx` (componente `DocumentosVencimiento`). Ver `docs/DATA_MODEL.md` y `docs/PRD_02B_Gestion_Personal.md` (función 9) para el diseño completo. **Aplicado y verificado contra Supabase real 2026-07-15** (sesión con MCP de Supabase conectado — no había quedado registrado en esta fila cuando se aplicó): tablas `tipos_documento_asistente`/`documentos_asistente`, columna `prestadoras.dias_aviso_vencimiento_documentos` y columnas `vencimiento_monotributo/art/seguro` ya eliminadas de `asistentes`, todo confirmado en producción. **Hallazgo al verificar:** el seed del catálogo sugerido y la fila de `configuracion_notificaciones` para el evento `vencimiento_documento_asistente` habían quedado sin correr (tabla completamente vacía, 0 filas para cualquier evento) — corregido en esta misma sesión, ahora hay 4 tipos de documento sugeridos y 1 fila de notificación para la prestadora existente. Commit `aa944ac` (2026-07-14) ya está incluido en el deploy de `panel` a Vercel del 2026-07-15 19:50, así que la UI también está en producción. **(2)** ~~`panel/src/pages/guardias/NuevaGuardiaModal.jsx:10` — `DIAS_GENERACION_SIN_VIGENCIA_HASTA = 90` (cuántos días adelante se generan guardias de una serie sin `vigente_hasta` definido).~~ **Resuelto en código 2026-07-14 (pendiente de aplicar contra Supabase real — ver fila nueva más abajo):** el Desarrollador aclaró que el esquema abierto (sin fecha de cierre) es el que usan todas las prestadoras y no es negociable — los 90 días eran siempre un artefacto técnico de generación por lotes, nunca una regla de negocio, y su ausencia de renovación era un bug real (una serie sin `vigente_hasta` dejaba de producir guardias nuevas en silencio pasado el día 90). Se separó en dos mecanismos: (a) `prestadoras.dias_generacion_series_guardia` (columna nueva, default 90, editable por cada `admin_prestadora` en Configuración → pestaña Servicios) reemplaza la constante fija; (b) cron nuevo `backend/src/utils/generacionSeriesGuardia.js` (`extenderSeriesGuardiaAbiertas`, registrado en `backend/src/server.js`, corre una vez por día) mantiene siempre ese horizonte de guardias generadas por delante de "hoy" para toda serie `activa` sin `vigente_hasta` — proceso 100% interno, nunca visible en ninguna pantalla. Además, durante la misma conversación el Desarrollador señaló que "cancelar una guardia" y "cancelar un servicio" son conceptos distintos y pidió diseñar en la misma sesión el **Cierre de servicio** a nivel Paciente (da de baja todas las Prestaciones/Paquetes vigentes y cancela todas las guardias futuras no iniciadas de un Paciente, con motivo obligatorio — `fin_demanda`/`fallecimiento`/`otro` —, sin borrar ningún registro): tabla nueva `cierres_servicio_paciente` con RLS restringida explícitamente a `admin_prestadora`+`coordinador` (instrucción explícita del Desarrollador, sin bypass de `superadmin` a diferencia del resto de las policies del proyecto), UI en `panel/src/pages/familias/PrestacionesPaciente.jsx`, backend de configuración en `backend/src/routes/panelConfiguracion.js` (rutas `/guardias/horizonte-generacion`). Código construido: `backend/src/db/schema_cierre_servicio_paciente.sql`, `backend/src/utils/generacionSeriesGuardia.js`, `backend/src/server.js`, `backend/src/routes/panelConfiguracion.js`, `panel/src/pages/guardias/NuevaGuardiaModal.jsx`, `panel/src/pages/familias/PrestacionesPaciente.jsx`, `panel/src/pages/Configuracion.jsx`, i18n completo en las 3 locales de `panel/src/i18n/translations.js`. **Aplicado y verificado contra Supabase real 2026-07-15** (sesión con MCP de Supabase conectado — no había quedado registrado en esta fila cuando se aplicó): tabla `cierres_servicio_paciente`, columna `prestadoras.dias_generacion_series_guardia` y policy `coordinador_y_admin_gestionan_cierres_servicio_paciente` (sin `es_superadmin()`, como estaba diseñado) confirmadas en producción. Commit `a726ee6` (2026-07-14) ya está incluido en el deploy de `panel` a Vercel del 2026-07-15 19:50 y el backend en Railway (deploy automático confirmado exitoso). **Prueba manual en navegador real con datos de prueba todavía no hecha en esta sesión** — queda como verificación pendiente, no como aplicación de esquema. **(3)** `panel/src/pages/guardias/GuardiaAcciones.jsx:236-239` + réplicas en las 3 locales de `panel/src/i18n/translations.js` — catálogo fijo de motivos de aviso previo (`salud`/`transporte`/`familiar`/`otro`) como `<select>` cerrado, aunque `alertas_tempranas_guardia.motivo` ya es TEXT libre en la base (`schema_modulo6_guardias_03.sql:64-65`) precisamente para no cerrar esta lista. **(4)** `panel/src/i18n/translations.js` (~139-150, con réplicas en/pt-BR) — `especialidades_labels`/`zonas_labels` (`zona_norte`/`zona_oeste`/`zona_sur`/`la_plata`, geografía AMBA) hardcodeados en el Panel para `Postulaciones.jsx`, duplicando lo que `zonas_cobertura` (`schema_etapa2h.sql`) ya resolvió como configurable por prestadora para el sitio público — el Panel no tiene ese mismo mecanismo. **(5)** `backend/src/db/schema_etapa2h.sql:47` — `zonas_cobertura.categoria` sigue con `CHECK IN ('caba','gba','otras')`, cerrado a geografía de Buenos Aires pese a que la tabla ya es por-prestadora. **(6)** `backend/src/routes/postulacionAsistente.js:8` — `IDIOMAS_SOPORTADOS = ['es-AR','en','pt-BR']` fijo en código (coincide con el `DEFAULT` de `schema_etapa2o.sql:4`); una prestadora en otro país/idioma no podría sumar el suyo sin tocar código. **Decisión del Desarrollador 2026-07-14: no parametrizar — dejar los 3 idiomas actuales.** Queda cerrado como "decidido no parametrizar", no como pendiente abierto. **(7)** `panel/src/pages/asistentes/VerificacionTab.jsx:10` + `backend/src/routes/panelCuentas.js:85-91` (`ETAPAS_INCORPORACION`) — el Proceso de Incorporación de Asistentes de 5 etapas fijas está duplicado en frontend/backend y además cerrado por el ENUM Postgres `etapa_filtro` (`schema_etapa2b.sql:102-104`); una prestadora con un proceso de 3 o 7 pasos no podría adaptarlo sin una migración de esquema. **(8)** `backend/src/utils/email.js:4-13` — el transporte SMTP (`smtp.gmail.com`, `SMTP_USER`/`SMTP_PASSWORD`) es uno solo para todas las prestadoras; los destinatarios ya son configurables (`configuracion_notificaciones`), pero no el remitente/servidor de envío. Los 8 candidatos fueron verificados línea por línea contra el código real antes de listarlos acá (no solo el reporte de la revisión). **Versión sin lenguaje técnico agregada 2026-07-13 a pedido del Desarrollador: `docs/Analisis_Parametrizacion_Prestadoras.md`** — mismos 8 puntos explicados como situaciones de negocio, sin referencias a código/base de datos, más un análisis de en cuáles de los 8 hay una oportunidad real de uso de Inteligencia Artificial (solo 2 de los 8: sugerir motivo de ausencia a partir de texto libre, y traducción automática asistida para sumar un idioma nuevo) y en cuáles no (los otros 6 son simples decisiones de configuración). Falta: que el Desarrollador revise esta lista y decida, ítem por ítem, cuál parametrizar (y en qué mecanismo — probablemente Módulo 8) y cuál dejar como está. **Decisiones tomadas por el Desarrollador 2026-07-27 (revisión caso por caso), nada implementado todavía para ninguna de las 5:** **(3)** Parametrizar por Prestadora — catálogo editable de motivos de aviso previo de guardia, sembrado con Salud/Transporte/Familiar/Otro como default de cualquier Prestadora nueva, editable después desde Configuración → Servicios (mismo lugar que `configuracion_escalada_relevo`, #8). **(4)** Parametrizar — el Panel (Postulaciones) deja de usar las labels fijas de `translations.js` (`zona_norte`/`zona_oeste`/`zona_sur`/`la_plata`) y pasa a leer las zonas reales cargadas por cada Prestadora en `zonas_cobertura`, sin ninguna lista hardcodeada. **(5)** Parametrizar — se quita el `CHECK IN ('caba','gba','otras')` de `zonas_cobertura.categoria` (`schema_etapa2h.sql:47`); cada Prestadora define sus propias categorías de zona sin límite geográfico, incluso fuera de Argentina. **(7)** Parametrizar — "cada prestadora debe tener la libertad de implementar su propio plan de incorporación de asistentes" (cantidad y nombre de etapas configurable, no fijo en 5). Es un cambio de modelo de datos (reemplaza el ENUM `etapa_filtro` fijo) — requiere primero el inventario + plan de `CLAUDE.md` §11, todavía no hecho. **(8)** Parametrizar — cada Prestadora, al configurar si quiere o no enviar notificaciones y por qué medio (email, WhatsApp, etc.), debe poder suministrar los datos necesarios; para email, una dirección propia desde la cual enviar (exclusiva o no), en vez de depender de la única cuenta Gmail compartida (`notificaciones.aurevia@gmail.com`) hoy usada por todas. Implica guardar credenciales de envío por Prestadora (dato sensible, nunca en variables de entorno globales ni hardcodeado — `CLAUDE.md` §6/§7 Regla 1) — también requiere diseño antes de implementar, dado que toca cómo se almacenan credenciales multi-tenant. **(9) Hallazgo 2026-07-13, durante el arranque de implementación del pendiente #9 (WhatsApp+IA):** `configuracion_notificaciones` (`backend/src/db/schema_etapa2h.sql:79-84`) **no tiene `prestadora_id`** — es una fila global por evento, compartida por todas las prestadoras licenciatarias (confirmado explícitamente excluida de la migración multi-tenant en `backend/src/db/schema_multitenant_02.sql:353-359`, mismo motivo que `configuracion_empresa`). A diferencia de los 8 candidatos anteriores (que son ajustes de negocio), este es un caso más serio: hoy, si dos prestadoras usaran el sistema, compartirían la misma lista de emails y el mismo on/off de notificaciones por evento — no es una preferencia sin resolver, es una brecha de aislamiento real. Bloquea directamente la construcción de la config de WhatsApp del pendiente #9 (que necesita vivir en esta misma tabla/mecanismo, por evento y por prestadora) — se resuelve como parte de esa construcción, agregando `prestadora_id` y RLS acotada, en vez de tratarse aparte. **Resuelto 2026-07-13** — `configuracion_notificaciones` ya tiene `prestadora_id UUID NOT NULL` (`backend/src/db/schema_whatsapp_ia_01.sql:21-28`), la PK pasó a ser compuesta `(evento, prestadora_id)` (línea 30), y las policies quedaron acotadas por tenant (`admin_gestiona_configuracion_notificaciones`/`coordinador_lee_configuracion_notificaciones`, líneas 34-46) — construido como parte del pendiente #9 (WhatsApp+IA), aplicado y verificado contra Supabase real junto con el resto de ese schema. **Implementado en código+base 2026-07-27 (las 5 decisiones de la revisión caso por caso de más arriba, aplicado y verificado contra Supabase real con MCP conectado):** **(3)** `backend/src/db/schema_motivos_aviso_previo_01.sql` (tabla `motivos_aviso_previo_guardia`, RLS `admin_prestadora_gestiona_.../coordinador_lee_...`, seed Salud/Transporte/Familiar/Otro por cada Prestadora existente), rutas `panelConfiguracion.js` (`/motivos-aviso-previo*`), hook `panel/src/hooks/useMotivosAvisoPrevio.js`, UI en `Configuracion.jsx` (pestaña Servicios) y `GuardiaAcciones.jsx` ya no usa el `<select>` fijo. **(4)+(5)** `backend/src/db/schema_zonas_cobertura_categoria_libre_01.sql` quita el `CHECK IN ('caba','gba','otras')` de `zonas_cobertura.categoria` (confirmado sin ese constraint en Supabase real tras aplicar); hook `panel/src/hooks/useZonasCobertura.js` reemplaza las labels fijas de i18n en `Postulaciones.jsx`/`PostulacionDetalle.jsx`; UI de administración de zonas ya existía en `Configuracion.jsx` contra las rutas `/zonas` ya existentes, ahora con categoría de texto libre. **(7)** `backend/src/db/schema_etapas_incorporacion_01.sql` (tabla `etapas_incorporacion_asistente`, `UNIQUE(prestadora_id, orden)` DEFERRABLE + función `intercambiar_orden_etapas_incorporacion` para reordenar atómicamente, seed de las 5 etapas actuales por Prestadora, migración de `verificaciones_asistente.etapa` de ENUM `etapa_filtro` a TEXT preservando valores); `VerificacionTab.jsx` y `panelCuentas.js` ya no usan el array fijo `ETAPAS_INCORPORACION`; UI de administración (alta/reordenar/activar-desactivar) en `Configuracion.jsx`. **Bug de seguridad propio detectado y corregido en esta misma sesión antes de exponerlo:** `intercambiar_orden_etapas_incorporacion` había quedado invocable por `anon`/`authenticated` vía RPC (el mismo patrón de bug ya documentado en `schema_whatsapp_ia_01.sql:128-132`) — detectado con `mcp__supabase__get_advisors`, corregido con un `REVOKE EXECUTE ... FROM anon, authenticated` explícito, confirmado limpio en advisors después. **(8)** Se decidió una tabla dedicada `configuracion_email_prestadora` (no columnas sobre `configuracion_notificaciones`, que es por-evento y no por-Prestadora — desviación deliberada de la redacción literal del plan aprobado, por Regla 12 de punto único de verdad) en `backend/src/db/schema_email_remitente_prestadora_01.sql`, con credencial SMTP guardada vía Supabase Vault (`guardar_credencial_smtp_prestadora`/`leer_credencial_smtp_prestadora`, mismo patrón que el token de WhatsApp, `service_role` únicamente desde el inicio); rutas `GET/PATCH /email-remitente` en `panelConfiguracion.js` (el GET nunca devuelve la contraseña ni el `credencial_secret_id`, solo un booleano `credencial_cargada`); `backend/src/utils/email.js` usa el remitente propio de la Prestadora si está activo y configurado, si no cae al transporter compartido de CeltaTech (sin romper nada para quien no lo configure); UI `TabEmailRemitente` en `Configuracion.jsx` (pestaña Notificaciones). i18n de las 4 (3/4/5/7/8 en realidad, 9 claves de (8) incluidas) completo en es-AR/en/pt-BR. **Todas las migraciones aplicadas contra Supabase real y verificadas con `get_advisors` (sin warnings de seguridad nuevos) y con SQL directo (seed correcto para "Prestadora Demo" y "Sandbox Superadmin").** **Deploy y verificación funcional en el Panel real 2026-07-27** (commit `3e3be25`, push a `main`, backend auto-desplegado en Railway y Panel desplegado explícitamente a Vercel con `vercel --prod`, alias `aurevia-panel.vercel.app` confirmado): con Playwright contra el sitio ya desplegado (no contra código local) — **(4)+(5)** alta real de una zona con categoría `"cordoba"` (fuera de caba/gba/otras) desde la pantalla "Zonas de cobertura", guardada sin error, con el mensaje "Defina la categoría según su propio criterio... No hay valores fijos por sistema" confirmando que el `<select>` fijo ya no existe. **(3)** pestaña Servicios muestra el catálogo real (Salud/Transporte/Familiar/Otro) editable, ya no el `<select>` cerrado de `GuardiaAcciones.jsx`. **(7)** pestaña Servicios muestra las 5 etapas reales de "Prestadora Demo" con reordenamiento probado en vivo (subir "Verificación de identidad" de la posición 2 a la 1 y revertido a su orden original después de confirmar que funcionaba) — la función `intercambiar_orden_etapas_incorporacion` corrigida quedó validada en producción. **(8)** pestaña Notificaciones muestra "Remitente de notificaciones por email" con los defaults esperados (`smtp.gmail.com`/465); se inspeccionó la respuesta real de `GET /api/panel/configuracion/email-remitente` y se confirmó que no incluye `credencial_secret_id` ni ningún campo de contraseña, solo `credencial_cargada:false`. **Sigue pendiente, no verificable sin intervención del Desarrollador:** el envío de un email real de prueba con una credencial SMTP propia (cuenta de prueba real, no la compartida) para confirmar que el email llega con el remitente configurado — requiere que el Desarrollador provea o autorice esa cuenta de prueba; no se puede simular sin credenciales reales |
| 19 | Posible modelo de facturación con intermediario (tipo cooperativa/agencia de personal) para Asistentes monotributistas: si la Asistente factura a un tercero designado en vez de a la prestadora directamente, la prestadora pagaría a ese tercero en bloque, que redistribuye a cada Asistente. Idea planteada por Claude Code durante la discusión de certificaciones (2026-07-12, ver `docs/PLAN_MULTITENANT_PLM.md` sección 5.4bis), validada por el Desarrollador como potencialmente provechosa ("no es ninguna tontería, se le podría encontrar un costado provechoso"), pero pertenece al módulo de facturación/pagos, todavía no discutido — **no resolver ahora**, retomar cuando se aborde ese módulo. Posible cruce con el riesgo legal art. 23 LCT / caso Cabify (`CLAUDE.md`) si un intermediario agrupa y paga Asistentes — evaluar en ese momento si afecta quién ejerce el control/subordinación | 2026-07-12 | Desarrollador (definir cuándo abordar el módulo de facturación/pagos) | Se retoma y resuelve cuando se diseñe el módulo de facturación/pagos | 🔴 Abierto (anotado para más adelante, no bloqueante) |
| 37 | Sin mecanismo de recuperación de MFA por pérdida de dispositivo (celular con la app de autenticación) para `superadmin`/`admin_plataforma`. Decisión explícita del Desarrollador el 2026-07-15, dentro del mismo hilo de aprobación del ítem H del pendiente #30: no construir nada esta sesión — si pasa, se resuelve manualmente por SQL (`DELETE FROM auth.mfa_factors WHERE user_id = ...` o desactivar el toggle `configuracion_plataforma.mfa_admin_obligatorio`). Como el toggle es configurable y arranca apagado, el caso extremo real (cuenta bloqueada sin poder ni siquiera apagar el toggle) requiere acceso directo a Supabase, no al Panel | 2026-07-15 | Desarrollador (decidir cómo seguir con el hallazgo de bloqueo de puerto SMTP antes de poder cerrar) | Email de recuperación llega realmente a la bandeja del usuario en una prueba de punta a punta contra Railway real — no alcanza con que el código esté armado, tiene que llegar el mail | 🔴 Abierto — diseño y código construidos (recuperación por email de un solo uso, sin nada nuevo que anotar de antemano — el Desarrollador rechazó explícitamente una primera propuesta de códigos pre-generados por esa razón), pero la verificación de punta a punta contra Railway real **encontró un problema de infraestructura más grande que el propio pendiente**: `backend/src/utils/email.js` nunca logra conectar a `smtp.gmail.com` desde el backend desplegado. Dos causas distintas encontradas y solo una corregida: **(a) corregida** — nodemailer 9.0.3 resuelve A y AAAA del host y elige una IP al azar (`family` del transporter no tiene ningún efecto real, confirmado leyendo su código fuente), y la salida IPv6 de Railway da `ENETUNREACH`; se resolvió forzando IPv4 real vía `dns.resolve4` + `servername` explícito (commit `28346cf`), verificado también localmente (mismo síntoma reproducido en esta máquina). **(b) sin corregir, más grave** — con la IPv4 ya forzada, la conexión sigue sin establecerse: un endpoint de diagnóstico temporal (agregado y ya retirado, commits `b2e9b1a`→`7ba84bd`) probó los 3 puertos SMTP salientes (25/465/587) directo desde Railway y los 3 dieron `ETIMEDOUT` en ~260ms uniformes — no es un timeout real de conexión (tardaría segundos/minutos), es la firma de un bloqueo de red a nivel de infraestructura del proveedor: Railway no deja salir tráfico por esos puertos. Si esto es así, **ningún email del sistema se está entregando hoy desde el backend en producción** (ausencias, vencimientos, recuperación de MFA, todo lo que pasa por `email.js`) — no es un problema exclusivo de este pendiente, reabre la premisa del pendiente #44 (que asumía que el techo de volumen de Gmail era la única preocupación futura; si el puerto está bloqueado, el volumen es irrelevante, hoy es 0% de entrega). **Pendiente de decisión del Desarrollador:** (1) confirmar con soporte de Railway si el bloqueo se puede levantar (algunos planes lo permiten a pedido), o (2) migrar el envío a un proveedor transaccional por API HTTPS (SendGrid/Postmark/Resend/SES — puerto 443, nunca bloqueado), aprovechando la investigación de precios ya hecha en `docs/DECISION_EMAIL_ESCALA.md` para el pendiente #44. Ninguna opción se implementó todavía — es un cambio de arquitectura de infraestructura compartida por toda Careonys (`CLAUDE.md` §11), no se avanza sin esa decisión. Datos de prueba de esta verificación (cuenta `admin_plataforma` temporal + filas de `mfa_codigos_recuperacion`) limpiados sin residuo, confirmado por consulta directa. **Actualización 2026-07-26 — se probó y descartó Resend**: con aprobación del Desarrollador se migró `email.js` a Resend (API HTTPS, evita el bloqueo de puertos de Railway), se guardó la API key, se instaló el paquete y se probó un envío real — **rechazado con 403**: en modo "sandbox" (sin dominio propio verificado) Resend solo deja enviar a la casilla dueña de la cuenta (`notificaciones.aurevia@gmail.com`), no a cualquier destinatario, y Careonys todavía no tiene dominio real (pendiente #40 ítem 9). Se evaluó también SendGrid y se descartó sin probarlo: su plan "gratis" es en realidad un trial de 60 días, no permanente (`docs/DECISION_EMAIL_ESCALA.md:57`), peor opción que seguir con Gmail. **Decisión tomada:** migrar a la API de Gmail (OAuth2, HTTPS, puerto 443) reutilizando la misma cuenta `notificaciones.aurevia@gmail.com` — no depende de verificar ningún dominio. Requiere pasos manuales del Desarrollador en Google Cloud Console (crear proyecto, habilitar Gmail API, configurar pantalla de consentimiento OAuth, generar credenciales, completar el flujo de autorización una vez para obtener un refresh token) antes de poder implementar el código — **estos pasos todavía no se hicieron**, quedaron interrumpidos para retomar después. Todo el código y las variables de Resend fueron revertidos/eliminados (`git checkout` de `email.js`/`package.json`, paquete `resend` desinstalado, `RESEND_API_KEY`/`RESEND_FROM` borradas de Railway y de `backend/.env`) para no dejar código muerto apuntando a un proveedor descartado — la API key de Resend queda solo registrada en `No hacer commit/claves y contraseñas.txt` como historial, sin usarse. El fix de IPv4/nodemailer (commit `28346cf`) sigue vigente en `email.js`, es inofensivo aunque insuficiente. Cierre de este pendiente sigue exigiendo la misma condición de siempre: el mail tiene que llegar de verdad en una prueba real, ahora vía Gmail API. **En pausa 2026-07-26** — decisión explícita del Desarrollador de posponer la implementación de la API de Gmail (OAuth2) hasta después del MVP; el caso extremo real (cuenta bloqueada) sigue resoluble a mano por SQL mientras tanto, mismo mecanismo ya documentado arriba. **Confirmado de nuevo por el Desarrollador 2026-07-27**: sigue en pausa hasta después del MVP, sin cambios de alcance ni de mecanismo |
| 40 | Rebautizo de infraestructura real del nombre anterior de la prestadora original/"PLM" a la identidad vigente (CeltaTech/Careonys), con el criterio corregido de que artefactos de plataforma (compartidos por todas las Prestadoras) se nombran `aurevia-*`, y artefactos específicos de la Prestadora de prueba se nombran `prestadora-demo-*` — nunca al revés. Inventario de 11 ítems reales presentado al Desarrollador 2026-07-16 | 2026-07-16 | Claude Code (ejecutar los ítems sin riesgo/con impacto verificado), Desarrollador (ítem 11, rename manual de la carpeta raíz local) | Los 11 ítems del inventario resueltos o explícitamente diferidos con motivo | 🟡 En curso — resuelto hasta ahora: (1) repo GitHub `BetoSP/[nombre anterior]`→`BetoSP/aurevia` (`gh repo rename`); (2) proyecto Vercel del Panel `[nombre-anterior]-panel`→`aurevia-panel` (`vercel project rename`, `projectId` sin cambios, citas de URL en el diario de obra y en `docs/PENDIENTES.md` actualizadas a `https://aurevia-panel.vercel.app`); (3) backups locales `backend/backups/[nombre-anterior]_pre_modulo6_*`→`aurevia_pre_modulo6_*`; (4) proyecto + servicio Railway `[nombre-anterior]-backend`→`aurevia-backend` (sin soporte de rename en Railway CLI — ejecutado vía mutaciones GraphQL `projectUpdate`/`serviceUpdate` de la API pública de Railway con el token de la sesión CLI local, verificado con `railway status`; dominio público `[nombre-anterior]-backend-production.up.railway.app` **sin tocar a propósito**, porque Railway no lo renombra junto con el servicio y cambiarlo requiere resincronizar `VITE_API_URL` en Vercel sin causar downtime — decisión pendiente aparte); `.github/workflows/deploy-backend.yml:29` actualizado en el mismo paso (`--service aurevia-backend`) para no romper el próximo deploy automático; citas del diario de obra y de `docs/PENDIENTES.md` corregidas — esto también cierra el hallazgo que había quedado abierto en el pendiente #35 sobre `deploy-backend.yml:29`. **Verificado 2026-07-16 contra la infraestructura real (no contra esta misma fila, que había quedado desactualizada tras un corte de proceso anterior):** (5) bucket R2 `[nombre-anterior]-backups`→`aurevia-backups` **YA ESTABA RESUELTO** — confirmado listando objetos reales del bucket vía API S3 con las credenciales de `backend/.env`, hay backups con ese nombre desde 2026-07-11; esta fila lo tenía mal registrado como pendiente. (6) Ídem bucket B2 `[nombre-anterior]-backups-mirror`→`aurevia-backups-mirror`, **YA ESTABA RESUELTO**, mismo método de verificación, backups desde 2026-07-11. (10) `PANEL_TEST_EMAIL` — **RESUELTO 2026-07-16**: no existía ninguna cuenta `admin_prestadora` de prueba (la Prestadora Demo original fue borrada por completo el 2026-07-15, pendiente #38); recreada exclusivamente para testing con el email provisto por el Desarrollador `aurevia.test.acount@gmail.com` (prestadora nueva `estado='prospecto'`, `prestadora_id=4e84b0e7-1729-4d07-9e70-56fdbd54cd89`; credenciales en `No hacer commit/claves y contraseñas.txt`; `.claude/settings.local.json` actualizado). **Verificado/resuelto además 2026-07-16:** (7) nombre visible del proyecto Supabase (nombre anterior de la prestadora original + "Salud")→"Careonys" — **RESUELTO**: la CLI local seguía logueada en una cuenta ajena sin acceso, pero el MCP de `supabase` tampoco expone rename; se encontró que el `SUPABASE_ACCESS_TOKEN` ya guardado en la sesión sí es válido contra la **Management API** de Supabase (`api.supabase.com/v1/projects`, distinta del MCP y de la CLI) — confirmado primero con `GET` (nombre anterior), y tras confirmación explícita del Desarrollador, ejecutado `PATCH /v1/projects/abcpmzfnnhpuiupmrsdi` con `{"name":"Careonys"}`; verificado con un segundo `GET` que devuelve `"name":"Careonys"`. Sin impacto en `ref`, connection strings ni claves — solo el nombre visible del dashboard. **Resuelto además 2026-07-16:** (9) dominio con el nombre anterior en `panel/.env VITE_SITE_URL` y `backend/src/db/schema_etapa2f.sql` — reemplazado por el placeholder `aurevia.app` (decisión explícita del Desarrollador: todavía no hay dominio real registrado), queda como placeholder hasta que se registre uno real. **Resuelto además 2026-07-18:** (11) carpeta raíz local — confirmado con `ls f:/proyectos/CeltaTech/` que ya se llama `Careonys` (renombrada por el Desarrollador). (8) `SMTP_USER` — el Desarrollador generó la contraseña de aplicación de `notificaciones.aurevia@gmail.com`; cargada en `backend/.env` (`SMTP_USER`/`SMTP_PASSWORD`) y como variables de Railway (`railway variables --set`, servicio `aurevia-backend`, verificado con `railway variables` mostrando ambos valores) y registrada en `No hacer commit/claves y contraseñas.txt`. Envío de emails reactivado. **Resuelto además 2026-07-18:** dominio de Railway — eliminado `sendler-backend-production.up.railway.app` (`railway domain delete`) y generado uno nuevo (`railway domain`); Railway no permitió el nombre limpio `aurevia-backend-production` (ya reservado/en uso), quedó `aurevia-backend-production-e7ee.up.railway.app` con sufijo aleatorio agregado por la plataforma. Downtime real: los pocos segundos entre el delete y el create (ambos comandos corridos en secuencia inmediata), sin ventana de espera adicional. `VITE_API_URL` de Vercel actualizado (`vercel env rm`/`vercel env add`) y Panel redeployado (`vercel --prod`); verificado con `curl` directo al nuevo dominio (200) y grepeando el bundle JS desplegado (`index-DMDGUe3u.js`) para confirmar que ya no queda ninguna referencia al dominio viejo. **Hallazgo nuevo en la verificación de este mismo paso:** el ítem (2) de esta misma fila (rename del proyecto Vercel del Panel) da por resuelta la URL `https://aurevia-panel.vercel.app`, pero esa URL devuelve 404 real — no existe. El alias que efectivamente sirve tráfico sigue siendo `https://sendler-panel.vercel.app` (200), más dos alias largos autogenerados (`aurevia-panel-betosps-projects.vercel.app`, `aurevia-panel-betosp-betosps-projects.vercel.app`). Es el mismo tipo de gap que tenía el dominio de Railway — resuelto aparte como pendiente #45 (`vercel domains add aurevia-panel.vercel.app` + eliminación del alias viejo `sendler-panel.vercel.app`). **Reaparición detectada y corregida 2026-07-24** (avance autónomo nocturno autorizado): el alias `sendler-panel.vercel.app` había vuelto a existir (`vercel alias ls` lo listaba de nuevo apuntando al último deployment, antigüedad "2d" — causa no determinada, probablemente Vercel lo recrea al reasignar el dominio "default" del proyecto en algún redeploy) y volvía a responder 200, contradiciendo el cierre de #45. Eliminado de nuevo con `vercel alias rm sendler-panel.vercel.app --yes`, verificado con `curl` que ahora da 404 y que `aurevia-panel.vercel.app` sigue sirviendo 200. Si vuelve a reaparecer en una sesión futura, revisar si el proyecto Vercel tiene algún dominio "raíz" configurado que lo esté regenerando automáticamente en cada deploy, en vez de asumir que alcanza con eliminarlo una vez más. **Re-verificado 2026-07-27 (revisión de prioridades de pendientes)**: se re-auditaron los 11 ítems contra la infraestructura real (no contra esta fila) — `curl` directo confirma `aurevia-panel.vercel.app` (200) y `aurevia-backend-production-e7ee.up.railway.app/health` (`{"status":"ok"}`), el bundle JS desplegado (`index-lNa4Umfd.js`) no tiene ninguna mención a "sendler" y sí trae horneado el dominio nuevo de Railway como `VITE_API_URL` — los ítems 1,3,4,5,6,7,8,9,10,11 siguen resueltos. **El alias `sendler-panel.vercel.app` había vuelto a reaparecer por 3ra vez** (`vercel alias ls` lo mostraba de nuevo, antigüedad "2d", sirviendo 200) — eliminado de nuevo (`vercel alias rm`), verificado 404. Se investigó la causa raíz esta vez: `panel/vercel.json` no define alias; `vercel domains ls` da 0 dominios registrados en la cuenta; no hay ninguna mención a "sendler" en `.github/workflows/`, `panel/package.json` ni `panel/.vercel/project.json` — no hay ninguna configuración en este repo que lo esté regenerando. Es, con alta probabilidad, un artefacto del lado de la plataforma Vercel (remanente del `vercel project rename` original que Vercel parece re-materializar en algunos redeploys de producción), no algo controlable desde el repo. **No se puede cerrar el ítem 2 como resuelto de forma permanente** — solo se puede seguir detectándolo y removiéndolo cada vez que se verifique el Panel en producción; se deja anotado para no volver a asumir "ya está" la próxima vez que se audite esta fila |
| 49 | Crear el sitio público real de Careonys (mostrando el software de CeltaTech/Careonys, no el de la Prestadora original) — reemplaza al que se dio de baja en el pendiente #48. El Desarrollador decidió explícitamente esperar: no se construye hasta que el software esté lo bastante completo como para mostrarse correctamente. **Material que ya existe y conviene mirar antes de arrancar de cero (comprobado el 2026-08-07):** el sitio viejo entero —41 archivos, con las pantallas de inicio, servicios, contacto, "solicitá el servicio", "trabajá con nosotros", privacidad y términos, ya traducidas a los tres idiomas— se puede recuperar del respaldo `respaldos-historicos/historia-vieja-aurevia-con-sitio-web-20260718.zip`; el `LEEME.md` de esa carpeta explica cómo. La marca de aquel sitio no se usa más y la idea de hoy es otra (un sitio por Prestadora con dominio propio), así que no se copia: se mira como trabajo ya hecho | 2026-07-18 | Desarrollador (decidir el momento apropiado) + Claude Code (implementar cuando se autorice) | El Desarrollador da la orden explícita de empezar, y existe un sitio nuevo con identidad Careonys real desplegado en producción | 🔴 Abierto — intencionalmente en espera, no bloqueante, sin fecha objetivo todavía |
| 53 | Modalidad de vínculo "Cooperativa de trabajo" (asociado con liquidación cooperativa, no monotributo ni relación de dependencia) — gap detectado al comparar Careonys contra un brief de investigación de mercado (segmento "Cooperativas de trabajo", ej. CUIDARnos). El motor `calcularCese` y el vínculo dual actual solo contemplan monotributo/dependencia. Explícitamente diferido a después del MVP por decisión del Desarrollador (2026-07-18) | 2026-07-18 | Desarrollador (priorizar) + Claude Code (implementar) | Existe una tercera modalidad de vínculo "cooperativa" soportada por el motor de liquidación/cese, parametrizada (no hardcodeada, ver CLAUDE.md §3), y validada contra al menos un caso de prueba en Sandbox | 🔴 Abierto — post-MVP |
| 54 | Documento legal de protección de datos personales (Ley 25.326, Argentina) — hoy `docs/legal/argentina.md` cubre solo riesgo laboral (indicio de subordinación, art. 23 LCT), no protección de datos personales/salud. Gap detectado al comparar Careonys contra un brief de investigación de mercado. Explícitamente diferido a después del MVP por decisión del Desarrollador (2026-07-18) | 2026-07-18 | Desarrollador (contenido legal) + Claude Code (incorporar al documento) | `docs/legal/argentina.md` incluye una sección de protección de datos personales, coherente con el resto del documento (fuente de advertencias configurable, no hardcodeada) | 🔴 Abierto — post-MVP |
| 55 | Rol "Auditor/regulador" de solo lectura (para programas estatales/registros oficiales que requieren trazabilidad, ej. Cuidarlos San Juan) — no existe hoy en el sistema de roles (`CLAUDE.md §5`); relacionado con el rol de solo lectura para financiadores ya identificado como futuro en `docs/CONTEXT.md:64-66`. Gap detectado al comparar Careonys contra un brief de investigación de mercado. Explícitamente diferido a después del MVP por decisión del Desarrollador (2026-07-18) | 2026-07-18 | Desarrollador (aprobar alcance y término de glosario) + Claude Code (implementar) | Término agregado y aprobado en el glosario (`CLAUDE.md §4`) antes de programar; rol implementado con el mismo principio de mínimo privilegio y aislamiento por Organización que los roles existentes | 🔴 Abierto — post-MVP |
| 57 | Módulo de gestión del ciclo de cobranza a obras sociales/prepagas (seguimiento de reclamos, rechazos y demoras de reintegro) — dolor real confirmado por investigación de mercado (2026-07-18): hay marco legal activo en Argentina de reclamos contra obras sociales/prepagas que demoran o rechazan cobertura de cuidador domiciliario (Superintendencia de Servicios de Salud, PMO como piso mínimo no techo), y afecta directo el flujo de caja de cualquier Prestadora que facture a obra social (perfil "Siempre"). Hoy el Módulo 8 (Precios/Prestaciones) solo resuelve precio/foto de precio por Prestación, no el estado de un reclamo de cobranza. Decisión del Desarrollador (2026-07-18): Careonys debe incorporar una solución a esto en su propuesta, no dejarlo fuera del alcance | 2026-07-18 | Desarrollador (PRD/alcance) + Claude Code (implementar) | Existe un PRD o extensión del Módulo 8 con estados de reclamo por Prestación/Paciente facturado a obra social (enviado/en demora/rechazado/cobrado), alertas de demora configurables, y trazabilidad de cada reclamo — sin datos hardcodeados (CLAUDE.md §1) | 🔴 Abierto — post-MVP, alcance ya decidido a incorporar |
| 58 | Dashboard/KPI explícito de rotación y retención de Asistentes — dolor real confirmado por investigación de mercado (2026-07-18): la rotación de cuidadores es el problema #1 reportado por agencias del sector (hasta 65% anual en mercados maduros, costo de reemplazo US$2.600-5.000 por Asistente), y la flexibilidad de horario pasó a ser la principal palanca de retención. Careonys hoy tiene Score de Riesgo de reclasificación (enfocado en riesgo legal de vínculo) y registro de Ausencias/Ceses, pero ningún KPI explícito de tasa de rotación/retención en el dashboard del Panel. Decisión del Desarrollador (2026-07-18): incorporar a la propuesta | 2026-07-18 | Desarrollador (definir métrica y umbral) + Claude Code (implementar) | Dashboard del Panel (Módulo 1) muestra tasa de rotación de Asistentes por período y por Prestadora, calculada desde `ceses`/`ausencias` ya existentes, sin tabla ni cálculo nuevo hardcodeado | 🔴 Abierto — post-MVP, alcance ya decidido a incorporar |
| 59 | Transparencia de facturación de cara a la Familia (detalle de cargos visible en el portal, no solo la reconciliación interna "a revisar" de precio de lista vs. Prestación) — dolor real confirmado por investigación de mercado (2026-07-18): "cobros indebidos/poco transparentes" es una queja recurrente de usuarios finales en el rubro (referencias España por escasez de reviews públicas de Argentina, mismo tipo de servicio). Hoy el Módulo 8 solo expone el precio final de la Prestación; no hay un desglose de cargos visible para la Familia en su propio portal. Decisión del Desarrollador (2026-07-18): incorporar a la propuesta. **Antes del desglose hay una pregunta de negocio sin contestar**, sumada el 2026-08-08 al rescatar la comparación de pantallas del 2026-08-02 (observación 11): ¿la Familia paga por el sistema, o paga por afuera y el sistema apenas le muestra lo que debe? Hoy, del lado de la Familia, lo único que existe de plata es la suscripción del marketplace y el código QR para pagar en efectivo (`pwa-familias/src/pages/SuscripcionMarketplace.jsx` y `POST /qr-cobro` en `backend/src/routes/appFamilias.js`); toda la facturación vive en el Panel. Si en el modelo el que cobra es la Obra Social y no la Familia, entonces está bien que no haya nada más y este pendiente se cierra contestando que no — pero contestado, no supuesto | 2026-07-18 | Desarrollador (definir qué detalle se expone, y contestar primero la pregunta de arriba) + Claude Code (implementar) | La PWA de Familias (Etapa 4) muestra un desglose de cargos de su Prestación/Paquete, coherente con el precio final ya congelado por Módulo 8, sin exponer información interna de otra Familia (aislamiento ya vigente) | 🔴 Abierto — post-MVP, alcance ya decidido a incorporar |
| 77 | **Residencia** — concepto explícitamente fuera de alcance de la Fase 6 (Hospitalización y Cese de servicio, `docs/PLAN` en `C:\Users\Usuario\.claude\plans\distributed-scribbling-wirth.md` sección "Fase 6"): un Servicio (`schema_servicios.sql`) pertenece siempre a una sola Familia por diseño explícito del Desarrollador ("No confundas Servicio con residencia, sería un error grosero. Servicio es la sumatoria de todas las prestaciones que se brindan al grupo familiar"). "Residencia" sería el concepto aparte que sí agrupa varios Pacientes de distintas Familias bajo un mismo establecimiento/domicilio compartido (ej. un geriátrico o residencia con varios Pacientes de Familias distintas) — no existe todavía ningún modelo de datos para esto | 2026-07-22 | Desarrollador (alcance, diferido explícitamente) | Existe un plan (cómo se modela una Residencia como entidad distinta de Servicio, cómo conviven Pacientes de Familias distintas bajo una misma Residencia sin romper el aislamiento de datos por Familia) presentado y aprobado por el Desarrollador antes de escribir código (CLAUDE.md §11, cambio de modelo de datos central) | 🔴 Abierto — diferido, no bloqueante |
| 76 | **Retención por jurisdicción** — concepto explícitamente fuera de alcance de la Fase 6: el archivado de `hospitalizaciones_paciente`/`cierres_servicio_paciente` hoy no borra nunca filas (coherente con CLAUDE.md §6), pero tampoco define todavía un plazo de retención configurable por jurisdicción — mismo tipo de brecha ya señalada para cálculos legales en el pendiente #72 (jurisdicción de cada Prestadora), pero para retención de datos en vez de cálculo de indemnización | 2026-07-22 | Desarrollador (alcance, diferido explícitamente) | Existe un documento por jurisdicción (`docs/legal/<país>.md`) que defina el plazo de retención exigido, y una decisión de cómo se aplica (soft-delete con fecha de purga, exportación antes de purgar, etc.) aprobada por el Desarrollador antes de escribir código | 🔴 Abierto — diferido, no bloqueante |

## Cómo se usa esta lista

- Cualquier pendiente nuevo que surja en cualquier sesión se agrega acá el mismo día, con los
  5 campos completos — no queda solo en el diario de obra.
- Antes de cerrar cualquier tarea, se recorre esta tabla completa y se reporta si algo
  cambió — aunque la respuesta sea "sigue exactamente igual".
- Cuando un ítem se cierra, no se borra la fila — se cambia el Estado a 🟢 Resuelto y se
  agrega la fecha. Cada tanto, las filas ya cerradas se sacan enteras de acá, sin tocarles una
  letra, y se guardan aparte, para que en esta lista quede solo lo que falta hacer.
